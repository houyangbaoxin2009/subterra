/**
 * 调度工具：供调度核心复用的轻量断言等纯 JDK 助手。
 * Scheduler utilities: light pure-JDK helpers (e.g. assertions) reused by the
 * scheduling core.
 * <p>
 * 含断言助手 {@link io.toterra.subterra.engine.optim.sched.util.Assertions}。纯 JVM，
 * 不碰 MC。
 * <p>
 * Holds the assertions helper {@link io.toterra.subterra.engine.optim.sched.util.Assertions}.
 * Pure JVM, no MC coupling.
 */
package io.toterra.subterra.engine.optim.sched.util;