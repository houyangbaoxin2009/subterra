/**
 * Pure-JDK engine layer (p.2.0): worldgen / optim / config / log / export /
 * migrate cores, aggregated into the main mod jar. Zero Minecraft classpath.
 * Iron law: engine depends only on {@code subterra-api}.
 */
package io.toterra.subterra.engine;
