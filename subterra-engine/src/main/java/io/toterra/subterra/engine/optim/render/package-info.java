/**
 * 渲染：自研渲染管线的预留位（clean-room；Sodium/Embeddium 仅供思路参考，
 * 遵循移植纪律）。
 * 本包为 p.2.0 模块重构保留的空壳/预留位，无实体类，行为不在此。
 * Rendering: reserved slot for the self-developed render pipeline (clean-room;
 * Sodium/Embeddium referenced for ideas only per the porting discipline).
 * This package is a reserved stub left by the p.2.0 module re-layout; it has no
 * concrete types and no behavior lives here.
 */
package io.toterra.subterra.engine.optim.render;