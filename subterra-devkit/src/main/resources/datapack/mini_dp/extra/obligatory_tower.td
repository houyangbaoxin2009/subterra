// devkit structure "obligatory_tower" — registered ONLY via pack.td entries
// (manifest-additive discovery, id devkit:structure/obligatory_tower). Lives
// outside data/ so the directory scan never sees it: one entry, one source.
type tie<data>
structure = [
  type = "minecraft:structure",
  biomes = [ "minecraft:desert" ],
]