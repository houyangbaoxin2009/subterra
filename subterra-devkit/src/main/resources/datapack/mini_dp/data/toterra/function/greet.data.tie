// toterra function "greet" — tie-bound: lib = dp_logic (tie namespace), fn
// explicit = seed_42 → symbol dp_logic$seed_42 called through the engine.tie
// bridge (0-arg so the MC-shell registrar invokes it via invoke0()).
type tie<data>
function = [
  lib = "dp_logic",
  fn = "seed_42",
]