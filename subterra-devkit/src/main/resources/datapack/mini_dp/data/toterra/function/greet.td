// toterra function "greet" — tie-bound: lib = dp_logic (tie namespace), fn
// explicit → symbol dp_logic$greet called through the engine.tie bridge.
type tie<data>
function = [
  lib = "dp_logic",
  fn = "greet",
]