# Subterra p.2.23 In-game Mod Hub / 游戏内模组 Hub · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.22 全轨 landing 后，按 p.2.22 交接单 §5 生成指向 p.2.23 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.22 fully landed, §5 of the p.2.22 handoff produces this p.2.23 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.23 里程碑**的交接单：p.2.23 =「In-game mod hub / 游戏内模组 Hub」（自原 p.2.8 顺移）——**td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令**（见 ROAD p.2.23 行）。**本里程碑承接 p.2.12.4 FormDataGen 的 kind→widget 固定映射（表单自动生成，UI 渲染此前留线）与 p.2.17 规则系统热重载（RuleReloader 原子 apply-or-keep-old / revision 单调，复用为配置编辑 + 热重载语义），玩家侧配置入口经 UI 面板接线（p.2.22 runtime.ui 门控壳与 engine.ui 管理视图数据面为前置）**。许可纪律总原则：permissive（MIT / Apache-2.0 / BSD / Unlicense / ISC）可移植并保留声明；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA 仅 clean-room 参考；存疑按不允许处理。执行模式与 p.2.22 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.23.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 p.2.22 UI 线落点（engine.ui + runtime.ui + UiProbe）与 p.2.12.4 FormDataGen 的 kind→widget 映射、p.2.17 RuleStore/RuleReloader 热重载语义与 `/subterra rule` 命令核心、runtime 门控壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.23-milestone handoff: p.2.23 = “In-game mod hub / 游戏内模组 Hub” (carried over from the original p.2.8) — **td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands** (per the ROAD p.2.23 row). **This milestone builds on the p.2.12.4 FormDataGen kind→widget fixed mapping (auto-generated forms; the UI rendering was previously deferred along this line) and the p.2.17 rule-system hot reload (RuleReloader atomic apply-or-keep-old / monotonic revision, reused as the config-edit + hot-reload semantics); the player-side config entry is wired through the UI panels (the p.2.22 runtime.ui gated shell and the engine.ui management-view data plane are the prerequisite)**. License discipline: permissive (MIT / Apache-2.0 / BSD / Unlicense / ISC) may be ported with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are clean-room reference only; uncertain defaults to not allowed. Execution mode identical to the p.2.22 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.23.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the p.2.22 UI-line landings (engine.ui + runtime.ui + UiProbe), the p.2.12.4 FormDataGen kind→widget mapping, the p.2.17 RuleStore/RuleReloader hot-reload semantics and the `/subterra rule` command core, the runtime gated-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.22 里程碑「UI / HUD core / engine.ui」已全部落地并已推 GitHub，最新 p 轨号 p.2.22.6（收尾提交）**：UI/HUD 核心落地——**p.2.22.1** engine.ui 数据层（AppleSkin HUD 数据层 Unlicense 全量移植，纯 JDK 确定性 FoodValues/FoodTooltipRow/HudData/FoodOverlay，许可材料 third-party/appleskin-3.0.6 注明来源/版本/适配范围、未捆绑源码）+ **p.2.22.2** engine.ui tooltip + 文档书籍核心（数据驱动 TooltipBook/TooltipRule（td 化零 json，clean-room 参考 DataTip GPL-3.0 仅思想）+ DocBookTd/BookDef/BookPage 文档书籍（td 驱动，clean-room 参考 Patchouli CC-BY-NC-SA 仅思想），固定序逐字节往返）+ **p.2.22.3** engine.ui 列表/详情/许可视图核心（ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore，ModMenu MIT 借鉴不复制代码，面板仅管理视图、玩家交互走 p.2.14 interact）+ **p.2.22.4** runtime.ui UiRuntime 门控壳（subterra.probe.ui 缺省 no-op、[Subterra ui] ok marker 并入 AsyncE2EProbe 同开服；HUD/tooltip/书籍消费接线面文档化为 p.2.23 接线点）+ **p.2.22.5** UiProbe 30 checks（AppleSkin 数据层 / tooltip+doc-book td 往返 / 视图核心 / 接线契约 / 许可盘点）接入 probeAcceptance。关键路径 **engine.ui + runtime.ui + UiProbe**。结论：**UI/HUD 核心落地——engine.ui 确定性数据面（AppleSkin Unlicense 移植 + DataTip/Patchouli clean-room td 化 + ModMenu 借鉴视图核心）+ runtime.ui 门控壳 + UiProbe 30 checks**；probeAcceptance 全绿（93 纯 JVM + 五次 E2E，新增 UiProbe）。/ p.2.22 “UI / HUD core / engine.ui” fully landed and pushed, latest track **p.2.22.6** (closing commit): the UI/HUD core landed — p.2.22.1 the engine.ui data layer (AppleSkin HUD data-layer full Unlicense port: pure-JDK deterministic FoodValues/FoodTooltipRow/HudData/FoodOverlay, license materials under third-party/appleskin-3.0.6 citing source/version/adaptation scope, no source bundled) + p.2.22.2 the engine.ui tooltip + doc-book cores (the data-driven TooltipBook/TooltipRule (td-ized zero-json, clean-room reference of DataTip GPL-3.0 ideas only) + the DocBookTd/BookDef/BookPage doc book (td-driven, clean-room reference of Patchouli CC-BY-NC-SA ideas only), fixed-order byte-identical round-trip) + p.2.22.3 the engine.ui list/detail/license view cores (ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore, ModMenu MIT pattern borrow without copying code, admin views only, player interaction via p.2.14 interact) + p.2.22.4 the runtime.ui UiRuntime gated shell (subterra.probe.ui default no-op, the [Subterra ui] ok marker merged into the AsyncE2EProbe boot; the HUD/tooltip/book consumption surfaces documented as p.2.23 wiring points) + p.2.22.5 UiProbe 30 checks (AppleSkin data layer / tooltip+doc-book td round-trip / view cores / wiring contract / license inventory) wired into probeAcceptance. Key path **engine.ui + runtime.ui + UiProbe**. Conclusion: **the UI/HUD core landed — the engine.ui deterministic data plane (AppleSkin Unlicense port + DataTip/Patchouli clean-room td-ization + ModMenu-borrowed view cores) + the runtime.ui gated shell + UiProbe 30 checks**; probeAcceptance all green (93 pure-JVM + five E2E boots, incl. UiProbe).
- **p.2.23 目标（ROAD p.2.23 行原文，本次承接）**：「In-game mod hub / 游戏内模组 Hub（自原 p.2.8 顺移）：td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令」。/ p.2.23 goal (the ROAD row verbatim, this handoff): “In-game mod hub / 游戏内模组 Hub (carried over from the original p.2.8): td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands”.
- **p.2.22 及更早里程碑（已落地，事实不变）**：p.2.22 UI/HUD 核心（上述，UI 线首项，UI 线第二项即本里程碑 p.2.23 In-game mod hub，ROAD 行序紧随）、p.2.21 动画核心 MIT 移植（engine.anim + AnimRuntime + AnimProbe）、p.2.27 自研渲染管线（engine.render + runtime.render 三 MC 钩子 + RenderFidelityProbe 62 checks）、p.2.27.1 Flywheel 实例化基座前哨、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移、p.2.18 导出枢纽、p.2.17 规则系统（engine.config.rules + `/subterra rule` 命令 + RuleReloader 热重载）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact，玩家侧世界内交互）、p.2.13 deterministic PCG、p.2.12 schema-first（engine.schema + engine.scaffold，**p.2.12.4 FormDataGen 的 kind→widget 固定映射**）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。Hub 线现状：p.2.12.4 已预留 FormDataGen 的 kind→widget 固定映射（UI 渲染此前留线，p.2.23 承接为表单自动生成）；p.2.17 已落 RuleStore 双层配置 + RuleReloader 热重载（原子 apply-or-keep-old、revision 单调、重入幂等）与 `/subterra rule` 命令共享确定性核心 runVerb；p.2.22 已落 runtime.ui 门控壳与 engine.ui 管理视图数据面（面板仅管理视图、玩家侧交互走 p.2.14）。/ p.2.22 and earlier milestones all landed (unchanged): p.2.22 the UI/HUD core (above, the first UI-line item; this milestone p.2.23 In-game mod hub is the second UI-line item, next ROAD row), p.2.21 the animation core MIT port (engine.anim + AnimRuntime + AnimProbe), p.2.27 the clean-room self-developed render pipeline, p.2.27.1 the Flywheel instancing-base guard, p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration, p.2.18 export hub, p.2.17 rule system (engine.config.rules + the `/subterra rule` command + the RuleReloader hot reload), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 diegetic interaction (engine.interact, player-side in-world interaction), p.2.13 engine.pcg, p.2.12 schema-first (engine.schema + engine.scaffold, **p.2.12.4 the FormDataGen kind→widget fixed mapping**), p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack, p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Hub line: p.2.12.4 already reserved the FormDataGen kind→widget fixed mapping (the UI rendering was deferred along this line; p.2.23 takes it up as auto-generated forms); p.2.17 already landed the RuleStore two-tier configuration + the RuleReloader hot reload (atomic apply-or-keep-old, monotonic revision, re-entrant idempotent) and the `/subterra rule` command over the shared deterministic core runVerb; p.2.22 already landed the runtime.ui gated shell and the engine.ui management-view data plane (panels are admin views only, player-side interaction via p.2.14).
- **probeAcceptance 现状（p.2.22 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**93 个纯 JVM 探针**（含新增 `UiProbe` 30 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.23 的 engine 纯 JDK 落点自动纳入。p.2.23 验收基线 = **probeAcceptance 全绿（93 纯 JVM + 5 次 E2E，新增 UiProbe）+ 本里程碑新增的 Hub 确定性/接线探针**。/ probeAcceptance (p.2.22 closing): green — **93 pure-JVM probes** (incl. the new UiProbe 30 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.23 pure-JDK engine landing is auto-covered. The p.2.23 acceptance baseline = **probeAcceptance all green (93 pure-JVM + 5 E2E boots, incl. UiProbe) + the new Hub determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.23 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.22 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.23): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.22; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.23 承接 p.2.22（UI/HUD 核心落地为既有验收门，Hub 确定性/接线探针叠加其上）与 p.2.12.4（FormDataGen kind→widget 映射为表单自动生成的数据底座）与 p.2.17（RuleStore 双层配置 + RuleReloader 热重载为配置编辑 + 热重载语义）；入口接线承接 p.2.22 runtime.ui 门控壳范式（面板仅管理视图、玩家侧交互走 p.2.14 interact）；服务器端 op 配置命令承接 p.2.17 `/subterra rule` 命令共享确定性核心范式与 p.2.2.7 `/subterra export` 指令的 E2E 确定性触发范式（`-D`/`-P` 属性钩子，startup 即跑同一核心）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.23 continues from p.2.22 (the UI/HUD core landing is the existing gate; the Hub determinism/wiring probes stack on it), from p.2.12.4 (the FormDataGen kind→widget mapping is the data base for auto-generated forms), and from p.2.17 (the RuleStore two-tier configuration + the RuleReloader hot reload supply the config-edit + hot-reload semantics); the entry wiring continues the p.2.22 runtime.ui gated-shell pattern (panels are admin views only, player-side interaction via p.2.14 interact); the server-side op config commands continue the p.2.17 `/subterra rule` shared-deterministic-core pattern and the p.2.2.7 `/subterra export` E2E deterministic-trigger pattern (`-D`/`-P` property hooks, running the same core at startup). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.22 closing patterns. p.2.22 “UI / HUD core / engine.ui” fully landed and pushed (latest track **p.2.22.6**, key path **engine.ui + runtime.ui + UiProbe**): the UI/HUD core landed — engine.ui deterministic data plane (the AppleSkin HUD data-layer full Unlicense port: pure-JDK deterministic FoodValues/FoodTooltipRow/HudData/FoodOverlay with license materials under third-party/appleskin-3.0.6, no source bundled; the data-driven TooltipBook/TooltipRule + the DocBookTd/BookDef/BookPage doc book, td-ized zero-json, clean-room reference of DataTip GPL-3.0 / Patchouli CC-BY-NC-SA ideas only; the ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore view cores, ModMenu MIT pattern borrow, admin views only, player interaction via p.2.14) + runtime.ui UiRuntime gated shell (subterra.probe.ui default no-op, [Subterra ui] ok marker merged into the AsyncE2EProbe boot, HUD/tooltip/book consumption surfaces documented as p.2.23 wiring points) + UiProbe 30 checks wired into probeAcceptance; conclusion: **the UI/HUD core landed**; probeAcceptance all green (**93 pure-JVM + five E2E boots**, incl. UiProbe). p.2.23 defines the In-game mod hub milestone — per the ROAD row verbatim: **td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands** (carried over from the original p.2.8). Everything pinned by p.2.12–p.2.22 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. Acceptance baseline: **probeAcceptance all green (93 pure-JVM + 5 E2E boots, incl. UiProbe) + the new Hub determinism/wiring probes**. Related: p.2.12.4 FormDataGen kind→widget (the auto-generated-forms data base), p.2.17 RuleStore/RuleReloader (the config-edit + hot-reload semantics), p.2.22 runtime.ui (the entry-wiring prerequisite). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.23 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.23 =「In-game mod hub / 游戏内模组 Hub」——**td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：p.2.22 UI 线落点现状（engine.ui 数据面 + runtime.ui UiRuntime 门控壳与消费接线面文档、UiProbe）；p.2.12.4 FormDataGen 的 kind→widget 固定映射（表单结构 / 渲染数据纯数据形态，UI 渲染留线点）；p.2.17 RuleStore/RuleReloader 热重载语义与 `/subterra rule` 命令共享确定性核心 runVerb；runtime 门控壳接线范式（subterra.probe.* 缺省 no-op + marker 并入既有 E2E）；probeAcceptance 既有接线（93 纯 JVM + 5 E2E）与 FFM/依赖面），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.23 行原文）：「In-game mod hub / 游戏内模组 Hub（自原 p.2.8 顺移）：td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令」。示意子项（示例，可合并）：
1. **engine.hub 表单自动生成核心（纯 JDK）**：承接 p.2.12.4 FormDataGen 的 kind→widget 固定映射，td schema → 确定性表单结构 / 渲染数据（字段序固定、逐字节一致；表单值往返恒等）。
2. **engine.hub 配置编辑 + 热重载核心（纯 JDK）**：承接 p.2.17 RuleStore/RuleReloader 语义——表单值 → 双层配置写入（全局档 + 存档覆盖档、resolve 胜出序）、逐字节变更检测 + 原子 apply-or-keep-old + revision 单调（禁时序）。
3. **runtime.hub 入口接线 + 服务器端 op 配置命令**：玩家侧配置入口经 UI 面板接线（承接 p.2.22 runtime.ui 门控壳范式，面板仅管理视图、玩家侧交互走 p.2.14 interact）+ 服务器端 op 配置命令（承接 p.2.17 `/subterra rule` 共享确定性核心范式；E2E 沿用 `-D`/`-P` 属性钩子确定性触发）。
4. **Hub 确定性/接线探针**：核心确定性（表单结构 / 配置编辑 + 热重载固定序逐字节一致、往返恒等、变更检测与 revision 单调）+ 接线契约（runtime 面与既有 E2E markers 稳定性）探针接入 probeAcceptance（93 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令** 是 ROAD 原文的**交付面**，贯穿每次设计；**许可红线**：p.2.23 不新增移植（全部复用既有纯 JDK 能力面：p.2.12.4 FormDataGen + p.2.17 RuleStore/RuleReloader + p.2.22 engine.ui 数据面），若摸底发现需要参考第三方，先按 p.2.22 纪律走（permissive 可移植保留声明、copyleft 仅 clean-room、存疑按不允许）；p.2.12–p.2.22 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.22: p.2.23 is the In-game mod hub milestone — **td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the p.2.22 UI-line landing state (the engine.ui data plane + the runtime.ui UiRuntime gated shell and the consumption-surface docs, UiProbe); the p.2.12.4 FormDataGen kind→widget fixed mapping (the pure-data form structure / render data; the deferred UI-rendering point); the p.2.17 RuleStore/RuleReloader hot-reload semantics and the `/subterra rule` shared deterministic core runVerb; the runtime gated-shell pattern (subterra.probe.* default no-op + markers merged into existing E2E); the existing probeAcceptance wiring (93 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.23 row verbatim: “In-game mod hub / 游戏内模组 Hub (carried over from the original p.2.8): td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands”. Illustrative directions (may merge): (1) **the engine.hub auto-generated-forms core (pure JDK)** — over the p.2.12.4 FormDataGen kind→widget fixed mapping, td schema → deterministic form structure / render data (fixed field order, byte-identical; form-value round-trip stable); (2) **the engine.hub config-edit + hot-reload core (pure JDK)** — over the p.2.17 RuleStore/RuleReloader semantics — form values → two-tier config writes (global tier + save-override tier, resolve-wins order), byte-identical change detection + atomic apply-or-keep-old + monotonic revision (no timing); (3) **runtime.hub entry wiring + the server-side op config commands** — the player-side config entry wired through the UI panels (the p.2.22 runtime.ui gated-shell pattern; panels are admin views only, player-side interaction via p.2.14 interact) + the server-side op config commands (the p.2.17 `/subterra rule` shared-deterministic-core pattern; E2E via the `-D`/`-P` property-hook deterministic trigger); (4) **the Hub determinism/wiring probes** — core determinism (form structure / config-edit + hot-reload fixed-order byte-identical, round-trip stable, change detection + monotonic revision) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 93 pure-JVM + 5 E2E). Note: **td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands** is the ROAD delivery surface and runs through every design; **the license red lines**: p.2.23 adds no new port (everything reuses the existing pure-JDK capability surfaces: p.2.12.4 FormDataGen + p.2.17 RuleStore/RuleReloader + the p.2.22 engine.ui data plane); if scouting finds a third-party reference, follow the p.2.22 discipline first (permissive portable with notices preserved, copyleft clean-room only, uncertain defaults to not allowed); the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.12–p.2.22 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.23 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.23 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.23 子项一律 **p.2.23.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；Hub 核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.23 红线）**：本里程碑**不新增移植**——全部复用既有纯 JDK 能力面（p.2.12.4 FormDataGen / p.2.17 RuleStore+RuleReloader / p.2.22 engine.ui 数据面）；若摸底发现需参考第三方，按 p.2.22 纪律执行（permissive 可移植并**保留声明**；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7）。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（Hub 若涉 native/FFM 按需）。
- **验收门（p.2.23 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**93 纯 JVM + 五次 E2E（新增 UiProbe 在内）**，**并新增本里程碑的 Hub 确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.23.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the Hub core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.23 red lines)**: this milestone **adds no new port** — everything reuses the existing pure-JDK capability surfaces (p.2.12.4 FormDataGen / p.2.17 RuleStore+RuleReloader / the p.2.22 engine.ui data plane); if scouting finds a third-party reference, follow the p.2.22 discipline (permissive may be ported with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7). Acceptance gate (the p.2.23 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **93 pure-JVM + five E2E boots (incl. UiProbe)**, **plus the new Hub determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.23 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.23 各子项落地、最新 p 轨号 p.2.23.x、关键路径、In-game mod hub 落地结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.24 AI behavior core / engine.ai——clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.24-ai-behavior-core.md`（bilingual），随当次提交一并提交。

**English**: After p.2.23 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.23 landing, latest track p.2.23.x, key path, the In-game mod hub conclusion; scope pointing to the next milestone, per ROAD row order **p.2.24 AI behavior core / engine.ai** — clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.24-ai-behavior-core.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.23 工作区建议：engine.hub 表单自动生成核心（承接 p.2.12.4 FormDataGen kind→widget）+ engine.hub 配置编辑 + 热重载核心（承接 p.2.17 RuleStore/RuleReloader 语义）+ runtime.hub 入口接线 + 服务器端 op 配置命令 + Hub 确定性/接线探针。
- **p.2.22 UI/HUD 核心（已落地，既有验收门）**：engine.ui 确定性数据面（AppleSkin Unlicense 移植 FoodValues/FoodTooltipRow/HudData/FoodOverlay + TooltipBook/DocBookTd td 化（DataTip/Patchouli clean-room）+ ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore（ModMenu MIT 借鉴，面板仅管理视图、玩家交互走 p.2.14））+ runtime.ui UiRuntime 门控壳（subterra.probe.ui 缺省 no-op、[Subterra ui] marker 并入 AsyncE2EProbe）+ UiProbe 30 checks；probeAcceptance 全绿（**93 纯 JVM + 五次 E2E**）。
- **承接数据底座**：p.2.12.4 FormDataGen 的 kind→widget 固定映射（表单结构 / 渲染数据纯数据，UI 渲染留线由本里程碑承接为表单自动生成）；p.2.17 RuleStore 双层配置（全局档 + 存档覆盖档、resolve=global→overrides 胜出）+ RuleReloader 热重载（逐字节变更检测、原子 apply-or-keep-old、重入幂等、revision 单调）+ `/subterra rule` 命令共享确定性核心 runVerb；p.2.22 runtime.ui 门控壳与消费接线面（HUD/tooltip/书籍 GUI 接线面文档，面板仅管理视图）。
- **许可登记（p.2.23 不新增移植）**：全部复用既有纯 JDK 能力面；若摸底发现需参考第三方，按 p.2.22 纪律执行（permissive 可移植并**保留声明**；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA **仅 clean-room 参考，不直接包含**；存疑按不允许处理；完整登记册见框架设计 §7）。
- **probeAcceptance 接线基准（p.2.22 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **93 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.23 在此之上新增 Hub 确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.22 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.hub 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.22 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.22 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.23 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.22 UI/HUD 核心（已落地，既有验收门；UI 线首项）/ p.2.24 AI behavior core（ROAD 行序紧随 p.2.23，下一里程碑）；p.2.23 为 UI 线第二项（承接 p.2.12.4 FormDataGen kind→widget 映射与 p.2.17 规则热重载、p.2.22 runtime.ui 入口接线）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.23 landing: the engine.hub auto-generated-forms core (over the p.2.12.4 FormDataGen kind→widget) + the engine.hub config-edit + hot-reload core (over the p.2.17 RuleStore/RuleReloader semantics) + runtime.hub entry wiring + the server-side op config commands + the Hub determinism/wiring probes. p.2.22 the UI/HUD core (landed; the existing acceptance gate): the engine.ui deterministic data plane (the AppleSkin Unlicense port FoodValues/FoodTooltipRow/HudData/FoodOverlay + the td-ized TooltipBook/DocBookTd (DataTip/Patchouli clean-room) + the ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore (ModMenu MIT borrow, admin views only, player interaction via p.2.14)) + the runtime.ui UiRuntime gated shell (subterra.probe.ui default no-op, [Subterra ui] marker merged into AsyncE2EProbe) + UiProbe 30 checks; probeAcceptance all green (**93 pure-JVM + five E2E boots**). Inherited data bases: the p.2.12.4 FormDataGen kind→widget fixed mapping (pure-data form structure / render data; the deferred UI-rendering point this milestone takes up as auto-generated forms); the p.2.17 RuleStore two-tier configuration (global tier + save-override tier, resolve=global→overrides wins) + the RuleReloader hot reload (byte-identical change detection, atomic apply-or-keep-old, re-entrant idempotent, monotonic revision) + the `/subterra rule` shared deterministic core runVerb; the p.2.22 runtime.ui gated shell and the consumption surfaces (HUD/tooltip/book GUI wiring-point docs, panels are admin views only). License (p.2.23 adds no new port): everything reuses the existing pure-JDK capability surfaces; if scouting finds a third-party reference, follow the p.2.22 discipline (permissive portable with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are **clean-room reference only, never directly included**; uncertain defaults to not allowed; the full register is in framework design §7). probeAcceptance wiring baseline (p.2.22 closing): **93 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.23 adds the Hub determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.22 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.hub wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.22): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.22 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.23 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.22 the UI/HUD core (landed; the existing gate; the first UI-line item) / p.2.24 AI behavior core (next ROAD row after p.2.23); p.2.23 is the second UI-line item (building on the p.2.12.4 FormDataGen kind→widget mapping and the p.2.17 rule hot reload, the p.2.22 runtime.ui entry wiring).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 93 纯 JVM + 五次 E2E 与新增 Hub 探针）+ ROAD p.2.23 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 93 pure-JVM + five E2E boots plus the new Hub probes), the “ROAD p.2.23 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
