# Subterra p.2.9 World-as-artifact 世界即产物 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.8 全轨 landing 后，按 p.2.8 交接单 §5 生成指向 p.2.9 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.8 fully landed, §5 of the p.2.8 handoff produces this p.2.9 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.9 里程碑**的交接单：p.2.9 =「World-as-artifact 世界即产物」——把「世界」整体当作一个可搬移、可再水化（rehydrate）的**产物（artifact）**：将 export + save + 数据包 + 藏录（Ledger）一键打包为统一的世界包（world pack），可整体搬运到另一环境/实例、再水化成同一世界状态；服务 Toterra 的镜像层（镜像世界派生的完整副本）与存档互导（把世界包作为存档读入/导出为另一处可用的世界）。p.2.3（Save rework，已落地：SaveContainer 统一存档形态）与 p.2.2（Datapack rework，已落地：DatapackPack 打包往返 + export 档案）为其数据底座，p.2.18（engine.export 导出器，未来）为导出端口的统一收口，p.2.8（engine.sim 增量确定性模拟底座，已落地）为下游世界推演地基；本里程碑把 p.2.3 已确立的统一存档容器语义与 p.2.2 已确立的打包往返/档案形态**消费为世界包的核心数据抽象**，并预留 p.2.18 导出端口的接线切面。核心原则：**世界 = 可搬移可再水化的产物优先 + 复用既有 SaveContainer/DatapackPack 抽象不另起炉灶 + export∘rehydrate 逐字节/逐语义一致 + 确定性归档（固定排序、无时序）+ 接 p.2.3/p.2.2 既有档案形态**；engine 纯 JDK 不碰 MC、世界包核心可独立探针。执行模式与 p.2.5 / p.2.6 / p.2.7 / p.2.8 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.9.x**（≤三级），禁止「阶段X / 第N块 / R1 / block N」等表述。工作方式：先**摸底**（确认 p.2.3 SaveContainer 各槽与 DatapackPack/两个 export 档案的可复用切面、定位藏录 Ledger 与领域档案在既有保存形态中的位置、明确 p.2.18 导出端口的预留接线点）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.9-milestone handoff: p.2.9 = “World-as-artifact” — treat the whole world as a **movable, rehydratable artifact**: bundle export + save +datapacks + Ledger into one uniform world pack, moving it as a whole to another environment/instance and rehydrating it back into the same world state; this serves Toterra's mirror layer (complete copies derived from mirror worlds) and save interchange (read a world pack back as a save and export it as a world usable elsewhere). p.2.3 (Save rework — landed: the SaveContainer unified save form) and p.2.2 (Datapack rework — landed: DatapackPack round-trip + export archives) are its data foundations, p.2.18 (engine.export exporter, future) is the unified export-port consolidation, p.2.8 (engine.sim incremental deterministic simulation base — landed) is the downstream world-simulation foundation; this milestone **consumes** the unified save-container semantics from p.2.3 and the pack-round-trip / archive forms from p.2.2 as the world pack's core data abstraction, and reserves the p.2.18 export-port wiring seam. Core principles: **world-first-as-movable-rehydratable-artifact + reuse the existing SaveContainer/DatapackPack abstractions without reinventing + export∘rehydrate byte/semantic-identical + deterministic archival (fixed sort, no timing) + plug into the existing p.2.3/p.2.2 archive forms**; engine pure JDK (no MC coupling), the world-pack core independently probeable. Execution mode identical to p.2.5 / p.2.6 / p.2.7 / p.2.8 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.9.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the reusable seams in the p.2.3 SaveContainer slots and the DatapackPack / two export archives, locate where the Ledger and domain archive sit in the existing save form, and pin the reserved p.2.18 export-port seam) to produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do a final unified review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.8 里程碑「engine.sim 增量确定性模拟底座」已全部落地并推 GitHub**（提交见 git log；编号 p.2.8.1–p.2.8.6，最新 p 轨号 **p.2.8.6**）。关键路径 **engine.sim**（纯 JDK 核心 + runtime 壳，跨 `subterra-engine\...\engine\sim\`）：
  * **p.2.8.1** 预算分级调度：`engine.sim.budget`（`BudgetTier`/`RegionKey`/`TickBudget`/`BudgetScheduler`）——per-simulant/region/global 三级 cap、原子记账、超预算确定性 key 序降级；p.2.26 engine.time BudgetScheduler 前哨。+ `SimBudgetProbe`（11 checks）。
  * **p.2.8.2** 分区空间索引：`engine.sim.spatial`（`CellCoord`/`SpatialIndex`）——半径邻域查询只访问 (2r+1)² 局部窗口 cell（**无全局扫描**），cell 键经 p.2.7 `KeyPartition` 双 long 路径入 bank。+ `SimSpatialProbe`（11 checks）。
  * **p.2.8.3** 增量确定性模拟核心：`engine.sim.core`（`SimKey`/`PerSimRandom`/`Simulator`/`SimWorld`）——`advance(dirty)` 只推进变化局部、逐 key 顺序无关、diff/回放与全量重算逐字节一致。+ `SimIncrementalProbe`（6 checks）。
  * **p.2.8.4** per-simulant 隔离 + 确定性归并门面：`engine.sim.run`（`SimRunner`）——直接消费 p.2.7 `ParallelRunner`（run/runSerial 逐字节一致、重复键显式拒绝）。+ `SimParallelProbe`（9 checks）。
  * **p.2.8.5** 全轨组合简景：`SimCombineProbe`（budget+spatial+core+parallel，8 checks）。
  * **p.2.8.6** runtime 壳：`runtime.sim.SimRuntime`——`-D/-Psubterra.probe.sim` 门控默认 no-op，`[Subterra sim]` marker + 四微型确定性校验（parallel-vs-serial / incremental-vs-full / budget-deferral / spatial-local-window）+ AsyncE2EProbe 同一次开服并入（5 次开服总数不变）。
- **p.2.3 Save rework（已落地，数据底座）**：统一 `SaveContainer`（世界 + 配置 + 藏录 Ledger + 领域档案 + 遗物条目 + 台账六槽）；`engine.zd` 通用 zd v2 载体 + zdt 混合文档（td 元数据 + base64 zd 载荷）；`SaveExportArchive` td 导出档案（与 `DatapackExportArchive` 同构，export∘rehydrate 逐字节）；双层配置 + subterra-migrate 首块迁移管线。
- **p.2.2 Datapack rework（已落地，数据底座）**：纯 td 直载 + `engine.datapack` 注册表 + tie 逻辑链；`DatapackPack`（目录↔单一 td 文档，base64 content，解包重载注册表/包名/td 载荷逐字节一致）；`DatapackExportArchive` 内容级导出档案（export∘rehydrate 逐字节恒等）；`/subterra export` 指令（p.2.18 engine.export 前哨）。
- **确定性档案形态结论（p.2.3 + p.2.2 核心产出）**：两套档案（datapack 的 `type tie<data>` `DatapackPack`/`DatapackExportArchive`、save 的 `SaveExportArchive`）都确立「单一 td 文档 + base64 载荷 + 固定排序 + export∘rehydrate 逐字节恒等」的规范形；这为 p.2.9 确立了「世界包 = 同类档案形态的统一分层」的默认语义。
- **p.2.8 增量确定性模拟底座（已预落地）**：`engine.sim` 四模块 + 组合探针 + `SimRuntime` 壳——p.2.9 的世界包作为被再水化后由下游模拟消费的**世界稳态载体**（世界包捕获稳态，模拟推进该稳态的增量演化，接 p.2.8）。p.2.9 不需要重算模拟，只需报出/复原可被 p.2.8 消费的确定性世界状态。
- **probeAcceptance 现状**：`gradlew :subterra-devkit:probeAcceptance` 通过——**77 个纯 JVM 探针**（含新增 `SimBudget`/`SimSpatial`/`SimIncremental`/`SimParallel`/`SimCombine`）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra sim]` 断言）。铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；新增 engine 包（含 p.2.9 engine.* 新宿主，若落 engine 层）由 iron law 常量池扫描自动覆盖。
- **ABI / 接线事实（已钉死，p.2.9 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；`engine.*` 既有 ABI 与确定性档案形态（`DatapackPack`/`DatapackExportArchive`/`SaveExportArchive`）既有协议；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链，不加新开服若可并入既有壳；staging 用 build/tmp）。`/subterra export` 的 save 接线留 p.2.18。
- **关联里程碑**：p.2.3 Save（已落地，数据底座）；p.2.2 Datapack（已落地，数据底座）；p.2.18 engine.export（未来，导出端口统一收口）；p.2.8 engine.sim（已落地，下游世界推演地基）。p.2.9 应为 engine / runtime 落地一个可在不改造 p.2.2/p.2.3/p.2.8 的前提下复用既有档案形态的「世界包」抽象。

**English**: See the Chinese block above — this handoff follows the exact p.2.8 closing patterns. p.2.8 “engine.sim incremental deterministic simulation base” fully landed and pushed (commits p.2.8.1–p.2.8.6, latest track **p.2.8.6**); the key path is **engine.sim** (pure JDK, in `subterra-engine\...\engine\sim\`): budget-tier scheduling keyed per-simulant/region/global with deterministic key-order degradation (p.2.8.1, `SimBudgetProbe` 11 checks), partitioned spatial index with local-window queries and no global scans keyed onto the p.2.7 KeyPartition two-long path (p.2.8.2, `SimSpatialProbe` 11 checks), the incremental deterministic core with dirty-set advance byte-identical to a full recompute (p.2.8.3, `SimIncrementalProbe` 6 checks), the per-simulant-isolation + deterministic-merge SimRunner facade over the p.2.7 ParallelRunner (p.2.8.4, `SimParallelProbe` 9 checks), the full-stack combination probe (p.2.8.5, 8 checks), and the gated runtime shell `runtime.sim.SimRuntime` merged into the AsyncE2EProbe boot (p.2.8.6). **Deterministic-archive-form conclusion** (from p.2.3 + p.2.2): the datapack archives (`DatapackPack` / `DatapackExportArchive`) and the save archive (`SaveExportArchive`) all establish the canonical form “single td document + base64 payload + fixed sort + export∘rehydrate byte-identical” — this sets the default semantics for p.2.9's uniform world-pack layering. Pre-landed reusable base: the p.2.3 SaveContainer six-slot unified save form + zdt hybrid docs, the p.2.2 pure-td direct-load + DatapackPack round-trip, and the existing runtime `-D`/`-P` gating style (default no-op, zero vanilla impact). probeAcceptance is all green: **77 pure-JVM probes** (incl. the five Sim* probes) + **5 E2E boots** (Boot → Datapack → Save → Network → Async+sim, mustRunAfter chain). Iron law auto-covers new engine packages. p.2.9 should land a “world pack” abstraction for engine/runtime that reuses the existing archive forms (without reworking p.2.2/p.2.3/p.2.8), captures the deterministic steady-state consumed downstream by engine.sim, and reserves the p.2.18 export-port seam.

## 2. 本次范围（p.2.9 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 p.2.3 SaveContainer 各槽与 DatapackPack/两个 export 档案的可复用切面、定位藏录 Ledger 与领域档案在既有保存形态中的位置、明确 p.2.18 导出端口的预留接线点），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 World-as-artifact 提炼统一世界包抽象，承接 p.2.3/p.2.2 档案形态 + 复用既有导出抽象）：
1. **世界包档案形态**：为世界包定义统一档案文档（单一 td 文档，内部分层 = save 槽 + 数据包 + 藏录 + 元数据，base64 载荷、固定排序），与 `DatapackExportArchive`/`SaveExportArchive` 同构。
2. **打包/再水化往返**：世界 → 世界包 → 再水化 → 同一世界状态；export∘rehydrate 逐字节/逐语义一致。
3. **数据包并入**：把世界所载数据包并入世界包（复用 `DatapackPack` 打包往返；再水化时一并复原注册表/包）。
4. **藏录 Ledger 并入**：把藏录/领域档案（p.2.3 SaveContainer 槽）并入世界包统一收纳。
5. **可搬移/互导语义**：世界包可整体搬运到另一环境/实例（同构解档）、可在存档互导（作存档读入/导出）；服务 Toterra 镜像层。
6. **确定性探针**：任意世界集打包→再水化→再打包逐字节一致 + 固定归档序断言；保持既有 markers 稳定。
7. **runtime 接线与门控**：runtime 壳默认 no-op（`-D`/`-P` 门控），E2E 尽量并入既有开服壳；iron law 覆盖新增包 + probeAcceptance 全量接线。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**世界 = 可搬移可再水化的产物优先 + 复用既有档案形态 + export∘rehydrate 逐字节/逐语义一致 + 确定性归档**贯穿每次设计；p.2.2/p.2.3 已证明的档案范式（单一 td 文档 + base64 载荷 + 固定排序 + 往返恒等）必须被复用为默认语义。

**English**: Same principle as p.2.5/p.2.6/p.2.7/p.2.8: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the reusable seams in the p.2.3 SaveContainer slots and the DatapackPack / two export archives, locate the Ledger and domain archive in the existing save form, pin the reserved p.2.18 export-port seam), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions distilling a uniform world-pack abstraction over the p.2.3/p.2.2 archive forms: (1) world-pack archive form — one canonical td doc with layered sections (save slots + datapacks + Ledger + metadata, base64 payload, fixed sort), isomorphic to DatapackExportArchive/SaveExportArchive; (2) pack/rehydrate round-trip — world → world pack → rehydrate → same world state, export∘rehydrate byte/semantic-identical; (3) datapack inclusion — bundle the pack's datapacks into the world pack (reuse DatapackPack round-trip; restore registry/packs on rehydrate); (4) Ledger inclusion — fold the Ledger/domain archive (p.2.3 SaveContainer slots) into the world pack; (5) movability/interchange semantics — the world pack moves to another environment/instance and interchanges as save; serves the Toterra mirror layer; (6) deterministic probes — any world pack→rehydrate→repack byte-identical + fixed archive-order assertion, stable markers; (7) runtime wiring with a default-off gate — runtime shell default no-op (`-D`/`-P` gated), E2E reusing an existing boot where possible, iron-law coverage + full probeAcceptance wiring. Note: **world-first-as-movable-rehydratable-artifact + reuse existing archive forms + export∘rehydrate byte/semantic-identical + deterministic archival** runs through every design; the p.2.2/p.2.3-proven archive paradigm (single td doc + base64 payload + fixed sort + round-trip identity) must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.9 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/注入核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.9 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.9 子项一律 **p.2.9.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性归档**（固定排序、禁时序）；世界包核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.9 World-as-artifact 为自研（clean-room），不新增抄录。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含 BootProbe / DatapackE2EProbe / SaveE2EProbe / NetworkE2EProbe / AsyncE2EProbe 五次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.9.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic archival** (fixed sort, no timing); the world-pack core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: reuse/derive permissively with attribution only; p.2.9 World-as-artifact is self-developed (clean-room), no new copying. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.9 全轨完成后，必须生成 p.2.10 同格式提示词并纳入记录：更新上下文段（p.2.9 各子项落地、最新 p 轨号 p.2.9.x、关键路径、世界包抽象结论）、更新本次范围段指向 p.2.10（Save verifiability 存档可验证，接 p.2.3 的 zd 区块 + 签名）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.10-save-verifiability.md`（bilingual），随当次提交一并提交。

**English**: After p.2.9 fully lands, produce the p.2.10 prompt in the same format (context updated with p.2.9 landing, latest track p.2.9.x, key path, the world-pack abstraction conclusion; scope pointing to p.2.10 Save verifiability — zd chunks + signatures over p.2.3; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.10-save-verifiability.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.9 工作区建议：`subterra-engine\...\engine\world\`（世界包核心抽象，纯 JDK）+ runtime MC 壳按需落根 sourceSet。
- **p.2.8 已落地（下游地基，可消费/衔接）**：`engine.sim` 全套（`engine.sim.budget` `BudgetTier`/`RegionKey`/`TickBudget`/`BudgetScheduler`；`engine.sim.spatial` `CellCoord`/`SpatialIndex`；`engine.sim.core` `SimKey`/`PerSimRandom`/`Simulator`/`SimWorld`；`engine.sim.run` `SimRunner`）+ 探针资产 `SimBudgetProbe`/`SimSpatialProbe`/`SimIncrementalProbe`/`SimParallelProbe`/`SimCombineProbe` + runtime 壳 `runtime.sim.SimRuntime`。p.2.9 的世界包捕获可被其再水化的确定性世界稳态。
- **p.2.3 save 落点（数据底座，复用/消费）**：`SaveContainer` 六槽 + `engine.zd` zd v2 载体 + zdt 混合文档 + `SaveExportArchive` td 导出档案（`runtime.save` 壳 + `SaveE2EProbe`）。
- **p.2.2 datapack 落点（数据底座，复用/消费）**：`engine.datapack` 注册表 + `DatapackPack`（目录↔单一 td 文档）+ `DatapackExportArchive` 内容级导出档案 + `/subterra export` 指令（+ `DatapackE2EProbe`）。
- **p.2.18 衔接（未来，导出端口预留）**：`engine.export`——p.2.9 的世界包为可导出的一等载体之一，ABI 预留对齐；save 指令接线留 p.2.18。
- **再水化消费方**：p.2.8 engine.sim（下游推演稳态）——世界包捕获稳态，p.2.8 增量推进该稳态演化。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 world 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.3 Save（已落地，数据底座）；p.2.2 Datapack（已落地，数据底座）；p.2.8 engine.sim（已落地，下游地基）；p.2.18 engine.export（未来，导出端口）。「World-as-artifact 世界即产物」为其统一世界包抽象。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.9 work dir: `subterra-engine\...\engine\world\` (world-pack core abstraction, pure JDK) + runtime MC shell in the root sourceSet as needed. p.2.8 already landed (downstream foundation, consume/hand off): the full engine.sim set — budget (BudgetTier/RegionKey/TickBudget/BudgetScheduler), spatial (CellCoord/SpatialIndex), core (SimKey/PerSimRandom/Simulator/SimWorld), run (SimRunner) — with the five Sim* probes and the runtime.sim.SimRuntime shell; p.2.9's world pack captures the deterministic steady-state it can rehydrate. p.2.3 save landing (data foundation): SaveContainer six slots + engine.zd vz carrier + zdt hybrid docs + SaveExportArchive td export archive (runtime.save shell + SaveE2EProbe). p.2.2 datapack landing (data foundation): engine.datapack registry + DatapackPack (dir ↔ single td doc) + DatapackExportArchive content-level export archive + `/subterra export` command (+ DatapackE2EProbe). p.2.18 handoff (future, export-port reserved): engine.export — the world pack is one first-class exportable carrier; ABI kept aligned; save-command wiring stays in p.2.18. Rehydration consumer: p.2.8 engine.sim (downstream evolution steady-state) — the world pack captures the steady state; p.2.8 advances it incrementally. New world probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.3 Save (landed, data foundation); p.2.2 Datapack (landed, data foundation); p.2.8 engine.sim (landed, downstream foundation); p.2.18 engine.export (future, export port). World-as-artifact is the unified world-pack abstraction.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.9 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.9 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.