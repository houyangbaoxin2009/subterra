# Subterra p.2.25 Entity Scale / engine.scale · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.24 全轨 landing 后，按 p.2.24 交接单 §5 生成指向 p.2.25 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.24 fully landed, §5 of the p.2.24 handoff produces this p.2.25 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.25 里程碑**的交接单：p.2.25 =「Entity scale / 实体缩放 engine.scale」（自原 p.3.2 前移入主线）——**Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）+ runtime scale binding**（见 ROAD p.2.25 行）。**本里程碑为 MIT 移植形态**（形态参考 p.2.21 GeckoLib MIT 移植纪律）：把 Pehkui-Rebuilt 的实体缩放核心模型（ScaleType / ScaleData / ScaleModifier，按维度/标签缩放、数据包缩放规则）以纯 JDK 确定性核心移植进 engine.scale，并经 runtime.scale 接线到 MC 实体生命周期，以确定性探针验收；许可走 **MIT 移植纪律**（保留 Virtuoel 原始版权与重建声明，声明材料随库保留并注明来源与版本；copyleft 仅 clean-room，存疑按不允许处理）。执行模式与 p.2.24 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.25.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 Pehkui-Rebuilt 发行物与许可/声明材料、engine 各纯 JDK 核心目录现状、runtime 门控壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.25-milestone handoff: p.2.25 = “Entity scale / 实体缩放 engine.scale” (moved up into the main line from the original p.3.2) — **the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice) + runtime scale binding** (per the ROAD p.2.25 row). **This milestone is an MIT-port shape** (form referencing the p.2.21 GeckoLib MIT-port discipline): it ports Pehkui-Rebuilt's entity-scaling core model (ScaleType / ScaleData / ScaleModifier, per-dimension/per-tag scaling, datapack scaling rules) as a pure-JDK deterministic core in engine.scale, wired into the MC entity lifecycle via runtime.scale, accepted by deterministic probes; license follows the **MIT-port discipline** (Virtuoel's original copyright and the rebuild notice preserved, notice materials kept with the library citing source and version; copyleft is clean-room reference only, uncertain defaults to not allowed). Execution mode identical to the p.2.24 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.25.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the Pehkui-Rebuilt distribution and its license/notice material, the current state of the engine pure-JDK core directories, the runtime gated-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.24 里程碑「AI behavior core / engine.ai」已全部落地并已推 GitHub，最新 p 轨号 p.2.24.5（收尾提交）**：AI behavior core 落地——**p.2.24.1** engine.ai Brain 编排核心（纯 JDK，clean-room）——Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask（类型化记忆槽、固定注册序、重复拒、确定性 sense→decide→execute 编排、禁时序同输入同输出，SmartBrainLib MPL-2.0 仅模型参考零代码包含，声明在 package-info）+ **p.2.24.2** engine.ai 行为选择 + 调度核心——BehaviorPriority（优先级降序裁决 / 同优先级注册序打破平局 / 预算跳过）、BrainScheduler（TickBudget 三 cap 对齐 p.2.8 budget 语义、超限确定性跳过）、AiRandom（委托 p.2.13 PcgSource、同种子同序列、fork 隔离）+ **p.2.24.3** runtime.ai AiRuntime 门控壳（subterra.probe.ai 缺省 no-op、[Subterra ai] ok marker 并入 AsyncE2EProbe 同开服）+ **p.2.24.4** AiProbe 29 checks（编排固定序 / 行为选择 + 预算调度 / AiRandom 同种子同序列 + fork 隔离 / 接线契约 / clean-room 盘点）接入 probeAcceptance。关键路径 **engine.ai + runtime.ai + AiProbe**。结论：**AI behavior core 落地——engine.ai 确定性 Brain 编排 + 行为选择/调度 + runtime.ai 门控壳 + AiProbe 29 checks**；probeAcceptance 全绿（95 纯 JVM + 五次 E2E，新增 AiProbe）。/ p.2.24 “AI behavior core / engine.ai” fully landed and pushed, latest track **p.2.24.5** (closing commit): the AI behavior core landed — p.2.24.1 the engine.ai Brain orchestration core (pure JDK, clean-room) — Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask (typed memory slots, fixed registration order, duplicate rejection, deterministic sense→decide→execute orchestration, no timing same-input-same-output, SmartBrainLib MPL-2.0 model reference only zero code inclusion, declared in package-info) + p.2.24.2 the engine.ai behavior-selection + scheduling core — BehaviorPriority (priority-descending arbitration / same-priority registration-order tie-break / budget skip), BrainScheduler (TickBudget three caps over the p.2.8 budget semantics, deterministic over-cap skip), AiRandom (delegates to the p.2.13 PcgSource, same-seed-same-sequence, fork isolation) + p.2.24.3 runtime.ai AiRuntime gated shell (subterra.probe.ai default no-op, the [Subterra ai] ok marker merged into the AsyncE2EProbe boot) + p.2.24.4 AiProbe 29 checks (orchestration fixed order / behavior-selection + budget scheduling / AiRandom same-seed-same-sequence + fork isolation / wiring contract / clean-room inventory) wired into probeAcceptance. Key path **engine.ai + runtime.ai + AiProbe**. Conclusion: **the AI behavior core landed — the engine.ai deterministic Brain orchestration + behavior-selection/scheduling + the runtime.ai gated shell + AiProbe 29 checks**; probeAcceptance all green (95 pure-JVM + five E2E boots, incl. AiProbe).
- **p.2.25 目标（ROAD p.2.25 行原文，本次承接）**：「Entity scale / 实体缩放 engine.scale（自原 p.3.2 前移入主线）：Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）：ScaleType/ScaleData/ScaleModifier 模型、按维度/标签缩放、数据包缩放规则 + runtime scale binding」。/ p.2.25 goal (the ROAD row verbatim, this handoff): “Entity scale / 实体缩放 engine.scale (moved up into the main line from the original p.3.2): the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice): ScaleType/ScaleData/ScaleModifier model, per-dimension/per-tag scaling, datapack scaling rules + runtime scale binding”.
- **p.2.24 及更早里程碑（已落地，事实不变）**：p.2.24 AI behavior core（上述，AI 线首项；p.2.25 为实体缩放线，ROAD 行序紧随）、p.2.23 In-game mod hub（engine.hub + runtime.hub + HubProbe）、p.2.22 UI/HUD 核心（engine.ui + UiRuntime + UiProbe）、p.2.21 动画核心 MIT 移植（engine.anim + AnimRuntime + AnimProbe）、p.2.27 自研渲染管线、p.2.27.1 Flywheel 实例化基座前哨、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移、p.2.18 导出枢纽、p.2.17 规则系统（engine.config.rules + `/subterra rule` 命令 + RuleReloader 热重载）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim（engine.sim 增量确定性模拟底座）、p.2.7 parallel（engine.parallel 确定性并行底座）、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。实体缩放线现状：p.2.13 已落同种子生成底座（XoroRandom 语义，确定性数据产出的随机源可复用）、p.2.8 已落增量确定性模拟底座（budget 预算分级调度，实体 tick 规模节流可参考）；Pehkui-Rebuilt 已在移植登记册登记（engine.scale，MIT 移植，保留 Virtuoel 原始版权）。/ p.2.24 and earlier milestones all landed (unchanged): p.2.24 AI behavior core (above, the first AI-line item; p.2.25 is the entity-scaling line, next ROAD row), p.2.23 the in-game mod hub (engine.hub + runtime.hub + HubProbe), p.2.22 the UI/HUD core (engine.ui + UiRuntime + UiProbe), p.2.21 the animation core MIT port (engine.anim + AnimRuntime + AnimProbe), p.2.27 the clean-room self-developed render pipeline, p.2.27.1 the Flywheel instancing-base guard, p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration, p.2.18 export hub, p.2.17 rule system (engine.config.rules + the `/subterra rule` command + the RuleReloader hot reload), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim (the engine.sim incremental deterministic simulation base), p.2.7 parallel (the engine.parallel deterministic parallel base), p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack, p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Entity-scaling line: p.2.13 already landed the same-seed generation base (XoroRandom semantics — reusable as the randomness source for deterministic data production); p.2.8 already landed the incremental deterministic simulation base (budget-tier scheduling — reusable as a scaling for entity-tick scale); Pehkui-Rebuilt is already registered in the port map (engine.scale, MIT port, preserving Virtuoel's original copyright).
- **probeAcceptance 现状（p.2.24 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**95 个纯 JVM 探针**（含新增 `AiProbe` 29 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.25 的 engine.scale 纯 JDK 落点自动纳入。p.2.25 验收基线 = **probeAcceptance 全绿（95 纯 JVM + 5 次 E2E）+ 本里程碑新增的缩放确定性/接线探针**。/ probeAcceptance (p.2.24 closing): green — **95 pure-JVM probes** (incl. the new AiProbe 29 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.25 pure-JDK engine.scale landing is auto-covered. The p.2.25 acceptance baseline = **probeAcceptance all green (95 pure-JVM + 5 E2E boots) + the new scaling determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.25 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.24 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.25): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.24; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.25 承接 p.2.24（AI behavior core 落地为既有验收门，缩放确定性/接线探针叠加其上）；「Pehkui-Rebuilt 移植」复用既有 port 许可纪律（ROAD Port Allocation Map：Pehkui-Rebuilt —— 实体缩放能力纳入框架 → engine.scale，MIT 移植，保留 Virtuoel 原始版权，planned / 已立项）。**许可衔接**：p.2.21 为 GeckoLib MIT 移植（形态见该类：保留原始版权与捆绑库声明，声明材料随库保留并注明来源与版本）；p.2.25 Pehkui-Rebuilt 同为 MIT 移植——**保留 Virtuoel 原始版权与重建声明**（形态参考 p.2.21 GeckoLib MIT 移植纪律）；copyleft 仅 clean-room，存疑按不允许处理（完整登记册见框架设计 §7）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.25 continues from p.2.24 (the AI behavior core landing is the existing gate; the scaling determinism/wiring probes stack on it); the “Pehkui-Rebuilt port” reuses the pinned port license discipline (ROAD Port Allocation Map: Pehkui-Rebuilt — entity-scaling capability into the framework → engine.scale, MIT port preserving Virtuoel's original copyright, planned). **License handover**: p.2.21 was a GeckoLib MIT port (that shape: original copyright and bundled-library notices preserved, notice materials kept with the library citing source and version); p.2.25 Pehkui-Rebuilt is likewise an MIT port — **Virtuoel's original copyright and the rebuild notice preserved** (the form references the p.2.21 GeckoLib MIT-port discipline); copyleft is clean-room reference only, uncertain defaults to not allowed (full register in framework design §7). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.24 closing patterns. p.2.24 “AI behavior core / engine.ai” fully landed and pushed (latest track **p.2.24.5**, key path **engine.ai + runtime.ai + AiProbe**): the AI behavior core landed — the engine.ai deterministic Brain orchestration (p.2.24.1 Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask, clean-room, SmartBrainLib MPL-2.0 model reference only zero code inclusion, fixed registration order / duplicate rejection / sense→decide→execute) + the engine.ai behavior-selection/scheduling core (p.2.24.2 BehaviorPriority arbitration / BrainScheduler TickBudget three caps over p.2.8 / AiRandom delegating PcgSource same-seed-same-sequence, fork isolation) + the runtime.ai AiRuntime gated shell (p.2.24.3, subterra.probe.ai default no-op, the [Subterra ai] ok marker merged into the AsyncE2EProbe boot) + AiProbe 29 checks (p.2.24.4) wired into probeAcceptance; conclusion: **the AI behavior core landed**; probeAcceptance all green (**95 pure-JVM + five E2E boots**, incl. AiProbe). p.2.25 defines the Entity Scale milestone — per the ROAD row verbatim: **the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice): the ScaleType/ScaleData/ScaleModifier model, per-dimension/per-tag scaling, datapack scaling rules + runtime scale binding** (engine.scale, moved up into the main line from the original p.3.2). Everything pinned by p.2.12–p.2.24 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. **License red line: Pehkui-Rebuilt is MIT (permissive) — preserve Virtuoel's original copyright and the rebuild notice (the form references the p.2.21 GeckoLib MIT-port discipline); copyleft is clean-room reference only.** Acceptance baseline: **probeAcceptance all green (95 pure-JVM + 5 E2E boots) + the new scaling determinism/wiring probes**. Related: p.2.26 Time scaling engine.time is the next ROAD row after p.2.25. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.25 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.25 =「Entity scale / engine.scale」——**Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）+ runtime scale binding**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：p.2.24 AI 线落点现状（engine.ai + runtime.ai + AiProbe）；Pehkui-Rebuilt 发行物与许可/声明材料（MIT 移植保留 Virtuoel 原始版权与重建声明的材料形态与落位，参考 p.2.21 GeckoLib MIT 搬运纪律）；既有确定性能力面（p.2.13 XoroRandom/种子语义、p.2.8 budget 预算调度、p.2.2 datapack 直载范式）); runtime 门控壳接线范式（subterra.probe.* 缺省 no-op + marker 并入既有 E2E）; probeAcceptance 既有接线（95 纯 JVM + 5 E2E）与 FFM/依赖面）,再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.25 行原文）：「Entity scale / 实体缩放 engine.scale（自原 p.3.2 前移入主线）：Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）：ScaleType/ScaleData/ScaleModifier 模型、按维度/标签缩放、数据包缩放规则 + runtime scale binding」。示意子项（示例，可合并）：
1. **engine.scale 实体缩放核心（纯 JDK，Pehkui-Rebuilt MIT 移植）**：ScaleType/ScaleData/ScaleModifier 确定性模型（实体缩放数据、按维度（宽/高/长跨度、渲染/物理等）缩放、按标签缩放、固定序逐字节一致）、数据包缩放规则（td 化规则装载、确定性解析，可承接 p.2.2 datapack 直载范式与 p.2.17 RuleStore 双层语义）；MIT 移植保留声明（Virtuoel 原始版权 + 重建声明，材料随库保留并注明来源与版本，形态参考 p.2.21 GeckoLib 纪律）。
2. **runtime.scale 接线（runtime scale binding）**：MC 实体缩放生命周期接线（缩放数据装载面 / 驱动与更新面 / 渲染消费面），门控 -D/-P 范式，缺省 no-op，marker 并入既有 E2E 或独立确定性断言。
3. **缩放确定性/接线探针**：核心确定性（模型固定序逐字节一致、往返恒等）+ 接线契约（runtime 面与既有 E2E markers 稳定性）探针接入 probeAcceptance（95 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）+ runtime scale binding** 是 ROAD 原文的**交付面**，贯穿每次设计；**许可红线**：Pehkui-Rebuilt MIT **移植须保留 Virtuoel 原始版权与重建声明**（形态参考 p.2.21 GeckoLib MIT 移植纪律：声明材料随库保留并注明来源与版本）；若摸底发现需要参考其他第三方，先按 p.2.24 纪律走（permissive 可移植保留声明、copyleft 仅 clean-room、存疑按不允许）；p.2.7–p.2.24 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.24: p.2.25 is the Entity Scale milestone — **the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice) + runtime scale binding**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the p.2.24 AI-line landing state (engine.ai + runtime.ai + AiProbe); the Pehkui-Rebuilt distribution and its license/notice material (the MIT-port notice form and placement preserving Virtuoel's original copyright and the rebuild notice, referencing the p.2.21 GeckoLib MIT-port discipline); the existing deterministic capability surfaces (p.2.13 XoroRandom/seed semantics, p.2.8 budget-tier scheduling, p.2.2 datapack direct-load pattern); the runtime gated-shell pattern (subterra.probe.* default no-op + markers merged into existing E2E); the existing probeAcceptance wiring (95 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.25 row verbatim: “Entity scale / 实体缩放 engine.scale (moved up into the main line from the original p.3.2): the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice): ScaleType/ScaleData/ScaleModifier model, per-dimension/per-tag scaling, datapack scaling rules + runtime scale binding”. Illustrative directions (may merge): (1) **the engine.scale entity-scaling core (pure JDK, Pehkui-Rebuilt MIT port)** — a deterministic ScaleType/ScaleData/ScaleModifier model (entity-scaling data, per-dimension (width/height/lift-span, render/physical, etc.) scaling, per-tag scaling, fixed-order byte-identical), datapack scaling rules (td-ized rule loading, deterministic parse, possibly over the p.2.2 datapack direct-load pattern and the p.2.17 RuleStore two-tier semantics); the MIT-port notices preserved (Virtuoel's original copyright + the rebuild notice, materials kept with the library citing source and version, the form referencing the p.2.21 GeckoLib discipline); (2) **runtime.scale wiring (the runtime scale binding)** — MC entity-scaling lifecycle wiring (scaling-data load surface / drive-and-update surface / render-consumption surface), gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts; (3) **the scaling determinism/wiring probes** — core determinism (model fixed-order byte-identical, round-trip stable) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 95 pure-JVM + 5 E2E). Note: **the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and the rebuild notice) + runtime scale binding** is the ROAD delivery surface and runs through every design; **the license red line**: Pehkui-Rebuilt MIT **port must preserve Virtuoel's original copyright and the rebuild notice** (the form references the p.2.21 GeckoLib MIT-port discipline: notice materials kept with the library citing source and version); if scouting finds another third-party reference, follow the p.2.24 discipline first (permissive portable with notices preserved, copyleft clean-room only, uncertain defaults to not allowed); the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.7–p.2.24 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.25 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.25 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.25 子项一律 **p.2.25.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；缩放核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.25 红线）**：Pehkui-Rebuilt 为 **MIT 移植——保留 Virtuoel 原始版权与重建声明**（形态参考 p.2.21 GeckoLib 的 MIT 移植纪律：License/notice 材料原样随库保留并注明来源与版本；重建声明一并保留）；permissive 可移植并**保留声明**；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。p.2.25 只做 Pehkui-Rebuilt 一库移植，不新增其他移植。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（缩放若涉 native/FFM 按需）。
- **验收门（p.2.25 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**95 纯 JVM + 五次 E2E（新增 AiProbe 在内）**，**并新增本里程碑的缩放确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.25.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the scaling core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.25 red line)**: Pehkui-Rebuilt is an **MIT port — Virtuoel's original copyright and the rebuild notice preserved** (the form references the p.2.21 GeckoLib MIT-port discipline: License/notice materials kept verbatim with the library citing source and version; the rebuild notice is preserved too); permissive may be ported with notices; copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. p.2.25 adds no other port beyond Pehkui-Rebuilt. Acceptance gate (the p.2.25 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **95 pure-JVM + five E2E boots (incl. AiProbe)**, **plus the new scaling determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.25 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.25 各子项落地、最新 p 轨号 p.2.25.x、关键路径、实体缩放移植结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.26 Time scaling engine.time——clean-room 自研：TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值、TimeScaleApi + runtime 接线（接 engine.sim p.2.8）；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.26-time-scaling.md`（bilingual），随当次提交一并提交。

**English**: After p.2.25 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.25 landing, latest track p.2.25.x, key path, the entity-scaling-core-port conclusion; scope pointing to the next milestone, per ROAD row order **p.2.26 Time scaling engine.time** — clean-room self-developed: the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring (over the p.2.8 engine.sim); if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.26-time-scaling.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.25 工作区建议：engine.scale 实体缩放核心（Pehkui-Rebuilt MIT 移植，纯 JDK）+ runtime.scale 接线（runtime scale binding）+ 缩放确定性/接线探针。
- **p.2.24 AI behavior core（已落地，既有验收门）**：engine.ai 确定性 Brain 编排（Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask，固定注册序/重复拒/sense→decide→execute）+ 行为选择/调度（BehaviorPriority/BrainScheduler/AiRandom）+ runtime.ai AiRuntime 门控壳（subterra.probe.ai 缺省 no-op、[Subterra ai] ok marker 并入 AsyncE2EProbe）+ AiProbe 29 checks；probeAcceptance 全绿（**95 纯 JVM + 五次 E2E**）。
- **Pehkui-Rebuilt 移植落点（ROAD Port Allocation Map）**：实体缩放能力纳入框架 → **engine.scale**，MIT 移植，保留 Virtuoel 原始版权与重建声明（planned / 已立项）。
- **许可登记（p.2.25 为 MIT 移植）**：Pehkui-Rebuilt 为 MIT（permissive）→ 可移植并**保留声明**（Virtuoel 原始版权 + 重建声明，材料随库保留并注明来源与版本，形态参考 p.2.21 GeckoLib 的 MIT 移植纪律）；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。
- **probeAcceptance 接线基准（p.2.24 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **95 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.25 在此之上新增缩放确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.24 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.scale 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.24 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.24 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.25 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.24 AI behavior core（已落地，既有验收门；AI 线首项）/ p.2.26 Time scaling（ROAD 行序紧随 p.2.25，下一里程碑）；p.2.25 为实体缩放线首项（承接 p.2.13 engine.pcg / p.2.8 budget 能力面与 p.2.24 runtime 门控壳范式）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.25 landing: the engine.scale entity-scaling core (Pehkui-Rebuilt MIT port, pure JDK) + runtime.scale wiring (the runtime scale binding) + the scaling determinism/wiring probes. p.2.24 the AI behavior core (landed; the existing acceptance gate): the engine.ai deterministic Brain orchestration (Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask, fixed registration order / duplicate rejection / sense→decide→execute) + behavior-selection/scheduling (BehaviorPriority/BrainScheduler/AiRandom) + the runtime.ai AiRuntime gated shell (subterra.probe.ai default no-op, the [Subterra ai] ok marker merged into AsyncE2EProbe) + AiProbe 29 checks; probeAcceptance all green (**95 pure-JVM + five E2E boots**). Pehkui-Rebuilt port landing (ROAD Port Allocation Map): entity-scaling capability into the framework → **engine.scale**, MIT port preserving Virtuoel's original copyright and the rebuild notice (planned). License (p.2.25 is an MIT port): Pehkui-Rebuilt is MIT (permissive) → portable with notices preserved (Virtuoel's original copyright + the rebuild notice, materials kept with the library citing source and version, the form referencing the p.2.21 GeckoLib MIT-port discipline); copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. probeAcceptance wiring baseline (p.2.24 closing): **95 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.25 adds the scaling determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.24 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.scale wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.24): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.24 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.25 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.24 the AI behavior core (landed; the existing gate; the first AI-line item) / p.2.26 Time scaling (next ROAD row after p.2.25); p.2.25 is the first entity-scaling-line item (building on the p.2.13 engine.pcg / p.2.8 budget capability surfaces and the p.2.24 runtime gated-shell pattern).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 95 纯 JVM + 五次 E2E 与新增缩放探针）+ ROAD p.2.25 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 95 pure-JVM + five E2E boots plus the new scaling probes), the “ROAD p.2.25 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.