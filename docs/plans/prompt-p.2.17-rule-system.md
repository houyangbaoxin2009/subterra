# Subterra p.2.17 Rule system / 规则系统 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.16 全轨 landing 后，按 p.2.16 交接单 §5 生成指向 p.2.17 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.16 fully landed, §5 of the p.2.16 handoff produces this p.2.17 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.17 里程碑**的交接单：p.2.17 =「Rule system / 规则系统 engine.config.rules」——**类型化规则、双层配置（全局 + 存档覆盖，承接 p.2.16 RuleRegistry 语义）、校验、热重载、`/subterra rule` 命令**（自原 p.2.4 顺移，参考 RollingGate 模型，见 ROAD p.2.17 行；示意子项可由收尾时按仓库现状给演示）；规则系统以 p.2.16 规则注册 API（Rule/RuleSet/RuleRegistry）为契约基座——**api 契约 + engine 实现**双层落地（p.2.16.2 定契约形状，p.2.17 落引擎实现），既有双层语义来源为 engine.datapack.DatapackRules（p.2.2.9）与 p.2.3.5 resolveTiered。执行模式与 p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.17.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 api.config.RuleRegistry 契约现状与既有双层语义基座：p.2.16.2 Rule/RuleSet/RuleRegistry、engine.datapack.DatapackRules、p.2.3.5 双层配置 resolveTiered、engine 现有 config 结构与 /subterra 命令接线范式，以及 devkit 探针资产与 p.2.16 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.17-milestone handoff: p.2.17 = “Rule system / engine.config.rules” — **typed rules, two-tier configuration (global + save overrides, carrying the p.2.16 RuleRegistry semantics), validation, hot reload, and the `/subterra rule` command** (moved from the former p.2.4, modeled on RollingGate, per the ROAD p.2.17 row; illustrative sub-items are given at closing time per current repo state); the rule system sits on the p.2.16 rule-registration API (Rule/RuleSet/RuleRegistry) as its contract base — **api contract + engine implementation** landed on two layers (p.2.16.2 states the contract shape, p.2.17 lands the engine implementation); the existing two-tier semantics sources are engine.datapack.DatapackRules (p.2.2.9) and p.2.3.5 resolveTiered. Execution mode identical to the p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.17.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the api.config.RuleRegistry contract state and the existing two-tier semantics bases: the p.2.16.2 Rule/RuleSet/RuleRegistry, engine.datapack.DatapackRules, the p.2.3.5 two-tier resolveTiered, the engine config structure and the /subterra command wiring pattern, plus the devkit probe assets and the post-p.2.16 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.16 里程碑「API contract surface / api 契约面」已全部落地并已推 GitHub，最新 p 轨号 p.2.16.6（收尾提交）**：subterra-api 契约面（全纯 JDK，无 engine/MC 依赖）——**p.2.16.1** worldgen 契约（`NoiseRouterContract`：15 字段规范名与 engine 镜像逐字同序 + golden 锚点 seed=44905237 / golden_version=1 + 12 固定采样点 + 位模式布局，p.2.15 金标的数据底座化为契约形状，数值底座以数据注入、不反向依赖 engine）+ **p.2.16.2** config(rules) 契约 + 规则注册 API（`Rule`/`RuleSet`/`RuleRegistry`：固定注册序、同 key 重复拒绝、双层解析与 render 语义对齐 `DatapackRules`，**p.2.17 消费方前哨**）+ **p.2.16.3** export 契约 + Exporter 注册表（`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`：注册序查表、重复拒绝、form=world/save/datapack 与 engine `ExportKind` 逐一一致，p.2.18 前哨）+ **p.2.16.4** event 契约（`EventKind`/`EventEnvelope`/`EventStream`：monotonic-seq 门拒乱序/重放、类型化载荷可回放，承接 p.2.4/2.5/2.11）+ **p.2.16.5** `ContractProbe` 38 checks（worldgen 锚点桥接 / 规则注册 vs DatapackRules / 注册表固定序与重复拒 / 事件单调序门）接入 probeAcceptance。关键路径 **subterra-api 契约面**。结论：**API 契约面定型——四契约面 + 双注册表全纯 JDK、确定性可探针、金标底座数据化注入**；probeAcceptance 全绿（85 纯 JVM + 五次 E2E，新增 ContractProbe）。/ p.2.16 “API contract surface” fully landed and pushed, latest track **p.2.16.6** (closing commit); the subterra-api contract surface (all pure JDK, no engine/MC dependency) — p.2.16.1 worldgen contract (`NoiseRouterContract`: 15 canonical field names verbatim-ordered against the engine mirror + golden anchor seed=44905237 / golden_version=1 + 12 fixed sample points + bit-pattern layout; the p.2.15 golden value base shaped into a contract, numbers injected as data, never reverse-depending on engine) + p.2.16.2 config(rules) contract + rule-registration API (`Rule`/`RuleSet`/`RuleRegistry`: fixed registration order, duplicate-key rejection, two-tier resolve/render aligned with `DatapackRules`, **the p.2.17 consumer precursor**) + p.2.16.3 export contract + Exporter registry (`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`: registration-order lookup, duplicate rejection, form=world/save/datapack 1:1 with engine `ExportKind`, the p.2.18 precursor) + p.2.16.4 event contract (`EventKind`/`EventEnvelope`/`EventStream`: monotonic-seq gate rejecting reorder/replay, typed replayable payloads, over p.2.4/2.5/2.11) + p.2.16.5 `ContractProbe` 38 checks (worldgen anchor bridge / rule registry vs DatapackRules / registry fixed-order + dup-reject / event monotonic-seq gate) wired into probeAcceptance. Key path **subterra-api contract surface**. Conclusion: the API contract surface is typed — four contract surfaces + two registries all pure JDK, deterministic and probeable, the golden base injected as data; probeAcceptance all green (85 pure-JVM + five E2E boots, incl. ContractProbe).
- **p.2.17 目标（ROAD p.2.17 行，本次承接）**：Rule system / 规则系统 engine.config.rules（自原 p.2.4 顺移，参考 RollingGate 模型）——**类型化规则、双层配置（全局 + 存档覆盖）、校验、热重载、`/subterra rule` 命令**。p.2.16.2 RuleRegistry 是 p.2.17 引擎侧实现的**契约基座**（api 契约 + engine 实现：Rule/RuleSet/RuleRegistry 的固定注册序、同 key 重复拒绝、双层 resolve/render 语义在 p.2.17 由 engine.config.rules 消费为引擎侧实现）。/ p.2.17 goal (the ROAD row, this handoff): Rule system / engine.config.rules (moved from the former p.2.4, modeled on RollingGate) — **typed rules, two-tier configuration (global + save overrides), validation, hot reload, and the `/subterra rule` command**. The p.2.16.2 RuleRegistry is the **contract base** for the p.2.17 engine-side implementation (api contract + engine implementation: the fixed registration order, duplicate-key rejection and two-tier resolve/render semantics of Rule/RuleSet/RuleRegistry are consumed by engine.config.rules as the engine-side implementation).
- **p.2.16 及更早里程碑（已落地，事实不变）**：p.2.16 api 契约面（上述）、p.2.15 engine migration + golden tests（engine.* 落点稳定 + 180 位模式金标）、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session（engine.session）、p.2.10 saveverify、p.2.9 world-as-artifact（engine.export 端口预留）、p.2.8 sim、p.2.5 P2P / tink v2 / tsha1f / zd 栈、p.2.4 server-interaction、p.2.3 Save（**双层配置 resolveTiered：全局 → 存档胜出**）、p.2.2 datapack（**规则前哨 DatapackRules**，p.2.2.9：pack 序 + save 覆盖双层优先级）。/ p.2.16 and earlier milestones all landed (unchanged): p.2.16 api contract surface (above), p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 engine.session, p.2.10 saveverify, p.2.9 world-as-artifact (engine.export port reservation), p.2.8 sim, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save (**two-tier resolveTiered: global → save wins**), p.2.2 datapack (**the rule precursor DatapackRules**, p.2.2.9: pack-order then save-override two-tier precedence).
- **probeAcceptance 现状（p.2.16 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**85 个纯 JVM 探针**（含新增 `ContractProbe` 38 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.16 closing): green — **85 pure-JVM probes** (incl. the new ContractProbe 38 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.17 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；api 模块 `subterra-api\...\api\`（现有子包 config / crypto / export / event / migrate / net / ser / time / worldgen）为对外契约宿主；td（tiedb-td 文档）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12/p.2.13/p.2.14/p.2.15/p.2.16 钉死并复用；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.17 规则系统消费 p.2.16 契约基座——engine.config.rules 类型化规则、双层配置、校验、热重载、`/subterra rule` 命令；双层语义对齐 p.2.16.2 RuleRegistry（resolve/resolveTiered/render）与既有 DatapackRules（p.2.2.9）/ resolveTiered（p.2.3.5）；`/subterra rule` 命令接线沿用 p.2.2.7 `/subterra export` 范式（gradle -P 属性钩子确定性触发）；参考 RollingGate 模型（LGPL-3.0 clean-room，资产 CC-BY-NC-ND 不用）；与 p.2.23 in-game mod hub（配置编辑 + 热重载）衔接。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、subterra-api、engine/config、engine/datapack 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.11/p.2.12/p.2.13/p.2.14/p.2.15/p.2.16 closing patterns. p.2.16 “API contract surface” fully landed and pushed (latest track **p.2.16.6**, key path **subterra-api contract surface**): the four pure-JDK contract surfaces — the worldgen contract (NoiseRouterContract: 15 canonical field names + golden anchor seed/version + 12 fixed sample points + bit-pattern layout, the p.2.15 golden base shaped into a contract, numbers injected as data), the config(rules) contract + rule-registration API (Rule/RuleSet/RuleRegistry: fixed registration order, duplicate-key rejection, two-tier resolve/render aligned with DatapackRules, **the p.2.17 consumer precursor**), the export contract + Exporter registry (form=world/save/datapack 1:1 with engine.ExportKind, the p.2.18 precursor) and the event contract (monotonic-seq gate rejecting reorder/replay, typed replayable payloads) — plus ContractProbe 38 checks wired into probeAcceptance; conclusion: the API contract surface is typed, four contract surfaces + two registries all pure JDK, deterministic and probeable. p.2.17 defines the Rule system — engine.config.rules: typed rules, two-tier configuration (global + save overrides), validation, hot reload, `/subterra rule` command, per the ROAD p.2.17 row (modeled on RollingGate); the p.2.16.2 RuleRegistry is the contract base for the engine-side implementation. Two-tier semantics sources: DatapackRules (p.2.2.9) and p.2.3.5 resolveTiered (global → save wins). Everything pinned by p.2.12–p.2.16 (td-first, byte-identical deterministic, no-timing) stays the default semantics. probeAcceptance all green: **85 pure-JVM probes** (incl. ContractProbe) + **5 E2E boots**. Iron law auto-covers new engine packages. Confirm remaining context facts against the repo state (ROAD, git log, subterra-api, engine/config, engine/datapack) — never fabricate.

## 2. 本次范围（p.2.17 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 api.config.RuleRegistry 契约现状与既有双层语义基座：p.2.16.2 Rule/RuleSet/RuleRegistry、engine.datapack.DatapackRules、p.2.3.5 双层配置 resolveTiered、engine 现有 config 结构与 /subterra 命令接线范式、devkit 探针资产与 p.2.16 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（承接 p.2.16 已落地的契约基座与双层语义来源，为 p.2.17 落地规则系统引擎实现）：目标见 ROAD p.2.17 行：「类型化规则、双层配置（全局 + 存档覆盖）、校验、热重载、`/subterra rule` 命令」。示意子项（示例）：
1. **类型化规则模型**：engine.config.rules 类型化规则核心（类型系统、td schema 定型的规则文档形态、固定注册序消费 p.2.16.2 RuleRegistry 契约基座，参考 RollingGate 模型 clean-room，不引新行为、不照抄资产）。
2. **双层配置（全局 + 存档覆盖）**：规则存储两档（全局 td 文档 + 存档覆盖），resolve/render 语义对齐 p.2.16.2 RuleRegistry 与 p.2.3.5 resolveTiered（全局 → 存档胜出）。
3. **规则校验**：确定性校验（未知键、类型违约、冲突/重复拒绝）+ 校验报告，接入规则装载路径。
4. **热重载**：规则变更检测 + 确定性重载（同输入同输出、重入安全），门控钩子。
5. **`/subterra rule` 命令**：命令接线（沿用 p.2.2.7 `/subterra export` 的 gradle -P 确定性触发范式），查看 / 设置 / 覆盖命令语义。
6. **确定性规则探针**：规则系统探针（注册序、双层覆盖、校验违约、热重载重入、命令往返），接入 probeAcceptance。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**契约基座复用（p.2.16.2 RuleRegistry）+ 双层语义对齐 + 确定性校验 + 热重载重入 + 命令确定性触发 + 接入 probeAcceptance** 贯穿每次设计；p.2.12/p.2.13/p.2.14/p.2.15/p.2.16 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」范式必须被复用为默认语义。

**English**: Same principle as p.2.11–p.2.16: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the api.config.RuleRegistry contract state and the existing two-tier semantics bases: the p.2.16.2 Rule/RuleSet/RuleRegistry, engine.datapack.DatapackRules, the p.2.3.5 two-tier resolveTiered, the engine config structure and the /subterra command wiring pattern, plus the devkit probe assets and the post-p.2.16 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.17 row: “typed rules, two-tier configuration (global + save overrides), validation, hot reload, `/subterra rule` command.” Illustrative directions: (1) **typed rule model** — engine.config.rules typed rule core (type system, td-schema-typed rule doc form, fixed registration order consuming the p.2.16.2 RuleRegistry contract base, RollingGate clean-room model reference, no new behavior, no asset copying); (2) **two-tier configuration (global + save overrides)** — two rule stores (global td doc + save overrides), resolve/render semantics aligned with p.2.16.2 RuleRegistry and p.2.3.5 resolveTiered (global → save wins); (3) **rule validation** — deterministic validation (unknown keys, type violations, conflict/duplicate rejection) + a validation report, wired into the rule loading path; (4) **hot reload** — rule-change detection + deterministic reload (same input → same output, re-entrant), gated hooks; (5) **`/subterra rule` command** — command wiring (following the p.2.2.7 `/subterra export` gradle `-P` deterministic-trigger pattern), view / set / override verb semantics; (6) **deterministic rule probes** — rule-system probes (registration order, two-tier override, validation violations, hot-reload re-entry, command round-trip), wired into probeAcceptance. Note: **contract-base reuse (p.2.16.2 RuleRegistry) + two-tier semantics alignment + deterministic validation + hot-reload re-entry + deterministic command trigger + wired into probeAcceptance** runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12–p.2.16 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.17 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、规则引擎子代理、命令子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/契约核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.17 runs as a cluster of sub-agents (scouting, rule-engine, command, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.17 子项一律 **p.2.17.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；规则核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine（p.2.16.2 契约在 api 层，p.2.17 实现落 engine.config.rules，实现可依赖 api 契约、契约不得依赖实现）。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.17 Rule system 参考 RollingGate 模型（LGPL-3.0 clean-room 模型参考，类型化规则 / 双层配置 / 命令面板；**资产 CC-BY-NC-ND 不用**），不新增抄录；td schema 能力复用 p.2.12 engine.schema 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（85 纯 JVM + 五次 E2E，新增 ContractProbe 在内）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.17.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the rule core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api; the api module must not depend on engine (the p.2.16.2 contract lives in the api layer; the p.2.17 implementation lands in engine.config.rules and may depend on the api contract, never the reverse). Licensing: permissive reuse/derive with attribution only; p.2.17 Rule system models RollingGate (LGPL-3.0 clean-room model reference — typed rules / two-tier configuration / command panel; **assets CC-BY-NC-ND are not used**), no new copying; td schema capability reuses the p.2.12 engine.schema built-in. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (85 pure-JVM + five E2E boots, incl. ContractProbe).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.17 全轨完成后，必须生成 p.2.18 同格式提示词并纳入记录：更新上下文段（p.2.17 各子项落地、最新 p 轨号 p.2.17.x、关键路径、Rule system 结论）、更新本次范围段指向 p.2.18（Export hub，见 ROAD p.2.18 行；engine.export 正式导出：language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令，承接 p.2.16.3 Exporter 注册表契约基座）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.18-export-hub.md`（bilingual），随当次提交一并提交。

**English**: After p.2.17 fully lands, produce the p.2.18 prompt in the same format (context updated with p.2.17 landing, latest track p.2.17.x, key path, the Rule-system conclusion; scope pointing to p.2.18 Export hub, per ROAD p.2.18 — engine.export full export: language-keys / config / world / registries / migrate-maps, td/zd dual formats, `/subterra export` command, carrying the p.2.16.3 Exporter-registry contract base; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.18-export-hub.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.17 工作区建议：规则核心落 `engine.config.rules`（纯 JDK，可探针）+ devkit 规则探针 + `/subterra rule` 命令接线（runtime 命令处，沿用既有命令范式）。
- **p.2.16 API contract surface / api 契约面（已落地，p.2.16.6 收尾）**：subterra-api 契约面全纯 JDK——NoiseRouterContract（worldgen 黄金契约）+ Rule/RuleSet/RuleRegistry（config(rules) 契约与规则注册 API）+ ExportKindSpec/ExportPortSpec/ExporterRegistry（export 契约与注册表）+ EventKind/EventEnvelope/EventStream（event 契约）+ ContractProbe 38 checks 接入 probeAcceptance（85 纯 JVM + 5 次 E2E）。**p.2.16.2 RuleRegistry 是 p.2.17 引擎侧实现的契约基座**。
- **既有双层语义来源（p.2.17 复用，事实不变）**：engine.datapack.DatapackRules（p.2.2.9 双层规则：pack 序 + save 覆盖）与 p.2.3.5 resolveTiered（全局 → 存档胜出）；p.2.16.2 RuleRegistry.resolve/resolveTiered/render 已与 DatapackRules 对齐——p.2.17 engine.config.rules 直接消费该契约语义。
- **RollingGate 模型参考（LGPL-3.0 clean-room）**：类型化规则 / 双层配置 / 命令面板 → engine.config.rules；资产 CC-BY-NC-ND 不用。
- **`/subterra` 命令接线范式（p.2.2.7 已证明）**：`/subterra export` 经 gradle `-P` 属性钩子确定性触发（`subterra.probe.export` 转发到 runServer，startup 即跑同一核心，规避 forked server JVM 的 stdin 双层中转不可靠）；p.2.17 `/subterra rule` 沿用同一范式。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12/p.2.13/p.2.14/p.2.15/p.2.16 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.17 的规则系统与规则探针沿用同一约束；同输入 → 同输出。
- **td 一等语言（p.2.2/p.2.3/p.2.9/p.2.14/p.2.15/p.2.16 已确立）**：engine.datapack / engine.save / engine.export / engine.world / engine.interact——「td schema → 文档/注册」范式在 p.2.17 复用于规则文档形态（全局档 + 存档覆盖档）。
- **依赖铁律（p.2.0/p.2.15/p.2.16 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine（p.2.16.2 契约在 api 层，p.2.17 实现落 engine.config.rules，实现依赖 api 契约、契约不得依赖实现）。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.16 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变（p.2.17 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新规则探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.16 API contract surface（契约基座）/ p.2.18 export hub（Exporter 注册表正式导出）/ p.2.23 in-game mod hub（配置编辑 + 热重载衔接）；p.2.17 把 p.2.16 的规则契约基座落地为引擎侧规则系统（类型化规则 + 双层配置 + 校验 + 热重载 + `/subterra rule` 命令）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.17 landing: the rule core in engine.config.rules (pure JDK, probeable) + devkit rule probes + `/subterra rule` command wiring (runtime command site, following the existing command pattern). p.2.16 API contract surface (landed, closing p.2.16.6): the subterra-api contract surface, all pure JDK — NoiseRouterContract (worldgen golden contract) + Rule/RuleSet/RuleRegistry (config(rules) contract + rule-registration API) + ExportKindSpec/ExportPortSpec/ExporterRegistry (export contract + registry) + EventKind/EventEnvelope/EventStream (event contract) + ContractProbe 38 checks wired into probeAcceptance (85 pure-JVM + 5 E2E boots). **The p.2.16.2 RuleRegistry is the contract base for the p.2.17 engine-side implementation.** Existing two-tier semantics sources (reused, unchanged): engine.datapack.DatapackRules (p.2.2.9 two-tier rules: pack order + save override) and p.2.3.5 resolveTiered (global → save wins); the p.2.16.2 RuleRegistry resolve/resolveTiered/render is already aligned with DatapackRules — p.2.17 engine.config.rules consumes that contract semantics directly. RollingGate model reference (LGPL-3.0 clean-room): typed rules / two-tier configuration / command panel → engine.config.rules; assets CC-BY-NC-ND are not used. The `/subterra` command wiring pattern (p.2.2.7 proven): `/subterra export` deterministically triggered via a gradle `-P` property hook forwarded to runServer, running the same core at startup (stdin double-hop through the forked server JVM is unreliable); p.2.17 `/subterra rule` follows the same pattern. Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12–p.2.16 pinned): byte-identical, fixed order, no timing. td first-class language (p.2.2/p.2.3/p.2.9/p.2.14/p.2.15/p.2.16): the td-schema → document/registration paradigm reused for the rule doc forms (global tier + save-override tier). Dependency iron law (p.2.0/p.2.15/p.2.16): api ← engine ← runtime/migrate; the api module must not depend on engine (the p.2.16.2 contract lives in the api layer; the p.2.17 implementation lands in engine.config.rules and depends on the api contract, never the reverse). The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.16 proven) still applies (gate-off no-op, markers merged into AsyncE2EProbe, staging via build/tmp, five boots unchanged). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.16 API contract surface (contract base) / p.2.18 export hub (Exporter-registry full export) / p.2.23 in-game mod hub (config editing + hot-reload convergence); p.2.17 lands the p.2.16 rule contract base as the engine-side rule system (typed rules + two-tier configuration + validation + hot reload + `/subterra rule` command).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.17 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.17 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
