# Subterra p.2.24 AI Behavior Core / engine.ai · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.23 全轨 landing 后，按 p.2.23 交接单 §5 生成指向 p.2.24 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.23 fully landed, §5 of the p.2.23 handoff produces this p.2.24 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.24 里程碑**的交接单：p.2.24 =「AI behavior core / engine.ai」（自原 p.2.9 顺移）——**clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding**（见 ROAD p.2.24 行）。**本里程碑承接 p.2.23 全轨落地（engine.hub 表单/配置数据面 + runtime.hub 门控壳 + HubProbe，最新 p 轨号 p.2.23.5）与既有确定性能力面（p.2.7 engine.parallel 并行底座、p.2.8 engine.sim 增量模拟底座、p.2.21 AnimStateMachine 确定性状态机、p.2.13 engine.pcg 同种子生成底座）**。许可纪律总原则：permissive（MIT / Apache-2.0 / BSD / Unlicense / ISC）可移植并保留声明；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA 仅 clean-room 参考；存疑按不允许处理。**SmartBrainLib 为 MPL-2.0（copyleft），本里程碑仅 clean-room 模型参考——不直接包含、不复制代码、不捆绑源码**。执行模式与 p.2.23 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.24.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 p.2.23 Hub 线落点（engine.hub + runtime.hub + HubProbe）、既有确定性能力面（engine.parallel p.2.7 / engine.sim p.2.8 / engine.pcg p.2.13 / engine.anim 状态机 p.2.21）、原版 Brain 与 SmartBrainLib 的公开模型语义（memory/sensor/behavior/task 分层编排，MPL-2.0 仅思想参考）、runtime 门控壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.24-milestone handoff: p.2.24 = “AI behavior core / engine.ai” (carried over from the original p.2.9) — **clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding** (per the ROAD p.2.24 row). **This milestone builds on the fully-landed p.2.23 (the engine.hub form/config data plane + the runtime.hub gated shell + HubProbe, latest track p.2.23.5) and the existing deterministic capability surfaces (the p.2.7 engine.parallel parallel base, the p.2.8 engine.sim incremental simulation base, the p.2.21 AnimStateMachine deterministic state machine, the p.2.13 engine.pcg same-seed generation base)**. License discipline: permissive (MIT / Apache-2.0 / BSD / Unlicense / ISC) may be ported with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are clean-room reference only; uncertain defaults to not allowed. **SmartBrainLib is MPL-2.0 (copyleft): this milestone is clean-room model reference only — no direct inclusion, no copied code, no bundled source**. Execution mode identical to the p.2.23 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.24.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the p.2.23 Hub-line landings (engine.hub + runtime.hub + HubProbe), the existing deterministic capability surfaces (engine.parallel p.2.7 / engine.sim p.2.8 / engine.pcg p.2.13 / the engine.anim state machine p.2.21), the public model semantics of vanilla Brain and SmartBrainLib (the memory/sensor/behavior/task layered orchestration, MPL-2.0 ideas only), the runtime gated-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.23 里程碑「In-game mod hub / 游戏内模组 Hub」已全部落地并已推 GitHub，最新 p 轨号 p.2.23.5（收尾提交）**：In-game mod hub 落地——**p.2.23.1** engine.hub 表单自动生成核心（FormField/FormStructure/FormRenderData/HubFormGen，复用 p.2.12.4 FormDataGen 的 kind→widget 固定映射，td schema → 类型化表单结构，toTd/fromTd 往返恒等、render 与 FormDataGen 去头注释逐字节一致）+ **p.2.23.2** engine.hub 配置编辑 + 热重载核心（HubConfigEditor/HubConfigReloader/HubFormValues，复用 p.2.17 RuleStore/RuleReloader 语义：双层写 global+overrides、resolve 胜出、逐字节变更检测、原子 apply-or-keep-old、revision 单调、重入幂等；Reloader 直接委托 RuleReloader 不重复实现）+ **p.2.23.3** runtime.hub HubRuntime 门控壳（subterra.probe.hub 缺省 no-op、[Subterra hub] ok marker 并入 AsyncE2EProbe 同开服）+ /subterra hub op 配置命令（view/form/set/override 共享确定性核心 runVerb，承接 p.2.17 /subterra rule 范式）+ **p.2.23.4** HubProbe 27 checks 接入 probeAcceptance。关键路径 **engine.hub + runtime.hub + HubProbe**。结论：**In-game mod hub 落地——engine.hub 确定性表单/配置数据面（复用 p.2.12.4 + p.2.17 能力面）+ runtime.hub 门控壳 + /subterra hub op 配置命令 + HubProbe 27 checks**；probeAcceptance 全绿（94 纯 JVM + 五次 E2E，新增 HubProbe）。玩家侧表单 UI 接线为 p.2.23 后接线点（见 HubRuntime javadoc）。/ p.2.23 “In-game mod hub / 游戏内模组 Hub” fully landed and pushed, latest track **p.2.23.5** (closing commit): the in-game mod hub landed — p.2.23.1 the engine.hub auto-generated-forms core (FormField/FormStructure/FormRenderData/HubFormGen over the p.2.12.4 FormDataGen kind→widget fixed map: td schema → typed form structure, toTd/fromTd round-trip identity, render byte-identical to FormDataGen minus its header comments) + p.2.23.2 the engine.hub config-edit + hot-reload core (HubConfigEditor/HubConfigReloader/HubFormValues over the p.2.17 RuleStore/RuleReloader semantics: two-tier global+overrides writes, resolve-wins, byte-exact change detection, atomic apply-or-keep-old, monotonic revision, re-entrant idempotent; the Reloader delegates to RuleReloader, no re-implementation) + p.2.23.3 the runtime.hub HubRuntime gated shell (subterra.probe.hub default no-op, the [Subterra hub] ok marker merged into the AsyncE2EProbe boot) + the /subterra hub op config command (view/form/set/override over the shared deterministic runVerb core, the p.2.17 /subterra rule pattern) + p.2.23.4 HubProbe 27 checks wired into probeAcceptance. Key path **engine.hub + runtime.hub + HubProbe**. Conclusion: **the in-game mod hub landed — the engine.hub deterministic form/config data plane (reusing the p.2.12.4 + p.2.17 capability surfaces) + the runtime.hub gated shell + the /subterra hub op config command + HubProbe 27 checks**; probeAcceptance all green (94 pure-JVM + five E2E boots, incl. HubProbe). The player-side form-UI wiring is a post-p.2.23 wiring point (see the HubRuntime javadoc).
- **p.2.24 目标（ROAD p.2.24 行原文，本次承接）**：「AI behavior core / engine.ai（自原 p.2.9 顺移）：clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding」。/ p.2.24 goal (the ROAD row verbatim, this handoff): “AI behavior core / engine.ai (carried over from the original p.2.9): clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding”.
- **p.2.23 及更早里程碑（已落地，事实不变）**：p.2.23 In-game mod hub（上述，Hub 线首项，AI 线首项即本里程碑 p.2.24，ROAD 行序紧随）、p.2.22 UI/HUD 核心（engine.ui + UiRuntime + UiProbe）、p.2.21 动画核心 MIT 移植（engine.anim + AnimRuntime + AnimProbe）、p.2.27 自研渲染管线、p.2.27.1 Flywheel 实例化基座前哨、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移、p.2.18 导出枢纽、p.2.17 规则系统（engine.config.rules + `/subterra rule` 命令 + RuleReloader 热重载）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim（engine.sim 增量确定性模拟底座）、p.2.7 parallel（engine.parallel 确定性并行底座）、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。AI 线现状：p.2.7 已落确定性并行（ParallelRunner 并行/串行逐字节一致、重复键显式拒绝，AI 多实体并行 tick 不破种子确定性的底座）；p.2.8 已落增量确定性模拟底座（budget 预算分级 / spatial 分区 / core 增量确定性，AI tick 预算与编排可复用）；p.2.21 已落确定性状态机（AnimStateMachine 注册序/重复拒/过渡线性缓动/tick 回绕，AI 状态转移参考）；p.2.13 已落同种子生成底座（XoroRandom 语义，AI 决策随机源可复用）；SmartBrainLib 已在移植登记册登记（engine.ai，MPL-2.0 clean-room 参考）。/ p.2.23 and earlier milestones all landed (unchanged): p.2.23 the in-game mod hub (above, the first Hub-line item; this milestone p.2.24 is the first AI-line item, next ROAD row), p.2.22 the UI/HUD core (engine.ui + UiRuntime + UiProbe), p.2.21 the animation core MIT port (engine.anim + AnimRuntime + AnimProbe), p.2.27 the clean-room self-developed render pipeline, p.2.27.1 the Flywheel instancing-base guard, p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration, p.2.18 export hub, p.2.17 rule system (engine.config.rules + the `/subterra rule` command + the RuleReloader hot reload), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 diegetic interaction (engine.interact), p.2.13 engine.pcg, p.2.12 schema-first (engine.schema + engine.scaffold), p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim (the engine.sim incremental deterministic simulation base), p.2.7 parallel (the engine.parallel deterministic parallel base), p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack, p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. AI line: p.2.7 already landed deterministic parallelism (ParallelRunner parallel/serial byte-identical, explicit duplicate-key rejection — the base for deterministic multi-entity AI ticks); p.2.8 already landed the incremental deterministic simulation base (budget-tier / spatial / incremental core — reusable as AI tick budgeting and orchestration); p.2.21 already landed the deterministic state machine (AnimStateMachine registration order / duplicate rejection / linearly-eased transitions / tick wrap — an AI state-transition reference); p.2.13 already landed the same-seed generation base (XoroRandom semantics — reusable as the AI decision randomness source); SmartBrainLib is already registered in the port map (engine.ai, MPL-2.0 clean-room reference).
- **probeAcceptance 现状（p.2.23 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**94 个纯 JVM 探针**（含新增 `HubProbe` 27 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.24 的 engine 纯 JDK 落点自动纳入。p.2.24 验收基线 = **probeAcceptance 全绿（94 纯 JVM + 5 次 E2E，新增 HubProbe）+ 本里程碑新增的 AI 确定性/接线探针**。/ probeAcceptance (p.2.23 closing): green — **94 pure-JVM probes** (incl. the new HubProbe 27 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.24 pure-JDK engine landing is auto-covered. The p.2.24 acceptance baseline = **probeAcceptance all green (94 pure-JVM + 5 E2E boots, incl. HubProbe) + the new AI determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.24 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.23 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.24): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.23; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.24 承接 p.2.23（Hub 线落地为既有验收门，AI 确定性/接线探针叠加其上）与 p.2.7（engine.parallel 为 AI 多实体确定性并行底座）与 p.2.8（engine.sim 增量模拟底座为 AI tick 预算/编排参考）与 p.2.21（AnimStateMachine 确定性状态机为 AI 状态转移参考）；runtime ai binding 承接 p.2.22/p.2.23 runtime 门控壳范式（subterra.probe.* 缺省 no-op + marker 并入既有 E2E）与 p.2.19 统一接线总表（docs/runtime-wiring.md）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.24 continues from p.2.23 (the Hub-line landing is the existing gate; the AI determinism/wiring probes stack on it), from p.2.7 (engine.parallel as the deterministic multi-entity AI parallel base), from p.2.8 (the engine.sim incremental simulation base as the AI tick-budget/orchestration reference), and from p.2.21 (the AnimStateMachine deterministic state machine as an AI state-transition reference); the runtime ai binding continues the p.2.22/p.2.23 runtime gated-shell pattern (subterra.probe.* default no-op + markers merged into existing E2E) and the p.2.19 unified wiring inventory (docs/runtime-wiring.md). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.23 closing patterns. p.2.23 “In-game mod hub / 游戏内模组 Hub” fully landed and pushed (latest track **p.2.23.5**, key path **engine.hub + runtime.hub + HubProbe**): the in-game mod hub landed — the engine.hub deterministic form/config data plane (the p.2.23.1 auto-generated-forms core over the p.2.12.4 FormDataGen kind→widget fixed map; the p.2.23.2 config-edit + hot-reload core over the p.2.17 RuleStore/RuleReloader semantics: two-tier writes, byte-exact change detection, atomic apply-or-keep-old, monotonic revision, re-entrant idempotent, the Reloader delegating to RuleReloader) + the runtime.hub HubRuntime gated shell (subterra.probe.hub default no-op, the [Subterra hub] ok marker merged into the AsyncE2EProbe boot) + the /subterra hub op config command (view/form/set/override over the shared deterministic runVerb core) + HubProbe 27 checks wired into probeAcceptance; conclusion: **the in-game mod hub landed**; probeAcceptance all green (**94 pure-JVM + five E2E boots**, incl. HubProbe). p.2.24 defines the AI behavior core milestone — per the ROAD row verbatim: **clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding** (carried over from the original p.2.9). Everything pinned by p.2.12–p.2.23 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. **License red line: SmartBrainLib is MPL-2.0 (copyleft) — clean-room model reference only, never directly included (no copied code, no bundled source).** Acceptance baseline: **probeAcceptance all green (94 pure-JVM + 5 E2E boots, incl. HubProbe) + the new AI determinism/wiring probes**. Related: p.2.7 engine.parallel (the deterministic multi-entity parallel base), p.2.8 engine.sim (the incremental simulation base), p.2.21 AnimStateMachine (the deterministic state-machine reference), p.2.23 runtime.hub (the gated-shell pattern for the ai binding). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.24 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.24 =「AI behavior core / engine.ai」——**clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：p.2.23 Hub 线落点现状（engine.hub 数据面 + runtime.hub 门控壳 + HubProbe）；既有确定性能力面（p.2.7 engine.parallel 并行/串行逐字节一致、p.2.8 engine.sim budget/spatial/core、p.2.21 AnimStateMachine、p.2.13 XoroRandom/种子语义）；原版 Brain 与 SmartBrainLib 的公开模型语义（memory/sensor/behavior/task 分层编排、优先级/节流、MPL-2.0 仅思想参考、零代码包含）；runtime 门控壳接线范式（subterra.probe.* 缺省 no-op + marker 并入既有 E2E）；probeAcceptance 既有接线（94 纯 JVM + 5 E2E）与 FFM/依赖面），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.24 行原文）：「AI behavior core / engine.ai（自原 p.2.9 顺移）：clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding」。示意子项（示例，可合并）：
1. **engine.ai Brain 编排核心（纯 JDK，clean-room）**：参考 SmartBrainLib 公开模型语义——memory（记忆槽）/ sensor（感知器）/ behavior / task（行为/任务）分层编排核心（brain 持有类型化 memory、固定注册序 sensor/behavior、确定性扫描序、禁时序、同输入同输出）；MPL-2.0 仅 clean-room 参考（不直接包含、不复制代码）。
2. **engine.ai 行为选择 + 调度核心（纯 JDK）**：Brain tick 确定性调度（固定序 behavior 选择 / 优先级裁决 / 节流与 tick 预算，可承接 p.2.8 budget 语义与 p.2.7 ParallelRunner 多实体确定性并行）、可回放（同序列同终态）；随机源复用 p.2.13 XoroRandom 语义（同种子同决策序）。
3. **runtime.ai 门控壳 + ai binding**：subterra.probe.ai 缺省 no-op + `[Subterra ai]` ok marker 并入 AsyncE2EProbe 同开服（承接 p.2.22/p.2.23 门控壳范式）；MC 侧实体 Brain 绑定面文档化为后续接线点（玩家/实体侧接线不引 UI 注入）。
4. **AI 确定性/接线探针**：编排确定性（固定序逐字节一致、同输入同输出、回放一致、同种子同决策）+ 接线契约（runtime 面与既有 E2E markers 稳定性）探针接入 probeAcceptance（94 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding** 是 ROAD 原文的**交付面**，贯穿每次设计；**许可红线**：SmartBrainLib MPL-2.0 **仅 clean-room 模型参考——不直接包含、不复制代码、不捆绑源码**（与 DataTip GPL-3.0 / Patchouli CC-BY-NC-SA 同等级处理：仅思想参考）；若摸底发现需要参考其他第三方，先按 p.2.23 纪律走（permissive 可移植保留声明、copyleft 仅 clean-room、存疑按不允许）；p.2.7–p.2.23 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.23: p.2.24 is the AI behavior core milestone — **clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the p.2.23 Hub-line landing state (the engine.hub data plane + the runtime.hub gated shell + HubProbe); the existing deterministic capability surfaces (p.2.7 engine.parallel parallel/serial byte-identical, p.2.8 engine.sim budget/spatial/core, p.2.21 AnimStateMachine, p.2.13 XoroRandom/seed semantics); the public model semantics of vanilla Brain and SmartBrainLib (the memory/sensor/behavior/task layered orchestration, priority/throttling, MPL-2.0 ideas only, zero code inclusion); the runtime gated-shell pattern (subterra.probe.* default no-op + markers merged into existing E2E); the existing probeAcceptance wiring (94 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.24 row verbatim: “AI behavior core / engine.ai (carried over from the original p.2.9): clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding”. Illustrative directions (may merge): (1) **the engine.ai Brain orchestration core (pure JDK, clean-room)** — over the public SmartBrainLib model semantics: a typed-memory / sensor / behavior / task layered orchestration core (a brain holding typed memories, fixed registration order for sensors/behaviors, deterministic scan order, no timing, same input → same output; MPL-2.0 clean-room reference only — no direct inclusion, no copied code); (2) **the engine.ai behavior-selection + scheduling core (pure JDK)** — deterministic Brain-tick scheduling (fixed-order behavior selection / priority arbitration / throttling and tick budgets, possibly over the p.2.8 budget semantics and the p.2.7 ParallelRunner deterministic multi-entity parallelism), replayable (same sequence → same final state); the randomness source reuses the p.2.13 XoroRandom semantics (same seed → same decision order); (3) **the runtime.ai gated shell + ai binding** — subterra.probe.ai default no-op + a [Subterra ai] ok marker merged into the AsyncE2EProbe boot (the p.2.22/p.2.23 gated-shell pattern); the MC-side entity-Brain binding surface documented as a post-p.2.24 wiring point (no player-UI injection); (4) **the AI determinism/wiring probes** — orchestration determinism (fixed-order byte-identical, same input → same output, replay-consistent, same seed → same decisions) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 94 pure-JVM + 5 E2E). Note: **clean-room Brain orchestration core (referencing the SmartBrainLib model) + runtime ai binding** is the ROAD delivery surface and runs through every design; **the license red lines**: SmartBrainLib MPL-2.0 is **clean-room model reference only — never directly included (no copied code, no bundled source)** (same tier as DataTip GPL-3.0 / Patchouli CC-BY-NC-SA: ideas only); if scouting finds another third-party reference, follow the p.2.23 discipline first (permissive portable with notices preserved, copyleft clean-room only, uncertain defaults to not allowed); the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.7–p.2.23 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.24 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.24 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.24 子项一律 **p.2.24.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；AI 编排核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.24 红线）**：**SmartBrainLib 为 MPL-2.0（copyleft），本里程碑仅 clean-room 模型参考——不直接包含、不复制代码、不捆绑源码**（与 DataTip GPL-3.0 / Patchouli CC-BY-NC-SA 同等级处理：仅思想参考，零代码包含）；其余第三方按 p.2.23 纪律执行（permissive 可移植并**保留声明**；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7）。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（AI 若涉 native/FFM 按需）。
- **验收门（p.2.24 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**94 纯 JVM + 五次 E2E（新增 HubProbe 在内）**，**并新增本里程碑的 AI 确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.24.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the AI orchestration core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.24 red lines)**: **SmartBrainLib is MPL-2.0 (copyleft): this milestone is clean-room model reference only — never directly included (no copied code, no bundled source)** (same tier as DataTip GPL-3.0 / Patchouli CC-BY-NC-SA: ideas only, zero code inclusion); other third parties follow the p.2.23 discipline (permissive may be ported with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7). Acceptance gate (the p.2.24 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **94 pure-JVM + five E2E boots (incl. HubProbe)**, **plus the new AI determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.24 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.24 各子项落地、最新 p 轨号 p.2.24.x、关键路径、AI behavior core 落地结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.25 Entity scale / engine.scale——Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）+ runtime scale binding；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.25-entity-scale.md`（bilingual），随当次提交一并提交。

**English**: After p.2.24 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.24 landing, latest track p.2.24.x, key path, the AI behavior core conclusion; scope pointing to the next milestone, per ROAD row order **p.2.25 Entity scale / engine.scale** — the Pehkui-Rebuilt core port (MIT, preserving Virtuoel's original copyright and rebuild notice) + runtime scale binding; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.25-entity-scale.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.24 工作区建议：engine.ai Brain 编排核心（clean-room，参考 SmartBrainLib 模型）+ engine.ai 行为选择/调度核心（确定性、可回放）+ runtime.ai 门控壳 + AI 确定性/接线探针。
- **p.2.23 In-game mod hub（已落地，既有验收门）**：engine.hub 确定性表单/配置数据面（p.2.23.1 表单自动生成复用 p.2.12.4 FormDataGen kind→widget；p.2.23.2 配置编辑 + 热重载复用 p.2.17 RuleStore/RuleReloader：双层写、原子 apply-or-keep-old、revision 单调，Reloader 直接委托）+ runtime.hub HubRuntime 门控壳（subterra.probe.hub 缺省 no-op、[Subterra hub] ok marker 并入 AsyncE2EProbe）+ /subterra hub op 配置命令（view/form/set/override 共享 runVerb）+ HubProbe 27 checks；probeAcceptance 全绿（**94 纯 JVM + 五次 E2E**）。
- **可复用能力面（AI 编排的确定性底座）**：p.2.7 engine.parallel（ParallelRunner 并行/串行逐字节一致、重复键显式拒绝——多实体确定性并行 tick 底座）；p.2.8 engine.sim（budget 预算分级调度 / spatial 分区索引 / core 增量确定性——AI tick 预算与编排可复用）；p.2.21 AnimStateMachine（注册序/重复拒/过渡线性缓动/tick 回绕——AI 状态转移参考）；p.2.13 engine.pcg（XoroRandom 同种子同输出——AI 决策随机源）；p.2.16 api 契约面（event 契约 EventStream 单调序号门）。
- **许可登记（p.2.24 红线）**：**SmartBrainLib MPL-2.0 仅 clean-room 模型参考，不直接包含**（与 DataTip GPL-3.0 / Patchouli CC-BY-NC-SA 同等级处理：仅思想参考，零代码包含）；若摸底发现需参考其他第三方，按 p.2.23 纪律执行（permissive 可移植并**保留声明**；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA **仅 clean-room 参考，不直接包含**；存疑按不允许处理；完整登记册见框架设计 §7）。
- **probeAcceptance 接线基准（p.2.23 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **94 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.24 在此之上新增 AI 确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.23 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.ai 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.23 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.23 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.24 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.23 In-game mod hub（已落地，既有验收门；Hub 线首项）/ p.2.25 Entity scale（ROAD 行序紧随 p.2.24，下一里程碑）；p.2.24 为 AI 线首项（承接 p.2.7 engine.parallel / p.2.8 engine.sim / p.2.21 AnimStateMachine 能力面与 p.2.23 runtime 门控壳范式）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.24 landing: the engine.ai Brain orchestration core (clean-room, referencing the SmartBrainLib model) + the engine.ai behavior-selection/scheduling core (deterministic, replayable) + the runtime.ai gated shell + the AI determinism/wiring probes. p.2.23 the in-game mod hub (landed; the existing acceptance gate): the engine.hub deterministic form/config data plane (the p.2.23.1 auto-generated-forms core over the p.2.12.4 FormDataGen kind→widget; the p.2.23.2 config-edit + hot-reload core over the p.2.17 RuleStore/RuleReloader: two-tier writes, atomic apply-or-keep-old, monotonic revision, the Reloader delegating) + the runtime.hub HubRuntime gated shell (subterra.probe.hub default no-op, the [Subterra hub] ok marker merged into AsyncE2EProbe) + the /subterra hub op config command (view/form/set/override over the shared runVerb) + HubProbe 27 checks; probeAcceptance all green (**94 pure-JVM + five E2E boots**). Reusable capability surfaces (the deterministic base for AI orchestration): p.2.7 engine.parallel (ParallelRunner parallel/serial byte-identical, explicit duplicate-key rejection — the deterministic multi-entity parallel tick base); p.2.8 engine.sim (budget-tier scheduling / spatial index / incremental deterministic core — reusable as AI tick budgets and orchestration); p.2.21 AnimStateMachine (registration order / duplicate rejection / linearly-eased transitions / tick wrap — an AI state-transition reference); p.2.13 engine.pcg (XoroRandom same-seed-same-output — the AI decision randomness source); p.2.16 api contract surface (the event contract EventStream monotonic-seq gate). License (the p.2.24 red lines): **SmartBrainLib MPL-2.0 is clean-room model reference only, never directly included** (same tier as DataTip GPL-3.0 / Patchouli CC-BY-NC-SA: ideas only, zero code inclusion); if scouting finds another third-party reference, follow the p.2.23 discipline (permissive portable with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are **clean-room reference only, never directly included**; uncertain defaults to not allowed; the full register is in framework design §7). probeAcceptance wiring baseline (p.2.23 closing): **94 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.24 adds the AI determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.23 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.ai wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.23): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.23 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.24 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.23 the in-game mod hub (landed; the existing gate; the first Hub-line item) / p.2.25 Entity scale (next ROAD row after p.2.24); p.2.24 is the first AI-line item (building on the p.2.7 engine.parallel / p.2.8 engine.sim / p.2.21 AnimStateMachine capability surfaces and the p.2.23 runtime gated-shell pattern).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 94 纯 JVM + 五次 E2E 与新增 AI 探针）+ ROAD p.2.24 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 94 pure-JVM + five E2E boots plus the new AI probes), the “ROAD p.2.24 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
