# Subterra p.2.6 C2ME 直接包含（自原 p.3.0 前移）· 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.5 全轨 landing 后，按 p.2.5 交接单 §5 生成指向 p.2.6 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.5 fully landed, §5 of the p.2.5 handoff produces this p.2.6 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.6 里程碑**的交接单：p.2.6 =「C2ME 直接包含」（自原 p.3.0 前移）——**异步区块生成（chunk generation / loading / I/O）**核心（**engine.worldgen.async**，纯 JDK）+ **runtime 异步壳**（MC 接线），**随框架发布，MIT 声明**（C2ME 为 MIT 许可，NeoForge ver/1.21.1，逐字包含需保留原版权声明）。核心原则：多核并行**不破种子确定性**；落实顺序保持的确定性并行底座（与 p.2.7 engine.parallel 一脉相承，p.2.6 先行落地 worldgen 侧）。执行模式与 p.2.4 / p.2.5 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.6.x**（≤三级），禁止「阶段X / 第N块 / R1 / block N」等表述。工作方式：先**摸底**（读 C2ME 规范/源码、评估可并入的异步原语与确定性约束、定位既有 engine.worldgen.pipeline 的可异步化切面）并产出全轨子项拆分（6–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.6-milestone handoff: p.2.6 = “C2ME bundled directly” (moved up from original p.3.0) — **asynchronous chunk generation / loading / I/O** core (**engine.worldgen.async**, pure JDK) plus a **runtime async shell** (MC wiring), **shipped with the framework under an MIT notice** (C2ME is MIT-licensed, NeoForge 1.21.1 ver, verbatim inclusion must keep the original copyright notice). Core principle: multi-core parallelism that **never breaks seed determinism**; lands an order-preserving deterministic parallel base (consistent with p.2.7 engine.parallel — p.2.6 delivers the worldgen side first). Execution mode is identical to p.2.4 / p.2.5 closing: the main agent only dispatches and trust-but-verifies; scoping/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.6.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (read the C2ME spec/source, assess which async primitives can be incorporated and the determinism constraints, and locate the asynchronizable surfaces in the existing engine.worldgen.pipeline) to produce the full-track sub-item split (6–8), then implement sub-item by sub-item, commit after each, and do a final unified review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.5 P2P 去中心化网络已全部落地并推 GitHub**（提交见 git log；编号 p.2.5.1–p.2.5.8，最新 p 轨号 **p.2.5.8**）。engine.p2p 纯 JDK 落地（全部对齐 p.2.4 ABI，**复用 p.2.4 engine.network 同栈**）：
  * **p.2.5.1** 可行性基准 `P2pChannelBenchmarkProbe`：**zd 作通道可行性结论：zd 作载荷通道可行**——raw-vs-zd 体积比约 0.47–0.51（即 zd 体积约为 raw 的 **2× 以内**），编解码吞吐（如 medium-table encode 5.1 / decode 19.7 ops/ms）满足自建通道需求；帧级强校验开销恒定（payload=256 帧 16.4%→1MiB 0.004%，1MiB strong 仅 42B 表头）。
  * **p.2.5.2 engine.p2p**：`NodeId`／`NodeAddr`（direct/viaRelay）／`AddressBook`——节点寻址。
  * **p.2.5.3 engine.p2p.handshake**：确定性握手 `HandshakeKey/Frame/Peer/Result`，x25519 密钥 + NodeId 双向绑定、篡改/错钥/伪造/乱序/重放拒绝。
  * **p.2.5.4 engine.p2p.transport**：帧级传输 `P2pChannel/Packet/SeqCounter/ChannelResult`——tink v2 stream split/join 上的会话序号、有序重排、去重、确定性丢包/篡改/重放拒绝。
  * **p.2.5.5 engine.p2p.payload**：zd 载荷通道 ABI `ZdChannelCodec/RawRows/P2pMessageType`——payload==zd doc + 类型信封，channel meta 走 tink v2 ext key9。
  * **p.2.5.6 engine.p2p.plan**：`ChannelPlanner/ChannelProfile/P2pSemantics/PlanReport`——策略选型 + 带宽优化的编排。
  * **p.2.5.7 engine.p2p.discovery**：`KademliaLite/KademliaResult/RouteCandidate`——DHT 无中心发现（结构核心，RPC/线上层留 runtime）。
  * **p.2.5.8 engine.p2p.relay**：`RelayForwarder/RelayEndpoint/ForwardRequest/ForwardResult`——志愿 relay 兜底（登记→转发→投递、未登记 REJECT、超跳 DROPPED、shutdown 清表）；**relay 关停钩子已接 MC 壳**：`runtime.network.EnhancedChannelRuntime` 在 `ServerStoppingEvent` 上清空 relay 登记表（新增 `[Subterra relay]` marker 行，仅增量、既有 `[Subterra network]` markers 与 `original-path-preserved` 原样保留）。
- **去中心化原则已全量落实**：打洞直连为主（NodeAddr.direct）+ DHT 无中心发现（KademliaLite，p.2.5.7）+ 志愿 relay 兜底（p.2.5.8）——无中心账号/会话。
- **probeAcceptance 全绿**：`gradlew :subterra-devkit:probeAcceptance` 通过——61 个纯 JVM 探针（含 p.2.4 六探针、p.2.5 八探针）+ 四次 E2E 开服（BootProbe / DatapackE2EProbe / SaveE2EProbe / NetworkE2EProbe）全部 PASS，86 个 p.2.5 相关 + 原有断言全过；铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）被 ironLawProbe 恒定覆盖。
- **已有可复用世界生成堆栈**（p.1.8 框架化基线，p.2.6 异步化的上游）：`engine.worldgen.pipeline`（九维模型 `/dimension/`、密度函数 `/noise/`、噪声 `/noise/simplex|perlin/`、router `/router/`、surface `/surfacerules/`、grid `/grid/`、composite `/composite/`）+ `flowSched`/调度原语（p.1.4.23，`engine.optim.sched`）+ 既有 runtime 世界生成 MC 壳（`SameSeedCompareHook`/`WorldProfilerHook`）与确定性 `-D`/`-P` 钩子范式。
- **ABI / 接线事实（p.2.4 / p.2.5 已钉死，p.2.6 复用）**：`FrameV2` 线格式 / `tsha1f` / `SecureChannel` 等网络 ABI 与本次无关，但**同栈复用纪律适用**；p.2.6 主要承接 `engine.worldgen.*` 已有 ABI 与 runtime 世界生成壳的装载范式。分层铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；MC 壳落根 sourceSet；iron law 探针自动覆盖新增 engine.worldgen.async 包（常量池扫描）。
- **C2ME 参照（C2ME 规范/源码）**：C2ME（Concurrent Chunk Management Engine，ishland，MIT）异步区块生成/加载/I/O 设计；NeoForge ver / MC 1.21.1。MIT 直接包含需保留原版权声明；逐字取用需与现有 worldgen 管线接口对齐，不可耦合 MC 类（shell 侧接线）。
- **原 p.3.0 前置（本里程碑原是 p.3.0）**：异步区块生成/加载/I/O + FlowSched 底座，p.1.4.13 可选采纳（升级为直接包含）。p.2.6 落地后 p.2.7（engine.parallel 确定性并行底座）与 p.2.8（engine.sim）可接续。

**English**: See the Chinese block above — this handoff follows the exact p.2.5 closing patterns. All 8 p.2.5 sub-items (p.2.5.1–p.2.5.8, latest track **p.2.5.8**) landed and pushed; key paths are `engine.p2p` (p2p / handshake / transport / payload / plan / discovery / relay), all ABI-aligned with p.2.4 and reusing the p.2.4 engine.network stack. Feasibility conclusion for the zd payload channel: **zd-as-channel works** — raw-vs-zd volume ratio ≈ 0.47–0.51 (zd ≈ within 2× of raw), encode/decode throughput meets the self-built channel requirement, and frame strong-integrity overhead is constant (1MiB strong ≈ 42 B header). Decentralization principle fully implemented (direct hole-punching + DHT discovery + voluntary relay fallback, no central account/session). Relay teardown hook is already wired into the MC shell `ServerStoppingEvent` (additive `[Subterra relay]` marker line; existing `[Subterra network]` markers and `original-path-preserved` untouched). `probeAcceptance` is all green (61 pure-JVM probes + 4 E2E boots PASS). The existing `engine.worldgen.pipeline` stack (dimension / noise / router / surfacerules / grid / composite) + `flowSched` scheduling primitives (p.1.4.23) + the runtime worldgen MC shells are the asynchronization upstream. C2ME is MIT (NeoForge ver / MC 1.21.1); verbatim inclusion must keep the original copyright; take care not to couple MC classes into the engine core — MC wiring belongs in the runtime shell.

## 2. 本次范围（p.2.6 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（读 C2ME 规范/源码、评估可并入异步原语、定位 engine.worldgen.pipeline 异步化切面、确认确定性约束），再定 6–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（承接现有 worldgen 堆栈 + 参考 C2ME 异步区块生成模型）：
1. **异步调度底座**：确定性任务调度/工作窃取原语（前置摸底：可否直接并入 FlowSched 原语或需在 engine.worldgen.async 单建）——顺序保持、同输入同产出。探针：确定性执行序 + 同种子并行与串行逐位一致。
2. **异步区块生成核心**：区块生成流水线的可并行分发/合并框架（区块生产者/消费者、优先队列、依赖排序）——产出与同步路径逐区块一致。探针：多任务并行 vs 串行结果逐位一致 + 确定性分发序。
3. **确定性随机与区块上下文隔离**：每个区块任务独立随机上下文，禁止跨任务共享易变状态（并行安全）。探针：并行吞吐区块序列与串行 golden 逐位一致 + 无竞态污染。
4. **加载 / I/O 异步壳**：区块加载 + zd/存档 I/O 的异步化（接既有 save/zd 栈），runtime 侧加载生命周期接线，默认关（不改变原版生命周期，渐进增强）。探针：异步 I/O 结果与同步一致 + 门控 marker。
5. **E2E 异步生成门控**（若必要）：真实世界生成生命周期下的异步生成开关，确定性 marker 断言逐区块一致性，保持既有 markers 稳定。探针：E2E 开服（沿用既有 mustRunAfter 链，不加新开服若可并入既有壳）。
6. **扫描确认 extension 声明 / 许可 / 构建**：MIT 声明与版权保留、probeAcceptance 全量接线、iron law 覆盖 engine.worldgen.async。
提示：以上为示意（6–8 个子项以摸底后最小闭环为准）；**不破种子确定性**与「engine 纯 JDK 不碰 MC」贯穿每次设计。

**English**: Same principle as p.2.4/p.2.5: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first**, then produce a 6–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions building on the existing worldgen stack and the C2ME async chunk-generation model: (1) a deterministic scheduling/async dispatch base with order preservation and run-identical-to-serial guarantees; (2) the async chunk-generation core with a parallel dispatch/merge framework that is block-for-block identical to the synchronous path; (3) per-chunk isolated random/context to make parallelism safe, verified against serial golden output; (4) async load/I-O shell (zd/save aware), runtime lifecycle-wired, off by default (progressive enhancement, vanilla lifecycle untouched); (5) an E2E async-generation gate that asserts block-for-block consistency and keeps existing markers stable (reuse the existing boot chain, no new boot probe unless unavoidable); (6) licensing/MIT notice/build & iron-law wiring confirmation. Never break seed determinism; engine stays pure JDK, no MC coupling (MC wiring only in the runtime shell).

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.6 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/注入核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.6 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.6 子项一律 **p.2.6.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；异步核心并行度有上限、不放大主线程热路径阻塞。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：C2ME 为 MIT 直接包含，**逐字包含必须保留原版权声明（LICENSE/源文件头）并随框架 MIT 声明**；copyleft 仅 clean-room；存疑按不允许处理。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含 BootProbe / DatapackE2EProbe / SaveE2EProbe / NetworkE2EProbe 四次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.6.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound parallel core, don't block the main thread hot path. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. C2ME is MIT direct-inclusion: keep the original copyright (LICENSE/source header) and ship under the framework MIT notice. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the four E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.6 全轨完成后，必须生成 p.2.7 同格式提示词并纳入记录：更新上下文段（p.2.6 各子项落地、最新 p 轨号 p.2.6.x、关键路径 engine.worldgen.async、异步确定性结论）、更新本次范围段指向 p.2.7（engine.parallel 确定性并行执行底座，服务 p.2.6 C2ME 与 p.2.8 engine.sim）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.7-parallel.md`（bilingual），随当次提交一并提交。

**English**: After p.2.6 fully lands, produce the p.2.7 prompt in the same format (context updated with p.2.6 landing, latest track p.2.6.x, key path engine.worldgen.async, async-determinism conclusion; scope pointing to p.2.7 engine.parallel; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.7-parallel.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.6 工作区建议：`subterra-engine\...\engine\worldgen\async\`（异步区块生成核心，纯 JDK）与既有 `engine\worldgen\pipeline\`（可异步化上游）；MC 壳落 `subterra-runtime\...\runtime\worldgen\async\`（异步壳）。
- **p.2.5 已落地（可复用/避免重复）**：`engine.p2p` 全套（端点/握手/传输/zd 载荷/规划/发现/relay，纯 JDK）+ `engine.network` 同栈（p.2.4）+ relay 关停钩子已接 `runtime.network.EnhancedChannelRuntime`（ServerStoppingEvent）。
- **p.2.6 异步生成关键路径参考（既有 C2ME 规范 / 原 p.3.0 前置）**：① 调度原语：`engine.optim.sched` `StatusAdvancingScheduler` + p.1.4.23 `FlowSchedProbe`（先摸底可否直接并入）；② 可异步化上游：`engine.worldgen.pipeline/dimension|noise|router|surfacerules|grid|composite`（各探针已在 probeAcceptance，异步化不得破坏其逐位 golden）；③ 确定性随机/上下文：`engine.worldgen.pipeline/noise`（`XoroRandom`/`LegacyRandom`/`PositionalRand` 已有可复现随机）+ C2ME 的每区块独立任务语义；④ runtime 壳范式：`runtime.worldgen.compare.SameSeedCompareHook` / `runtime.worldgen.profiler.WorldProfilerHook`（事件驱动 + 确定性 `-D`/`-P` 钩子）+ `EnhancedChannelRuntime` 门控范式（默认 no-op、原版零接触）；⑤ 存档/IO 异步接入：`engine.save` + `engine.zd`（p.2.3）+ `runtime.save.SaveRuntime`。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 async 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用 has mustRunAfter 既有开服门；staging 用 build/tmp）。
- 关联里程碑：p.2.4（网络同栈）/p.2.5（P2P，已落地）；p.2.7 engine.parallel（地基）；p.2.8 engine.sim（下游）；既立项「C2ME MIT 直接包含（NeoForge ver/1.21.1）」。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.6 work dirs: `subterra-engine\...\engine\worldgen\async\` (core, pure JDK) over the existing `engine\worldgen\pipeline\` (the asynchronizable upstream); MC shell in `subterra-runtime\...\runtime\worldgen\async\`. p.2.5 already landed (reuse/avoid duplication): the full `engine.p2p` set + p.2.4 `engine.network` stack + relay teardown hook wired in `runtime.network.EnhancedChannelRuntime`. p.2.6 async-generation key paths (per C2ME spec and the former p.3.0 predecessor): scheduling primitives `engine.optim.sched` `StatusAdvancingScheduler` + p.1.4.23 `FlowSchedProbe`; the asynchronizable worldgen pipeline (dimension/noise/router/surfacerules/grid/composite — must not break its per-bit golden probes); deterministic random/context (XoroRandom/LegacyRandom/PositionalRand + per-chunk task semantics); runtime shell patterns (SameSeedCompareHook / WorldProfilerHook / the EnhancedChannelRuntime gating style — default no-op, zero vanilla impact); save/zd I-O async ingest (engine.save + engine.zd + runtime.save.SaveRuntime). New async probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.4/p.2.5 (landed); p.2.7 engine.parallel (foundation); p.2.8 engine.sim (downstream); the registered "C2ME MIT direct-inclusion (NeoForge 1.21.1 ver)".

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与四次开服）+ ROAD p.2.6 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the four boots), the “ROAD p.2.6 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.