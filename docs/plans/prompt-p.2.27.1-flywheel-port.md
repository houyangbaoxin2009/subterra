# Subterra p.2.27.1 Flywheel port / 飞轮移植前哨（形态 A）· 全轨一次性交接单（一库一子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.20 全轨 landing 后，按 p.2.20 交接单 §5 生成指向 p.2.27.1 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.20 fully landed, §5 of the p.2.20 handoff produces this p.2.27.1 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.27.1 里程碑**的交接单：p.2.27.1 =「Flywheel port / 飞轮移植前哨」（2026-09-09 新增，**形态 A**）——**Flywheel 1.0.6（1.21.1/NeoForge 线，MIT 直接包含，保留声明）→ engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 钩子）+ 装载与后端确定性探针；作为 p.2.27 实例化基座前哨（一库一子项）**（见 ROAD p.2.27.1 行）。**本里程碑为「一库一子项」形态**：单个自足最小闭环（Flywheel 纯 JDK 实例化核心移植 + runtime.render 接线 + 确定性探针），可按仓库现状精简为单一子项（p.2.27.1.1）完成；p.2.20 已把 devkit 完整化并以最小「颠覆性模组」官方示例兼作框架自举验收（probeAcceptance 全绿 89 纯 JVM + 5 次 E2E），本里程碑在**既有验收门之上**叠加 Flywheel 装载与后端确定性探针。执行模式与 p.2.20 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.27.1.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 Flywheel 1.0.6 发行物与许可/声明材料、既有 engine.render 落点与 runtime 壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.27.1-milestone handoff: p.2.27.1 = “Flywheel port / 飞轮移植前哨” (added 2026-09-09, **form A**) — **Flywheel 1.0.6 (the 1.21.1/NeoForge line, MIT direct-include with notices preserved) → engine.render.instancing (instance format / shader templates / backend SPI, pure JDK) + runtime.render wiring (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher hooks) + load-and-backend deterministic probe; the instancing-base guard for p.2.27 (one library, one sub-item)** (per the ROAD p.2.27.1 row). **This milestone is a one-library one-sub-item shape**: a single self-contained minimal closure (Flywheel pure-JDK instancing-core port + runtime.render wiring + a deterministic probe), which may be slimmed to one sub-item (p.2.27.1.1) per the current repo state; p.2.20 already completed the devkit and used the minimal “vanilla-overturning mod” official sample as the framework self-bootstrap acceptance (probeAcceptance all green, 89 pure-JVM + five E2E boots), and this milestone adds the Flywheel load-and-backend deterministic probe **on top of the existing acceptance gate**. Execution mode identical to the p.2.20 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.27.1.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the Flywheel 1.0.6 distribution and its license/notice material, the existing engine.render landing points and the runtime-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.20 里程碑「devkit complete / devkit 完整化 + 官方示例模组脚手架」已全部落地并已推 GitHub，最新 p 轨号 p.2.20.6（收尾提交）**：devkit 完整化 + 官方示例模组脚手架——**p.2.20.1** devkit 接线盘点（probeAcceptance 任务面全量审计：93 任务 = 88 纯 JVM + 5 E2E 全接线、零缺口、E2E 链完整）+ **p.2.20.2** devkit 任务面完整化（探针统一收编 verification 分组、一次性生成器隔离 devkit 分组：VanillaGoldenGen / WorldProfileAcceptance 不入 probeAcceptance、验收描述更新 93 任务，零行为变化）+ **p.2.20.3** 官方示例模组骨架 `samples/overturn-minimal`（双语 README + 最小 NeoForge 装配清单 neoforge.mods.toml + 纯 JDK 可编译入口 OverturnMinimal + 最小 td 规则包 pack.td + settings include）+ **p.2.20.4** 示例能力面（OverturnMinimal 逐一显式消费框架纯 JDK 能力面：规则双层 RuleStore 覆盖档胜出 / td 数据包直载 / export td+zd 逐字节恒等 / tie skip-or-ok，确定性报告行、同输入同字节）+ **p.2.20.5** `SampleBootstrapProbe` 13 checks「示例即验收」（跑 OverturnMinimal.main、System.out 捕获还原，断言固定序报告行 + 从 classpath 盘点示例结构，接入 probeAcceptance：94 任务 = 89 纯 JVM + 5 E2E）。关键路径 **devkit（subterra-devkit/build.gradle probeAcceptance 任务面）+ samples/overturn-minimal + SampleBootstrapProbe**。结论：**devkit 完整化 + 官方示例模组脚手架——probeAcceptance 93/94 任务全量接线零缺口 + devkit 任务面完整化（verification 分组 + 生成器隔离）+ 最小颠覆性模组示例 samples/overturn-minimal（双语 README + 清单式结构 + 能力面：规则双层 / td 数据包直载 / export td/zd 恒等 / tie skip-or-ok，确定性报告）+ SampleBootstrapProbe 13 checks「示例即验收」接入 probeAcceptance**；probeAcceptance 全绿（89 纯 JVM + 五次 E2E，新增 SampleBootstrapProbe）。/ p.2.20 “devkit complete / devkit 完整化 + official sample mod scaffold” fully landed and pushed, latest track **p.2.20.6** (closing commit): devkit complete + official sample mod scaffold — p.2.20.1 devkit wiring inventory (full probeAcceptance task-surface audit: 93 tasks = 88 pure-JVM + 5 E2E fully wired, zero gaps, E2E chain intact) + p.2.20.2 devkit task-surface completion (probe tasks grouped under verification, one-time generators isolated to the devkit group — VanillaGoldenGen / WorldProfileAcceptance stay out of probeAcceptance, acceptance description updated to 93 tasks, no behavior change) + p.2.20.3 the official sample scaffold `samples/overturn-minimal` (bilingual README + minimal NeoForge assembly manifest + compilable pure-JDK entry OverturnMinimal + minimal td rules pack + settings include) + p.2.20.4 the sample capability surfaces (OverturnMinimal explicitly exercises each framework pure-JDK surface: two-tier RuleStore overrides win / td datapack direct load / export td+zd byte-identical identity / tie skip-or-ok, deterministic report lines, same input → same bytes) + p.2.20.5 `SampleBootstrapProbe` 13 checks — “sample-is-the-acceptance” (runs OverturnMinimal.main, System.out captured/restored, asserts the fixed-order report lines + inventories the sample structure from classpath, wired into probeAcceptance: 94 tasks = 89 pure-JVM + 5 E2E). Key path **devkit (the probeAcceptance task surface in subterra-devkit/build.gradle) + samples/overturn-minimal + SampleBootstrapProbe**. Conclusion: **devkit complete + official sample mod scaffold — probeAcceptance 93/94 tasks fully wired with zero gaps + the devkit task surface completed (verification grouping + generator isolation) + the minimal vanilla-overturning sample samples/overturn-minimal (bilingual README + manifest structure + capability surfaces: two-tier rules / td datapack direct load / export td/zd identity / tie skip-or-ok, deterministic report) + SampleBootstrapProbe 13 checks “sample-is-the-acceptance” wired into probeAcceptance**; probeAcceptance all green (89 pure-JVM + five E2E boots, incl. SampleBootstrapProbe).
- **p.2.27.1 目标（ROAD p.2.27.1 行原文，本次承接）**：「Flywheel port / 飞轮移植前哨（2026-09-09 新增，形态 A）：Flywheel 1.0.6（1.21.1/NeoForge 线，MIT 直接包含，保留声明）→ engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 钩子）+ 装载与后端确定性探针；作为 p.2.27 实例化基座前哨（一库一子项）」。/ p.2.27.1 goal (the ROAD row verbatim, this handoff): “Flywheel port / 飞轮移植前哨 (added 2026-09-09, form A): Flywheel 1.0.6 (the 1.21.1/NeoForge line, MIT direct-include with notices preserved) → engine.render.instancing (instance format / shader templates / backend SPI, pure JDK) + runtime.render wiring (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher hooks) + a load-and-backend deterministic probe; the instancing-base guard for p.2.27 (one library, one sub-item)”.
- **p.2.20 及更早里程碑（已落地，事实不变）**：p.2.20 devkit 完整化 + 官方示例模组脚手架（上述）、p.2.19 runtime 接线迁移（六类迁入 + export 收编单核心）、p.2.18 导出枢纽（engine.export 七 form）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（td 直载 + `/subterra export` 范式）、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。渲染线现状：p.2.27.1 为渲染线首项，p.2.27 自研渲染管线在其后（ROAD 行序紧随）。/ p.2.20 and earlier milestones all landed (unchanged): p.2.20 devkit complete + official sample scaffold (above), p.2.19 runtime wiring migration (six-category fold-in + export single-core fold-in), p.2.18 export hub (engine.export seven forms), p.2.17 rule system (engine.config.rules + `/subterra rule`), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (td direct loading + the `/subterra export` pattern), p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Render line: p.2.27.1 is the first render-line item, followed by p.2.27 self-developed rendering (next ROAD row).
- **probeAcceptance 现状（p.2.20 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**89 个纯 JVM 探针**（含新增 `SampleBootstrapProbe` 13 checks「示例即验收」+ 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.27.1 的 engine.render.instancing 纯 JDK 落点自动纳入。p.2.27.1 验收基线 = **probeAcceptance 全绿（89 纯 JVM + 5 次 E2E，新增 SampleBootstrapProbe）+ 新增 Flywheel 装载与后端确定性探针**。/ probeAcceptance (p.2.20 closing): green — **89 pure-JVM probes** (incl. the new SampleBootstrapProbe 13 checks “sample-is-the-acceptance” + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.27.1 pure-JDK engine.render.instancing landing is auto-covered. The p.2.27.1 acceptance baseline = **probeAcceptance all green (89 pure-JVM + 5 E2E boots, incl. SampleBootstrapProbe) + the new Flywheel load-and-backend deterministic probe**.
- **ABI / 接线事实（已钉死，p.2.27.1 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.20 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.27.1): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.20; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.27.1 承接 p.2.20（devkit 完整化 + 示例自举验收为既有验收门，Flywheel 探针叠加其上）；「Flywheel 移植」复用既有 port 许可纪律（ROAD Port Allocation Map 第 96 行：Flywheel —— GPU 实例化渲染 + 着色器模板系统（实体 / 方块实体路径）→ engine.render.instancing + runtime.render，MIT 直接包含（形态 A，保留声明；p.2.27.1 前哨），planned / 已立项）；衔接 p.2.27 自研渲染管线（ROAD 行序紧随 p.2.27.1，本里程碑为其实例化基座前哨）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.27.1 continues from p.2.20 (devkit complete + the sample self-bootstrap acceptance is the existing gate; the Flywheel probe stacks on it); the “Flywheel port” reuses the pinned port license discipline (ROAD Port Allocation Map row 96: Flywheel — GPU instanced rendering + shader-template system (entity / block-entity paths) → engine.render.instancing + runtime.render, MIT direct-include (form A, notices preserved; the p.2.27.1 guard), planned); p.2.27 self-developed rendering follows (next ROAD row; this milestone is its instancing-base guard). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.12–p.2.20 closing patterns. p.2.20 “devkit complete + official sample mod scaffold” fully landed and pushed (latest track **p.2.20.6**, key path **devkit + samples/overturn-minimal + probeAcceptance**): the probeAcceptance 93/94-task surface fully wired with zero gaps, the devkit task surface completed (verification grouping + one-time generators isolated to the devkit group), the minimal vanilla-overturning official sample `samples/overturn-minimal` (bilingual README + manifest structure + the four capability surfaces: two-tier rules / td datapack direct load / export td/zd identity / tie skip-or-ok, deterministic report), and `SampleBootstrapProbe` 13 checks “sample-is-the-acceptance” wired into probeAcceptance; conclusion: **devkit complete + the framework self-bootstrap acceptance via the official sample**; probeAcceptance all green (**89 pure-JVM + five E2E boots**, incl. SampleBootstrapProbe). p.2.27.1 defines the Flywheel port guard — per the ROAD row verbatim: Flywheel 1.0.6 (the 1.21.1/NeoForge line, **MIT direct-include with notices preserved, form A**) → **engine.render.instancing** (instance format / shader templates / backend SPI, pure JDK) + **runtime.render wiring** (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher hooks) + a **load-and-backend deterministic probe**; the instancing-base guard for p.2.27 (one library, one sub-item). Everything pinned by p.2.12–p.2.20 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. Acceptance baseline: **probeAcceptance all green (89 pure-JVM + 5 E2E boots, incl. SampleBootstrapProbe) + the new Flywheel load-and-backend deterministic probe**. Related: p.2.27 self-developed rendering (next ROAD row after p.2.27.1). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.27.1 全轨，一库一子项，子项以摸底后最小闭环为准）· Scope (one library, one sub-item; sub-item split after scouting minimal closure)

**中文**：总原则：p.2.27.1 =「一库一子项」（形态 A 单库移植），子项拆分以**摸底后最小闭环**为准（可精简为单一子项 p.2.27.1.1 完成全轨）；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：Flywheel 1.0.6（1.21.1/NeoForge 线）发行物与许可/声明材料（MIT 直接包含、保留声明的材料形态与落位，form A 纪律）；engine.render 既有落点（若存在，否则新建 engine.render.instancing 纯 JDK 包：实例格式 / 着色器模板 / 后端 SPI）；runtime.render 接线点现状（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 三个 MC 钩子与门控壳范式）；probeAcceptance 既有接线（89 纯 JVM + 5 E2E）与 FFM/依赖面），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.27.1 行原文）：「Flywheel port / 飞轮移植前哨（2026-09-09 新增，形态 A）：Flywheel 1.0.6（1.21.1/NeoForge 线，MIT 直接包含，保留声明）→ engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 钩子）+ 装载与后端确定性探针；作为 p.2.27 实例化基座前哨（一库一子项）」。示意子项（示例，可合并）：
1. **Flywheel 实例化核心移植**：engine.render.instancing 纯 JDK 落点——实例格式（instance layout/format 确定性编码）、着色器模板（模板源/程序化生成，固定序）、后端 SPI（backend 接口与注册表，注册序/重复拒），MIT 直接包含保留声明（形态 A）；纯 JDK，不碰 MC。
2. **runtime.render 接线**：LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 三钩子（门控 -D/-P 范式，默认 no-op，marker 并入既有 E2E 或独立确定性断言）。
3. **装载与后端确定性探针**：Flywheel 装载（资源/类路径装载确定性）与后端（SPI 注册表固定序、实例格式/着色器模板逐字节一致、往返恒等）确定性探针接入 probeAcceptance（89 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（三钩子）+ 装载与后端确定性探针** 是 ROAD 原文的**交付面**，贯穿每次设计；**MIT 直接包含保留声明（形态 A）** 是许可纪律红线；p.2.12–p.2.20 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.20: p.2.27.1 is a one-library one-sub-item shape (form A single-library port); the sub-item split follows the post-scouting minimal closure (may be a single sub-item p.2.27.1.1 covering the whole track); each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the Flywheel 1.0.6 (1.21.1/NeoForge line) distribution and its license/notice material (the MIT direct-include notice form and placement, the form-A discipline); the existing engine.render landing points (or create the engine.render.instancing pure-JDK package: instance format / shader templates / backend SPI); the runtime.render wiring state (the LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher hooks and the gated-shell pattern); the existing probeAcceptance wiring (89 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.27.1 row verbatim: “Flywheel port / 飞轮移植前哨 (added 2026-09-09, form A): Flywheel 1.0.6 (the 1.21.1/NeoForge line, MIT direct-include with notices preserved) → engine.render.instancing (instance format / shader templates / backend SPI, pure JDK) + runtime.render wiring (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher hooks) + a load-and-backend deterministic probe; the instancing-base guard for p.2.27 (one library, one sub-item)”. Illustrative directions (may merge): (1) **the Flywheel instancing-core port** — the engine.render.instancing pure-JDK landing: instance format (deterministic instance-layout encoding), shader templates (template source / procedural generation, fixed order), backend SPI (backend interface + registry, registration-order/dup-reject), MIT direct-include with notices preserved (form A); pure JDK, no MC; (2) **runtime.render wiring** — the three hooks LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher (gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts); (3) **the load-and-backend deterministic probe** — Flywheel loading (deterministic resource/classpath load) + backend (SPI registry fixed order, instance format / shader templates byte-identical, round-trip stable) probe wired into probeAcceptance (added on top of the 89 pure-JVM + 5 E2E). Note: **engine.render.instancing (instance format / shader templates / backend SPI, pure JDK) + runtime.render wiring (three hooks) + the load-and-backend deterministic probe** is the ROAD delivery surface and runs through every design; **MIT direct-include with notices preserved (form A)** is the license red line; the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.12–p.2.20 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.27.1 由子代理集群执行**（一库一子项形态可精简：摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.27.1 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the one-library one-sub-item shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.27.1 子项一律 **p.2.27.1.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；实例格式 / 着色器模板 / 后端注册表核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.27.1 红线）**：Flywheel 为 **MIT 直接包含（形态 A）**——permissive 可移植并**保留声明**（License/notice 材料原样随库/随文档保留并注明来源与版本 1.0.6）；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。p.2.27.1 只做 Flywheel 一库移植，不新增其他移植。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（Flywheel 若涉 native/FFM 按需）。
- **验收门（p.2.27.1 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**89 纯 JVM + 五次 E2E（新增 SampleBootstrapProbe 在内）**，**并新增 Flywheel 装载与后端确定性探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.27.1.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the instance-format / shader-template / backend-registry cores are free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.27.1 red line)**: Flywheel is an **MIT direct-include (form A)** — permissive may be ported with **notices preserved** (the license/notice material kept verbatim with the library and in docs, source and version 1.0.6 cited); copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. p.2.27.1 ports only this one library, no other ports. Acceptance gate (the p.2.27.1 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **89 pure-JVM + five E2E boots (incl. SampleBootstrapProbe)**, **plus the new Flywheel load-and-backend deterministic probe wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.27.1 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.27.1 各子项落地、最新 p 轨号 p.2.27.1.x、关键路径、Flywheel 前哨结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.27 Self-developed rendering / 自研渲染管线——渲染线次项，形态可相应调整；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.27-self-rendering.md`（bilingual），随当次提交一并提交。

**English**: After p.2.27.1 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.27.1 landing, latest track p.2.27.1.x, key path, the Flywheel-guard conclusion; scope pointing to the next milestone, per ROAD row order **p.2.27 Self-developed rendering / 自研渲染管线** — the second render-line item, whose shape may be adjusted accordingly; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.27-self-rendering.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.27.1 工作区建议：Flywheel port 前哨（形态 A，一库一子项）——engine.render.instancing 纯 JDK 落点 + runtime.render 三钩子接线 + 装载与后端确定性探针。
- **p.2.20 devkit 完整化 + 官方示例模组脚手架（已落地，p.2.27.1 验收门基础）**：probeAcceptance 93/94 任务全量接线零缺口 + devkit 任务面完整化（verification 分组 + 生成器隔离）+ samples/overturn-minimal（双语 README + 清单式结构 + 能力面：规则双层 / td 数据包直载 / export td/zd 恒等 / tie skip-or-ok，确定性报告）+ SampleBootstrapProbe 13 checks「示例即验收」；probeAcceptance 全绿（**89 纯 JVM + 五次 E2E**）。
- **Flywheel 许可登记（仓库内已立项）**：ROAD Port Allocation Map 第 96 行——Flywheel：GPU 实例化渲染 + 着色器模板系统（实体 / 方块实体路径）→ engine.render.instancing + runtime.render，MIT 直接包含（形态 A，保留声明；p.2.27.1 前哨），planned / 已立项。发行物与许可材料在摸底时确认（版本 1.0.6，1.21.1/NeoForge 线）。
- **probeAcceptance 接线基准（p.2.20 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **89 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.27.1 在此之上新增 Flywheel 装载与后端确定性探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19/p.2.20 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.render 接线沿用。
- **依赖铁律（p.2.0/p.2.15–p.2.20 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.20 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.27.1 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.20 devkit 完整化（已落地，验收门基础）/ p.2.27 自研渲染管线（ROAD 行序紧随 p.2.27.1，本里程碑为其实例化基座前哨）；p.2.27.1 把 Flywheel 实例化基座落地并把确定性探针接入 probeAcceptance。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.27.1 landing: the Flywheel port guard (form A, one library one sub-item) — the engine.render.instancing pure-JDK landing + the runtime.render three-hook wiring + the load-and-backend deterministic probe. p.2.20 devkit complete + official sample scaffold (landed; the p.2.27.1 acceptance-gate base): the probeAcceptance 93/94-task surface fully wired with zero gaps + the devkit task surface completed (verification grouping + generator isolation) + samples/overturn-minimal (bilingual README + manifest structure + capability surfaces: two-tier rules / td datapack direct load / export td/zd identity / tie skip-or-ok, deterministic report) + SampleBootstrapProbe 13 checks “sample-is-the-acceptance”; probeAcceptance all green (**89 pure-JVM + five E2E boots**). Flywheel license register (already planned in-repo): ROAD Port Allocation Map row 96 — Flywheel: GPU instanced rendering + shader-template system (entity / block-entity paths) → engine.render.instancing + runtime.render, MIT direct-include (form A, notices preserved; the p.2.27.1 guard), planned. Confirm the distribution and license material at scouting time (version 1.0.6, the 1.21.1/NeoForge line). probeAcceptance wiring baseline (p.2.20 closing): **89 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.27.1 adds the Flywheel load-and-backend deterministic probe on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19/p.2.20 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; runtime.render wiring reuses it. Dependency iron law (p.2.0/p.2.15–p.2.20): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.20 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.27.1 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.20 devkit complete (landed; the acceptance-gate base) / p.2.27 self-developed rendering (next ROAD row after p.2.27.1; this milestone is its instancing-base guard); p.2.27.1 lands the Flywheel instancing base and wires its deterministic probe into probeAcceptance.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 89 纯 JVM + 五次 E2E 与新增 Flywheel 探针）+ ROAD p.2.27.1 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 89 pure-JVM + five E2E boots plus the new Flywheel probe), the “ROAD p.2.27.1 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
