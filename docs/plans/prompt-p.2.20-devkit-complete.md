# Subterra p.2.20 devkit complete / devkit 完整化 + 官方示例模组脚手架 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.19 全轨 landing 后，按 p.2.19 交接单 §5 生成指向 p.2.20 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.19 fully landed, §5 of the p.2.19 handoff produces this p.2.20 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.20 里程碑**的交接单：p.2.20 =「devkit complete / devkit 完整化 + 官方示例模组脚手架」——**probes 全量接线 + 最小「颠覆性模组」示例（兼作框架自举验收）**（自原 p.3.1 前移，见 ROAD p.2.20 行；示意子项可由收尾时按仓库现状给演示）。**devkit 侧已积累大量探针资产**（p.2.1–p.2.19 全部纯 JVM 探针 + 五次 E2E 开服均已接入 probeAcceptance）——本次把 devkit 完整化（探针全量接线盘点与缺口补全、devkit 任务面/文档面收编）并以最小「颠覆性模组」示例作为**框架自举验收**：示例模组用框架自身能力（td 数据包 / tie 逻辑 / 规则 / 导出 / runtime 壳）颠覆原版某一小面，兼作「框架真的可用」的验收判据。执行模式与 p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 / p.2.18 / p.2.19 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.20.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 devkit 现状：probeAcceptance 接线与探针资产清单、devkit build.gradle 任务面、既有示例/文档资产、可作「颠覆性模组」最小示例的既有能力面——td 数据包 / tie / 规则 / export / runtime 门控壳，以及 p.2.19 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.20-milestone handoff: p.2.20 = “devkit complete / devkit 完整化 + official sample mod scaffold” — **probes fully wired + a minimal “vanilla-overturning mod” sample (doubling as the framework self-bootstrap acceptance)** (moved forward from the former p.3.1, per the ROAD p.2.20 row; illustrative sub-items are given at closing time per current repo state). **The devkit already hosts a large probe asset base** (every p.2.1–p.2.19 pure-JVM probe plus the five E2E boots all wired into probeAcceptance) — this milestone completes the devkit (a full probe-wiring inventory with gap closure, a consolidated devkit task/docs surface) and uses a minimal “vanilla-overturning mod” sample as the **framework self-bootstrap acceptance**: the sample mod overturns a small vanilla facet with framework capabilities (td datapack / tie logic / rules / export / runtime shells), doubling as the “the framework actually works” usability gate. Execution mode identical to the p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 / p.2.18 / p.2.19 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.20.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the devkit state: the probeAcceptance wiring and the probe asset list, the devkit build.gradle task surface, existing sample/docs assets, the capability surfaces available for a minimal “vanilla-overturning mod” sample — td datapack / tie / rules / export / runtime gated shells, plus the post-p.2.19 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.19 里程碑「Runtime wiring migration / runtime 接线迁移」已全部落地并已推 GitHub，最新 p 轨号 p.2.19.7（收尾提交）**：runtime 统一接线迁移——**p.2.19.1** 统一接线总表 `docs/runtime-wiring.md`（双语盘点 20 行壳/命令表 + 六类缺口 + export 收编目标）+ **p.2.19.2** `LaunchRuntime`（`subterra.probe.launch` 门控 JVM 健康检查，L1 日志文本逐字保留）+ `FixRuntime`（`subterra.probe.fix` 门控 Java25Gaps boot 收编）+ **p.2.19.3** `ConfigRuntime`（`subterra.probe.cfglog` 门控 td 双层样例装载 + RuleStore/RuleReloader 热重载门）+ `LogRuntime`（同门控 engine.log MC 壳装载验证）+ **p.2.19.4** `TieRuntime`（`subterra.probe.tie` 门控 FFM tie-bridge MC 壳，真实 ABI 调用 tiefib$add(2,3)==5 打 ok、无 tie lib 确定性 skip）+ **p.2.19.5** `/subterra export` 指令收编（`ExportCommandCore` 单核心——datapack 路径与 form 路径共享一棵命令树，export cmd / export hub markers 逐字不变；`DatapackExportCommand` 收缩委托壳；`ExportHubCommand`/`ExportHubRuntime` 删除；`ExportRuntime` 双门控）+ **p.2.19.6** `RuntimeWiringProbe` 52 checks（六类落点 + 门控常量 + marker 清单）接入 probeAcceptance；worldgen / optim-shell 落点确认（既有壳盘点入表）。关键路径 **runtime 接线（runtime.launch / runtime.fix / runtime.cfglog / runtime.tie / runtime.export）+ docs/runtime-wiring.md**。结论：**runtime 统一接线迁移——接线总表 + 六类迁入（launch/fix/cfglog/tie 四新壳 + worldgen/optim-shell 落点确认）+ export 指令收编单核心**，全部确定性可探针；probeAcceptance 全绿（88 纯 JVM + 五次 E2E，新增 RuntimeWiringProbe，AsyncE2EProbe 同开服断言 launch/fix/cfglog/tie marker）。/ p.2.19 “Runtime wiring migration / runtime 接线迁移” fully landed and pushed, latest track **p.2.19.7** (closing commit): the unified runtime wiring migration — p.2.19.1 the bilingual master wiring table `docs/runtime-wiring.md` (20-row shell/command table + six-category gaps + export fold-in target) + p.2.19.2 `LaunchRuntime` (the `subterra.probe.launch`-gated JVM health check, L1 log text kept verbatim) + `FixRuntime` (the `subterra.probe.fix`-gated Java25Gaps boot fold-in) + p.2.19.3 `ConfigRuntime` (the `subterra.probe.cfglog`-gated td two-tier sample load + RuleStore/RuleReloader hot-reload gate) + `LogRuntime` (same gate, engine.log MC-shell load verification) + p.2.19.4 `TieRuntime` (the `subterra.probe.tie`-gated FFM tie-bridge MC shell, real ABI call tiefib$add(2,3)==5 → ok, deterministic skip without a tie lib) + p.2.19.5 the `/subterra export` command fold-in (single `ExportCommandCore` — the datapack path and the form path share one command tree, export cmd / export hub markers character-for-character unchanged; `DatapackExportCommand` shrunk to a delegating shell; `ExportHubCommand`/`ExportHubRuntime` removed; `ExportRuntime` dual-gated) + p.2.19.6 `RuntimeWiringProbe` 52 checks (six-category landing presence + gated-hook property constants + marker checklist) wired into probeAcceptance; worldgen / optim-shell landing confirmed (existing shells inventoried). Key path **runtime wiring (runtime.launch / runtime.fix / runtime.cfglog / runtime.tie / runtime.export) + docs/runtime-wiring.md**. Conclusion: **the unified runtime wiring migration — inventory table + six-category fold-in (four new shells launch/fix/cfglog/tie + worldgen/optim-shell landing confirmation) + single-core export command fold-in**, all deterministic and probeable; probeAcceptance all green (88 pure-JVM + five E2E boots, incl. RuntimeWiringProbe, AsyncE2EProbe asserting launch/fix/cfglog/tie markers in the same boot).
- **p.2.20 目标（ROAD p.2.20 行，本次承接）**：devkit complete / devkit 完整化 + 官方示例模组脚手架（自原 p.3.1 前移，**可用判据**）——**probes 全量接线 + 最小「颠覆性模组」示例（兼作框架自举验收）**。devkit 侧探针资产已全量接入 probeAcceptance（p.2.1–p.2.19）；本次**把 devkit 完整化**（探针全量接线盘点与缺口补全、devkit 任务面/文档面收编）并以最小「颠覆性模组」官方示例脚手架作为**框架自举验收**：示例模组用框架自身能力颠覆原版某一小面，证明「框架真的可用」。/ p.2.20 goal (the ROAD row, this handoff): devkit complete / devkit 完整化 + official sample mod scaffold (moved forward from the former p.3.1, **the usability gate**) — **probes fully wired + a minimal “vanilla-overturning mod” sample (doubling as the framework self-bootstrap acceptance)**. The devkit probe asset base is already fully wired into probeAcceptance (p.2.1–p.2.19); this milestone **completes the devkit** (a full probe-wiring inventory with gap closure, a consolidated devkit task/docs surface) and delivers the minimal “vanilla-overturning mod” official sample scaffold as the **framework self-bootstrap acceptance**: the sample overturns a small vanilla facet with framework capabilities, proving “the framework actually works”.
- **p.2.19 及更早里程碑（已落地，事实不变）**：p.2.19 runtime 接线迁移（上述）、p.2.18 导出枢纽（engine.export 七 form + `/subterra export hub`，p.2.19 收编进单核心）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（td 直载 + `/subterra export` 范式）、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线（优化集 / td 配置 / 日志 / 世界生成管线 / L1 启动 / L2 兼容 / L3 API）。/ p.2.19 and earlier milestones all landed (unchanged): the p.2.19 runtime wiring migration (above), p.2.18 export hub (engine.export seven forms + `/subterra export hub`, folded into one core by p.2.19), p.2.17 rule system, p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (td direct loading + the `/subterra export` pattern), p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline (optimization set / td config / logging / worldgen pipeline / L1 launch / L2 compat / L3 API).
- **probeAcceptance 现状（p.2.19 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**88 个纯 JVM 探针**（含新增 `RuntimeWiringProbe` 52 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；DatapackE2EProbe 35 项断言已含 export + export hub + rule markers，AsyncE2EProbe 同开服并入 async/sim/world/saveverify/session/launch/fix/cfglog/tie 九壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.19 closing): green — **88 pure-JVM probes** (incl. the new RuntimeWiringProbe 52 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; DatapackE2EProbe 35 asserts incl. the export + export hub + rule markers, AsyncE2EProbe asserting async+sim+world+saveverify+session+launch+fix+cfglog+tie markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.20 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.19 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。
- **关联里程碑**：p.2.20 承接 p.2.19（runtime 六类迁入 + export 收编全部落地，devkit 完整化直接消费这些能力面）；「颠覆性模组」示例复用 p.2.2 td 数据包 / p.2.1 tie / p.2.17 规则 / p.2.18+p.2.19 export / p.2.19 runtime 门控壳 的能力面；衔接 p.2.27.1 渲染前哨（ROAD 行序紧随 p.2.20）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.12–p.2.19 closing patterns. p.2.19 “Runtime wiring migration” fully landed and pushed (latest track **p.2.19.7**, key path **runtime wiring + docs/runtime-wiring.md**): the unified runtime wiring migration — bilingual master wiring table (20 rows) + six-category fold-in (four new shells launch/fix/cfglog/tie, each gated no-op, real FFM ABI call ok/skip for tie + worldgen/optim-shell landing confirmed) + the `/subterra export` command fold-in (single ExportCommandCore, markers character-for-character unchanged) + RuntimeWiringProbe 52 checks; conclusion: the unified runtime wiring migration is deterministic and probeable; probeAcceptance all green (88 pure-JVM + five E2E boots). p.2.20 defines devkit complete — per the ROAD p.2.20 row (moved forward from the former p.3.1, **the usability gate**): **probes fully wired + a minimal “vanilla-overturning mod” official sample scaffold (doubling as the framework self-bootstrap acceptance)**; the devkit probe asset base is already fully wired (p.2.1–p.2.19), so this milestone completes the devkit (probe-wiring inventory + gap closure, consolidated task/docs surface) and delivers the minimal sample as the acceptance that the framework actually works. Everything pinned by p.2.12–p.2.19 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. probeAcceptance all green: **88 pure-JVM probes** (incl. RuntimeWiringProbe) + **5 E2E boots**. Iron law auto-covers new engine packages. Related: p.2.27.1 render guard (next row in ROAD after p.2.20). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.20 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点 devkit 现状：probeAcceptance 接线与探针资产清单（88 纯 JVM + 五次 E2E 的任务面与 mustRunAfter 链）、devkit build.gradle 任务面、既有示例/脚手架资产（若有）、可作「颠覆性模组」最小示例的既有能力面——td 数据包（p.2.2）/ tie（p.2.1）/ 规则（p.2.17）/ export（p.2.18+p.2.19 收编单核心）/ runtime 门控壳（p.2.19），以及 p.2.19 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.20 行原文）：「devkit complete / devkit 完整化 + 官方示例模组脚手架（自原 p.3.1 前移，**可用判据**）：probes 全量接线 + 最小「颠覆性模组」示例（兼作框架自举验收）」。示意子项（示例）：
1. **devkit 接线盘点**：probeAcceptance 全量接线盘点（88 纯 JVM + 五次 E2E 任务面与既有缺口核对，产出接线清单；devkit 任务面/文档面收编）。
2. **探针缺口补全**：probeAcceptance 接线缺口补全（盘点出的未接线探针/任务面收编进验收门）。
3. **devkit 任务面完整化**：devkit build.gradle 任务面完整化（探针任务分组、E2E 链、一次性生成器与常规验收任务隔离）。
4. **官方示例模组脚手架·骨架**：最小「颠覆性模组」官方示例骨架（独立示例模组目录/工程，清单式最小结构）。
5. **示例模组·td 数据包能力面**：示例用 td 数据包能力（p.2.2 直载 + tie 逻辑链）颠覆原版某一小面。
6. **示例模组·规则/导出/runtime 能力面**：示例用规则（p.2.17）、导出（p.2.18+p.2.19 收编单核心）、runtime 门控壳（p.2.19）能力面。
7. **自举验收探针**：示例模组自举验收探针（确定性断言「示例即验收」——框架能力面在示例中逐项命中）。
8. **文档收编**：devkit 完整化 + 示例脚手架文档（双语，devkit 使用说明 + 示例模组清单）。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**probes 全量接线 + 最小「颠覆性模组」示例兼作框架自举验收** 是 ROAD 原文的**可用判据**，贯穿每次设计；p.2.12–p.2.19 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.19: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory the devkit state: the probeAcceptance wiring and the probe asset list (the 88 pure-JVM + five E2E task surface and mustRunAfter chain), the devkit build.gradle task surface, existing sample/scaffold assets (if any), the capability surfaces available for a minimal “vanilla-overturning mod” sample — td datapack (p.2.2) / tie (p.2.1) / rules (p.2.17) / export (p.2.18+p.2.19 single-core fold-in) / runtime gated shells (p.2.19), plus the post-p.2.19 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.20 row: “devkit complete / devkit 完整化 + official sample mod scaffold (moved forward from the former p.3.1, **the usability gate**): probes fully wired + a minimal ‘vanilla-overturning mod’ sample (doubling as the framework self-bootstrap acceptance)”. Illustrative directions: (1) **devkit wiring inventory** — the full probeAcceptance wiring inventory (the 88 pure-JVM + five E2E task surface with existing-gap verification, producing a wiring checklist; consolidated devkit task/docs surface); (2) **probe gap closure** — wire the inventoried gaps into the acceptance gate; (3) **devkit task-surface completion** — consolidate the devkit build.gradle task surface (probe task grouping, the E2E chain, one-time generators isolated from routine acceptance tasks); (4) **official sample scaffold · skeleton** — the minimal “vanilla-overturning mod” official sample skeleton (a standalone sample-mod directory/project, minimal manifest structure); (5) **sample · td datapack surface** — the sample overturns a small vanilla facet via td datapack (p.2.2 direct loading + tie logic chain); (6) **sample · rules/export/runtime surfaces** — the sample exercises rules (p.2.17), export (p.2.18+p.2.19 single core), and the runtime gated shells (p.2.19); (7) **self-bootstrap acceptance probe** — a deterministic probe asserting “the sample is the acceptance” (each framework surface hit by the sample); (8) **docs** — bilingual devkit-completion + sample-scaffold docs (devkit usage + the sample manifest). Note: **probes fully wired + the minimal “vanilla-overturning mod” sample doubling as the framework self-bootstrap acceptance** is the ROAD **usability gate** and runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12–p.2.19 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.20 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、各接线子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/devkit 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.20 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.20 子项一律 **p.2.20.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；devkit 接线核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.20 为 devkit 完整化 + 官方示例模组脚手架，示例只消费仓库内已钉死的能力面与许可结论，不新增移植。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（示例若涉 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（**88 纯 JVM + 五次 E2E，新增 RuntimeWiringProbe 在内，含示例自举验收探针**）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.20.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the devkit wiring core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. Licensing: permissive reuse/derive with attribution only; p.2.20 is devkit completion + an official sample scaffold — the sample only consumes the capability surfaces and pinned license conclusions already in the repo, no new ports. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (**88 pure-JVM + five E2E boots, incl. RuntimeWiringProbe, plus the sample self-bootstrap acceptance probe**) before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.20 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.20 各子项落地、最新 p 轨号 p.2.20.x、关键路径、devkit-complete 结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.27.1 Flywheel port 前哨——渲染线首项，一库一子项形态可相应精简；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.27.1-flywheel-port.md`（bilingual），随当次提交一并提交。

**English**: After p.2.20 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.20 landing, latest track p.2.20.x, key path, the devkit-complete conclusion; scope pointing to the next milestone, per ROAD row order **p.2.27.1 the Flywheel port guard** — the first render-line item, a one-library one-sub-item shape that may be slimmed accordingly; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.27.1-flywheel-port.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.20 工作区建议：devkit 完整化 + 官方示例模组脚手架（probes 全量接线 + 最小「颠覆性模组」示例兼作框架自举验收）。
- **p.2.19 runtime 接线（已落地，p.2.20 能力面来源）**：接线总表 docs/runtime-wiring.md（20 行壳/命令表）+ 六类迁入（launch LaunchRuntime / runtime-fix FixRuntime / cfglog ConfigRuntime+LogRuntime / tie TieRuntime（FFM 真实 ABI 调用 ok/skip）/ worldgen / optim-shell 落点确认）+ `/subterra export` 指令收编（ExportCommandCore 单核心、markers 逐字不变、ExportRuntime 双门控）；p.2.20 的示例模组直接消费这些能力面。
- **probeAcceptance 现状（p.2.19 收官，p.2.20 接线盘点基准）**：`gradlew :subterra-devkit:probeAcceptance` —— **88 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；示例模组的 runtime 面沿用。
- **示例「颠覆性模组」能力面（仓库内已钉死）**：td 数据包（p.2.2 直载 + tie 逻辑链）、tie bridge（p.2.1，FFM 装载）、规则（p.2.17 engine.config.rules + `/subterra rule`）、导出（p.2.18 engine.export 七 form + p.2.19 收编单核心）、runtime 门控壳（p.2.19 六类 + 既有 save/world/network/saveverify/session/sim/async）。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12–p.2.19 钉死）**：逐字节一致（byte-identical）、固定序、禁时序；同输入 → 同输出。
- **依赖铁律（p.2.0/p.2.15–p.2.19 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.19 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.20 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.19 runtime 接线（已落地，能力面来源）/ p.2.27.1 渲染前哨（ROAD 行序紧随 p.2.20）；p.2.20 把 devkit 完整化并以最小「颠覆性模组」示例兼作框架自举验收。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.20 landing: devkit complete + official sample mod scaffold (probes fully wired + a minimal “vanilla-overturning mod” sample doubling as the framework self-bootstrap acceptance). p.2.19 runtime wiring (landed; the p.2.20 capability source): the bilingual master wiring table docs/runtime-wiring.md (20 rows) + the six-category fold-in (launch LaunchRuntime / runtime-fix FixRuntime / cfglog ConfigRuntime+LogRuntime / tie TieRuntime (real FFM ABI call ok/skip) / worldgen / optim-shell landing confirmed) + the `/subterra export` command fold-in (single ExportCommandCore, markers character-for-character unchanged, ExportRuntime dual-gated); the p.2.20 sample consumes these surfaces directly. probeAcceptance (p.2.19 closing; the p.2.20 wiring-inventory baseline): **88 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp). The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the sample’s runtime surface reuses it. Sample “vanilla-overturning mod” capability surfaces (all pinned in-repo): td datapack (p.2.2 direct loading + tie logic chain), tie bridge (p.2.1, FFM loading), rules (p.2.17 engine.config.rules + `/subterra rule`), export (p.2.18 engine.export seven forms + the p.2.19 single-core fold-in), the runtime gated shells (p.2.19 six categories + existing save/world/network/saveverify/session/sim/async). Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12–p.2.19 pinned): byte-identical, fixed order, no timing. Dependency iron law (p.2.0/p.2.15–p.2.19): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.19 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.20 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.19 runtime wiring (landed; the capability source) / p.2.27.1 render guard (next ROAD row after p.2.20); p.2.20 completes the devkit with a minimal “vanilla-overturning mod” sample as the framework self-bootstrap acceptance.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.20 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.20 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
