# Subterra p.2.28 LOD 多级细节渲染 / engine.render.lod · 全轨一次性交接单 · 开发任务提示词

> 本文件为 p.2.28 里程碑的全轨开发交接单（延续 p.2.26 收尾的机制：p.2.26 全轨 landing 后，ROAD p.2.26 行注明「下一里程碑待 ROAD 更新」，本里程碑为 ROAD 更新后新增的 p.2.28 渲染线续项）。本节 0–7 结构与既有交接单完全一致。
> This file is the p.2.28 milestone handoff prompt (continuing the mechanism: after p.2.26 fully landed, the ROAD p.2.26 row noted "the next milestone awaits a ROAD update"; p.2.28 is the render-line continuation added by that update). The 0–7 section structure matches prior handoffs exactly.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.28 里程碑**的交接单：p.2.28 =「LOD / 多级细节渲染 engine.render.lod」——**clean-room 自研 + tie 化热点**（Distant Horizons 因 LGPL-3.0 copyleft 已评估为「仅思想参考」，能力自研替代）：**多级细节（LOD）数据模型、生成管线（列柱降采样 + 水平面合并）、确定性视景管理（距离选择 / 剔除 / 预算调度）、实例化渲染后端、zd 持久化缓存、api.lod 契约 + runtime 接线**（见 ROAD p.2.28 行）。**目标**：把原版视距之内的区块渲染延伸到视距之外——远处地形以逐级降低的细节渲染（Distant Horizons 式能力），但全部为 Subterra 自研（纯 JDK 确定性核心 + tie 热点实现 + runtime 门控壳）。**本里程碑为 clean-room 自研 + tie 化形态**（形态参考 p.2.24 AI clean-room 纪律 / p.2.27 自研渲染纪律 / p.2.1 tie bridge 纪律）：不引入任何第三方源码或按 Copy Dead 保留的库；**Distant Horizons（LGPL-3.0）仅参考公开思想（mip 金字塔、列柱剖面、贪婪合并、视锥剔除均属公开技术常识），零源码包含、不捆绑**；capability 面以纯 JDK 确定性核心落 engine.*，热点路径（生成管线 polygonizer/merger）以 **tie 语言实现**（tiec → DLL → engine.tie FFM 装载，数据走 zd 字节 ABI 字节进字节出），无 tie lib 时确定性 skip（沿用 TieRuntime 门控范式）；经 runtime 门控壳接线到 MC 客户端渲染生命周期，以确定性探针验收。执行模式与 p.2.26 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.28.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 engine.render / engine.render.instancing 现状、runtime.render RenderRuntime + RenderHooksClient 接线、engine.tie TieLibrary/TieFunction ABI 与 TieRuntime 门控、probeAcceptance 既有接线、p.2.8 engine.sim BudgetScheduler 语义、p.2.3 engine.zd 载体、p.2.10 saveverify 语义），并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.28-milestone handoff: p.2.28 = “LOD / multi-level-of-detail rendering engine.render.lod” — **clean-room self-developed + tie-ized hot paths** (Distant Horizons was assessed and excluded to reference-of-ideas only for its LGPL-3.0 copyleft; a self-developed replacement is used): **the LOD data model, the generation pipeline (column-stack downsampling + horizontal-surface merging), deterministic view management (distance selection / culling / budget scheduling), an instanced render backend, zd-persisted caching, the api.lod contract + runtime wiring** (per the ROAD p.2.28 row). **Goal**: extend rendering beyond the vanilla build radius — distant terrain rendered at progressively reduced detail (a Distant-Horizons-style capability) entirely self-developed: pure-JDK deterministic cores + tie hot-path implementation + a gated runtime shell. **This milestone is a clean-room + tie-ized shape** (referencing the p.2.24 AI clean-room discipline / the p.2.27 self-render discipline / the p.2.1 tie-bridge discipline): no third-party source or copy-dead-on-retained library is introduced; **Distant Horizons (LGPL-3.0) informs public ideas only (mip pyramids, column stacks, greedy merging, frustum culling are common public technology), zero source included, not bundled**; the capability lands as a pure-JDK deterministic core in engine.*, the hot paths (generation pipeline polygonizer/merger) are implemented in **the tie language** (tiec → DLL → engine.tie FFM load, data via the zd byte ABI in→out), deterministic skip without a tie lib (the TieRuntime gated pattern); wired into the MC client render lifecycle via a gated runtime shell, accepted by deterministic probes. Execution mode identical to the p.2.26 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.28.x** (≤3 levels); never say "phase X / block N / R1". Workflow: first **scout** (inventory the current engine.render / engine.render.instancing state, the runtime.render RenderRuntime + RenderHooksClient wiring, the engine.tie TieLibrary/TieFunction ABI and the TieRuntime gate, the existing probeAcceptance wiring, the p.2.8 engine.sim BudgetScheduler semantics, the p.2.3 engine.zd carrier, the p.2.10 saveverify semantics), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do the final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.26 里程碑「Time scaling / engine.time」已全部落地并已推 GitHub，最新 p 轨号 p.2.26.6（收尾提交 a05bed6）**：时间缩放 clean-room 自研全轨落地——**p.2.26.1** engine.time 四域时间缩放核心 + **p.2.26.2** api.time TimeScaleApi 契约面 + **p.2.26.3** TimeBudgetScheduler tick 预算调度（复用 p.2.8 engine.sim.budget 内核）+ **p.2.26.4** runtime.time TimeRuntime 门控壳（subterra.probe.time、[Subterra time] marker 并入 AsyncE2EProbe）+ **p.2.26.5** TimeProbe 43 checks。结论：**时间缩放核心 clean-room 落地——engine.time 四域缩放 + api.time 契约 + tick 预算调度 + runtime.time 壳 + TimeProbe 43 checks**；probeAcceptance 全绿（**97 纯 JVM + 五次 E2E = 102 任务**，新增 TimeProbe）。ROAD p.2.26 行注明「p.2.26 为当前 p 轨收尾：里程碑表无后续 p 轨行，下一里程碑待 ROAD 更新」。/ p.2.26 “Time scaling / engine.time” fully landed and pushed, latest track **p.2.26.6** (closing commit a05bed6): the clean-room time-scaling line fully landed — p.2.26.1 the engine.time four-domain scaling core + p.2.26.2 the api.time TimeScaleApi contract + p.2.26.3 TimeBudgetScheduler tick-budget scheduling (over the p.2.8 engine.sim.budget kernel) + p.2.26.4 the runtime.time TimeRuntime gated shell (subterra.probe.time, the [Subterra time] marker merged into AsyncE2EProbe) + p.2.26.5 TimeProbe 43 checks. Conclusion: **the time-scaling core clean-room landed — engine.time four-domain scaling + api.time contract + tick-budget scheduling + runtime.time shell + TimeProbe 43 checks**; probeAcceptance all green (**97 pure-JVM + five E2E boots = 102 tasks**, incl. TimeProbe). The ROAD p.2.26 row notes “p.2.26 wraps up the current p-track: the milestone map has no further p-track row; the next milestone awaits a ROAD update”.
- **p.2.28 目标（ROAD p.2.28 行原文，本次承接）**：「LOD / 多级细节渲染 engine.render.lod（紧接 p.2.27 自研渲染线，clean-room 自研 + tie 化热点）：LodLevel 数据模型（等比递减细节层级 + 固定距离阈值表）、生成管线（列柱降采样 + 水平面合并，热点 tie 化，zd 字节 ABI）、确定性视景管理（距离选择 / 视锥剔除 / 预算调度接 p.2.8）、实例化渲染后端（复用 p.2.27.1）、zd 持久化缓存（复用 p.2.3 / p.2.10 语义）、api.lod 契约 + runtime 接线」。/ p.2.28 goal (the ROAD row verbatim, this handoff): “LOD / multi-level-of-detail rendering engine.render.lod (right after the p.2.27 self-render line, clean-room self-developed + tie-ized hot paths): the LodLevel data model (geometric-ladder detail levels + a fixed distance-threshold table), the generation pipeline (column-stack downsampling + horizontal-surface merging, hot path tie-ized over the zd byte ABI), deterministic view management (distance selection / frustum culling / budget scheduling over p.2.8), an instanced render backend (reusing p.2.27.1), zd-persisted caching (reusing p.2.3 / p.2.10 semantics), the api.lod contract + runtime wiring”.
- **p.2.26 及更早里程碑（已落地，事实不变）**：p.2.26 时间缩放线（上述，最新 p 轨号 p.2.26.6）、p.2.25 实体缩放线（engine.scale + runtime.scale + ScaleProbe）、p.2.24 AI behavior core、p.2.23 In-game mod hub、p.2.22 UI/HUD 核心、p.2.21 动画核心 MIT 移植、**p.2.27 自研渲染管线**（engine.render 原版保真渲染面模型——RenderStage 10 阶段固定序 / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B + runtime.render 三 MC 钩子注入 RenderHooks/RenderHooksClient（RenderLevelStageEvent，门控 no-op）+ RenderFidelityProbe 62 checks）、**p.2.27.1 Flywheel 实例化基座前哨**（engine.render.instancing——InstanceFormat / ShaderTemplate / RenderBackend + BackendRegistry + runtime.render RenderRuntime 门控壳 + FlywheelInstancingProbe 34 checks）、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移、p.2.18 导出枢纽、p.2.17 规则系统、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 engine.interact、p.2.13 engine.pcg、p.2.12 engine.schema/scaffold、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim（engine.sim 增量确定性模拟底座 + BudgetScheduler/TickBudget 预算分级调度）、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack、p.2.1 tie bridge、p.2.0 模块重构、p.1 框架基线。/ p.2.26 and earlier milestones all landed (unchanged): p.2.26 the time-scaling line (above, latest track p.2.26.6), p.2.25 the entity-scale line (engine.scale + runtime.scale + ScaleProbe), p.2.24 the AI behavior core, p.2.23 the in-game mod hub, p.2.22 the UI/HUD core, p.2.21 the animation core MIT port, **p.2.27 the self-developed render pipeline** (the engine.render vanilla-fidelity surface model — RenderStage 10-stage fixed order / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B + the runtime.render three-MC-hook injection RenderHooks/RenderHooksClient (RenderLevelStageEvent, gated no-op) + RenderFidelityProbe 62 checks), **p.2.27.1 the Flywheel instancing-base guard** (engine.render.instancing — InstanceFormat / ShaderTemplate / RenderBackend + BackendRegistry + the runtime.render RenderRuntime gated shell + FlywheelInstancingProbe 34 checks), p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration, p.2.18 export hub, p.2.17 the rule system, p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim (the engine.sim incremental deterministic base + the BudgetScheduler/TickBudget budget-tier scheduling), p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack, p.2.1 tie bridge, p.2.0 module re-layout, the p.1 framework baseline.
- **渲染线现状（p.2.28 的基座）**：p.2.27/p.2.27.1 已落地——engine.render（RenderStage / MaterialSurface / RenderSurface / VertexLayout + package-info）与 engine.render.instancing（InstanceFormat / ShaderTemplate / RenderBackend / BackendRegistry）；runtime.render **RenderRuntime**（subterra.probe.render 门控壳、[Subterra render] marker 并入 AsyncE2EProbe）与 **RenderHooksClient**（@EventBusSubscriber 监听 **RenderLevelStageEvent**，对真实 LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 只读核对——**p.2.28 LOD pass 的注入点**）。LOD 顶点格式与 `engine.render.VertexLayout` 同风格（字段序/偏移/对齐/byteSize 固定序逐字节一致，javadoc 对照 1.21.1 规范名，零 MC import）。/ The render-line state (the p.2.28 base): p.2.27/p.2.27.1 landed — engine.render (RenderStage / MaterialSurface / RenderSurface / VertexLayout + package-info) and engine.render.instancing (InstanceFormat / ShaderTemplate / RenderBackend / BackendRegistry); runtime.render **RenderRuntime** (the subterra.probe.render gated shell, the [Subterra render] marker merged into AsyncE2EProbe) and **RenderHooksClient** (@EventBusSubscriber listening on **RenderLevelStageEvent**, read-only checking the real LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher — **the p.2.28 LOD-pass injection point**). The LOD vertex formats follow the `engine.render.VertexLayout` style (fixed-order field order / offset / alignment / byteSize, byte-identical, javadoc citing the 1.21.1 canonical names, zero MC imports).
- **tie 桥现状（p.2.28 tie 化复用）**：p.2.1 engine.tie——**TieLibrary.load(dll)**（FFM libraryLookup + Arena 生命周期）+ **TieFunction**（定形便捷调用 invoke0/invokeI64/invokeI64I64/invokeF64 + 通用 `handle(FunctionDescriptor)` 自取任意形态——zd 字节进出可经 (addr, len) -> addr 自建 descriptor）；runtime.tie **TieRuntime** 门控壳（`subterra.tie.lib` 系统属性或类路径捆绑资源定位 dll，无 lib 时确定性 `skip (no tie lib)`，装载后确定性校验调用 tiefib$add(2,3)==5 → `[Subterra tie] ok/mismatch`，运行期需 `--enable-native-access`，ModDevGradle dev run 默认注入）；devkit TieBridgeProbe 与其 tieBridgeProbe 任务已接线。**p.2.28 tie 化沿用同一范式**：LOD 生成管线热点（polygonizer/merger）以 tie 实现（tiec → DLL），数据传递走 **zd 字节 ABI（模块.函数(字节进)→字节出）**——语言无关、与 p.2.5 tink 同栈；无 tie lib 时确定性 skip，不失败。/ The tie-bridge state (reused by the p.2.28 tie-ization): p.2.1 engine.tie — **TieLibrary.load(dll)** (FFM libraryLookup + Arena lifecycle) + **TieFunction** (shaped convenience calls invoke0/invokeI64/invokeI64I64/invokeF64 + the generic `handle(FunctionDescriptor)` for arbitrary signatures — zd byte in/out rides a custom (addr, len) -> addr descriptor); runtime.tie **TieRuntime** gated shell (locates the dll via the `subterra.tie.lib` system property or a bundled classpath resource, deterministic `skip (no tie lib)` when absent, one deterministic verification call tiefib$add(2,3)==5 → `[Subterra tie] ok/mismatch` after a successful load, needs `--enable-native-access` which ModDevGradle dev runs inject by default); the devkit TieBridgeProbe and its tieBridgeProbe task are wired. **p.2.28 reuses the same pattern**: the LOD generation-pipeline hot paths (polygonizer/merger) are implemented in tie (tiec → DLL), data via the **zd byte ABI (module.function(bytes in) → bytes out)** — language-agnostic, same stack as the p.2.5 tink; deterministic skip without a tie lib, never a failure.
- **probeAcceptance 现状（p.2.26 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**97 个纯 JVM 探针**（含 p.2.26 TimeProbe 43 checks + 渲染线 FlywheelInstancingProbe 34 checks / RenderFidelityProbe 62 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.28 的 engine.render.lod 纯 JDK 落点自动纳入。p.2.28 验收基线 = **probeAcceptance 全绿（97 纯 JVM + 5 次 E2E）+ 本里程碑新增的 LOD 确定性/接线探针**。/ probeAcceptance (p.2.26 closing): green — **97 pure-JVM probes** (incl. the p.2.26 TimeProbe 43 checks + the render-line FlywheelInstancingProbe 34 checks / RenderFidelityProbe 62 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.28 pure-JDK engine.render.lod landing is auto-covered. The p.2.28 acceptance baseline = **probeAcceptance all green (97 pure-JVM + 5 E2E boots) + the new LOD determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.28 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主（**api.lod 落此**）；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.26 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.28): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface (**api.lod lands here**); td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.26; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.28 承接 p.2.27/p.2.27.1 渲染线（engine.render 原版保真模型 + instancing 实例化基座为 LOD 网格渲染的既有底座；RenderHooksClient 的 RenderLevelStageEvent 为 LOD pass 注入点；RenderStage 的 TRANSLUCENT「按 view distance 排序」语义为 LOD 视距级渲染的相关抽象）；直接复用 **p.2.8 engine.sim 的 BudgetScheduler/TickBudget 预算分级调度语义**（LOD 生成/上载帧预算确定性调度）、**p.2.7 DeterministicDispatcher/WorkerBank**（LOD 生成并行与内存复用）、**p.2.3 engine.zd 载体**（LOD 缓存落盘规格）与 **p.2.10 saveverify 校验语义**（缓存逐块 tsha1f 防篡改）；**api.lod 契约面**沿用 api.time「api 定义形状 / engine 镜像实现」模式。**许可衔接（p.2.28 红线：clean-room 自研 + tie 化，不新增移植项）**：本品不引用任何第三方代码；**Distant Horizons（LGPL-3.0 copyleft）已评估为「仅思想参考」**——LOD 的公开技术常识（mip 金字塔 / 列柱剖面 / 水平面合并 / 视锥剔除 / 遮挡剔除）以自研实现，零源码包含、不捆绑、NOTICE 级说明（如需可在 package-info 注明参考来源与 clean-room 立场，同 p.2.26 TimeScaleLib / p.2.27 Sodium-Embeddium 先例）；copyleft 仅 clean-room 参考思想，**不按原样复制代码**；存疑按不允许处理（完整登记册见框架设计 §7）。**tie 为本仓库自家语言（TPL 2.0），非第三方依赖**，tie 化走 p.2.1 既有桥，无 lib 时确定性 skip。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.28 continues the p.2.27/p.2.27.1 render line (the engine.render vanilla-fidelity model + the instancing base are the existing substrate for LOD mesh rendering; the RenderHooksClient RenderLevelStageEvent is the LOD-pass injection point; the RenderStage TRANSLUCENT “sorted by view distance” semantic is the relevant abstraction for LOD view-distance rendering); it reuses the **p.2.8 engine.sim BudgetScheduler/TickBudget budget-tier scheduling semantics** (deterministic frame budgeting for LOD generation/upload), the **p.2.7 DeterministicDispatcher/WorkerBank** (parallel LOD generation with memory reuse), the **p.2.3 engine.zd carrier** (the LOD-cache on-disk spec), and the **p.2.10 saveverify validation semantics** (per-chunk tsha1f cache tamper-rejection); the **api.lod contract** follows the api.time “api defines the shape / engine mirrors the implementation” pattern. **License handover (the p.2.28 red line: clean-room self-developed + tie-ized, no new port)**: no third-party code is referenced; **Distant Horizons (LGPL-3.0 copyleft) was assessed to reference-of-ideas only** — the public LOD techniques (mip pyramids / column stacks / horizontal-surface merging / frustum culling / occlusion culling) are implemented self-developed, zero source included, not bundled, with package-info-level attribution if needed (same precedent as the p.2.26 TimeScaleLib / p.2.27 Sodium-Embeddium assessments); copyleft is reference-of-ideas only, no verbatim copying; uncertain defaults to not allowed (full register in framework design §7). **tie is the house language (TPL 2.0), not a third-party dependency** — tie-ization goes through the existing p.2.1 bridge with a deterministic skip when the lib is absent. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.26 closing patterns, with the render-line base (p.2.27/p.2.27.1) and the tie-bridge reuse (p.2.1). Latest track **p.2.26.6** (closing commit a05bed6): the clean-room time-scaling line landed — engine.time four-domain scaling + api.time TimeScaleApi + TimeBudgetScheduler + runtime.time TimeRuntime + TimeProbe 43 checks; probeAcceptance all green (**97 pure-JVM + five E2E boots = 102 tasks**). p.2.28 defines the LOD milestone per the ROAD row verbatim: LOD multi-level-of-detail rendering engine.render.lod (right after the p.2.27 self-render line, clean-room self-developed + tie-ized hot paths): the LodLevel data model / the generation pipeline / deterministic view management / an instanced render backend / zd-persisted caching / the api.lod contract + runtime wiring. Everything pinned by p.2.12–p.2.26 stays the default semantics. **License red line: p.2.28 is clean-room self-developed + tie-ized — Distant Horizons (LGPL-3.0) is reference-of-ideas only, zero source included, not bundled; tie is the house language (TPL 2.0) via the p.2.1 bridge, deterministic skip without a lib.** Acceptance baseline: **probeAcceptance all green (97 pure-JVM + 5 E2E boots) + the new LOD determinism/wiring probes**. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.28 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.28 =「LOD / engine.render.lod」——**clean-room 自研 + tie 化热点**：**LodLevel 等比递减数据模型、生成管线（列柱降采样 + 水平面合并，热点 tie 化 zd 字节 ABI）、确定性视景管理（距离选择 / 视锥剔除 / 预算调度接 p.2.8）、实例化渲染后端（复用 p.2.27.1）、zd 持久化缓存（复用 p.2.3 / p.2.10）、api.lod 契约 + runtime 接线**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：渲染线现状（engine.render / engine.render.instancing / runtime.render RenderRuntime + RenderHooksClient）；tie 桥 ABI 现状（engine.tie TieLibrary/TieFunction 定形调用面与 handle(FunctionDescriptor) 通用形态、TieRuntime 门控、tiefib_probe dll 装配方式）；p.2.8 BudgetScheduler/TickBudget 预算语义；p.2.7 DeterministicDispatcher/WorkerBank；p.2.3 engine.zd 载体六字段 wire2 记录与 zdt 文档形态；p.2.10 saveverify 逐块 tsha1f；probeAcceptance 既有接线（97 纯 JVM + 5 E2E））,再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意子项（目标见 ROAD p.2.28 行原文；一库一子项）：
1. **p.2.28.1 engine.render.lod 数据模型（纯 JDK）**：LodLevel（等比递减细节层级固定序 + 固定距离阈值表）、LodSection/LodQuad（列柱剖面 + 水平合并 quad，固定字段序、逐字节一致编码）、**LodVertexLayout 紧凑量化布局**（int16 量化位置 + 颜色索引 + 朝向位，风格对齐 p.2.27 VertexLayout、逐字节一致、零 MC import）、LodConfigDoc（td 化档位文档：启用/最大层数/距离阈值/质量档/缓存策略，toTd/fromTd 往返恒等）；**该字节布局即 tie 热点的 zd 输入输出定格**（p.2.28.2 的 ABI 依据）。
2. **p.2.28.2 engine.render.lod 生成管线（热点 tie 化）**：LodPolygonizer（level 0：区块区域 → 列柱降采样 → 分离垂直剖面 + 水平面合并，固定采样规则：top-most 表面 + 表面色/亮度规则）、LodMerger（level n → n+1 继承合并，同一合并函数逐级递归 mip 金字塔；合并角点固定序确定性）、增量 dirty 集（区块变更 → budget 批次重建，复用 p.2.7 Dispatcher/WorkerBank）；**polygonizer/merger 以 tie 实现**（tiec → DLL → engine.tie FFM，zd 字节 ABI 进/出；无 tie lib 时确定性 skip 或使用纯 JDK 参考路径，探针双路对照同输入同输出逐字节一致并黄金化）；预算经 p.2.8 BudgetScheduler 语义限制生成量。
3. **p.2.28.3 engine.render.lod 确定性视景管理（纯 JDK）**：LodDistanceSelector（确定性距离选择：玩家坐标 → 每 layer 期望等级矩阵，整数运算同输入同输出）、视锥剔除（section AABB）+ 水平合并剔除（确定性）、LodBudgetScheduler（复用 p.2.8 engine.sim.budget 内核：per-level cap、原子记账、超预算确定性跳过）。
4. **p.2.28.4 engine.render.lod 渲染后端（复用 p.2.27.1 instancing）**：LodRenderBackend 注册进 BackendRegistry（固定注册序、重复拒、确定性缺省选择）、LodShaderTemplate（clean-room 固定序模板编译 → 规范 GLSL 逐字节一致）、Level0 边缘接缝对齐 + 远景雾规则（防裂隙确定性几何断言）。
5. **p.2.28.5 engine.render.lod 持久化（LodCache）**：缓存落盘 **zd 载体**（p.2.3 engine.zd 规格：版本头 + 六字段 wire2 记录，或 zdt td+base64 文档形态——摸底定）；逐块 tsha1f 快/强校验（p.2.10 saveverify 语义，篡改拒绝），加载命中跳过重生成；与 p.2.9 world 包 / p.2.18 export 的协调（缓存为可再生物，可导出可排除）。
6. **p.2.28.6 api.lod 契约 + runtime 接线（收尾）**：api.lod LodQualitySpec/LodDistanceSpec/LodApi（固定档位序 + 阈值表契约常量；api 纯 JDK 不依赖 engine；engine.render.lod 镜像实现——同输入同输出对照断言）+ runtime.render.lod LodRuntime 门控壳（subterra.probe.lod 缺省 no-op、[Subterra lod] marker 并入 AsyncE2EProbe 同开服）+ RenderHooksClient 扩展（RenderLevelStageEvent 注入 LOD pass + 区块可用回调接线，只读 patch 不改变原版输出路径）+ 配置规则（p.2.17 RuleStore 读 LOD 档位 + p.2.23 hub 表单热重载）+ LodProbe 接入 probeAcceptance。
提示：以上为示意；**clean-room 自研 + tie 化**是 ROAD 原文的**交付红线**，贯穿每次设计；**许可红线**：p.2.28 **不引用任何第三方源码；Distant Horizons（LGPL-3.0）仅思想参考、零源码包含、不捆绑；tie 为自家语言非第三方依赖，经 p.2.1 桥使用、无 lib 确定性 skip**；copyleft 仅参考思想，禁止原样复制；若摸底发现需要参考其他第三方，先按既有纪律走（permissive 可移植保留声明、copyleft 仅 clean-room、存疑按不允许）；p.2.7–p.2.26 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.26: p.2.28 is the LOD milestone — **clean-room self-developed + tie-ized hot paths**: **the LodLevel geometric-ladder data model, the generation pipeline (column-stack downsampling + horizontal-surface merging, hot path tie-ized over the zd byte ABI), deterministic view management (distance selection / frustum culling / budget scheduling over p.2.8), an instanced render backend (reusing p.2.27.1), zd-persisted caching (reusing p.2.3 / p.2.10), the api.lod contract + runtime wiring**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the render-line state (engine.render / engine.render.instancing / runtime.render RenderRuntime + RenderHooksClient); the tie-bridge ABI (engine.tie TieLibrary/TieFunction shaped calls + the generic handle(FunctionDescriptor), the TieRuntime gate, the tiefib_probe dll assembly); the p.2.8 BudgetScheduler/TickBudget semantics; the p.2.7 DeterministicDispatcher/WorkerBank; the p.2.3 engine.zd six-field wire2 records and the zdt doc form; the p.2.10 per-chunk tsha1f; the existing probeAcceptance wiring (97 pure-JVM + 5 E2E)), then produce the sub-item split (adjustable, never expand scope unilaterally). Illustrative directions (target per the ROAD p.2.28 row verbatim; one library = one sub-item): (1) **p.2.28.1 the engine.render.lod data model (pure JDK)** — LodLevel (fixed geometric-ladder order + the fixed distance-threshold table), LodSection/LodQuad (column stacks + horizontal merged quads, fixed field order, byte-identical encoding), **LodVertexLayout compact quantized layout** (int16 quantized positions + color index + facing bits, styled after the p.2.27 VertexLayout, byte-identical, zero MC imports), LodConfigDoc (td-ized quality doc: enabled / max levels / distance thresholds / quality tier / cache policy, toTd/fromTd round-trip identity); **that byte layout is the pinned zd in/out of the tie hot path** (the p.2.28.2 ABI basis). (2) **p.2.28.2 the generation pipeline (hot path tie-ized)** — LodPolygonizer (level 0: region → column-stack downsampling → separated vertical profiles + horizontal-surface merging, fixed sampling rules: top-most surface + surface color/light rules), LodMerger (level n → n+1 inherited merging, the same merge function recursing level by level, mip pyramid; deterministic fixed-order join corners), an incremental dirty set (chunk changes → budgeted batch rebuild, reusing the p.2.7 Dispatcher/WorkerBank); **the polygonizer/merger is implemented in tie** (tiec → DLL → engine.tie FFM, zd byte ABI in/out; deterministic skip or the pure-JDK reference path without a tie lib; probe dual-path same-input-same-output byte-identical golden comparison); generation volume budgeted by the p.2.8 BudgetScheduler semantics. (3) **p.2.28.3 deterministic view management (pure JDK)** — LodDistanceSelector (player position → per-layer expected-level matrix, integer ops same-input-same-output), frustum culling (section AABBs) + horizontal-merge culling (deterministic), LodBudgetScheduler (reusing the p.2.8 engine.sim.budget kernel: per-level caps, atomic accounting, deterministic over-budget skip). (4) **p.2.28.4 the render backend (reusing p.2.27.1 instancing)** — LodRenderBackend registered into BackendRegistry (fixed registration order, duplicate rejection, deterministic default selection), LodShaderTemplate (clean-room fixed-order template compilation → byte-identical canonical GLSL), Level0 edge-seam alignment + distance-fog rules (deterministic crack-free geometry assertions). (5) **p.2.28.5 persistence (LodCache)** — on-disk zd carrier (the p.2.3 engine.zd spec: version header + six-field wire2 records, or the zdt td+base64 doc form — decide at scouting); per-chunk tsha1f fast/strong validation (p.2.10 saveverify semantics, tamper-rejection), cache-hit skips regeneration; coordinated with the p.2.9 world pack / p.2.18 export (a regenerable, exportable or excludable cache). (6) **p.2.28.6 the api.lod contract + runtime wiring (closing)** — api.lod LodQualitySpec/LodDistanceSpec/LodApi (fixed quality-tier order + threshold-table contract constants; api pure JDK with no engine dependency; engine.render.lod mirrors as implementation — same-input-same-output assertions) + runtime.render.lod LodRuntime gated shell (subterra.probe.lod default no-op, the [Subterra lod] marker merged into the AsyncE2EProbe boot) + a RenderHooksClient extension (LOD pass injected at RenderLevelStageEvent + chunk-available callback wiring, read-only patch without changing the vanilla output path) + config rules (the p.2.17 RuleStore reads the LOD tier + the p.2.23 hub form hot-reload) + LodProbe wired into probeAcceptance. Note: **clean-room self-developed + tie-ized** is the ROAD **delivery red line** and runs through every design; **the license red line**: p.2.28 **references no third-party source; Distant Horizons (LGPL-3.0) is reference-of-ideas only, zero source included, not bundled; tie is the house language, not a third-party dependency, used through the p.2.1 bridge with a deterministic skip without a lib**; copyleft is reference-of-ideas only, verbatim copying is forbidden; if scouting finds another reference, follow the pinned discipline first (permissive portable with notices, copyleft clean-room only, uncertain defaults to not allowed); the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.7–p.2.26 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.28 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译；tie DLL 装配先于 runtime/tie 接线）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线/tie 装配）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.28 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile; tie DLL assembly before runtime/tie wiring). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.28 子项一律 **p.2.28.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；LOD 生成管线无全局扫描级依赖；内存优先（紧凑字节布局、WorkerBank 复用，用户核心诉求）。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.28 红线）**：p.2.28 **clean-room 自研 + tie 化，不引用任何第三方源码**——**Distant Horizons（LGPL-3.0 copyleft）评估为「仅思想参考」**（mip 金字塔 / 列柱剖面 / 水平面合并 / 视锥剔除均属公开技术常识，自研实现，零源码包含、不捆绑；如需可于 package-info 注明 clean-room 立场，同 p.2.26/p.2.27 先例）；copyleft 仅参考思想、禁原样复制；存疑按不允许处理；完整登记册见框架设计 §7。**tie 为自家语言（TPL 2.0），非第三方依赖**，经 p.2.1 engine.tie 桥使用（tiec → DLL → FFM），无 tie lib 时确定性 skip；本里程碑**不新增任何第三方移植项**。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；tie/FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（tie 化按需）。
- **验收门（p.2.28 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**97 纯 JVM + 五次 E2E**，**并新增本里程碑的 LOD 确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.28.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the LOD generation pipeline is free of global-scan-style dependencies; memory-first (compact byte layouts, WorkerBank reuse — a core user requirement). Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.28 red line)**: p.2.28 is **clean-room self-developed + tie-ized with no third-party source** — **Distant Horizons (LGPL-3.0 copyleft) was assessed to reference-of-ideas only** (mip pyramids / column stacks / horizontal-surface merging / frustum culling are common public technology implemented self-developed, zero source included, not bundled; a package-info clean-room note is allowed if desired, same precedent as p.2.26/p.2.27); copyleft is reference-of-ideas only, no verbatim copying; uncertain defaults to not allowed (full register in framework design §7). **tie is the house language (TPL 2.0), not a third-party dependency** — used through the p.2.1 engine.tie bridge (tiec → DLL → FFM) with a deterministic skip without a tie lib; this milestone **adds no third-party port**. Acceptance gate (the p.2.28 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **97 pure-JVM + five E2E boots**, **plus the new LOD determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.28 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.28 各子项落地、最新 p 轨号 p.2.28.x、关键路径、LOD clean-room 自研 + tie 化结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.28 之后的下一个里程碑；**若届时 ROAD 行序有变以 ROAD 为准**）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.<next>.md`（bilingual），随当次提交一并提交。

**English**: After p.2.28 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.28 landing, latest track p.2.28.x, key path, the LOD clean-room + tie-ized conclusion; scope pointing to the next milestone after p.2.28 per the then-current ROAD row order; **if the ROAD order changes by then, follow ROAD**; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.<next>.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.28 工作区建议：engine.render.lod（数据模型 + 生成管线 + 视景管理 + 渲染后端 + 持久化，纯 JDK 确定性核心 + polygonizer/merger tie 热点）+ api.lod（api 模块契约，纯 JDK）+ runtime.render.lod 接线（LodRuntime 门控壳 + RenderHooksClient RenderLevelStageEvent 扩展）+ LodProbe。
- **渲染线基座（p.2.27/p.2.27.1 已落地，p.2.28 复用）**：engine.render（RenderStage 10 阶段固定序 / MaterialSurface / RenderSurface / VertexLayout BLOCK 32B / ENTITY 28B）+ engine.render.instancing（InstanceFormat 实例布局 / ShaderTemplate / RenderBackend + BackendRegistry 固定注册序）/ runtime.render RenderRuntime（subterra.probe.render 门控壳、[Subterra render] marker）+ RenderHooksClient（RenderLevelStageEvent 注入点，门控 no-op）。
- **tie 桥（p.2.1 已落地，p.2.28 tie 化复用）**：engine.tie TieLibrary.load(dll) + TieFunction（invoke0/invokeI64/invokeI64I64/invokeF64 + handle(FunctionDescriptor) 通用形态）；runtime.tie TieRuntime（subterra.tie.lib 属性/捆绑资源定位 dll、无 lib 确定性 skip、[Subterra tie] ok/mismatch）；devkit TieBridgeProbe + tieBridgeProbe 任务。LOD 热点 tie 化：zd 字节 ABI（模块.函数(字节进)→字节出），tiec 编译 DLL 装配随摸底定。
- **预算/并行/持久化语义（复用）**：p.2.8 engine.sim.budget BudgetScheduler/TickBudget（per/region/global cap、原子记账、确定性 key 序降级）；p.2.7 DeterministicDispatcher/WorkerBank（重复键显式拒绝、并行/串行逐位一致）；p.2.3 engine.zd（六字段 wire2 记录、zdt 文档形态）；p.2.10 saveverify（逐块 tsha1f 双档、篡改拒绝）。
- **probeAcceptance 接线基准（p.2.26 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **97 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.28 在此之上新增 LOD 确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.26 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.render.lod 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.26 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.26 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.28 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处（LodProbe）；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.27/p.2.27.1 渲染线（已落地，p.2.28 基座）/ p.2.28 LOD（本里程碑，ROAD 行序紧随 p.2.26 之后新增的渲染线续项）；p.2.28 为 LOD 线首项（承接 p.2.27 自研渲染模型、p.2.27.1 实例化基座、p.2.8 预算调度、p.2.3/p.2.10 持久化与校验、p.2.1 tie 桥）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.28 landing: engine.render.lod (data model + generation pipeline + view management + render backend + persistence — pure-JDK deterministic cores with the polygonizer/merger tie hot path) + api.lod (in the api module, pure JDK) + runtime.render.lod wiring (the LodRuntime gated shell + the RenderHooksClient RenderLevelStageEvent extension) + LodProbe. The render-line base (p.2.27/p.2.27.1 landed, reused by p.2.28): engine.render (RenderStage 10-stage fixed order / MaterialSurface / RenderSurface / VertexLayout BLOCK 32B / ENTITY 28B) + engine.render.instancing (InstanceFormat / ShaderTemplate / RenderBackend + BackendRegistry fixed registration order) / runtime.render RenderRuntime (the subterra.probe.render gated shell, the [Subterra render] marker) + RenderHooksClient (the RenderLevelStageEvent injection point, gated no-op). The tie bridge (p.2.1 landed, reused by the p.2.28 tie-ization): engine.tie TieLibrary.load(dll) + TieFunction (invoke0/invokeI64/invokeI64I64/invokeF64 + the generic handle(FunctionDescriptor)); runtime.tie TieRuntime (the subterra.tie.lib property / bundled-resource dll location, deterministic skip without a lib, [Subterra tie] ok/mismatch); the devkit TieBridgeProbe + tieBridgeProbe task. LOD hot-path tie-ization: the zd byte ABI (module.function(bytes in) → bytes out); the tiec DLL assembly is settled at scouting. Budget/parallel/persistence semantics (reused): the p.2.8 engine.sim.budget BudgetScheduler/TickBudget (per/region/global caps, atomic accounting, deterministic key-order degradation); the p.2.7 DeterministicDispatcher/WorkerBank (explicit duplicate-key rejection, byte-identical parallel/serial); the p.2.3 engine.zd (six-field wire2 records, the zdt doc form); the p.2.10 per-chunk tsha1f dual-tier validation (tamper-rejection). probeAcceptance wiring baseline (p.2.26 closing): **97 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.28 adds the LOD determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.26 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.render.lod wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.26): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.26 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.28 adds deterministic markers only as needed). New probes under the devkit probes (LodProbe); wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.27/p.2.27.1 the render line (landed, the p.2.28 base) / p.2.28 LOD (this milestone, the render-line continuation added to the ROAD right after p.2.26); p.2.28 is the first LOD-line item (building on the p.2.27 self-render model, the p.2.27.1 instancing base, the p.2.8 budget scheduling, the p.2.3/p.2.10 persistence & validation, and the p.2.1 tie bridge).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 97 纯 JVM + 五次 E2E 与新增 LOD 探针）+ ROAD p.2.28 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 97 pure-JVM + five E2E boots plus the new LOD probes), the "ROAD p.2.28 landed" note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.