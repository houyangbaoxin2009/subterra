# Subterra p.2.8 engine.sim 增量确定性模拟底座 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.7 全轨 landing 后，按 p.2.7 交接单 §5 生成指向 p.2.8 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.7 fully landed, §5 of the p.2.7 handoff produces this p.2.8 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.8 里程碑**的交接单：p.2.8 =「engine.sim 增量确定性模拟底座」——世界推演（时间 / 物理 / 生态 / 经济 / 势力）的**增量确定性模拟基础设施**：只推进发生变化的局部、不重算全局；每次运行同输入同产出，与串行/既有路径逐位一致。p.2.7（engine.parallel 确定性并行执行底座，已落地）为其执行地基，p.2.6（C2ME 直接包含：engine.worldgen.async，已落地）为同族范式参考，p.2.9 World-as-artifact 为后续下游；本里程碑把 p.2.7 已确立的确定性并行范式（通用键分区 + 有界 bank + 重复键显式拒绝 + key 序归并 + 并行/串行逐位一致）消费为模拟核心的并发底座。核心原则：**确定性优先 + 增量计算 + 预算分级 + 分区空间索引无全局扫描 + 接 engine.parallel（p.2.7）**；engine 纯 JDK 不碰 MC、有界并行不放大主线程热路径阻塞。执行模式与 p.2.5 / p.2.6 / p.2.7 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.8.x**（≤三级），禁止「阶段X / 第N块 / R1 / block N」等表述。工作方式：先**摸底**（评估 engine.parallel（p.2.7）与 engine.worldgen.async（p.2.6）可复用的确定性原语、确认预算分级/分区索引/增量计算在既有底座上的落点、定位可复用的 key 域与确定性归并切面）并产出全轨子项拆分（6–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.8-milestone handoff: p.2.8 = “engine.sim incremental deterministic simulation foundation” — an **incremental deterministic simulation infrastructure** for world evolution (time / physics / ecology / economy / factions): advance only the parts that change locally, never re-simulate the whole world; same input → same output every run, byte-identical to serial / existing paths. p.2.7 (engine.parallel deterministic parallel execution base — landed) is its execution foundation, p.2.6 (C2ME bundled directly: engine.worldgen.async — landed) is the sibling-pattern reference, p.2.9 World-as-artifact is the downstream; this milestone consumes the deterministic parallel paradigm established in p.2.7 (generic key partition + bounded bank + explicit duplicate-key rejection + key-order merge + parallel/serial byte-identical) as the simulation core's concurrency base. Core principles: **determinism-first + incremental computation + budget tiers + partitioned spatial index without global scans + plugged into engine.parallel (p.2.7)**; engine pure JDK (no MC coupling), bounded parallelism that does not amplify main-thread hot-path blocking. Execution mode identical to p.2.5 / p.2.6 / p.2.7 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.8.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (assess the reusable deterministic primitives in engine.parallel (p.2.7) and engine.worldgen.async (p.2.6), confirm where budget tiers / spatial index / incremental computation land on the existing base, and locate the reusable key domains and deterministic merge surfaces) to produce the full-track sub-item split (6–8), then implement sub-item by sub-item, commit after each, and do a final unified review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.7 里程碑「engine.parallel 确定性并行执行底座」已全部落地并推 GitHub**（提交见 git log；编号 p.2.7.1–p.2.7.5，最新 p 轨号 **p.2.7.5**）。关键路径 **engine.parallel**（纯 JDK 核心，跨 `subterra-engine\...\engine\parallel\`）：
  * **p.2.7.1** 通用确定性键分区：`KeyPartition`（任意键域 → bank 的纯确定性划分；long-hash / 双 long / Object 三载，bankCount 与 p.2.6 `ChunkPartition` 逐位一致，双 long 路径与 `ChunkPartition.bankIndex` 逐位互操）。+ `ParallelPartitionProbe`（5 checks，含 interop 断言）。
  * **p.2.7.2** 有界串行 worker bank 泛化：`WorkerBank`（bank 内串行、bank 间并发、有界队列、exactly-once、awaitIdle）。+ `ParallelBankProbe`（7 checks）。
  * **p.2.7.3** 泛型确定性分发器：`DeterministicDispatcher`（提交→bank 路由→全部 join 后按确定性键序归并；**重复键显式拒绝 IllegalArgumentException**，契约升级替代 p.2.6 last-write-wins 非确定性保证）。+ `ParallelDispatchProbe`（11 checks）。
  * **p.2.7.4** 门面：`TaskResult` + `ParallelRunner`（并行 run / 串行 runSerial **逐位一致**、批次生命周期、无线程泄漏、输入序无关）。+ `ParallelEquivalenceProbe`（8 checks）。
  * **p.2.7.5** optim.sched 组合：`engine.optim.sched` 池化暂存经 engine.parallel 运行**逐位一致**不破确定性。+ `ParallelSchedProbe`（6 checks）。
- **p.2.6 C2ME 直接包含（已落地，首个消费方）**：`engine.worldgen.async` 全套（`ChunkPartition`/`AsyncWorkerBank`/`DeterministicDispatcher`/`ChunkKey`/`ChunkTask`/`ChunkProducer`/`ChunkConsumer`/`AsyncChunkPipeline`/`ctx\*`/`io\*`）+ runtime 壳 `runtime.worldgen.async.AsyncChunkRuntime`；ChunkKey 域归并序与 p.2.7 一致（双 long 路径逐位一致）。
- **确定性并行底座结论（p.2.7 核心产出）**：通用键分区 + 有界 bank + 重复键显式拒绝 + key 序归并 + 并行/串行逐位一致——使 engine.parallel **可被任何键域复用**（不再绑定区块坐标）；task 语义与执行器无关、完成后按确定性键序归并；这为 p.2.8 确立了「per-simulant 隔离 + 确定性归并」的默认语义。
- **p.2.8 可复用的前置底座（已预落地）**：`engine.optim.sched` `StatusAdvancingScheduler` + `FlowSchedProbe`（调度原语，p.1.4.23）；p.2.7 `engine.parallel` 全套（任意键域分发/归并）；p.2.6 `engine.worldgen.async` 的 bank/join/ordered-merge 范式；既有 runtime 确定性 `-D`/`-P` 钩子范式（SameSeedCompareHook/WorldProfilerHook/EnhancedChannelRuntime/AsyncChunkRuntime 门控——默认 no-op、原版零接触）。
- **probeAcceptance 现状**：`gradlew :subterra-devkit:probeAcceptance` 通过——**72 个纯 JVM 探针**（含新增 `ParallelPartition`/`ParallelBank`/`ParallelDispatch`/`ParallelEquivalence`/`ParallelSched`）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；新增 engine 包（含 p.2.8 engine.sim）由 iron law 常量池扫描自动覆盖。
- **ABI / 接线事实（已钉死，p.2.8 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；`engine.parallel`（p.2.7）既有 ABI 与确定性分发/归并装载范式；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链，不加新开服若可并入既有壳；staging 用 build/tmp）。
- **关联里程碑**：p.2.7 engine.parallel（已落地，执行地基）；p.2.6 C2ME（已落地，同族范式参考）；p.2.9 World-as-artifact（下游）；p.2.26 engine.time（时间缩放，预算分级调度 BudgetScheduler 衔接）。p.2.8 应为 engine.sim 落地一个通用、可在不改造 p.2.7/p.2.6 的前提下被复用的增量确定性模拟底座。

**English**: See the Chinese block above — this handoff follows the exact p.2.7 closing patterns. p.2.7 “engine.parallel deterministic parallel execution base” fully landed and pushed (commits p.2.7.1–p.2.7.5, latest track **p.2.7.5**); the key path is **engine.parallel** (pure JDK, in `subterra-engine\...\engine\parallel\`): KeyPartition (generic deterministic key partition, bankCount / two-long path bit-identical to p.2.6 ChunkPartition), WorkerBank (bounded serial lanes), DeterministicDispatcher (explicit duplicate-key rejection — contract upgrade from p.2.6 last-write-wins to IllegalArgumentException, deterministic key-order merge), TaskResult + ParallelRunner (parallel run / serial runSerial byte-identical, batch lifecycle, no thread leak, input-order independent), and the optim.sched combination probe (pooled scratch stays byte-identical). p.2.6 C2ME (engine.worldgen.async) is landed as the first consumer; ChunkKey-domain merge order matches p.2.7. **Deterministic-parallel-base conclusion**: generic key partition + bounded bank + explicit duplicate-key rejection + key-order merge + parallel/serial byte-identical make engine.parallel **reusable for any key domain** — task semantics independent of the executor, deterministic key-order merge after completion; this sets the default per-simulant-isolation + deterministic-merge semantics for p.2.8. Pre-landed reusable base: engine.optim.sched StatusAdvancingScheduler + FlowSched (p.1.4.23), the full engine.parallel set (p.2.7), the p.2.6 bank/join/ordered-merge paradigm, and the existing runtime `-D`/`-P` gating style (default no-op, zero vanilla impact). probeAcceptance is all green: 72 pure-JVM probes (incl. the five Parallel* probes) + 5 E2E boots (Boot → Datapack → Save → Network → Async, mustRunAfter chain). Iron law auto-covers new engine packages. p.2.8 should land a general incremental deterministic simulation base for engine.sim, reusable by p.2.9 (downstream) and aligned with p.2.26 engine.time (BudgetScheduler) without reworking p.2.7/p.2.6.

## 2. 本次范围（p.2.8 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（评估 engine.parallel（p.2.7）与 engine.worldgen.async（p.2.6）可复用的确定性原语、确认预算分级/分区索引/增量计算在既有底座上的落点、定位可复用的 key 域与确定性归并切面），再定 6–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 engine.sim 提炼增量确定性模拟底座，承接 p.2.7 范式 + 复用既有确定性底座）：
1. **预算分级调度**：BudgetScheduler 前哨（接 p.2.26 engine.time）——按 tick 预算分级（per-simulant / 区域 / 全局），超预算确定性降级，不破种子确定性。
2. **分区空间索引**：分区化空间查询/邻居发现（无全局扫描），接 engine.parallel 键分区（key 域 = 空间单元坐标）。
3. **增量确定性模拟原语**：同输入同产出、逐位一致——只推进变化局部，增量 diff/回放与全量重算逐位一致。
4. **per-simulant 隔离 + 确定性归并**：复用 engine.parallel（p.2.7）——simulant 按 key 分区隔离，任务并行/串行逐位一致、完成后按确定性键序归并。
5. **确定性探针**：任意 simulant 集并行 vs 串行逐位一致 + 固定分区/归并序断言；保持既有 markers 稳定。
6. **runtime 接线与门控**：runtime.sim 壳默认 no-op（`-D`/`-P` 门控），E2E 尽量并入既有开服壳；iron law 覆盖 engine.sim 包 + probeAcceptance 全量接线。
提示：以上为示意（6–8 个子项以摸底后最小闭环为准）；**确定性优先 + 增量计算 + 预算分级 + 分区空间索引无全局扫描**贯穿每次设计；p.2.7 已证明的范式（per-key 隔离 + 确定性归并 + 并行/串行逐位一致）必须被复用为默认语义。

**English**: Same principle as p.2.5/p.2.6/p.2.7: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (assess the reusable deterministic primitives in engine.parallel (p.2.7) and engine.worldgen.async (p.2.6), confirm where budget tiers / spatial index / incremental computation land, locate reusable key domains and deterministic merge surfaces), then produce a 6–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions distilling an engine.sim incremental deterministic simulation base over the p.2.7 paradigm: (1) budget-tier scheduling — a BudgetScheduler precursor (aligned with p.2.26 engine.time), per-tick budget tiers (per-simulant / region / global), deterministic degradation under budget overrun without breaking seed determinism; (2) partitioned spatial index — partitioned spatial queries / neighbor discovery without global scans, keyed by spatial-cell coordinates onto engine.parallel key partitioning; (3) incremental deterministic simulation primitives — same input → same output byte-identical, advance only the changed local parts, incremental diff/replay byte-identical to a full recompute; (4) per-simulant isolation + deterministic merge — reusing engine.parallel (p.2.7): simulants isolated by key partition, tasks byte-identical parallel vs serial, merged in deterministic key order after completion; (5) deterministic probes — arbitrary simulant sets parallel vs serial byte-identical + fixed partition/merge-order assertions, stable markers; (6) runtime wiring with a default-off gate — runtime.sim shell default no-op (`-D`/`-P` gated), E2E reusing an existing boot where possible, iron-law coverage of engine.sim + full probeAcceptance wiring. Note: determinism-first + incremental computation + budget tiers + partitioned spatial index without global scans run through every design; the p.2.7-proven paradigm (per-key isolation + deterministic merge + parallel/serial byte-identical) must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.8 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/注入核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.8 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.8 子项一律 **p.2.8.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；并行度有上限、不放大主线程热路径阻塞；**无全局扫描**（分区空间索引查询）。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.6 C2ME（MIT）派生设计声明已在根 NOTICE，p.2.7 沿用既有派生头与声明；p.2.8 engine.sim 为自研（clean-room），不新增抄录。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含 BootProbe / DatapackE2EProbe / SaveE2EProbe / NetworkE2EProbe / AsyncE2EProbe 五次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.8.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound parallel core, don't block the main thread hot path; **no global scans** (partitioned spatial-index queries). Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: reuse/derive permissively with attribution only; p.2.6 C2ME (MIT) derived-design notice already lives in the root NOTICE — p.2.7 follows existing derived headers and declarations, and p.2.8 engine.sim is self-developed (clean-room), no new copying. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.8 全轨完成后，必须生成 p.2.9 同格式提示词并纳入记录：更新上下文段（p.2.8 各子项落地、最新 p 轨号 p.2.8.x、关键路径 engine.sim、增量确定性模拟底座结论）、更新本次范围段指向 p.2.9（World-as-artifact 下游）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.9-artifact.md`（bilingual），随当次提交一并提交。

**English**: After p.2.8 fully lands, produce the p.2.9 prompt in the same format (context updated with p.2.8 landing, latest track p.2.8.x, key path engine.sim, the incremental-deterministic-simulation-base conclusion; scope pointing to p.2.9 World-as-artifact; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.9-artifact.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.8 工作区建议：`subterra-engine\...\engine\sim\`（增量确定性模拟底座，纯 JDK）。
- **p.2.7 已落地（可复用/消费，勿另起冲突命名）**：`engine.parallel` 全套（`KeyPartition`/`WorkerBank`/`DeterministicDispatcher`/`TaskResult`/`ParallelRunner`）+ 探针资产 `ParallelPartitionProbe`/`ParallelBankProbe`/`ParallelDispatchProbe`/`ParallelEquivalenceProbe`/`ParallelSchedProbe`。p.2.8 直接消费其任意键域分发/归并范式。
- **p.2.6 async 参考（同族范式）**：`engine.worldgen.async` 全套（`ChunkPartition`/`AsyncWorkerBank`/`DeterministicDispatcher`/`ChunkKey`/`ChunkTask`/`ChunkProducer`/`ChunkConsumer`/`AsyncChunkPipeline`/`ctx\*`/`io\*`）+ runtime 壳 `runtime.worldgen.async.AsyncChunkRuntime`。
- **p.2.26 衔接**：`engine.time`（`TimeDomain`/`BudgetScheduler`，未来）——p.2.8 预算分级调度为其前哨，ABI 预留对齐。
- **p.2.8 复用前置**：`engine.optim.sched` `StatusAdvancingScheduler` + p.1.4.23 `FlowSchedProbe`（调度原语）；既有确定性随机载体 `engine.worldgen.pipeline/noise`（`XoroRandom`/`LegacyRandom`/`PositionalRand`）；runtime 门控范式（`EnhancedChannelRuntime`/`AsyncChunkRuntime`——默认 no-op、原版零接触）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 sim 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.7 engine.parallel（已落地，执行地基）；p.2.6 C2ME（已落地，参考）；p.2.9 World-as-artifact（下游）；p.2.26 engine.time（衔接）。「engine.sim 增量确定性模拟底座」为其通用地基。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.8 work dir: `subterra-engine\...\engine\sim\` (incremental deterministic simulation base, pure JDK). p.2.7 already landed (reuse/consume, no colliding naming): the full engine.parallel set — KeyPartition / WorkerBank / DeterministicDispatcher / TaskResult / ParallelRunner — with the five Parallel* probes; p.2.8 consumes its any-key-domain dispatch/merge paradigm directly. p.2.6 async reference (sibling pattern): the full engine.worldgen.async set plus the runtime shell async.AsyncChunkRuntime. p.2.26 handoff: engine.time (TimeDomain / BudgetScheduler, future) — p.2.8's budget-tier scheduling is its precursor; keep ABI aligned. Reusable predecessors: engine.optim.sched StatusAdvancingScheduler + FlowSched (p.1.4.23), the deterministic random carriers in engine.worldgen.pipeline/noise, and the runtime gating style (EnhancedChannelRuntime / AsyncChunkRuntime — default no-op, zero vanilla impact). New sim probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.7 engine.parallel (landed, execution foundation); p.2.6 C2ME (landed, reference); p.2.9 World-as-artifact (downstream); p.2.26 engine.time (handoff). engine.sim is the general foundation.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.8 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.8 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
