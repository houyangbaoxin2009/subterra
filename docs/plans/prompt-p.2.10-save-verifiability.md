# Subterra p.2.10 Save verifiability 存档可验证 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.9 全轨 landing 后，按 p.2.9 交接单 §5 生成指向 p.2.10 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.9 fully landed, §5 of the p.2.9 handoff produces this p.2.10 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.10 里程碑**的交接单：p.2.10 =「Save verifiability 存档可验证」——把「存档」当作一个**可验证的账本（verifiable ledger）**：在既有存档形态之上引入**强签名 / 内容校验**，使存档（及其数据块 zd 区块）可**损坏自检（corruption self-check）**与**防篡改（tamper-evident）**——任意字节被破坏 / 篡改 / 换序都能被确定性发现；zd 区块 + ed25519（或 tsha1f）签名，完善 p.2.3（Save rework，已落地：SaveContainer 统一存档形态 + engine.zd zd v2 载体 + zdt 混合文档）的存档可信性底座，并复用 p.2.4/p.2.5 已确立的 `tsha1f` 便携强校验（快/强双档）与 p.2.9（已落地：世界包统一档案形态）的确定性归档/往返范式。核心原则：**存档 = 可验证账本优先 + 复用既有 p.2.3 SaveContainer/engine.zd/zdt 文档形态不另起炉灶 + zd 区块级校验元数据（固定排序、无时序）+ ed25519/tsha1f 强签名 + 损坏/篡改确定性探针**；engine 纯 JDK 不碰 MC、签名与校验核心可独立探针。执行模式与 p.2.5 / p.2.6 / p.2.7 / p.2.8 / p.2.9 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.10.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 p.2.3 SaveContainer 各槽与 engine.zd 载体 / zdt 混合文档的可复用切面、p.2.9 WorldPack 的确定性归档/往返契约、p.2.4/p.2.5 的 tsha1f 便携实现位置与强弱两档语义、明确 ed25519 在纯 JDK 的可用路径——确定性注入的公钥/私钥如何落地）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.10-milestone handoff: p.2.10 = “Save verifiability” — treat the save as a **verifiable ledger**: add **strong signatures / content verification** to the existing save form so the save (and its zd chunks) is **corruption self-checking** and **tamper-evident** — any corrupted / tampered / reordered byte is deterministically detected. Put zd chunks + ed25519 (or tsha1f) signatures over the p.2.3 data foundation (Save rework — landed: the SaveContainer unified save form + engine.zd zd v2 carrier + zdt hybrid docs), and reuse the portable `tsha1f` (fast/strong two slots) established in p.2.4/p.2.5 plus the deterministic archival / round-trip paradigm proven in p.2.9 (landed: the uniform world-pack archive form). Core principles: **save-first-as-verifiable-ledger + reuse the existing p.2.3 SaveContainer/engine.zd/zdt doc forms without reinventing + zd-chunk-level verification metadata (fixed sort, no timing) + ed25519/tsha1f strong signing + deterministic corruption/tamper probes**; engine pure JDK (no MC coupling), the signature & verification core independently probeable. Execution mode identical to p.2.5 / p.2.6 / p.2.7 / p.2.8 / p.2.9 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.10.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the reusable seams in the p.2.3 SaveContainer slots + the engine.zd carrier / zdt hybrid docs, the p.2.9 WorldPack deterministic archival / round-trip contract, the p.2.4/p.2.5 portable tsha1f implementation location and its fast/strong semantics, and pin how ed25519 lands in pure JDK — how a deterministically-injected keypair is provisioned) to produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.9 里程碑「World-as-artifact 世界即产物」已全部落地并推 GitHub**（提交 `b72e57d` p.2.9.1 → `f1208e1` p.2.9.2 → `5c89ae5` p.2.9.3 → `b0bd50f` p.2.9.5 → `c3e8fd4` p.2.9.4 → `b657589` p.2.9.6，编号 p.2.9.1–p.2.9.6，最新 p 轨号 **p.2.9.6**、随本次收尾落到 p.2.9.7）。世界包抽象结论：engine.world `WorldPack`（单一 td 文档 `world=[version,meta,save,datapack]`，save/datapack 为 base64 载荷，meta TreeMap 排序、段序固定）+ `export(rehydrate(export(w))).equals(export(w))` 逐字节往返契约 + p.2.8 稳态 meta 键衔接；`WorldPackPacker` 世界工作树目录↔单 td 打包/还原（只扫六固定 SaveSlot 目录、无全局遍历、路径穿越防护，可搬移互导）；`engine.export` 导出端口预留（`ExportKind`/`ExportPort`/`ExportHub`，preload 确定性注册 WORLD_PACK + SAVE 两内建端口，p.2.18 前哨）；runtime `WorldRuntime` 门控壳（-Dsubterra.probe.world 默认 no-op、[Subterra world] marker）。关键路径 **engine.world / engine.export**（纯 JDK，`subterra-engine\...\engine\world\` + `engine\export\`）。/ p.2.9 “World-as-artifact” fully landed and pushed (commits p.2.9.1→…→p.2.9.6; the closing commit is p.2.9.7); see git log. The world-pack abstraction conclusion: engine.world `WorldPack` (single td doc world=[version,meta,save,datapack], base64 payloads, TreeMap-sorted meta, fixed section order) + byte-exact export(rehydrate(export(w))).equals(export(w)) round-trip + p.2.8 steady-state meta keys; `WorldPackPacker` dir↔single-td pack/restore (only the six SaveSlot dirs, no global scan, path-traversal guards, movable); `engine.export` port cut reserved (`ExportKind`/`ExportPort`/`ExportHub`, WORLD_PACK + SAVE preloaded, p.2.18 seam); the gated `runtime.world.WorldRuntime` shell ([Subterra world] markers). Key path engine.world / engine.export (pure JDK).
- **p.2.3 Save rework（已落地，数据底座，p.2.10 直接消费）**：统一 `SaveContainer`（世界 + 配置 + 藏录 Ledger + 领域档案 + 遗物条目 + 台账六槽）；`engine.zd` 通用 zd v2 载体（10 字节头精确保持 + 固定六字段 wire2 记录读写 + 树平铺往返）+ zdt 混合文档（td 元数据 + base64 zd 载荷）；`SaveExportArchive` td 导出档案（与 `DatapackExportArchive` 同构，export∘rehydrate 逐字节）；双层配置 + subterra-migrate 首块迁移管线。/ p.2.3 Save rework (landed, p.2.10's data foundation): the SaveContainer six-slot unified form, engine.zd zd v2 carrier (pinned 10-byte header, six-field wire2 records, tree round-trip) + zdt hybrid docs (td metadata + base64 zd payload), the SaveExportArchive td export archive (byte-identical rehydrate), two-tier config, and the first migrate pipeline.
- **确定性档案形态结论（p.2.3 + p.2.2 + p.2.9 核心产出）**：datapack 的 `DatapackPack`/`DatapackExportArchive`、save 的 `SaveExportArchive`、世界包的 `WorldPack`，都确立「单一 td 文档 + base64 载荷 + 固定排序 + export∘rehydrate 逐字节恒等」的规范形；p.2.10 的校验元数据/签名字段应复用该归档范式，作为存档 zdt 文档内一层确定性附加。
- **tsha1f 便携强校验（p.2.4/p.2.5 已落地）**：`engine.network.integrity` 的 tsha1f 纯 JDK 实现（快档 n=8 → 8B、强档 n=48 → 32B，单字节篡改即拒，tie-main 金标 KAT 全中）。p.2.10 可复用为内容校验的轻量可移植路径之一；签名语义候选 ed25519（纯 JDK 可用路径，密钥确定性注入）。
- **probeAcceptance 现状（p.2.9 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**78 个纯 JVM 探针**（含新增 `WorldPackProbe` 20 checks + 既有 Sim*/Network*/P2P*/Datapack*/Save* 全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra world]` 断言，与既有 [Subterra async]/[Subterra sim] 一起，三壳联合）。铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。
- **ABI / 接线事实（已钉死，p.2.10 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；`engine.*` 既有 ABI 与确定性档案形态（`DatapackPack`/`DatapackExportArchive`/`SaveExportArchive`/`WorldPack`）既有协议；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链，不加新开服若可并入既有壳；staging 用 build/tmp）。`/subterra export` 的 save 接线留 p.2.18。
- **关联里程碑**：p.2.3 Save（已落地，数据底座）；p.2.2 Datapack（已落地，数据底座）；p.2.4/p.2.5 network（已落地，tsha1f 关键夹具）；p.2.9 World-as-artifact（已落地，世界包/确定性归档范式）；p.2.18 engine.export（未来，导出端口统一收口）。p.2.10 应为 engine / runtime 落地一个可在不改造 p.2.2/p.2.3/p.2.9 的前提下复用既有 save/zdt 文档形态的「可验证存档案」抽象。

**English**: See the Chinese block above — this handoff follows the exact p.2.5/p.2.9 closing patterns. p.2.9 “World-as-artifact” fully landed and pushed (commits p.2.9.1–p.2.9.6, latest track **p.2.9.6**, closing p.2.9.7); the world-pack abstraction (engine.world `WorldPack` uniform td form + `WorldPackPacker` dir round-trip + `engine.export` world/save port cut) is the deterministic-archival paradigm p.2.10 mirrors. p.2.3 Save rework (landed) is p.2.10's data foundation: the SaveContainer six slots, engine.zd zd v2 carrier, zdt hybrid docs, and the SaveExportArchive. tsha1f (p.2.4/p.2.5, `engine.network.integrity`) is the portable fast/strong checksum fixture; ed25519 (pure-JDK path, deterministically-injected keypair) is the signature candidate. probeAcceptance is all green: **78 pure-JVM probes** (incl. WorldPackProbe 20 checks) + **5 E2E boots** (Boot → Datapack → Save → Network → Async+world, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world markers together). Iron law auto-covers new engine packages. p.2.10 should land a “verifiable-save archive” abstraction for engine/runtime that reuses the existing save/zdt doc forms (without reworking p.2.2/p.2.3/p.2.9) and proofs each zd chunk.

## 2. 本次范围（p.2.10 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 p.2.3 SaveContainer 各槽与 engine.zd 载体 / zdt 混合文档的可复用切面、p.2.9 WorldPack 确定性归档/往返契约、p.2.4/p.2.5 tsha1f 实现位置与强弱两档、ed25519 纯 JDK 可用路径与确定性密钥注入方式），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 Save verifiability 提炼可验证存档案抽象，承接 p.2.3 SaveContainer/zdt + engine.zd 文档形态 + 复用 tsha1f/确定性签名）：
1. **zd 区块划分与校验元数据**：为存档 zdt 文档内的 zd 区块定义确定性划分与校验元数据（固定排序、无时序），损坏自检——逐字节破坏 / 篡改 / 换序都被确定性发现。
2. **ed25519 / tsha1f 强签名**：对存档内容施加强签名（候选 ed25519 纯 JDK 签名，或复用 tsha1f 便携快/强双档）；**密钥确定性注入**（同一输入恒得同一签名，签名结果可复核）。
3. **损坏 / 篡改确定性探针**：探针任意构造损坏（单字节翻转 / 截断 / 换序 / 错钥 / 重放）并断言全部判定为「不通过」；完好存档「通过」；纯 JVM 可独立跑。
4. **runtime 门控壳并入既有开服**：runtime 校验壳默认 no-op（`-Dsubterra.probe.saveverify` 门控），E2E 尽量并入现有 AsyncE2EProbe 开服，保持五次开服不变。
5. **与 p.2.3 文档形态同构**：校验元数据 / 签名作为存档 zdt 文档内的确定性附加（复用单一 td 文档 + base64 载荷 + 固定排序范式），不新增平行文档体系。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**存档 = 可验证账本优先 + 复用既有 p.2.3 文档形态 + 确定性归档/签名 + 损坏/篡改确定性探针**贯穿每次设计；p.2.2/p.2.3/p.2.9 已证明的档案范式（单一 td 文档 + base64 载荷 + 固定排序 + 往返恒等）必须被复用为默认语义。

**English**: Same principle as p.2.5/…/p.2.9: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the reusable seams in the p.2.3 SaveContainer slots + engine.zd carrier / zdt hybrid docs, the p.2.9 WorldPack deterministic archival / round-trip contract, the p.2.4/p.2.5 tsha1f implementation and its fast/strong slots, the pure-JDK ed25519 path and deterministic keypair injection), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions distilling a verifiable-save archive over the p.2.3 SaveContainer/zdt + engine.zd forms: (1) zd-chunk partitioning + verification metadata — deterministic chunk boundaries and per-chunk verify metadata (fixed sort, no timing), corruption self-check — single-byte corruption / tampering / reorder deterministically detected; (2) ed25519 / tsha1f strong signature — sign the save content (ed25519 pure JDK, or reuse portable tsha1f fast/strong), **deterministically-injected keys** (same input → same signature, re-verifiable); (3) deterministic corruption / tamper probe — construct corruptions (single-byte flip / truncation / reorder / wrong key / replay) and assert all are rejected, intact save accepted, pure-JVM standalone; (4) a gated runtime shell merged into the existing boot chain — default no-op (`-Dsubterra.probe.saveverify`), E2E reusing the AsyncE2EProbe boot (five boots unchanged); (5) isomorphic to the p.2.3 doc form — verify metadata / signature as a deterministic addition inside the save zdt doc (single td doc + base64 + fixed sort), no parallel doc system. Note: **save-first-as-verifiable-ledger + reuse the existing p.2.3 doc forms + deterministic archival/signing + corruption/tamper probes** runs through every design; the p.2.2/p.2.3/p.2.9-proven archive paradigm (single td doc + base64 payload + fixed sort + round-trip identity) must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.10 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/注入核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.10 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.10 子项一律 **p.2.10.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性归档**（固定排序、禁时序）；签名/校验核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.10 Save verifiability 为自研（clean-room），不新增抄录；ed25519 采用纯 JDK 既有可用路径（若涉及第三方库，仅 permissive 且保留声明，否则用 JDK 内建或不引库）。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五探针/A*E2E 探针五次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.10.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic archival** (fixed sort, no timing); the signature/verification core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.10 Save verifiability is self-developed (clean-room), no new copying; ed25519 uses only existing pure-JDK paths (if a third-party lib is needed, permissive + attribution only, else JDK built-in or lib-free). Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.10 全轨完成后，必须生成 p.2.11 同格式提示词并纳入记录：更新上下文段（p.2.10 各子项落地、最新 p 轨号 p.2.10.x、关键路径、可验证存档案抽象结论）、更新本次范围段指向 p.2.11（Session programmable 会话可编程，见 ROAD p.2.11 行；外部进程经 tink 帧驱动游戏运行时，接 p.2.5）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.11-session-programmable.md`（bilingual），随当次提交一并提交。

**English**: After p.2.10 fully lands, produce the p.2.11 prompt in the same format (context updated with p.2.10 landing, latest track p.2.10.x, key path, the verifiable-save archive conclusion; scope pointing to p.2.11 Session programmable, per ROAD p.2.11; external processes drive the game runtime via tink frames, over p.2.5; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.11-session-programmable.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.10 工作区建议：`subterra-engine\...\engine\saveverify\`（可验证存档案核心抽象，纯 JDK）+ runtime MC 壳按需落根 sourceSet。
- **p.2.9 已落地（世界包 / 确定性归档范式，可消费/衔接）**：`engine.world` `WorldPack`/`WorldPackResult`/`WorldPackPacker` + `engine.export` `ExportKind`/`ExportPort`/`ExportHub`（WORLD_PACK + SAVE 两内建端口；p.2.18 前哨）+ runtime 壳 `runtime.world.WorldRuntime` + 探针 `WorldPackProbe`。统一档案形态（单一 td 文档 + base64 载荷 + TreeMap 固定排序 + export∘rehydrate 逐字节恒等）为 p.2.10 校验元数据/签名的默认范式。
- **p.2.3 save 落点（数据底座，复用/消费）**：`SaveContainer` 六槽 + `engine.zd` zd v2 载体 + zdt 混合文档（td 元数据 + base64 zd 载荷）+ `SaveExportArchive` td 导出档案（`runtime.save` 壳 + `SaveE2EProbe`）。
- **tsha1f 便携强校验（p.2.4/p.2.5，复用）**：`engine.network.integrity` tsha1f 纯 JDK 实现（快档 n=8 → 8B、强档 n=48 → 32B、单字节篡改即拒、tie-main 金标 KAT）；p.2.10 作为内容校验可移植路径之一；ed25519 纯 JDK 可用路径候选 + 确定性密钥注入。
- **p.2.18 衔接（未来，导出端口预留）**：`engine.export`——校验元数据/签名作为可导出仓库字段；save 指令接线留 p.2.18。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 saveverify 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.3 Save（已落地，数据底座）；p.2.4/p.2.5 network（已落地，tsha1f 关键夹具）；p.2.9 World-as-artifact（已落地，确定性归档/世界包范式）；p.2.18 engine.export（未来，导出端口）。「Save verifiability 存档可验证」为其可验证存档案抽象。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.10 work dir: `subterra-engine\...\engine\saveverify\` (verifiable-save archive core abstraction, pure JDK) + runtime MC shell in the root sourceSet as needed. p.2.9 already landed (world-pack / deterministic-archival paradigm, consume/hand off): engine.world WorldPack/WorldPackResult/WorldPackPacker + engine.export ExportKind/ExportPort/ExportHub (WORLD_PACK + SAVE built-ins; p.2.18 seam) + runtime.world.WorldRuntime + WorldPackProbe; the uniform archive form (single td doc + base64 payload + TreeMap fixed sort + byte-exact rehydrate) is p.2.10's default paradigm for verify metadata / signatures. p.2.3 save landing (data foundation): SaveContainer six slots + engine.zd zd v2 carrier + zdt hybrid docs + SaveExportArchive td export archive (runtime.save shell + SaveE2EProbe). p.2.4/p.2.5 tsha1f (engine.network.integrity, portable fast=8B strong=32B, single-byte tamper reject, tie-main KAT) is a reusable content-verification path; ed25519 pure-JDK path candidate + deterministic keypair injection. New saveverify probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.3 Save (landed, data foundation); p.2.4/p.2.5 network (landed, tsha1f fixture); p.2.9 World-as-artifact (landed, deterministic archival paradigm); p.2.18 engine.export (future, export port). Save verifiability is the verifiable-save archive.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.10 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.10 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.