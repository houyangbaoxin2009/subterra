# Subterra p.2.13 Deterministic PCG 确定性内容生成 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.12 全轨 landing 后，按 p.2.12 交接单 §5 生成指向 p.2.13 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.12 fully landed, §5 of the p.2.12 handoff produces this p.2.13 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.13 里程碑**的交接单：p.2.13 =「Deterministic PCG 确定性内容生成」——**名称 / 文本 / 配方 / 遗物 / 群系变种 / 本地化的生成器编排底座**：纯函数、同种子一致，模组内容由「手写清单」变为「种子驱动的确定性生成」。核心原则：**确定性 PCG（同种子同输出、固定序、禁时序）+ 纯函数生成器（输入→输出的无副作用映射）+ 生成器编排底座（多个生成器组合与依赖，schema 驱动生成计划）+ 生成物可探针（脚手架自身含确定性 self-test）+ 接入既有 schema / worldgen 机制**；engine 纯 JDK 不碰 MC、生成核心可独立探针。执行模式与 p.2.5 / p.2.7 / p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.13.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 engine.schema / engine.scaffold 的模型与生成范式现状、worldgen 接入点、devkit 探针资产、生成计划该接的既有形态）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.13-milestone handoff: p.2.13 = “Deterministic PCG” — **a generator-orchestration base for names / text / recipes / relics / biome-variants / localization**: pure functions, reproducible from the same seed, shifting mod content from “hand-written lists” to “seed-driven deterministic generation”. Core principles: **deterministic PCG (same seed → same output, fixed order, no timing) + pure-function generators (side-effect-free input→output mapping) + a generator-orchestration base (multiple generators composed with dependencies, a schema-driven generation plan) + probeable generated artifacts (the base carries a deterministic self-test) + plugging into the existing schema / worldgen mechanisms**; engine pure JDK (no MC coupling), the generation core independently probeable. Execution mode identical to p.2.5 / p.2.7 / p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.13.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the engine.schema / engine.scaffold model and generation paradigms, the worldgen hook points, the devkit probe assets, and the existing generation-plan shapes to hook), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.12 里程碑「Schema-first scaffolding 模式先行脚手架」已全部落地并已推 GitHub，最新 p 轨号 p.2.12.6（收尾提交）**：engine `subterra-engine\...\engine\schema\` + `...\engine\scaffold\`（纯 JDK）——td schema 模型 `Schema`/`SchemaField`/`SchemaKind`/`SchemaCodec`/`SchemaReport`/`SchemaViolationException`（确定性定型 / 校验 / 遍历，td 为单一事实源、固定字段序）+ 确定性生成器 `engine.scaffold.RegisterCodeGen`（schema→注册接线代码，同 schema 同字节、往返恒等）、`SchemaStubGen`（schema→探针骨架 + 开发文档文本）、`FormDataGen`（schema→表单结构 / 渲染数据纯数据，kind→widget 固定映射，UI 留 p.2.23）；纯 JVM 探针 `ScaffoldProbe` 40 项确定性断言（schema 定型 / 违约 / validate + 三生成器逐字节一致与往返恒等）；顺带修复 **TdTable 首现去重吞掉重复键**的死代码缺陷（`duplicates()` 附加 API）。关键路径 **engine.schema + engine.scaffold**（纯 JDK）。结论：**td schema 为单一事实源的确定性脚手架——定型 / 注册码 / 探针文档 / 表单四生成器同 schema 同字节**，ScaffoldProbe 接入 probeAcceptance；p.2.13 生成计划由此底座驱动。/ p.2.12 “Schema-first scaffolding” fully landed and pushed (commits p.2.12.1→…→p.2.12.5; closing commit p.2.12.6); engine.schema (td-schema model: Schema/SchemaField/SchemaKind/SchemaCodec/SchemaReport/SchemaViolationException, deterministic parse/validate/walk, td as the single source of truth, fixed field order) + engine.scaffold generators (RegisterCodeGen schema→register-wiring / SchemaStubGen schema→probe-skeleton+dev-doc / FormDataGen schema→form-structure pure data, all same schema → same bytes, round-trip stable); ScaffoldProbe 40 deterministic pure-JVM checks; incidental TdTable first-occurs-dedup-swallows-duplicate-keys dead-code fix (duplicates() added). Key path engine.schema + engine.scaffold. Conclusion: a td-schema-as-single-source-of-truth deterministic scaffold — typing / register-code / probe-doc / form-data four generators share one schema and one byte stream. p.2.13's generation plan is driven by this base.
- **p.2.5 P2P / tink v2 / tsha1f / zd 栈（已落地，事实不变）**：engine.network + engine.p2p，语言无关 ABI。/ p.2.5 P2P / tink v2 / tsha1f / zd stack (landed, unchanged): engine.network + engine.p2p, language-agnostic ABI.
- **p.2.3 Save（已落地，数据底座，事实不变）**：统一 `SaveContainer` 六槽 + `engine.zd` zd v2 载体 + zdt 混合文档 + `SaveExportArchive` td 导出档案。/ p.2.3 Save rework (landed, unchanged): the SaveContainer six-slot unified form, engine.zd carrier + zdt hybrid docs, the SaveExportArchive td export archive.
- **p.2.4 server-interaction（已落地，事实不变）**：engine.network 五子引擎层 + runtime EnhancedChannelRuntime 门控壳。/ p.2.4 server-interaction (landed, unchanged): five pure-JVM engine.network cores + the gated runtime shell.
- **p.2.8 sim / p.2.9 world / p.2.10 saveverify / p.2.11 session（已落地，事实不变）**：engine.sim / engine.world（WorldPack + engine.export 端口预留在 p.2.12 后仍不变）/ engine.saveverify / engine.session 及其门控壳，「门控壳并入既有 boot」范式。/ p.2.8 sim / p.2.9 world (WorldPack + engine.export port cut unchanged) / p.2.10 saveverify / p.2.11 session (landed, unchanged): the gate-shell-into-existing-boot paradigm.
- **probeAcceptance 现状（p.2.12 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**81 个纯 JVM 探针**（含新增 `ScaffoldProbe` 40 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.12 closing): green — **81 pure-JVM probes** (incl. ScaffoldProbe 40 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.13 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；td（tiedb-td 文档）为本框架数据一等语言，既有 engine.datapack / engine.save / engine.export / engine.world 均已确立「td schema → 文档/注册」范式；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12 脚手架复用并钉死；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.13 为**革命·新增**，直接消费 p.2.12 的 td-schema 确定性脚手架底座、接 p.2.14 engine.interact；「无尽探索」（随机名称生成世界 / 随机配方 / 遗物 / 群系变种 / 本地化）的框架化底座正是其目标。（接 schema-first p.2.12 / worldgen）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、engine/schema 与 engine/scaffold 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.5/p.2.9/p.2.10/p.2.11/p.2.12 closing patterns. p.2.12 “Schema-first scaffolding” fully landed and pushed (commits p.2.12.1–p.2.12.5, latest track **p.2.12.6**): engine.schema (td-schema model — Schema/SchemaField/SchemaKind/SchemaCodec/SchemaReport/SchemaViolationException, deterministic parse/validate/walk, td as the single source of truth, fixed field order) + engine.scaffold deterministic generators (RegisterCodeGen / SchemaStubGen / FormDataGen, same schema → same bytes, round-trip stable) is the p.2.13 handoff basis; p.2.13's generation plan is driven by this base. p.2.5 / p.2.3 / p.2.4 / p.2.8 / p.2.9 / p.2.10 / p.2.11 all landed unchanged (see Chinese block). td is the framework's first-class data language, with an established “td schema → document/registration” paradigm across engine.datapack / engine.save / engine.export / engine.world; the deterministic byte-identical generation paradigm is proven by p.2.7/p.2.8 and pinned by p.2.12. probeAcceptance all green: **81 pure-JVM probes** (incl. ScaffoldProbe 40 checks) + **5 E2E boots** (AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages. p.2.13 should land a deterministic, pure-function PCG generator-orchestration base (names / text / recipes / relics / biome-variants / localization, same-seed reproducible) that builds on the p.2.12 schema-driven deterministic scaffold, per the ROAD p.2.13 milestone.

## 2. 本次范围（p.2.13 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 engine.schema / engine.scaffold 的模型与生成范式现状、worldgen 接入点、devkit 探针资产、生成计划该接的既有形态），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 p.2.13 提炼由种子驱动的确定性 PCG 生成器编排底座，承接 p.2.12 已确立的 td-schema 确定性脚手架范式）：目标见 ROAD p.2.13 行：「**名称 / 文本 / 配方 / 遗物 / 群系变种 / 本地化的生成器编排底座**——纯函数、同种子一致（接 schema-first p.2.12 / worldgen）；「无尽探索」的框架化底座」。示意子项（示例）：
1. **确定性生成核心**：种子扩散 / 可复现随机流（自种子的确定性 RNG、固定序、可回放），作为所有生成器的确定性基底（纯 JDK、同种子 → 同一序列）。
2. **名称 / 文本生成**：由种子确定性生成名称与文本（音节 / 词素 / 模板，固定序、同种子一致）。
3. **内容生成器**：配方 / 遗物 / 群系变种 / 本地化的生成器（纯函数生成器，复用 p.2.2 datapack / p.2.3 藏录次元 / p.2.16 规则语义，生成结果稳定）。
4. **schema 驱动生成计划**：生成器编排底座——多个生成器的组合与依赖由 schema 驱动的生成计划描述（接 p.2.12 engine.schema td-schema 模型，p.2.16 契约语义）。
5. **确定性 PCG 探针**：PcgProbe 脚手架自身含确定性 self-test（给定种子 → 期望输出逐字节一致），纯 JVM 可独立跑。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**确定性 PCG（同种子同输出）+ 纯函数生成器 + 生成器编排（schema 驱动生成计划）+ 生成物可探针 + 生成计划接入既有注册机制**贯穿每次设计；p.2.12 已钉死的「单 schema 单字节流」确定性生成范式与 p.2.7/p.2.8 逐字节一致范式必须被复用为默认语义。

**English**: Same principle as p.2.7–p.2.12: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the engine.schema / engine.scaffold model and generation paradigms, the worldgen hook points, the devkit probe assets, and the existing generation-plan shapes to hook), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.13 row: “a **generator-orchestration base for names / text / recipes / relics / biome-variants / localization** — pure functions, same-seed reproducible (builds on schema-first p.2.12 / worldgen); the framework base for Endless Exploration.” Illustrative directions: (1) a deterministic generation core — seed diffusion / reproducible random stream (deterministic, fixed-order, replayable) as the deterministic substrate for all generators (same seed → the same sequence); (2) name / text generation — deterministic naming & prose from a seed (syllables / morphemes / templates, fixed order); (3) content generators — recipes / relics / biome-variants / localization generators (pure-function, reusing p.2.2 datapack / p.2.3 relic dimension / p.2.16 rule semantics, stable output); (4) a schema-driven generation plan — the generator-orchestration base describing composition & dependencies of multiple generators via a schema (over the p.2.12 engine.schema td-schema model, p.2.16 contract semantics); (5) a deterministic PCG probe — PcgProbe carries a deterministic self-test (given seed → expected byte-identical output), pure-JVM standalone. Note: **deterministic PCG (same seed → same output) + pure-function generators + generator orchestration (schema-driven plan) + probeable generated artifacts + the plan plugging into the existing registration mechanism** runs through every design; the pinned “one schema → one byte stream” deterministic generation paradigm of p.2.12 and the byte-identical paradigm proven by p.2.7/p.2.8 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.13 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、devkit/脚手架子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/生成核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.13 runs as a cluster of sub-agents (scouting, engine, devkit/scaffold, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.13 子项一律 **p.2.13.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性生成**（固定序、禁时序；同种子同输出）；schema/生成核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.13 Deterministic PCG 为自研（clean-room），不新增抄录；td schema 能力复用 p.2.12 engine.schema 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五次 E2E 开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.13.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic generation** (fixed order, no timing; same seed → same output); the schema/generation core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.13 Deterministic PCG is self-developed (clean-room), no new copying; td schema capability reuses the p.2.12 engine.schema built-in. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.13 全轨完成后，必须生成 p.2.14 同格式提示词并纳入记录：更新上下文段（p.2.13 各子项落地、最新 p 轨号 p.2.13.x、关键路径、确定性 PCG 生成器编排底座抽象结论）、更新本次范围段指向 p.2.14（Diegetic interaction，见 ROAD p.2.14 行；engine.interact「无系统气味」的可落实交互层——世界内交互规范与零 HUD 化渲染规则）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.14-interact.md`（bilingual），随当次提交一并提交。

**English**: After p.2.13 fully lands, produce the p.2.14 prompt in the same format (context updated with p.2.13 landing, latest track p.2.13.x, key path, the deterministic-PCG generator-orchestration conclusion; scope pointing to p.2.14 Diegetic interaction, per ROAD p.2.14; engine.interact “no system scent” interaction layer — in-world interaction spec & zero-HUD rendering rules; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.14-interact.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.13 工作区建议：engine 纯 JDK 落 `subterra-engine\...\engine\pcg\`（确定性生成核心 + 生成器 + 生成计划）+ devkit 脚手架（生成计划接线 / PcgProbe）按需落 `subterra-devkit`（生成物落点可探针）。
- **p.2.12 schema-first 脚手架（已落地，p.2.12.6）**：engine.schema（td-schema 模型 Schema/SchemaField/SchemaKind/SchemaCodec + ScaffoldProbe 40 checks）+ engine.scaffold（RegisterCodeGen / SchemaStubGen / FormDataGen，同 schema 同字节、往返恒等）+ probeAcceptance 81 纯 JVM + 5 次 E2E。p.2.13 生成计划由该底座驱动。
- **确定性生成范式（p.2.7/p.2.8 已证明、p.2.12 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.13 生成核心沿用同一约束；同种子 → 同输出。
- **td 一等语言（p.2.2/p.2.3/p.2.9 已确立）**：engine.datapack / engine.save / engine.export / engine.world——「td schema → 文档/注册」范式在 p.2.13 复用于生成计划与生成结果。
- **worldgen 接入（接 p.2.13 / worldgen）**：生成结果经既有注册机制接入（p.2.2 worldgen→CONFIGURED_FEATURE 真注册范式；群系变种生成器等产物落点按需确认）。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.12 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变（p.2.13 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 PCG 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.2 datapack / p.2.3 save / p.2.16 API 契约面 / p.2.20 devkit 完整化；p.2.14 Diegetic interaction（engine.interact）接 p.2.13。「Deterministic PCG」为其种子驱动的确定性内容生成抽象。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.13 work dirs: engine pure JDK in `subterra-engine\...\engine\pcg\` (deterministic generation core + generators + generation plan) + devkit scaffold (plan wiring / PcgProbe) in `subterra-devkit` (generated-artifact landing spots probeable). p.2.12 schema-first scaffolding (landed, closing p.2.12.6): engine.schema (td-schema model) + engine.scaffold (three generators, same schema → same bytes) + ScaffoldProbe 40 checks + probeAcceptance 81 pure-JVM + 5 E2E boots; p.2.13's generation plan is driven by this base. Deterministic generation paradigm (p.2.7/p.2.8 proven, p.2.12 pinned): byte-identical, fixed order, no timing — p.2.13's generation core follows the same constraints (same seed → same output). td first-class language (p.2.2/p.2.3/p.2.9 proven): the “td schema → document/registration” paradigm reused for the generation plan and generated outputs. worldgen hookup (per p.2.13 / worldgen): outputs plug into the existing registration mechanism (the p.2.2 worldgen→CONFIGURED_FEATURE real-registration paradigm; landing spots for outputs such as biome-variant generators confirmed on demand). The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.12 proven): gate-off no-op, markers merged into the AsyncE2EProbe boot, staging via build/tmp, five boots unchanged (p.2.13 adds deterministic markers only where required). New probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.2 datapack / p.2.3 save / p.2.16 API contract surface / p.2.20 devkit completion; p.2.14 Diegetic interaction (engine.interact) builds on p.2.13. Deterministic PCG is the seed-driven deterministic content-generation abstraction.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.13 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.13 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.