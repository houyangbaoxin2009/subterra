# Subterra p.2.19 Runtime wiring migration / runtime 接线迁移 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.18 全轨 landing 后，按 p.2.18 交接单 §5 生成指向 p.2.19 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.18 fully landed, §5 of the p.2.18 handoff produces this p.2.19 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.19 里程碑**的交接单：p.2.19 =「Runtime wiring migration / runtime 接线迁移」——**launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入 runtime，export 指令接线（接 p.2.18）**（自原 p.2.5 顺移，见 ROAD p.2.19 行；示意子项可由收尾时按仓库现状给演示）。**export hub 指令已在 p.2.18 部分落地**（`/subterra export hub` 接线 + `subterra.probe.exportHub` 门控钩子）——本次把已有 runtime 壳/命令（p.2.2.7 `/subterra export`、p.2.17.4 rule、p.2.18.5 export hub，及 p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6、p.2.8 async+sim 等门控壳）收编进统一接线盘点。执行模式与 p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 / p.2.18 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.19.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 runtime 现状：p.2.18 export hub 指令接线、既有 runtime 壳/命令清单与门控钩子范式、launch / runtime-fix / worldgen / optim-shell / tie / cfglog 各引擎侧落点与 MC 壳缺口，以及 devkit 探针资产与 p.2.18 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.19-milestone handoff: p.2.19 = “Runtime wiring migration / runtime 接线迁移” — **migrating launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime, plus the export-command wiring (over p.2.18)** (moved from the former p.2.5, per the ROAD p.2.19 row; illustrative sub-items are given at closing time per current repo state). **The export-hub command is already partially landed in p.2.18** (the `/subterra export hub` wiring + the `subterra.probe.exportHub` gated hook) — this milestone folds the existing runtime shells/commands (the p.2.2.7 `/subterra export`, the p.2.17.4 rule, the p.2.18.5 export hub, plus the p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6, p.2.8 async+sim gated shells) into a unified wiring inventory. Execution mode identical to the p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 / p.2.18 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.19.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the runtime state: the p.2.18 export-hub command wiring, the existing runtime shells/commands and the gated-hook pattern, the engine-side landing points and MC-shell gaps of launch / runtime-fix / worldgen / optim-shell / tie / cfglog, plus the devkit probe assets and the post-p.2.18 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.18 里程碑「Export hub / 导出器 engine.export」已全部落地并已推 GitHub，最新 p 轨号 p.2.18.7（收尾提交）**：engine.export 正式导出枢纽——**p.2.18.1** 功能性 `ExportProducer`（td/zd 双格式 + export∘rehydrate∘export 逐字节恒等契约）+ `ExportKind` 扩为七 form（world/save/datapack/language_keys/config/registries/migrate_maps）+ `ExportHub` 注册序/重复拒对齐 p.2.16.3 `api.ExporterRegistry`（`preload()` 保持 p.2.9.5 语义、`preloadFormal()` 全量注册）+ **p.2.18.2–.4** 五个确定性导出器（language-keys / config（global+overrides+effective 三块字典序、恒等、对齐 RuleStore.resolve）/ world（WorldPack）/ registries / migrate-maps，各字典序固定字段序禁时间戳）+ **p.2.18.5** `/subterra export hub` 接线（`exportForm` 共享核心、`ExportHubCommand`/`ExportHubRuntime`、`subterra.probe.exportHub` 门控钩子 + DatapackE2EProbe 追加 3 断言、既有 32 项不动）+ **p.2.18.6** `ExportHubProbe` 41 checks；另修复提交 ContractProbe 导出 form 断言改前缀语义。关键路径 **engine.export + runtime.export + DatapackE2EProbe**。结论：**导出枢纽七 form 双格式落地 + 修复 ContractProbe**——功能性导出契约（td/zd 双格式 + 回水化恒等）+ 五个确定性导出器 + `/subterra export hub` 接线，全部确定性可探针；probeAcceptance 全绿（87 纯 JVM + 五次 E2E，新增 ExportHubProbe）。/ p.2.18 “Export hub / engine.export” fully landed and pushed, latest track **p.2.18.7** (closing commit): the engine.export formal export hub — p.2.18.1 functional `ExportProducer` (td/zd dual formats + byte-identical export∘rehydrate∘export identity contract) + `ExportKind` grown to seven forms (world/save/datapack/language_keys/config/registries/migrate_maps) + `ExportHub` registration-order/dup-reject aligned with the p.2.16.3 `api.ExporterRegistry` (`preload()` keeps p.2.9.5 semantics, `preloadFormal()` registers all seven) + p.2.18.2–.4 five deterministic exporters (language-keys / config (global+overrides+effective three layers key-sorted, identity, aligned with RuleStore.resolve) / world (WorldPack) / registries / migrate-maps, each key-sorted fixed-field no-timestamps) + p.2.18.5 the `/subterra export hub` wiring (shared `exportForm` core, `ExportHubCommand`/`ExportHubRuntime`, the `subterra.probe.exportHub` gated hook + three new DatapackE2EProbe asserts, existing 32 untouched) + p.2.18.6 `ExportHubProbe` 41 checks; plus the ContractProbe exporter-form assertion fixed to prefix semantics. Key path **engine.export + runtime.export + DatapackE2EProbe**. Conclusion: **the export hub landed in seven forms dual-format + the ContractProbe fix** — a functional export contract (td/zd dual formats + rehydrate identity) + five deterministic exporters + the `/subterra export hub` wiring, all deterministic and probeable; probeAcceptance all green (87 pure-JVM + five E2E boots, incl. ExportHubProbe).
- **p.2.19 目标（ROAD p.2.19 行，本次承接）**：Runtime wiring migration / runtime 接线迁移（自原 p.2.5 顺移）——**launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入 runtime，export 指令接线（接 p.2.18）**。export hub 指令已在 p.2.18 部分落地（`/subterra export hub` + `subterra.probe.exportHub` 门控钩子）——本次**把已有 runtime 壳/命令（p.2.2.7 export、p.2.17.4 rule、p.2.18.5 export hub，及 p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6、p.2.8 async+sim 等门控壳）收编进统一接线盘点**，并完成六类迁入。/ p.2.19 goal (the ROAD row, this handoff): Runtime wiring migration / runtime 接线迁移 (moved from the former p.2.5) — **migrating launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime, plus the export-command wiring (over p.2.18)**. The export-hub command is already partially landed in p.2.18 (the `/subterra export hub` + the `subterra.probe.exportHub` gated hook) — this milestone **folds the existing runtime shells/commands (the p.2.2.7 export, the p.2.17.4 rule, the p.2.18.5 export hub, and the p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6, p.2.8 async+sim gated shells) into a unified wiring inventory**, and completes the six migrations.
- **p.2.18 及更早里程碑（已落地，事实不变）**：p.2.18 导出枢纽（上述，**export 指令接线 p.2.19 承接**）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面（p.2.16.3 export 契约）、p.2.15 engine migration + golden tests（**engine 迁移落点审计范式，p.2.19 迁入复用**）、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session（engine.session）、p.2.10 saveverify、p.2.9 world-as-artifact（engine.export 端口预留 p.2.9.5）、p.2.8 sim、p.2.5 P2P / tink v2 / tsha1f / zd 栈、p.2.4 server-interaction、p.2.3 Save（SaveExportArchive td 导出档案 p.2.3.6）、p.2.2 datapack（**`/subterra export` 指令范式** p.2.2.7）。/ p.2.18 and earlier milestones all landed (unchanged): the export hub (above, **export-command wiring carried to p.2.19**), p.2.17 rule system (engine.config.rules + `/subterra rule`), p.2.16 api contract surface (p.2.16.3 export contract), p.2.15 engine migration + golden tests (**the engine landing-audit pattern, reused by the p.2.19 migrations**), p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 engine.session, p.2.10 saveverify, p.2.9 world-as-artifact (engine.export port cut p.2.9.5), p.2.8 sim, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save (SaveExportArchive td export archive p.2.3.6), p.2.2 datapack (**the `/subterra export` command pattern** p.2.2.7).
- **probeAcceptance 现状（p.2.18 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**87 个纯 JVM 探针**（含新增 `ExportHubProbe` 41 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；DatapackE2EProbe 35 项断言已含 export + export hub + rule markers，AsyncE2EProbe 同开服并入 async/sim/world/saveverify/session 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.18 closing): green — **87 pure-JVM probes** (incl. the new ExportHubProbe 41 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; DatapackE2EProbe 35 asserts incl. the export + export hub + rule markers, AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.19 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.18 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。
- **关联里程碑**：p.2.19 承接 p.2.18 export 指令接线（export hub 指令已部分落地——收编既有 runtime 壳/命令进统一接线盘点）；六类迁入沿用 p.2.15 engine 迁移的落点审计范式（零行为变化、旧 FQCN 缺席确认）；衔接 p.2.20 devkit complete（probes 全量接线 + 官方示例模组脚手架，可用判据）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、runtime 各壳、engine 各核心目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.12–p.2.18 closing patterns. p.2.18 “Export hub” fully landed and pushed (latest track **p.2.18.7**, key path **engine.export + runtime.export + DatapackE2EProbe**): the engine.export formal export hub — functional ExportProducer (td/zd dual formats + byte-identical export∘rehydrate∘export identity) + seven ExportKind forms + ExportHub registration-order/dup-reject aligned with p.2.16.3 api.ExporterRegistry + five deterministic exporters (language-keys / config (three key-sorted layers aligned with RuleStore.resolve) / world (WorldPack) / registries / migrate-maps) + the `/subterra export hub` wiring (shared exportForm core, ExportHubCommand/ExportHubRuntime, subterra.probe.exportHub gated hook) + ExportHubProbe 41 checks + the ContractProbe exporter-form prefix-semantics fix; conclusion: the export hub landed in seven forms dual-format + the ContractProbe fix; probeAcceptance all green (87 pure-JVM + five E2E boots). p.2.19 defines Runtime wiring migration — per the ROAD p.2.19 row (moved from the former p.2.5): **migrating launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime, plus the export-command wiring (over p.2.18)**; the export-hub command is already partially landed in p.2.18, so this milestone folds the existing runtime shells/commands into a unified wiring inventory. Everything pinned by p.2.12–p.2.18 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. probeAcceptance all green: **87 pure-JVM probes** (incl. ExportHubProbe) + **5 E2E boots**. Iron law auto-covers new engine packages. Related: p.2.20 devkit complete (probes fully wired + official sample mod scaffold, the usability gate). Confirm remaining context facts against the repo state (ROAD, git log, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.19 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点 runtime 现状：p.2.18 export hub 指令接线（`/subterra export hub` + `subterra.probe.exportHub`）、既有 runtime 壳/命令清单与门控钩子范式（p.2.2.7 export / p.2.17.4 rule / p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6、p.2.8 async+sim）、launch / runtime-fix / worldgen / optim-shell / tie / cfglog 各引擎侧纯 JDK 落点与 MC 壳缺口，以及 devkit 探针资产与 p.2.18 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.19 行原文）：「Runtime wiring migration / runtime 接线迁移：launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入 runtime，export 指令接线（接 p.2.18）」；export hub 指令已部分落地——把已有 runtime 壳/命令收编进统一接线盘点。示意子项（示例）：
1. **runtime 接线盘点**：统一接线盘点（收编既有 runtime 壳/命令——export / export hub / rule / save / world / network / saveverify / session / async+sim 的门控钩子与 marker 清单，产出接线总表；export 指令接线承接 p.2.18）。
2. **launch 迁入**：launch 接线迁入 runtime（引擎侧启动核心落点确认 + MC 壳接线，沿用既有 boot 范式）。
3. **runtime-fix 迁入**：runtime-fix 接线迁入 runtime（JVM/平台修补类迁入 + 确定性门控）。
4. **worldgen 迁入**：worldgen 接线迁入 runtime（世界生成管线 MC 壳接线，纯 JDK 核心已落 engine）。
5. **optim-shell 迁入**：optim-shell 接线迁入 runtime（优化集 MC 壳接线，纯 JDK 核心已落 engine.optim）。
6. **tie 迁入**：tie 接线迁入 runtime（tie bridge FFM 装载 MC 壳接线，接 p.2.1）。
7. **cfglog 迁入**：cfglog 接线迁入 runtime（td 配置 / 日志 MC 壳接线，纯 JDK 核心已落 engine.config / engine.log）。
8. **export 指令接线收编**：export 指令接线（接 p.2.18——export hub 指令已部分落地，收编 `/subterra export` 与 `/subterra export hub` 到统一指令核心，保持既有 marker 稳定）。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**统一接线盘点（收编既有 runtime 壳/命令）+ 门控 -D/-P 钩子范式 + 保持既有 markers 稳定 + 接入 probeAcceptance** 贯穿每次设计；p.2.12–p.2.18 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.18: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory the runtime state: the p.2.18 export-hub command wiring (`/subterra export hub` + `subterra.probe.exportHub`), the existing runtime shells/commands and the gated-hook pattern (p.2.2.7 export / p.2.17.4 rule / p.2.3.2 save / p.2.9.6 world / p.2.4.6 network / p.2.10.5 saveverify / p.2.11.5 session / p.2.6, p.2.8 async+sim), the engine-side pure-JDK landing points and MC-shell gaps of launch / runtime-fix / worldgen / optim-shell / tie / cfglog, plus the devkit probe assets and the post-p.2.18 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.19 row: “Runtime wiring migration — launching launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime, plus the export-command wiring (over p.2.18)”; the export-hub command is already partially landed — fold the existing runtime shells/commands into a unified wiring inventory. Illustrative directions: (1) **runtime wiring inventory** — unify the inventory of existing runtime shells/commands (export / export hub / rule / save / world / network / saveverify / session / async+sim gated hooks and markers, producing a wiring master table; export-command wiring over p.2.18); (2) **launch** — launch wiring into runtime (engine-side launch-core landing + MC shell, existing boot pattern); (3) **runtime-fix** — runtime-fix wiring into runtime (JVM/platform patches + deterministic gate); (4) **worldgen** — worldgen wiring into runtime (worldgen-pipeline MC shell, pure-JDK core already in engine); (5) **optim-shell** — optim-shell wiring into runtime (optimization MC shell, pure-JDK core already in engine.optim); (6) **tie** — tie wiring into runtime (tie bridge FFM-loading MC shell, over p.2.1); (7) **cfglog** — cfglog wiring into runtime (td-config / logging MC shell, pure-JDK cores already in engine.config / engine.log); (8) **export-command wiring fold-in** — export-command wiring over p.2.18 (the export-hub command is already partially landed; fold `/subterra export` and `/subterra export hub` into one command core, existing markers stable). Note: **unified wiring inventory (folding existing shells/commands) + the gated -D/-P hook pattern + keeping existing markers stable + wired into probeAcceptance** runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12–p.2.18 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.19 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、各接线子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.19 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.19 子项一律 **p.2.19.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；runtime 接线核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.19 为接线迁移，原则上不新增移植；如需参考既有移植（如 p.2.1 tie bridge）只沿用仓库内已钉死的许可结论。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（**87 纯 JVM + 五次 E2E，新增 ExportHubProbe 在内**）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.19.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the runtime-wiring core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. Licensing: permissive reuse/derive with attribution only; p.2.19 is wiring migration and in principle adds no new ports — any reference to existing ports (e.g. the p.2.1 tie bridge) reuses the pinned license conclusions already in the repo. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (**87 pure-JVM + five E2E boots, incl. ExportHubProbe**) before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.19 全轨完成后，必须生成 p.2.20 同格式提示词并纳入记录：更新上下文段（p.2.19 各子项落地、最新 p 轨号 p.2.19.x、关键路径、Runtime wiring migration 结论）、更新本次范围段指向 p.2.20（devkit complete，见 ROAD p.2.20 行；probes 全量接线 + 最小「颠覆性模组」官方示例脚手架，可用判据）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.20-devkit-complete.md`（bilingual），随当次提交一并提交。

**English**: After p.2.19 fully lands, produce the p.2.20 prompt in the same format (context updated with p.2.19 landing, latest track p.2.19.x, key path, the Runtime-wiring-migration conclusion; scope pointing to p.2.20 devkit complete, per ROAD p.2.20 — probes fully wired + a minimal “vanilla-overturning mod” official sample scaffold, the usability gate; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.20-devkit-complete.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.19 工作区建议：runtime 接线迁移（launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入 runtime）+ export 指令接线收编（接 p.2.18）。
- **p.2.18 export hub（已落地，p.2.19 接线承接）**：engine.export 七 form 正式导出枢纽（ExportProducer/ExportKind/ExportHub）+ `/subterra export hub` 接线（`exportForm` 共享核心、`ExportHubCommand`/`ExportHubRuntime`、`subterra.probe.exportHub` 门控钩子）；p.2.19 把既有 runtime 壳/命令收编进统一接线盘点。
- **既有 runtime 壳/命令（p.2.2–p.2.18 已落地，门控 -D/-P 钩子范式）**：`/subterra export`（p.2.2.7，subterra.probe.export）、export hub（p.2.18.5，subterra.probe.exportHub）、rule（p.2.17.4，subterra.probe.rule）、save（p.2.3.2）、world（p.2.9.6）、network（p.2.4.6）、saveverify（p.2.10.5）、session（p.2.11.5）、async+sim（p.2.6/p.2.8）——默认 no-op、marker 并入既有 E2E。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；p.2.19 沿用。
- **launch / runtime-fix / worldgen / optim-shell / tie / cfglog（引擎侧纯 JDK 核心已落 engine.*）**：启动核心、JVM/平台修补、世界生成管线、优化集（engine.optim）、tie bridge（p.2.1，FFM 装载）、td 配置与日志（engine.config / engine.log）——p.2.19 迁入 runtime 的 MC 壳接线，沿用 p.2.15 落点审计范式（零行为变化、旧 FQCN 缺席确认）。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12–p.2.18 钉死）**：逐字节一致（byte-identical）、固定序、禁时序；同输入 → 同输出。
- **依赖铁律（p.2.0/p.2.15/p.2.16/p.2.17/p.2.18 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.18 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.19 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.18 导出枢纽（已落地，export 指令接线承接）/ p.2.20 devkit complete（probes 全量接线 + 官方示例模组脚手架，可用判据）；p.2.19 把 launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入 runtime，并把既有 runtime 壳/命令（export / export hub / rule / save / world / network / saveverify / session / async+sim）收编进统一接线盘点。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.19 landing: runtime wiring migration (launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime) + the export-command wiring fold-in (over p.2.18). p.2.18 export hub (landed; the p.2.19 wiring carry-over): the engine.export seven-form formal export hub (ExportProducer/ExportKind/ExportHub) + the `/subterra export hub` wiring (shared `exportForm` core, `ExportHubCommand`/`ExportHubRuntime`, the `subterra.probe.exportHub` gated hook); p.2.19 folds the existing runtime shells/commands into a unified wiring inventory. Existing runtime shells/commands (p.2.2–p.2.18, the gated -D/-P hook pattern): `/subterra export` (p.2.2.7, subterra.probe.export), export hub (p.2.18.5, subterra.probe.exportHub), rule (p.2.17.4, subterra.probe.rule), save (p.2.3.2), world (p.2.9.6), network (p.2.4.6), saveverify (p.2.10.5), session (p.2.11.5), async+sim (p.2.6/p.2.8) — default no-op, markers merged into existing E2E. The gated -D/-P hook pattern (p.2.2.7 proven): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup (the stdin double-hop through the forked server JVM is unreliable); p.2.19 reuses it. launch / runtime-fix / worldgen / optim-shell / tie / cfglog (pure-JDK cores already in engine.*): launch core, JVM/platform patches, the worldgen pipeline, optimizations (engine.optim), the tie bridge (p.2.1, FFM loading), td config and logging (engine.config / engine.log) — p.2.19 wires the MC shells into runtime, following the p.2.15 landing-audit pattern (zero behavior change, old FQCNs confirmed absent). Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12–p.2.18 pinned): byte-identical, fixed order, no timing. Dependency iron law (p.2.0/p.2.15–p.2.18): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.18 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.19 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.18 export hub (landed; export-command wiring carried over) / p.2.20 devkit complete (probes fully wired + official sample mod scaffold, the usability gate); p.2.19 migrates launch / runtime-fix / worldgen / optim-shell / tie / cfglog into runtime and folds the existing runtime shells/commands (export / export hub / rule / save / world / network / saveverify / session / async+sim) into a unified wiring inventory.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.19 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.19 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
