# Subterra p.2.11 Session programmable 会话可编程 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.10 全轨 landing 后，按 p.2.10 交接单 §5 生成指向 p.2.11 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.10 fully landed, §5 of the p.2.10 handoff produces this p.2.11 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.11 里程碑**的交接单：p.2.11 =「Session programmable 会话可编程」——让**外部进程（调试器 / 编辑器 / tie 脚本 / 工具）经 tink 帧协议驱动游戏运行时**：把游戏当作一个可编程运行时，外部会话（session）通过既有 p.2.5 确立的 tink v2 帧 / tsha1f / zd 载荷栈（engine.network）向运行时下达命令、读取状态、驱动会话状态机。核心原则：**外部进程经 tink 帧驱动游戏运行时 + 复用 p.2.5 tink v2 帧 / tsha1f / zd 载荷 ABI（不另起炉灶）+ 会话状态机指令化 + 确定性会话探针**；engine 纯 JDK 不碰 MC、帧协议/会话状态机核心可独立探针。执行模式与 p.2.5 / p.2.6 / p.2.7 / p.2.8 / p.2.9 / p.2.10 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.11.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 p.2.5 tink v2 帧 / tsha1f / zd 载荷 ABI 的可复用切面、engine.network 帧载体现状、会话状态机该接的既有运行时壳、外部进程帧协议入口形态）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.11-milestone handoff: p.2.11 = “Session programmable” — let **external processes (debuggers / editors / tie scripts / tools) drive the game runtime via tink-frame protocol**: treat the game as a programmable runtime where an external session (session) issues commands, reads state, and drives the session state machine through the tink v2 frame / tsha1f / zd payload stack established in p.2.5 (engine.network). Core principles: **external process drives the game runtime over tink frames + reuse the p.2.5 tink v2/tsha1f/zd ABI (no reinvention) + command-ized session state machine + deterministic session probes**; engine pure JDK (no MC coupling), the frame protocol / session state machine core independently probeable. Execution mode identical to p.2.5 / p.2.6 / p.2.7 / p.2.8 / p.2.9 / p.2.10 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.11.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the reusable seams in the p.2.5 tink v2 / tsha1f / zd ABI, the current engine.network frame-carrier state, which existing runtime shell the session state machine should hook, and the external-process frame-protocol entry shape) to produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.10 里程碑「Save verifiability 存档可验证」已全部落地并推 GitHub**（提交 `880769a` p.2.10.1 → `58d603d` p.2.10.2 → `b40fb7a` p.2.10.3 → `03ce6e9` p.2.10.4 → `ced03c3` p.2.10.5 → `741f64e` p.2.10.6，编号 p.2.10.1–p.2.10.6，最新 p 轨号 **p.2.10.6**、随本次收尾落到 p.2.10.7）。可验证存档案抽象结论：engine.saveverify（`engine/saveverify/`，纯 JDK）——chunk（`Chunk`/`ChunkPlan`/`ChunkVerifier`，zd 区块划分 + 逐块 tsha1f 快/强校验元数据）+ sig（`Ed25519Keys`/`SaveSigner`，RFC 8032 确定性签名 + 确定性密钥注入）+ 根包（`SaveVerifyArchive`/`SaveVerifyLedger`/`VerifyResult`）：**单一 td 文档 saveverify=[version,meta,config,payload,chunks,sig]**（接 p.2.3 文档形态），ed25519 头签名 + tsha1f 双档块标 + seq 重放门 + **五类损坏确定性拒绝**（flip/truncate/reorder/wrong-key/replay）；runtime 壳 `runtime.saveverify.SaveVerifyRuntime`（-Dsubterra.probe.saveverify 门控默认 no-op、服务器启动即验账本、`[Subterra saveverify]` marker 并入 AsyncE2EProbe boot）。关键路径 **engine.saveverify**（纯 JDK，`subterra-engine\...\engine\saveverify\`）。/ p.2.10 “Save verifiability” fully landed and pushed (commits p.2.10.1→…→p.2.10.6; the closing commit is p.2.10.7); see git log. The verifiable-save archive conclusion: engine.saveverify (pure JDK) — chunk (Chunk/ChunkPlan/ChunkVerifier, zd-chunk partition + per-chunk tsha1f fast/strong verify metadata) + sig (Ed25519Keys/SaveSigner, RFC 8032 deterministic signatures + deterministic keypair injection) + root package (SaveVerifyArchive/SaveVerifyLedger/VerifyResult): **single td doc saveverify=[version,meta,config,payload,chunks,sig]** (over the p.2.3 doc form), Ed25519 head signature + tsha1f dual-chunk marks + seq replay gate + **five corruption classes deterministically rejected** (flip/truncate/reorder/wrong-key/replay); the runtime shell `runtime.saveverify.SaveVerifyRuntime` (gate-off no-op, ledger verified on server start, `[Subterra saveverify]` marker merged into the AsyncE2EProbe boot). Key path engine.saveverify (pure JDK).
- **p.2.5 P2P / tink v2 / tsha1f / zd 栈（已落地，p.2.11 直接消费）**：engine.network。tink v2 帧 + tsha1f 便携强校验（`engine.network.integrity` `Tsha1f`/`FrameV2`：快档 n=8→8B、强档 n=48→32B、单字节篡改即拒、tie-main 金标 KAT）+ frame 载体（`FrameV1`/`FrameConst`/`ZdFrameBridge`）+ strategy/bandwidth/crypto（x25519+AEAD）三级载荷策略；engine.p2p（`NodeId`/寻址/握手/传输/载荷/plan/`KademliaLite`/relay，纯 JDK 结构核心，线上 RPC/真实 socket 传输层留 runtime）。p.2.11 的 帧协议/会话入口应复用该 tink v2 / tsha1f / zd ABI（语言无关），作为外部进程驱动的载体。/ p.2.5 P2P / tink v2 / tsha1f / zd stack (landed, p.2.11 consumes it): engine.network. tink v2 frame + portable tsha1f (`engine.network.integrity` `Tsha1f`/`FrameV2`: fast n=8→8B, strong n=48→32B, single-byte tamper reject, tie-main golden-vector KAT) + frame carriers (`FrameV1`/`FrameConst`/`ZdFrameBridge`) + strategy/bandwidth/crypto (x25519+AEAD) three-tier payload strategy; engine.p2p (NodeId/address-book/handshake/transport/payload/plan/KademliaLite/relay, pure-JDK structural cores; wire RPC / real-socket transport deferred to runtime). p.2.11's frame protocol / session entry should reuse this tink v2 / tsha1f / zd ABI (language-agnostic) as the carrier for external-process driving.
- **p.2.3 Save（已落地，数据底座，事实不变）**：统一 `SaveContainer` 六槽（世界 + 配置 + 藏录 Ledger + 领域档案 + 遗物条目 + 台账）；`engine.zd` zd v2 载体 + zdt 混合文档（td 元数据 + base64 zd 载荷）；`SaveExportArchive` td 导出档案。/ p.2.3 Save rework (landed, data foundation, unchanged): the SaveContainer six-slot unified form, engine.zd zd v2 carrier + zdt hybrid docs, the SaveExportArchive td export archive.
- **p.2.4 server-interaction（已落地，事实不变）**：engine.network 五子引擎层 + runtime.network.EnhancedChannelRuntime 门控渐进增强壳（原版握手零接触）。/ p.2.4 server-interaction (landed, unchanged): five pure-JVM engine.network cores + the gated runtime EnhancedChannelRuntime shell.
- **p.2.9 World-as-artifact（已落地，事实不变）**：engine.world `WorldPack`（单一 td 文档 `world=[version,meta,save,datapack]`）+ `engine.export` 导出端口预留（`ExportKind`/`ExportPort`/`ExportHub`）+ runtime `WorldRuntime` 门控壳 + 确定性归档/往返范式。/ p.2.9 World-as-artifact (landed, unchanged): engine.world WorldPack + engine.export port cut + runtime WorldRuntime + the deterministic-archival/round-trip paradigm.
- **probeAcceptance 现状（p.2.10 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**79 个纯 JVM 探针**（含新增 `SaveVerifyProbe` 15 checks + 既有 WorldPack*/Sim*/Network*/P2P*/Datapack*/Save* 全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra saveverify]` 断言，与既有 [Subterra world]/[Subterra async]/[Subterra sim] 一起，四壳联合）。铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.10 closing): `gradlew :subterra-devkit:probeAcceptance` green — **79 pure-JVM probes** (incl. the new SaveVerifyProbe 15 checks + all existing WorldPack*/Sim*/Network*/P2P*/Datapack*/Save*) + **5 E2E boots** (Boot → Datapack → Save → Network → Async+world+saveverify, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.11 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；`engine.network` 既有帧 ABI（FrameV2 / tsha1f / zd payload bridge）既定协议；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链，不加新开服若可并入既有壳；staging 用 build/tmp）。
- **关联里程碑**：p.2.5 P2P（已落地，tink v2 / tsha1f / zd 帧栈基座）；p.2.4 network（已落地，增强通道）；p.2.10 Save verifiability（已落地，前一轨）；p.2.8 sim / p.2.9 world（已落地，运行时壳并入既有 boot 的范式）。p.2.11 应为 engine / runtime 落地一个可复用 p.2.5 帧栈、由外部进程驱动的「会话可编程运行时」抽象。

**English**: See the Chinese block above — this handoff follows the exact p.2.5/p.2.9/p.2.10 closing patterns. p.2.10 “Save verifiability” fully landed and pushed (commits p.2.10.1–p.2.10.6, latest track **p.2.10.6**, closing p.2.10.7); the verifiable-save archive conclusion (engine.saveverify chunk/sig/root, single td doc `saveverify=[version,meta,config,payload,chunks,sig]`, Ed25519 head sig + tsha1f dual-chunk marks + seq replay gate + five corruption classes rejected, gated runtime SaveVerifyRuntime with `[Subterra saveverify]` merged into the AsyncE2EProbe boot) is the p.2.11 handoff basis. p.2.5 (landed) is p.2.11's frame-stack foundation: tink v2 frame + tsha1f (`engine.network.integrity`) + zd payload ABI over engine.network / engine.p2p. p.2.3 Save, p.2.4 server-interaction, p.2.9 World-as-artifact all landed unchanged (see Chinese block). probeAcceptance is all green: **79 pure-JVM probes** (incl. SaveVerifyProbe 15 checks) + **5 E2E boots** (Boot → Datapack → Save → Network → Async+world+saveverify, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify markers together). Iron law auto-covers new engine packages. p.2.11 should land a “session-programmable runtime” abstraction for engine/runtime that reuses the p.2.5 tink/tsha1f/zd stack and lets external processes drive the game runtime through a command-ized session state machine — without reworking p.2.4/p.2.5/p.2.10.

## 2. 本次范围（p.2.11 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 p.2.5 tink v2 / tsha1f / zd 载荷 ABI 的可复用切面、engine.network 帧载体现状、会话状态机该接的既有运行时壳、外部进程帧协议入口形态），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 Session programmable 提炼会话可编程运行时抽象，承接 p.2.5 engine.network tink v2 / tsha1f / zd 栈 + 复用既有运行时壳并入范式）：
1. **会话与命令协议**：定义外部会话（session）与命令（command）核心（帧协议 ABI 复用 p.2.5 tink v2 / tsha1f / zd，确定性命令编码、会话序号）。
2. **会话状态机**：会话状态机指令化（连接 / 鉴权 / 命令派发 / 状态回读 / 断开，确定性状态转移）。
3. **运行时命令壳**：runtime 命令壳默认 no-op（门控），经帧协议接收外部命令驱动运行时；E2E 尽量并入现有 AsyncE2EProbe 开服，保持五次开服不变。
4. **确定性会话探针**：探针任意构造会话序列并断言状态机确定性 + 命令往返 + 坏帧 / 乱序 / 重放拒绝；纯 JVM 可独立跑。
5. **与 p.2.5 ABI 同构**：命令载荷走既有 zd 载荷通道语义，meta 走 tink v2 ext TLV（复用 p.2.5 既定 ABI），不新增平行协议体系。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**外部进程经 tink 帧驱动游戏运行时 + 复用既有 p.2.5 帧 ABI + 会话状态机指令化 + 确定性会话探针**贯穿每次设计；p.2.4/p.2.5 已确立的 tink v2 / tsha1f / zd 帧栈与 p.2.8/p.2.9/p.2.10 已证明的「门控壳并入既有 boot」范式必须被复用为默认语义。

**English**: Same principle as p.2.5/…/p.2.10: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the reusable seams in the p.2.5 tink v2 / tsha1f / zd ABI, the current engine.network frame-carrier state, which existing runtime shell the session state machine hooks, and the external-process frame-protocol entry shape), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions distilling a session-programmable runtime over the p.2.5 engine.network tink v2 / tsha1f / zd stack + the proven gate-shell-into-existing-boot paradigm: (1) session & command protocol — session/command core (frame ABI reusing p.2.5 tink v2 / tsha1f / zd, deterministic command encoding, session sequence); (2) session state machine — command-ized (connect / auth / dispatch / state read-back / disconnect, deterministic transitions); (3) a gated runtime command shell — default no-op, receives external commands over the frame protocol to drive the runtime, E2E reusing the AsyncE2EProbe boot where possible (five boots unchanged); (4) deterministic session probe — construct session sequences and assert state-machine determinism + command round-trip + bad-frame / reorder / replay rejection, pure-JVM standalone; (5) isomorphic to the p.2.5 ABI — command payloads go over the existing zd payload-channel semantics, meta via tink v2 ext TLV (reuse the p.2.5-proven ABI), no parallel protocol system. Note: **external process drives the game runtime over tink frames + reuse the existing p.2.5 frame ABI + command-ized session state machine + deterministic session probes** runs through every design; the p.2.4/p.2.5 tink v2 / tsha1f / zd frame stack and the p.2.8/p.2.9/p.2.10-proven “gate-shell merged into existing boot” paradigm must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.11 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/协议核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.11 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.11 子项一律 **p.2.11.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性状态机**（固定序、禁时序）；帧/会话核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.11 Session programmable 为自研（clean-room），不新增抄录；帧协议复用既有 p.2.5 engine.network 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五探针/A*E2E 探针五次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.11.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic state machine** (fixed order, no timing); the frame/session core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.11 Session programmable is self-developed (clean-room), no new copying; the frame protocol reuses the existing p.2.5 engine.network built-in implementation. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.11 全轨完成后，必须生成 p.2.12 同格式提示词并纳入记录：更新上下文段（p.2.11 各子项落地、最新 p 轨号 p.2.11.x、关键路径、会话可编程运行时抽象结论）、更新本次范围段指向 p.2.12（Schema-first scaffolding，见 ROAD p.2.12 行；td schema → 自动生成注册码 / 探针 / 文档 / 表单——模组编写从「写代码」变「写数据」）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.12-schema-first-scaffolding.md`（bilingual），随当次提交一并提交。

**English**: After p.2.11 fully lands, produce the p.2.12 prompt in the same format (context updated with p.2.11 landing, latest track p.2.11.x, key path, the session-programmable-runtime conclusion; scope pointing to p.2.12 Schema-first scaffolding, per ROAD p.2.12; td schema → auto-generate registration code / probes / docs / forms — mod-authoring from “writing code” to “writing data”; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.12-schema-first-scaffolding.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.11 工作区建议：`subterra-engine\...\engine\session\`（会话/命令核心，纯 JDK）+ runtime MC 壳按需落根 sourceSet。
- **p.2.5 tink v2 / tsha1f / zd 栈（p.2.11 帧协议基座，复用/消费）**：`engine.network.integrity` `Tsha1f`/`FrameV2`/`FrameV2Result`（快档 n=8 → 8B、强档 n=48 → 32B、单字节篡改即拒、tie-main 金标 KAT）+ `engine.network.frame` `FrameV1`/`FrameConst`/`ZdFrameBridge` + `engine.network.payload` zd 载荷通道 ABI（payload==zd doc + 类型信封，meta 走 tink v2 ext key9）+ engine.p2p（`NodeId` 等结构核心）。p.2.11 的外部进程帧协议 / 会话入口应复用此 ABI（语言无关），不另起炉灶。
- **p.2.10 saveverify 落点（已落地）**：engine.saveverify（chunk / sig / 根包 SaveVerifyArchive+SaveVerifyLedger+VerifyResult）+ runtime shell `runtime.saveverify.SaveVerifyRuntime` + 探针 `SaveVerifyProbe`（15 checks）+ `probeAcceptance` 79 纯 JVM + 5 次 E2E。
- **运行时壳并入既有 boot 范式（p.2.8/p.2.9/p.2.10 已证明）**：runtime 壳默认 no-op（`-D`/`-P` 门控）、marker 并入 AsyncE2EProbe 开服、staging 用 build/tmp、保持五次开服不变。
- **p.2.3 / p.2.4 / p.2.9 已落地（事实不变）**：SaveContainer 六槽 + engine.zd + zdt；engine.network 增强通道 + EnhancedChannelRuntime；engine.world WorldPack + engine.export 端口预留。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 session 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.5 P2P（已落地，tink v2 / tsha1f / zd 帧栈基座）；p.2.4 network（已落地，增强通道）；p.2.10 Save verifiability（已落地，前一轨）；p.2.8 sim / p.2.9 world（已落地，运行时壳并入既有 boot 的范式）。「Session programmable 会话可编程」为其会话可编程运行时抽象。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.11 work dir: `subterra-engine\...\engine\session\` (session/command core, pure JDK) + runtime MC shell in the root sourceSet as needed. p.2.5 tink v2 / tsha1f / zd stack (p.2.11's frame-protocol base, reuse/consume): engine.network.integrity `Tsha1f`/`FrameV2`/`FrameV2Result` (fast=8B strong=32B, single-byte tamper reject, tie-main KAT) + engine.network.frame `FrameV1`/`FrameConst`/`ZdFrameBridge` + engine.network.payload zd payload-channel ABI (payload==zd doc + type envelope, meta via tink v2 ext key9) + engine.p2p structural cores (NodeId etc.). p.2.11's external-process frame protocol / session entry should reuse this ABI (language-agnostic), never reinvent. p.2.10 saveverify landing: engine.saveverify (chunk / sig / root SaveVerifyArchive+SaveVerifyLedger+VerifyResult) + runtime.saveverify.SaveVerifyRuntime + SaveVerifyProbe (15 checks) + probeAcceptance 79 pure-JVM + 5 E2E boots. The runtime-shell-into-existing-boot paradigm (p.2.8/p.2.9/p.2.10 proven): gate-off no-op shell (`-D`/`-P`), marker merged into the AsyncE2EProbe boot, staging via build/tmp, five boots unchanged. p.2.3 / p.2.4 / p.2.9 landed unchanged (SaveContainer six slots + engine.zd + zdt; engine.network enhanced channel + EnhancedChannelRuntime; engine.world WorldPack + engine.export port cut). New session probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.5 P2P (landed, tink v2 / tsha1f / zd frame-stack basis); p.2.4 network (landed, enhanced channel); p.2.10 Save verifiability (landed, previous track); p.2.8 sim / p.2.9 world (landed, the gate-shell-into-existing-boot paradigm). Session programmable is the session-programmable runtime abstraction.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.11 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.11 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.