# Subterra p.2.16 API contract surface / api 契约面 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.15 全轨 landing 后，按 p.2.15 交接单 §5 生成指向 p.2.16 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.15 fully landed, §5 of the p.2.15 handoff produces this p.2.16 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.16 里程碑**的交接单：p.2.16 =「API contract surface / api 契约面」——**worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API**（自原 p.2.1 顺移，见 ROAD p.2.16 行；示意子项可由收尾时按仓库现状给演示）；契约面为纯 JDK 可探针的确定性接口，golden 化的数值底座（p.2.15）与既有 engine 核心为契约的真实来源。执行模式与 p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.16.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 api / engine 模块现状与既有契约基座：p.2.15 golden 数值底座、engine.datapack 规则前哨 DatapackRules、engine.export 端口预留 ExportKind/ExportPort/ExportHub、engine.network/session 事件流，以及 devkit 探针资产与 p.2.15 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.16-milestone handoff: p.2.16 = “API contract surface” — **worldgen / config(rules) / export / event contracts + an Exporter registry + a rule-registration API** (moved from the former p.2.1, per the ROAD p.2.16 row; illustrative sub-items are given at closing time per current repo state); the contract surface is pure-JDK, probeable, deterministic interfaces whose real sources are the golden value base (p.2.15) and the existing engine cores. Execution mode identical to the p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.16.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the api / engine module state and the existing contract bases: the p.2.15 golden value base, the engine.datapack rule precursor DatapackRules, the engine.export port reservation ExportKind/ExportPort/ExportHub, the engine.network/session event streams, plus the devkit probe assets and the post-p.2.15 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.15 里程碑「Engine migration + golden tests engine 迁移」已全部落地并已推 GitHub，最新 p 轨号 p.2.15.4（收尾提交）**：engine `subterra-engine\...\engine\`（纯 JDK）——**p.2.15.1** engine 落点审计（optim/config/log 纯 JDK 核心全部确认落于 engine.*，17 处双语 package-info 稳定化、零行为变化、旧 FQCN 确认缺席）+ **p.2.15.2** vanilla 数值对照黄金化（`VanillaGoldenGen` 冻结 seed=44905237 的 NoiseRouter 镜像 **12 点×15 场共 180 个位模式**为 td golden 资产 `subterra-devkit\src\main\resources\golden\vanilla-router-seed44905237.td`，golden_version=1 锚点、int64 位模式逐位无损往返；一次性生成器刻意不接 probeAcceptance）+ **p.2.15.3** `VanillaGoldenProbe` 黄金门禁回归探针（180 项逐位回归 / 确定性再入 / 文档往返 / 元数据锚定）接入 probeAcceptance。关键路径 **engine/worldgen pipeline router（NoiseRouter 镜像）+ devkit golden 资产**（纯 JDK）。结论：**engine 迁移落点稳定（optim/config/log 全在 engine.*，零行为变化）+ vanilla 数值对照 golden-test 化回归（180 位模式冻结 + 逐位探针）**。/ p.2.15 “Engine migration + golden tests” fully landed and pushed, latest track **p.2.15.4** (closing commit); engine (pure JDK) — p.2.15.1 engine landing audit (optim/config/log pure-JDK cores all confirmed in engine.*, 17 bilingual package-infos stabilized, zero behavior change, old FQCNs confirmed absent) + p.2.15.2 vanilla-fidelity golden asset (VanillaGoldenGen froze the seed-44905237 NoiseRouter mirror — **12 points × 15 fields = 180 bit patterns** — into a td golden doc at subterra-devkit\src\main\resources\golden\vanilla-router-seed44905237.td, golden_version=1 anchor, lossless int64 bit-pattern round-trip; the one-time generator deliberately NOT wired into probeAcceptance) + p.2.15.3 VanillaGoldenProbe golden-gate regression (180 bit-exact regressions / determinism re-entry / doc round-trip / metadata anchor) wired into probeAcceptance. Key path **engine/worldgen pipeline router (NoiseRouter mirror) + devkit golden asset**. Conclusion: engine migration landed stably (optim/config/log all in engine.*, zero behavior change) + vanilla value comparisons hardened as golden-test regressions (180 frozen bit patterns + bit-exact probe).
- **p.2.16 目标（ROAD p.2.16 行，本次承接）**：worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API（自原 p.2.1 顺移；示意子项由收尾时按仓库现状给演示）。/ p.2.16 goal (the ROAD row, this handoff): worldgen / config(rules) / export / event contracts + an Exporter registry + a rule-registration API (moved from the former p.2.1; illustrative sub-items given at closing time per current repo state).
- **p.2.15 及更早里程碑（已落地，事实不变）**：p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg，同种子同输出）、p.2.12 schema-first（engine.schema + engine.scaffold，同 schema 同字节）、p.2.11 session（engine.session，tink v2 帧入口）、p.2.10 saveverify、p.2.9 world-as-artifact（engine.export 端口预留）、p.2.8 sim、p.2.5 P2P / tink v2 / tsha1f / zd 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（engine.datapack 纯 td 直载 + **规则前哨 DatapackRules**，p.2.2.9：pack 序 + save 覆盖双层优先级）。/ p.2.15 and earlier milestones all landed (unchanged): p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 engine.session, p.2.10 saveverify, p.2.9 world-as-artifact (engine.export port reservation), p.2.8 sim, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (engine.datapack pure-td direct loading + the rule precursor DatapackRules, p.2.2.9: pack-order then save-override two-tier precedence).
- **probeAcceptance 现状（p.2.15 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**84 个纯 JVM 探针**（含新增 `VanillaGoldenProbe` + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.15 closing): green — **84 pure-JVM probes** (incl. VanillaGoldenProbe + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.16 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；api 模块 `subterra-api\...\api\`（现有子包 crypto / migrate / net / ser / time / worldgen）为对外契约宿主候选；td（tiedb-td 文档）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12/p.2.13/p.2.14/p.2.15 钉死并复用；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.16 API 契约面为框架公开面——worldgen 契约承接 p.2.15 golden 化的数值底座（NoiseRouter 镜像 180 位模式即契约金标）；config(rules) 契约承接 p.2.2.9 DatapackRules 双层规则前哨；export 契约承接 p.2.9.5 engine.export 端口预留（ExportKind/ExportPort/ExportHub 已存在，正式导出落地 p.2.18）；event 契约承接 p.2.4/2.5/2.11 的 tink v2 帧与事件流；规则注册 API 为 p.2.17 Rule system（engine.config.rules，参考 RollingGate 模型）的消费方前哨。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、subterra-api、engine/worldgen、engine/datapack、engine/export 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.10/p.2.11/p.2.12/p.2.13/p.2.14/p.2.15 closing patterns. p.2.15 “Engine migration + golden tests” fully landed and pushed (latest track **p.2.15.4**, key path **engine/worldgen pipeline router + devkit golden asset**): the engine landing audit (optim/config/log all in engine.*, zero behavior change) + the vanilla-fidelity golden asset (180 frozen NoiseRouter mirror bit patterns, golden_version=1, bit-exact int64 round-trip) + the VanillaGoldenProbe golden-gate regression wired into probeAcceptance. p.2.16 defines the API contract surface — worldgen / config(rules) / export / event contracts + an Exporter registry + a rule-registration API, per the ROAD p.2.16 row; the golden value base, the DatapackRules precursor, the engine.export port reservation and the tink v2 event streams are the existing bases. Everything pinned by p.2.12/p.2.13/p.2.14/p.2.15 (td-first, byte-identical deterministic, no-timing) stays the default semantics. probeAcceptance all green: **84 pure-JVM probes** (incl. VanillaGoldenProbe) + **5 E2E boots** (AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages. Confirm remaining context facts against the repo state (ROAD, git log, subterra-api, engine/worldgen, engine/datapack, engine/export) — never fabricate.

## 2. 本次范围（p.2.16 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 api / engine 模块现状与既有契约基座：p.2.15 golden 数值底座、engine.datapack 规则前哨 DatapackRules、engine.export 端口预留、engine.network/session 事件流、devkit 探针资产与 p.2.15 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（承接 p.2.15 已落地的 engine.* 稳定落点与 golden 数值底座，为 p.2.16 提炼 API 契约面）：目标见 ROAD p.2.16 行：「worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API」。示意子项（示例）：
1. **worldgen 契约面**：把 p.2.15 golden 化的 NoiseRouter 镜像数值底座（seed=44905237 的 180 位模式即契约金标）定型为确定性 worldgen API 契约，供 runtime/MC 壳与 Toterra 消费，不引新行为。
2. **config(rules) 契约面 + 规则注册 API**：双层配置规则（DatapackRules.resolveTiered 语义）契约化 + 确定性规则注册 API（固定序、禁覆盖歧义），接 p.2.17 消费方前哨。
3. **export 契约面 + Exporter 注册表**：engine.export 端口预留（ExportKind/ExportPort/ExportHub）定型为 Exporter 注册表契约（确定性注册序、查表、拒绝重复），正式导出落地留 p.2.18。
4. **event 契约面**：tink v2 帧 / 生命周期事件的确定性事件契约（固定事件序、类型化载荷），承接 p.2.4/2.5/2.11。
5. **确定性契约探针**：给契约面加确定性探针（契约逐项命中、注册表确定性序、金标位模式逐位一致），接入 probeAcceptance。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**契约确定性 + 金标逐位一致 + 注册表固定序 + 接入 probeAcceptance** 贯穿每次设计；p.2.12/p.2.13/p.2.14/p.2.15 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」范式必须被复用为默认语义。

**English**: Same principle as p.2.10–p.2.15: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the api / engine module state and the existing contract bases: the p.2.15 golden value base, the DatapackRules precursor, the engine.export port reservation, the engine.network/session event streams, and the devkit probe assets plus the post-p.2.15 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.16 row: “worldgen / config(rules) / export / event contracts + an Exporter registry + a rule-registration API.” Illustrative directions: (1) **worldgen contract** — pin the p.2.15 golden NoiseRouter mirror base (the 180 seed-44905237 bit patterns are the contract gold standard) into a deterministic worldgen API contract for runtime/MC-shell and Toterra consumers, no new behavior; (2) **config(rules) contract + rule-registration API** — contract the two-tier rule semantics (DatapackRules.resolveTiered) with a deterministic registration API (fixed order, no override ambiguity), the p.2.17 consumer precursor; (3) **export contract + Exporter registry** — pin the engine.export port reservation (ExportKind/ExportPort/ExportHub) as a deterministic Exporter-registry contract (fixed registration order, lookup, duplicate rejection); full export landing stays in p.2.18; (4) **event contract** — deterministic event contracts for tink v2 frames / lifecycle events (fixed event order, typed payloads) over p.2.4/2.5/2.11; (5) **deterministic contract probes** — probes hitting the contract surface item-by-item (gold-standard bits bit-exact, registry fixed order), wired into probeAcceptance. Note: **contract determinism + gold-standard bit-exactness + fixed registry order + wired into probeAcceptance** runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12/p.2.13/p.2.14/p.2.15 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.16 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、契约子代理、devkit/脚手架子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/契约核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.16 runs as a cluster of sub-agents (scouting, contract, devkit/scaffold, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.16 子项一律 **p.2.16.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；契约核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.16 API contract surface 为对既有自研核心的契约面定型（clean-room），不新增抄录；td schema 能力复用 p.2.12 engine.schema 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（84 纯 JVM + 五次 E2E，新增 VanillaGoldenProbe 在内）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.16.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the contract core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api; the api module must not depend on engine. Licensing: permissive reuse/derive with attribution only; p.2.16 API contract surface types the contract surface over existing self-developed cores (clean-room), no new copying; td schema capability reuses the p.2.12 engine.schema built-in. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (84 pure-JVM + five E2E boots, incl. VanillaGoldenProbe).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.16 全轨完成后，必须生成 p.2.17 同格式提示词并纳入记录：更新上下文段（p.2.16 各子项落地、最新 p 轨号 p.2.16.x、关键路径、API contract surface 结论）、更新本次范围段指向 p.2.17（Rule system，见 ROAD p.2.17 行；engine.config.rules 类型化规则、双层配置、校验、热重载、`/subterra rule` 命令）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.17-rule-system.md`（bilingual），随当次提交一并提交。

**English**: After p.2.16 fully lands, produce the p.2.17 prompt in the same format (context updated with p.2.16 landing, latest track p.2.16.x, key path, the API-contract-surface conclusion; scope pointing to p.2.17 Rule system, per ROAD p.2.17 — engine.config.rules typed rules, two-tier config, validation, hot reload, `/subterra rule` command; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.17-rule-system.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.16 工作区建议：契约面于 `subterra-api\...\api\`（现有子包 crypto / migrate / net / ser / time / worldgen）或 engine 契约包（纯 JDK，落点可探针）+ devkit 契约探针。
- **p.2.15 Engine migration + golden tests（已落地，p.2.15.4 收尾）**：engine/worldgen pipeline router（NoiseRouter 镜像）+ devkit golden 资产（`subterra-devkit\src\main\resources\golden\vanilla-router-seed44905237.td`，180 个位模式、golden_version=1）+ VanillaGoldenProbe 接入 probeAcceptance + probeAcceptance 84 纯 JVM + 5 次 E2E。p.2.16 契约面把该数值底座定型为 worldgen 契约金标，遵循同一验收门。
- **既有契约基座（p.2.16 复用，事实不变）**：engine.datapack.DatapackRules（p.2.2.9 双层规则：pack 序 + save 覆盖）→ config(rules) 契约与规则注册 API 的真实来源；engine.export.{ExportKind,ExportPort,ExportHub}（p.2.9.5 端口预留）→ Exporter 注册表契约的真实来源（正式导出落地 p.2.18）；engine.network / engine.session（p.2.4/2.5/2.11 tink v2 帧 + 事件流）→ event 契约的真实来源。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12/p.2.13/p.2.14/p.2.15 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.16 的契约面与契约探针沿用同一约束；同输入 → 同输出。
- **td 一等语言（p.2.2/p.2.3/p.2.9/p.2.14/p.2.15 已确立）**：engine.datapack / engine.save / engine.export / engine.world / engine.interact——「td schema → 文档/注册」范式在 p.2.16 复用于契约资产的定型。
- **依赖铁律（p.2.0/p.2.15 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；契约若需金标数值底座，以参数/数据形式注入而非反向依赖。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.15 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变（p.2.16 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新契约探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.15 engine migration + golden tests（数据底座）/ p.2.17 rule system（规则注册 API 消费方）/ p.2.18 export hub（Exporter 注册表正式导出）/ p.2.20 devkit 完整化；p.2.16 把既有 engine 核心与 golden 底座定型为框架公开 API 契约面。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.16 landing: contract surface in subterra-api\...\api\ (existing sub-packages crypto / migrate / net / ser / time / worldgen) or engine contract packages (pure JDK, probeable) + devkit contract probes. p.2.15 engine migration + golden tests (landed, closing p.2.15.4): engine/worldgen pipeline router (NoiseRouter mirror) + devkit golden asset (vanilla-router-seed44905237.td, 180 bit patterns, golden_version=1) + VanillaGoldenProbe wired into probeAcceptance + probeAcceptance 84 pure-JVM + 5 E2E boots; p.2.16 pins that value base as the worldgen contract gold standard and follows the same gate. Existing contract bases (reused, unchanged): engine.datapack.DatapackRules (p.2.2.9 two-tier rules: pack order + save override) → the real source for the config(rules) contract and the rule-registration API; engine.export.{ExportKind,ExportPort,ExportHub} (p.2.9.5 port reservation) → the real source for the Exporter-registry contract (full export landing in p.2.18); engine.network / engine.session (p.2.4/2.5/2.11 tink v2 frames + event streams) → the real source for the event contract. Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12–p.2.15 pinned): byte-identical, fixed order, no timing. td first-class language (p.2.2/p.2.3/p.2.9/p.2.14/p.2.15): the td-schema → document/registration paradigm reused for contract-typed assets. Dependency iron law (p.2.0/p.2.15): api ← engine ← runtime/migrate; the api module must not depend on engine; inject gold-standard bases as parameters/data rather than reverse dependencies. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.15 proven) still applies (gate-off no-op, markers merged into AsyncE2EProbe, staging via build/tmp, five boots unchanged). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.15 engine migration + golden tests (data base) / p.2.17 rule system (rule-registration-API consumer) / p.2.18 export hub (Exporter-registry full export) / p.2.20; p.2.16 types the existing engine cores and the golden base into the framework's public API contract surface.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.16 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.16 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
