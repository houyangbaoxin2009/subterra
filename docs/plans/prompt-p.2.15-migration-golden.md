# Subterra p.2.15 Engine migration + golden tests / engine 迁移 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.14 全轨 landing 后，按 p.2.14 交接单 §5 生成指向 p.2.15 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.14 fully landed, §5 of the p.2.14 handoff produces this p.2.15 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.15 里程碑**的交接单：p.2.15 =「Engine migration + golden tests / engine 迁移」——**现有 optim / config / log 纯 JDK 核心迁入 engine.*；vanilla 数值对照固化为 golden tests 随改动回归**（自原 p.2.2 顺移，见 ROAD p.2.15 行；示意子项可由收尾时按仓库现状给演示）；engine 纯 JDK 不碰 MC、迁移与数值对照核心可独立探针。执行模式与 p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.15.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认现有 optim / config / log 纯 JDK 核心的实际落点与边界、engine.pcg / engine.schema 的确定性范式、可固化为 golden tests 的 vanilla 数值对照资产、devkit 探针资产与 p.2.14 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.15-milestone handoff: p.2.15 = “Engine migration + golden tests” — **migrate the existing optim / config / log pure-JDK cores into engine.*, and harden vanilla value comparisons as golden tests that regress on any change** (moved from the former p.2.2, per the ROAD p.2.15 row; illustrative sub-items are given at closing time per current repo state); engine pure JDK (no MC coupling), the migration and value-comparison core independently probeable. Execution mode identical to the p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.15.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the actual landing points/boundaries of the existing optim/config/log pure-JDK cores, the deterministic paradigm of engine.pcg / engine.schema, the vanilla-value assets hardening into golden tests, and the devkit probe assets plus the post-p.2.14 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.14 里程碑「Diegetic interaction 世界内交互」已全部落地并已推 GitHub，最新 p 轨号 p.2.14.5（收尾提交）**：engine `subterra-engine\...\engine\interact\`（纯 JDK）——世界内交互规范模型 `InteractKind/Attitude/InteractAction/InteractSurface/InteractSpec` + `InteractSpecParser` + `InteractViolationException`（td schema 定型、固定序、无系统气味词汇面；顺带修复 schemaTd 缺方括号成无效 td 文档的缺陷）+ 确定性交互规则引擎 `InteractState/InteractResult/InteractRuleEngine`（固定序表面/动作/态度/状态规则、可回放、世界内语言消息、零 HUD）+ 零 HUD 渲染规则/表面映射 `SurfaceTextMapper+ZeroHudGuard+SurfaceText`（世界内语言表面文本确定性映射、系统气味黑名单守卫，纯数据、渲染留 p.2.22）；纯 JVM 探针 `InteractProbe` 30 项确定性断言（spec 往返/违约、规则引擎脚本化可回放、零 HUD 表面映射与系统气味黑名单）接入 probeAcceptance。关键路径 **engine/interact**（纯 JDK）。结论：**世界内交互抽象——td schema 定型的交互规范模型 + 确定性规则引擎（固定序可回放、世界内语言消息、零 HUD）+ 表面映射与系统气味黑名单守卫，InteractProbe 30 项断言接入 probeAcceptance**。/ p.2.14 “Diegetic interaction” fully landed and pushed, latest track **p.2.14.5** (closing commit); engine/interact (pure JDK): an in-world interaction-spec model (InteractKind/Attitude/InteractAction/InteractSurface/InteractSpec + InteractSpecParser + InteractViolationException — td-schema typed, fixed order, no-system-scent vocabulary; incidental fix of the schemaTd missing-square-brackets defect that yielded invalid td docs) + a deterministic interaction-rule engine (InteractState/InteractResult/InteractRuleEngine — fixed-order surface/action/attitude/state rules, replayable, in-world-language messages, zero HUD) + zero-HUD rendering rules / surface mapping (SurfaceTextMapper+ZeroHudGuard+SurfaceText — deterministic in-world-language surface text, a system-scent blacklist guard, pure data, rendering deferred to p.2.22); the pure-JVM InteractProbe 30 deterministic checks (spec round-trip / violations, scripted replayable rule-engine scenarios, zero-HUD surface mapping + system-scent guard) wired into probeAcceptance. Key path **engine/interact**. Conclusion: the in-world interaction abstraction — a td-schema-typed interaction-spec model + a deterministic rule engine (fixed-order, replayable, in-world-language messages, zero HUD) + surface mapping guarded by a system-scent blacklist; InteractProbe 30 checks wired into probeAcceptance.
- **p.2.15 目标（ROAD p.2.15 行，本次承接）**：现有 optim / config / log 纯 JDK 核心迁入 engine.*；vanilla 数值对照固化为 golden tests 随改动回归（自原 p.2.2 顺移；示意子项由收尾时按仓库现状给演示）。/ p.2.15 goal (the ROAD row, this handoff): migrate the existing optim / config / log pure-JDK cores into engine.*; harden vanilla value comparisons as golden tests that regress on any change (moved from the former p.2.2; illustrative sub-items given at closing time per current repo state).
- **p.2.13 Deterministic PCG（已落地，事实不变）**：engine.pcg 确定性生成底座（PcgSource/PcgPick 于 XoroRandom PRNG、PcgTextSample、PcgContentGen、PcgSchemaGen——同种子同输出）+ PcgProbe 29 checks 接入 probeAcceptance。/ p.2.13 (landed, unchanged): engine.pcg deterministic generation base (same seed → same output) + PcgProbe 29 checks.
- **p.2.12 Schema-first（已落地，事实不变）**：engine.schema td schema 模型 + engine.scaffold 三生成器（同 schema 同字节、往返恒等）。/ p.2.12 (landed, unchanged): engine.schema td-schema model + engine.scaffold generators.
- **p.2.5 P2P / tink v2 / tsha1f / zd 栈（已落地，事实不变）**、**p.2.3 Save**、**p.2.4 server-interaction**、**p.2.8 sim / p.2.9 world / p.2.10 saveverify / p.2.11 session（已落地，事实不变）**。/ p.2.5 P2P / tink v2 / tsha1f / zd stack, p.2.3 Save, p.2.4 server-interaction, p.2.8 / p.2.9 / p.2.10 / p.2.11 all landed (unchanged).
- **probeAcceptance 现状（p.2.14 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**83 个纯 JVM 探针**（含新增 `InteractProbe` 30 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.14 closing): green — **83 pure-JVM probes** (incl. InteractProbe 30 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.15 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；td（tiedb-td 文档）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12/p.2.13/p.2.14 钉死并复用；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.15 为**纯迁移/加固**（自原 p.2.2 顺移），把 p.1 / p.2.0 时代遗留的纯 JDK 优化/配置/日志核心整固为 engine.* 稳定资产并将 vanilla 数值对照黄金化；p.2.16 API 契约面为其自然接续（golden 化的数值对照正是 api 契约面的数据底座）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、engine/optim、engine/config、engine/log 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.9/p.2.10/p.2.11/p.2.12/p.2.13/p.2.14 closing patterns. p.2.14 “Diegetic interaction” fully landed and pushed (latest track **p.2.14.5**, key path **engine/interact**): the interaction-spec model + the deterministic rule engine + zero-HUD surface mapping + InteractProbe 30 checks wired into probeAcceptance; the schemaTd missing-square-brackets defect was incidentally fixed. p.2.15 migrates the existing optim / config / log pure-JDK cores into engine.* and hardens vanilla value comparisons as golden tests, per the ROAD p.2.15 row. Everything pinned by p.2.12/p.2.13/p.2.14 (td-first, byte-identical deterministic, no-timing) stays the default semantics. probeAcceptance all green: **83 pure-JVM probes** (incl. InteractProbe 30 checks) + **5 E2E boots** (AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages. Confirm remaining context facts against the repo state (ROAD, git log, engine/optim, engine/config, engine/log) — never fabricate.

## 2. 本次范围（p.2.15 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认现有 optim / config / log 纯 JDK 核心的实际落点与边界、engine.pcg / engine.schema 的确定性范式、可固化为 golden tests 的 vanilla 数值对照资产、devkit 探针资产与 p.2.14 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（承接 p.2.14 已落地的 engine.interact 与 p.2.12/p.2.13 确定性范式，为 p.2.15 提炼 optim/config/log 迁移与 vanilla 黄金对照）：目标见 ROAD p.2.15 行：「现有 optim / config / log 纯 JDK 核心迁入 engine.*；vanilla 数值对照固化为 golden tests 随改动回归」。示意子项（示例）：
1. **optim/config/log 落点摸底与迁移**：把现有纯 JDK 优化核心、双层配置、日志核心的归属厘清并落入 engine.* 稳定包位（铁律探针恒定覆盖），不引新行为。
2. **vanilla 数值对照黄金化**：将已存在的 vanilla 保真数值对照（p.1.8.20–32 默认预设）固化为 golden tests——同输入同输出逐字节/逐数值一致，随改动回归。
3. **确定性迁移/golden 探针**：给迁移后的 engine.* 核心加确定性探针（数值对照逐项命中、覆盖既有行为），接入 probeAcceptance。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**`engine.` 归属稳定 + 行为不变（逐字节/逐数值）+ golden-test 化回归 + 接入 probeAcceptance** 贯穿每次设计；p.2.12/p.2.13/p.2.14 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」范式必须被复用为默认语义。

**English**: Same principle as p.2.9–p.2.14: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the actual landing points/boundaries of the existing optim/config/log pure-JDK cores, the deterministic paradigm of engine.pcg / engine.schema, the vanilla-value assets hardening into golden tests, and the devkit probe assets plus the post-p.2.14 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.15 row: “migrate the existing optim / config / log pure-JDK cores into engine.*; harden vanilla value comparisons as golden tests that regress on any change.” Illustrative directions: (1) **scout + migrate the optim/config/log cores** into stable engine.* slots (iron-law probe auto-covers, no new behavior); (2) **harden the vanilla-fidelity numeric comparisons** (the p.1.8.20–32 default presets) into golden tests — same input → byte/value-identical, regression on any change; (3) **deterministic migration/golden probes** for the migrated engine.* cores (value comparisons hit item-by-item, covering existing behavior), wired into probeAcceptance. Note: **engine.* slot stability + unchanged behavior (byte/value-identical) + golden-test regression + wired into probeAcceptance** runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12/p.2.13/p.2.14 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.15 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、devkit/脚手架子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/迁移核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.15 runs as a cluster of sub-agents (scouting, engine, devkit/scaffold, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.15 子项一律 **p.2.15.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；迁移/golden 核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.15 Engine migration + golden tests 为对既有自研核心的整理与黄金化（clean-room），不新增抄录；td schema 能力复用 p.2.12 engine.schema 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五次 E2E 开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.15.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the migration/golden core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.15 Engine migration + golden tests tidies and golden-izes existing self-developed cores (clean-room), no new copying; td schema capability reuses the p.2.12 engine.schema built-in. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.15 全轨完成后，必须生成 p.2.16 同格式提示词并纳入记录：更新上下文段（p.2.15 各子项落地、最新 p 轨号 p.2.15.x、关键路径、engine 迁移 + golden tests 结论）、更新本次范围段指向 p.2.16（API contract surface，见 ROAD p.2.16 行；worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.16-api-contract-surface.md`（bilingual），随当次提交一并提交。

**English**: After p.2.15 fully lands, produce the p.2.16 prompt in the same format (context updated with p.2.15 landing, latest track p.2.15.x, key path, the engine-migration + golden-tests conclusion; scope pointing to p.2.16 API contract surface, per ROAD p.2.16 — worldgen / config(rules) / export / event contracts + Exporter registry + rule-registration API; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.16-api-contract-surface.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.15 工作区建议：engine 纯 JDK 于 `subterra-engine\...\engine\` 既有包位（optim/config/log）+ devkit 脚手架（迁移/golden 探针）按需落 `subterra-devkit`（落点可探针）。
- **p.2.14 Diegetic interaction（已落地，p.2.14.5 收尾）**：engine/interact（世界内交互规范模型 + 确定性规则引擎 + 零 HUD 表面映射，td schema 定型固定序无系统气味词汇面，顺带修复 schemaTd 缺方括号缺陷）+ InteractProbe 30 checks 接入 probeAcceptance + probeAcceptance 83 纯 JVM + 5 次 E2E。p.2.15 迁移/golden 化工作遵循同一验收门。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12/p.2.13/p.2.14 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.15 的 golden 数值对照与迁移探针沿用同一约束；同输入 → 同输出。
- **td 一等语言（p.2.2/p.2.3/p.2.9/p.2.14 已确立）**：engine.datapack / engine.save / engine.export / engine.world / engine.interact——「td schema → 文档/注册」范式在 p.2.15 复用于 config/golden 资产的定型。
- **optim/config/log 现状（p.2.0 模块重构后）**：优化/配置/日志纯 JDK 核心已在 `engine.{optim,config,log}` 包位；p.2.15 主要做落点稳定、行为不变交接与 vanilla 数值对照的 golden-test 化（摸底后以仓库现状为准，不臆造）。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.14 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变（p.2.15 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新迁移/golden 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.13 deterministic PCG / p.2.14 diegetic interaction / p.2.16 API 契约面（接续后台）/ p.2.20 devkit 完整化；p.2.15 Engine migration + golden tests 把 p.1 / p.2.0 遗留纯 JDK 核心整固为 engine.* 稳定资产。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.15 landing: engine pure JDK in the existing engine.* slots (optim/config/log) + devkit scaffold (migration/golden probes) in subterra-devkit (landing spots probeable). p.2.14 diegetic interaction (landed, closing p.2.14.5): engine/interact (interaction-spec model + deterministic rule engine + zero-HUD surface mapping; td-schema typed, fixed order, no-system-scent vocabulary; incidental schemaTd bracket fix) + InteractProbe 30 checks wired into probeAcceptance + probeAcceptance 83 pure-JVM + 5 E2E boots; p.2.15 migration/golden work follows the same gate. Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12/p.2.13/p.2.14 pinned): byte-identical, fixed order, no timing. td first-class language (p.2.2/p.2.3/p.2.9/p.2.14): the td-schema → document/registration paradigm reused for config/golden-typed assets. optim/config/log (post p.2.0 re-layout) already sit in engine.{optim,config,log}; p.2.15 stabilizes landing, hands off behavior unchanged, and hardens vanilla value comparisons as golden tests (confirm against repo state after scouting — never fabricate). The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.14 proven) still applies (gate-off no-op, markers merged into AsyncE2EProbe, staging via build/tmp, five boots unchanged). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.13 deterministic PCG / p.2.14 diegetic interaction / p.2.16 API contract surface (next) / p.2.20; p.2.15 consolidates the p.1/p.2.0-era pure-JDK cores into stable engine.* assets.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.15 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.15 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.