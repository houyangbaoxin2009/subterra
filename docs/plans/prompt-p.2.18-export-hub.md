# Subterra p.2.18 Export hub / 导出器 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.17 全轨 landing 后，按 p.2.17 交接单 §5 生成指向 p.2.18 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.17 fully landed, §5 of the p.2.17 handoff produces this p.2.18 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.18 里程碑**的交接单：p.2.18 =「Export hub / 导出器 engine.export」——**language-keys / config / world / registries / migrate-maps 五类正式导出，td/zd 双格式，`/subterra export` 指令**（自原 p.2.3 顺移，见 ROAD p.2.18 行；示意子项可由收尾时按仓库现状给演示）；导出器以 p.2.16 export 契约（ExportKindSpec/ExportPortSpec/ExporterRegistry）为契约基座——**api 契约 + engine 实现**双层落地（p.2.16.3 定契约形状，p.2.18 落引擎正式导出），既有端口预留为 p.2.9.5 engine.export（ExportKind/ExportPort/ExportHub，WORLD_PACK + SAVE 两内建端口确定性 preload，p.2.18 seam），save 命令接线承接 p.2.3.6 SaveExportArchive（export∘rehydrate 逐字节，接线留 p.2.18）。执行模式与 p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.18.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 api.export ExporterRegistry 契约现状与既有导出基座：p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry、p.2.9.5 engine.export 端口预留、p.2.3.6 SaveExportArchive、p.2.2.7 `/subterra export` 指令接线范式、engine.export 现有结构，以及 devkit 探针资产与 p.2.17 收官后的验收门）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.18-milestone handoff: p.2.18 = “Export hub / engine.export” — **full formal export of language-keys / config / world / registries / migrate-maps, td/zd dual formats, and the `/subterra export` command** (moved from the former p.2.3, per the ROAD p.2.18 row; illustrative sub-items are given at closing time per current repo state); the exporter sits on the p.2.16 export contract (ExportKindSpec/ExportPortSpec/ExporterRegistry) as its contract base — **api contract + engine implementation** landed on two layers (p.2.16.3 states the contract shape, p.2.18 lands the engine-side formal export); the existing port cut is p.2.9.5 engine.export (ExportKind/ExportPort/ExportHub, WORLD_PACK + SAVE built-ins deterministically preloaded — the p.2.18 seam), and the save command wiring carries over from p.2.3.6 SaveExportArchive (byte-exact export∘rehydrate, wiring deferred to p.2.18). Execution mode identical to the p.2.10 / p.2.11 / p.2.12 / p.2.13 / p.2.14 / p.2.15 / p.2.16 / p.2.17 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.18.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the api.export ExporterRegistry contract state and the existing export bases: the p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry, the p.2.9.5 engine.export port cut, the p.2.3.6 SaveExportArchive, the p.2.2.7 `/subterra export` command wiring pattern, the engine.export structure, plus the devkit probe assets and the post-p.2.17 acceptance gate), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.17 里程碑「Rule system / 规则系统 engine.config.rules」已全部落地并已推 GitHub，最新 p 轨号 p.2.17.6（收尾提交）**：engine.config.rules 规则系统——**p.2.17.1** 类型化规则核心（`RuleType`/`RuleKey`/`RuleSpec` + `RuleValidator`/`RuleReport`/`RuleViolation`：UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER 四类确定性违约、固定序输出，值载体复用 p.2.16.2 `api.config.Rule` 契约、td rule-doc 文档形态对齐 DatapackRules.fromManifest）+ **p.2.17.2** `RuleStore` 双层配置（全局档 + 存档覆盖档，全量校验违约抛 `RuleViolationException` 全有或全无装载，resolve=global→overrides 胜出、render TreeMap 字典序，语义对齐 `RuleRegistry.resolveTiered` 与 p.2.3.5）+ **p.2.17.3** `RuleReloader` 热重载（逐字节变更检测、原子 apply-or-keep-old、重入幂等、revision 单调、禁时序）+ **p.2.17.4** `/subterra rule` 命令（view/set/override 共享确定性核心 runVerb）+ `RulesRuntime` 门控壳（subterra.probe.rule 钩子、ServerStarted 接线）+ DatapackE2EProbe 32 项断言（含新 rule marker，既有断言不动）+ **p.2.17.5** `RuleSystemProbe` 43 checks（注册序/重复拒/双层覆盖 vs RuleRegistry 与 DatapackRules/校验违约/渲染对齐/热重载重入/文档往返）接入 probeAcceptance。关键路径 **engine.config.rules + runtime.rules + DatapackE2EProbe**。结论：**规则系统引擎侧落地——类型化规则模型 + 确定性校验 + 双层配置 + 热重载 + `/subterra rule` 命令，全部确定性可探针**；probeAcceptance 全绿（86 纯 JVM + 五次 E2E，新增 RuleSystemProbe）。/ p.2.17 “Rule system / engine.config.rules” fully landed and pushed, latest track **p.2.17.6** (closing commit): the engine-side rule system — p.2.17.1 typed rule core (`RuleType`/`RuleKey`/`RuleSpec` + `RuleValidator`/`RuleReport`/`RuleViolation`: four deterministic violation kinds UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER, fixed-order output, value carrier reusing the p.2.16.2 `api.config.Rule` contract, td rule-doc form aligned with DatapackRules.fromManifest) + p.2.17.2 `RuleStore` two-tier configuration (global tier + save-override tier, all-or-nothing load raising `RuleViolationException`, resolve = global→overrides wins, TreeMap-ordered render, aligned with `RuleRegistry.resolveTiered` and p.2.3.5) + p.2.17.3 `RuleReloader` hot reload (byte-identical change detection, atomic apply-or-keep-old, re-entrant idempotent, monotonic revision, no timing) + p.2.17.4 the `/subterra rule` command (view/set/override over a shared deterministic core) + the `RulesRuntime` gated shell (subterra.probe.rule hook, ServerStarted wiring) + DatapackE2EProbe 32 asserts incl. the new rule marker (existing asserts untouched) + p.2.17.5 `RuleSystemProbe` 43 checks (registration order / dup-reject / two-tier override vs RuleRegistry and DatapackRules / validation violations / render alignment / hot-reload re-entry / doc round-trip) wired into probeAcceptance. Key path **engine.config.rules + runtime.rules + DatapackE2EProbe**. Conclusion: the rule system landed engine-side — typed rule model + deterministic validation + two-tier configuration + hot reload + the `/subterra rule` command, all deterministic and probeable; probeAcceptance all green (86 pure-JVM + five E2E boots, incl. RuleSystemProbe).
- **p.2.18 目标（ROAD p.2.18 行，本次承接）**：Export hub / 导出器 engine.export（自原 p.2.3 顺移）——**language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令**。p.2.16.3 Exporter 注册表（`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`：注册序查表、重复拒绝、form=world/save/datapack 与 engine `ExportKind` 逐一一致）是 p.2.18 引擎侧正式导出的**契约基座**（api 契约 + engine 实现：契约形状在 api 层，p.2.18 由 engine.export 消费为正式导出实现）；p.2.9.5 已预留 engine.export 导出端口（ExportKind/ExportPort/ExportHub，WORLD_PACK + SAVE 两内建端口确定性 preload）；p.2.3.6 SaveExportArchive 的 save 命令接线留 p.2.18。/ p.2.18 goal (the ROAD row, this handoff): Export hub / engine.export (moved from the former p.2.3) — **language-keys / config / world / registries / migrate-maps, td/zd dual formats, and the `/subterra export` command**. The p.2.16.3 Exporter registry (`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`: registration-order lookup, duplicate rejection, form=world/save/datapack 1:1 with engine `ExportKind`) is the **contract base** for the p.2.18 engine-side formal export (api contract + engine implementation: the contract shape lives in the api layer, consumed by engine.export as the formal export implementation); the p.2.9.5 engine.export port cut is reserved (ExportKind/ExportPort/ExportHub, WORLD_PACK + SAVE built-ins deterministically preloaded); the p.2.3.6 SaveExportArchive save-command wiring is deferred to p.2.18.
- **p.2.17 及更早里程碑（已落地，事实不变）**：p.2.17 规则系统（上述）、p.2.16 api 契约面（**p.2.16.3 export 契约 = p.2.18 前哨**）、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session（engine.session）、p.2.10 saveverify、p.2.9 world-as-artifact（**engine.export 端口预留** p.2.9.5）、p.2.8 sim、p.2.5 P2P / tink v2 / tsha1f / zd 栈、p.2.4 server-interaction、p.2.3 Save（**SaveExportArchive td 导出档案** p.2.3.6：与 DatapackExportArchive 同构、export∘rehydrate 逐字节；save 命令接线留 p.2.18）、p.2.2 datapack（**`/subterra export` 指令范式** p.2.2.7）。/ p.2.17 and earlier milestones all landed (unchanged): the rule system (above), p.2.16 api contract surface (**p.2.16.3 export contract = the p.2.18 precursor**), p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 engine.session, p.2.10 saveverify, p.2.9 world-as-artifact (**the engine.export port cut** p.2.9.5), p.2.8 sim, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save (**SaveExportArchive td export archive** p.2.3.6: isomorphic to DatapackExportArchive, byte-exact export∘rehydrate; save-command wiring deferred to p.2.18), p.2.2 datapack (**the `/subterra export` command pattern** p.2.2.7).
- **probeAcceptance 现状（p.2.17 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**86 个纯 JVM 探针**（含新增 `RuleSystemProbe` 43 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；DatapackE2EProbe 已含 export + rule markers，AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.17 closing): green — **86 pure-JVM probes** (incl. the new RuleSystemProbe 43 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; DatapackE2EProbe already asserts export + rule markers, AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.18 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；api 模块 `subterra-api\...\api\`（现有子包 config / crypto / export / event / migrate / net / ser / time / worldgen）为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12/p.2.13/p.2.14/p.2.15/p.2.16/p.2.17 钉死并复用；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.18 导出枢纽消费 p.2.16.3 契约基座 + p.2.9.5 端口预留 + p.2.3.6 SaveExportArchive 的 save 接线；`/subterra export` 指令接线沿用 p.2.2.7 gradle -P 属性钩子确定性触发范式（已证明，subterra.probe.export 转发到 runServer，startup 即跑同一核心）；language-keys 导出参考 Export-Language-Keys-for-Compasses（MIT 借鉴，声明保留）；td/zd 双格式编排复用 p.2.3 engine.zd 通用载体与既有 td 档案形态；与 p.2.19 runtime wiring migration（export 指令接线承接）衔接。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、subterra-api、engine/export、engine/config 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.12/p.2.13/p.2.14/p.2.15/p.2.16/p.2.17 closing patterns. p.2.17 “Rule system” fully landed and pushed (latest track **p.2.17.6**, key path **engine.config.rules + runtime.rules + DatapackE2EProbe**): the engine-side rule system — typed rule model (RuleType/RuleKey/RuleSpec over the p.2.16.2 api.config.Rule contract) + deterministic validation (UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER, all-or-nothing RuleViolationException) + two-tier RuleStore (global → save overrides win, TreeMap render, aligned with resolveTiered) + RuleReloader hot reload (byte-identical, apply-or-keep-old, monotonic revision) + `/subterra rule` command (view/set/override + RulesRuntime gate) + RuleSystemProbe 43 checks + DatapackE2EProbe 32 asserts (rule marker); conclusion: the rule system landed engine-side, all deterministic and probeable; probeAcceptance all green (86 pure-JVM + five E2E boots). p.2.18 defines the Export hub — engine.export: full formal export of **language-keys / config / world / registries / migrate-maps, td/zd dual formats, `/subterra export` command**, per the ROAD p.2.18 row (moved from the former p.2.3); the p.2.16.3 Exporter registry is the contract base for the engine-side formal export, the p.2.9.5 engine.export port cut is the seam (WORLD_PACK + SAVE built-ins preloaded), and the p.2.3.6 SaveExportArchive save-command wiring is deferred to p.2.18. Everything pinned by p.2.12–p.2.17 (td-first, byte-identical deterministic, no-timing) stays the default semantics. probeAcceptance all green: **86 pure-JVM probes** (incl. RuleSystemProbe) + **5 E2E boots**. Iron law auto-covers new engine packages. Confirm remaining context facts against the repo state (ROAD, git log, subterra-api, engine/export, engine/config) — never fabricate.

## 2. 本次范围（p.2.18 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 api.export ExporterRegistry 契约现状与既有导出基座：p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry、p.2.9.5 engine.export 端口预留（ExportKind/ExportPort/ExportHub）、p.2.3.6 SaveExportArchive、p.2.2.7 `/subterra export` 指令接线范式、engine.export 现有结构，以及 devkit 探针资产与 p.2.17 收官后的验收门），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（承接 p.2.16.3 已落地的 export 契约基座与 p.2.9.5 端口预留，为 p.2.18 落地引擎侧正式导出）：目标见 ROAD p.2.18 行：「engine.export 正式导出——language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令」。示意子项（示例）：
1. **导出枢纽形态**：engine.export 正式导出枢纽（承接 p.2.9.5 ExportKind/ExportPort/ExportHub 端口预留，消费 p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry 契约基座——注册序查表、重复拒绝、form 对齐；td/zd 双格式确定性编排，参考 Export-Language-Keys-for-Compasses MIT 借鉴于 language-keys 导出）。
2. **language-keys 导出**：语言键全量导出（引擎侧导出器，确定性、固定序，td/zd 双格式）。
3. **config 导出**：配置导出（engine.config 现有配置确定性导出，接双层配置语义）。
4. **world / registries / migrate-maps 导出**：world 导出（接 p.2.9 WorldPack/SaveContainer 档案）、registries 导出（注册表快照确定性导出）、migrate-maps 导出（p.2.3.5 迁移映射表确定性导出）。
5. **`/subterra export` 指令接线**：指令接线（沿用 p.2.2.7 gradle -P 确定性触发范式，扩展既有 export 命令核心，接 p.2.3.6 save 接线）。
6. **确定性导出探针**：导出枢纽探针（注册序、重复拒、五类导出、双格式往返、逐字节一致），接入 probeAcceptance。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**契约基座复用（p.2.16.3 ExporterRegistry）+ 端口预留承接（p.2.9.5）+ 双格式确定性导出 + 指令确定性触发 + 接入 probeAcceptance** 贯穿每次设计；p.2.12/p.2.13/p.2.14/p.2.15/p.2.16/p.2.17 已钉死的「单 schema 单字节流」与「逐字节一致 / 固定序 / 禁时序」范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.17: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the api.export ExporterRegistry contract state and the existing export bases: the p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry, the p.2.9.5 engine.export port cut (ExportKind/ExportPort/ExportHub), the p.2.3.6 SaveExportArchive, the p.2.2.7 `/subterra export` command wiring pattern, the engine.export structure, plus the devkit probe assets and the post-p.2.17 acceptance gate), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.18 row: “engine.export full formal export — language-keys / config / world / registries / migrate-maps, td/zd dual formats, `/subterra export` command.” Illustrative directions: (1) **export hub form** — the engine.export formal export hub (carrying the p.2.9.5 ExportKind/ExportPort/ExportHub port cut, consuming the p.2.16.3 ExportKindSpec/ExportPortSpec/ExporterRegistry contract base — registration-order lookup, duplicate rejection, form alignment; deterministic td/zd dual-format orchestration, language-keys borrowing from Export-Language-Keys-for-Compasses MIT); (2) **language-keys export** — full language-key export (engine-side exporter, deterministic fixed-order, td/zd dual formats); (3) **config export** — deterministic export of the engine.config configuration (carrying the two-tier semantics); (4) **world / registries / migrate-maps export** — world export (over the p.2.9 WorldPack/SaveContainer archive), registries export (deterministic registry snapshot), migrate-maps export (deterministic export of the p.2.3.5 migration maps); (5) **`/subterra export` command wiring** — command wiring (following the p.2.2.7 gradle `-P` deterministic-trigger pattern, extending the existing export command core, carrying the p.2.3.6 save wiring); (6) **deterministic export probes** — export-hub probes (registration order, dup-reject, five export kinds, dual-format round-trip, byte-identical), wired into probeAcceptance. Note: **contract-base reuse (p.2.16.3 ExporterRegistry) + port-cut carry-over (p.2.9.5) + dual-format deterministic export + deterministic command trigger + wired into probeAcceptance** runs through every design; the pinned “one schema → one byte stream” and “byte-identical / fixed-order / no-timing” deterministic paradigms of p.2.12–p.2.17 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.18 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、导出枢纽子代理、指令子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/契约核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.18 runs as a cluster of sub-agents (scouting, export-hub, command, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.18 子项一律 **p.2.18.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；导出核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine（p.2.16.3 契约在 api 层，p.2.18 实现落 engine.export，实现可依赖 api 契约、契约不得依赖实现）。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.18 Export hub 的 language-keys 导出参考 Export-Language-Keys-for-Compasses（**MIT 借鉴，保留声明**），不新增抄录；td/zd 双格式能力复用 p.2.3 engine.zd 与既有 td 档案形态（p.2.9/p.2.3.6）。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（**86 纯 JVM + 五次 E2E，新增 RuleSystemProbe 在内**）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.18.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the export core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api; the api module must not depend on engine (the p.2.16.3 contract lives in the api layer; the p.2.18 implementation lands in engine.export and may depend on the api contract, never the reverse). Licensing: permissive reuse/derive with attribution only; p.2.18 Export hub borrows Export-Language-Keys-for-Compasses for language-keys export (**MIT, notice preserved**), no new copying; td/zd dual-format capability reuses the p.2.3 engine.zd carrier and the existing td archive forms (p.2.9 / p.2.3.6). Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (**86 pure-JVM + five E2E boots, incl. RuleSystemProbe**) before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.18 全轨完成后，必须生成 p.2.19 同格式提示词并纳入记录：更新上下文段（p.2.18 各子项落地、最新 p 轨号 p.2.18.x、关键路径、Export hub 结论）、更新本次范围段指向 p.2.19（Runtime wiring migration，见 ROAD p.2.19 行；launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入，export 指令接线（接 p.2.18））、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.19-runtime-wiring.md`（bilingual），随当次提交一并提交。

**English**: After p.2.18 fully lands, produce the p.2.19 prompt in the same format (context updated with p.2.18 landing, latest track p.2.18.x, key path, the Export-hub conclusion; scope pointing to p.2.19 Runtime wiring migration, per ROAD p.2.19 — launch / runtime-fix / worldgen / optim-shell / tie / cfglog migrated in, export-command wiring (over p.2.18); sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.19-runtime-wiring.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.18 工作区建议：导出枢纽落 `engine.export`（纯 JDK，可探针）+ devkit 导出探针 + `/subterra export` 指令接线（runtime 命令处，沿用既有命令范式）。
- **p.2.16.3 export 契约与 Exporter 注册表（已落地，p.2.18 契约基座）**：`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`——注册序查表、重复拒绝、form=world/save/datapack 与 engine `ExportKind` 逐一一致；**p.2.18 引擎侧正式导出消费该契约语义**。
- **p.2.9.5 engine.export 端口预留（p.2.18 seam）**：`ExportKind`/`ExportPort`/`ExportHub`——WORLD_PACK + SAVE 两内建端口确定性 preload；p.2.18 落正式导出实现。
- **p.2.3.6 SaveExportArchive**：td 导出档案，与 DatapackExportArchive 同构、export∘rehydrate 逐字节；save 命令接线留 p.2.18。
- **`/subterra export` 指令接线范式（p.2.2.7 已证明）**：gradle `-P` 属性钩子确定性触发（`subterra.probe.export` 转发到 runServer，startup 即跑同一核心，规避 forked server JVM 的 stdin 双层中转不可靠）；p.2.18 扩展该指令核心并复用同一范式。
- **Export-Language-Keys-for-Compasses（MIT 借鉴，声明保留）**：语言键全量导出参考 → engine.export language-keys。
- **td/zd 双格式（p.2.2/p.2.3/p.2.9 已确立）**：td 一等语言（td schema → 文档/注册范式）+ zd 通用载体（engine.zd，10 字节头、固定字段 wire2）——p.2.18 导出枢纽按类目确定性编排双格式。
- **确定性范式（p.2.7/p.2.8 已证明、p.2.12/p.2.13/p.2.14/p.2.15/p.2.16/p.2.17 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.18 导出枢纽与探针沿用同一约束；同输入 → 同输出。
- **依赖铁律（p.2.0/p.2.15/p.2.16/p.2.17 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine（p.2.16.3 契约在 api 层，p.2.18 实现落 engine.export，实现依赖 api 契约、契约不得依赖实现）。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.17 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.18 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新导出探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.16 API contract surface（p.2.16.3 export 契约基座）/ p.2.17 规则系统（已落地）/ p.2.19 runtime wiring migration（export 指令接线承接）；p.2.18 把 p.2.16.3 的 export 契约基座 + p.2.9.5 端口预留落地为引擎侧正式导出枢纽（language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.18 landing: the export hub in engine.export (pure JDK, probeable) + devkit export probes + `/subterra export` command wiring (runtime command site, following the existing command pattern). p.2.16.3 export contract + Exporter registry (landed; the p.2.18 contract base): ExportKindSpec/ExportPortSpec/ExporterRegistry — registration-order lookup, duplicate rejection, form=world/save/datapack 1:1 with engine ExportKind; consumed by the p.2.18 engine-side formal export. p.2.9.5 engine.export port cut (the p.2.18 seam): ExportKind/ExportPort/ExportHub — WORLD_PACK + SAVE built-ins deterministically preloaded. p.2.3.6 SaveExportArchive: td export archive isomorphic to DatapackExportArchive, byte-exact export∘rehydrate; save-command wiring deferred to p.2.18. The `/subterra export` command wiring pattern (p.2.2.7 proven): deterministically triggered via a gradle `-P` property hook forwarded to runServer, running the same core at startup (stdin double-hop through the forked server JVM is unreliable); p.2.18 extends that command core and reuses the same pattern. Export-Language-Keys-for-Compasses (MIT borrow, notice preserved): the language-keys export reference → engine.export language-keys. td/zd dual formats (p.2.2/p.2.3/p.2.9): td as the first-class language (td-schema → document/registration) + the engine.zd generic carrier (10-byte header, six-field wire2 records) — the p.2.18 hub deterministically orchestrates both formats per kind. Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12–p.2.17 pinned): byte-identical, fixed order, no timing. Dependency iron law (p.2.0/p.2.15/p.2.16/p.2.17): api ← engine ← runtime/migrate; the api module must not depend on engine (the p.2.16.3 contract lives in the api layer; the p.2.18 implementation lands in engine.export and depends on the api contract, never the reverse). The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.17 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.16 API contract surface (p.2.16.3 export contract base) / p.2.17 rule system (landed) / p.2.19 runtime wiring migration (export-command wiring carry-over); p.2.18 lands the p.2.16.3 export contract base + the p.2.9.5 port cut as the engine-side formal export hub (language-keys / config / world / registries / migrate-maps, td/zd dual formats, `/subterra export` command).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.18 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.18 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
