# Subterra p.2.22 UI / HUD Core / engine.ui · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.21 全轨 landing 后，按 p.2.21 交接单 §5 生成指向 p.2.22 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.21 fully landed, §5 of the p.2.21 handoff produces this p.2.22 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.22 里程碑**的交接单：p.2.22 =「UI / HUD core / engine.ui」（自原 p.2.7 顺移）——**AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）**（见 ROAD p.2.22 行）。**本里程碑为混合移植形态**（形态参考 p.2.21 GeckoLib MIT 移植与 p.2.27.1 Flywheel MIT 直接包含纪律）：AppleSkin 数据层以 **Unlicense 全量移植**（数据层纯 JDK 确定性核心进 engine.ui，保留声明）；ModMenu 仅**借鉴交互模型与元数据 API 模式**（MIT 借鉴，NeoForge 接线自研）；DataTip（GPL-3.0）与 Patchouli（CC-BY-NC-SA）**仅 clean-room 参考**（不直接包含，td 化、零 json），引擎侧以确定性探针验收；UI 渲染/HUD 消费面经 runtime.ui 接线（门控壳范式），列表/详情/许可视图核心仅作管理视图，玩家侧交互走 p.2.14 interact。许可纪律总原则：permissive（MIT / Apache-2.0 / BSD / Unlicense / ISC）可移植并保留声明；copyleft（GPL / LGPL / AGPL / MPL）与 CC-BY-NC-SA 仅 clean-room 参考；存疑按不允许处理。执行模式与 p.2.21 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.22.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 AppleSkin / ModMenu / DataTip / Patchouli 发行物与许可/声明材料、p.2.21 动画线落点（engine.anim + runtime.anim + AnimProbe）与 runtime 门控壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.22-milestone handoff: p.2.22 = “UI / HUD core / engine.ui” (carried over from the original p.2.7) — **the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)** (per the ROAD p.2.22 row). **This milestone is a mixed-port shape** (forms referencing the p.2.21 GeckoLib MIT port and the p.2.27.1 Flywheel MIT direct-include discipline): the AppleSkin data layer lands as a **full Unlicense port** (pure-JDK deterministic data-layer core in engine.ui, notices preserved); ModMenu is **borrowed for its interaction model and metadata API pattern only** (MIT, NeoForge wiring self-developed); DataTip (GPL-3.0) and Patchouli (CC-BY-NC-SA) are **clean-room reference only** (not directly included, td-ized, zero json), accepted engine-side by deterministic probes; the UI render/HUD consumption surface is wired via runtime.ui (the gated-shell pattern); the list/detail/license view core is an admin view only, player-side interaction goes through p.2.14 interact. License discipline: permissive (MIT / Apache-2.0 / BSD / Unlicense / ISC) may be ported with notices preserved; copyleft (GPL / LGPL / AGPL / MPL) and CC-BY-NC-SA are clean-room reference only; uncertain defaults to not allowed. Execution mode identical to the p.2.21 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.22.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the AppleSkin / ModMenu / DataTip / Patchouli distributions and their license/notice material, the p.2.21 animation-line landings (engine.anim + runtime.anim + AnimProbe) and the runtime gated-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.21 里程碑「Animation / 动画 engine.anim」已全部落地并已推 GitHub，最新 p 轨号 p.2.21.4（收尾提交）**：动画核心 MIT 移植落地——**p.2.21.1** engine.anim 动画核心（GeckoLib 4.8 MIT 移植，纯 JDK：AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler 确定性采样（线性 / 最短弧 / 固定 clamp）+ AnimStateMachine 确定性状态机（注册序 / 重复拒 / 过渡线性缓动 / tick 回绕）+ MIT 声明与捆绑库声明材料 third-party/geckolib-4.8（LICENSE + NOTICE，注明 1.21.1 分支未带 LICENSE、MIT 原文取自 main 分支、未捆绑源码））+ **p.2.21.2** runtime.anim AnimRuntime 门控壳（subterra.probe.anim 钩子缺省 no-op、[Subterra anim] ok marker 并入 AsyncE2EProbe 同开服）+ **p.2.21.3** AnimProbe 30 checks（数据模型固定序 / 采样逐位一致含 clamp/NaN / 状态机过渡确定性 / 接线契约 / 许可盘点）接入 probeAcceptance。关键路径 **engine.anim + runtime.anim + AnimProbe**。结论：**动画核心 MIT 移植落地——engine.anim 确定性动画核心 + runtime.anim 门控壳 + AnimProbe 30 checks**；probeAcceptance 全绿（92 纯 JVM + 五次 E2E，新增 AnimProbe）。/ p.2.21 “Animation / 动画 engine.anim” fully landed and pushed, latest track **p.2.21.4** (closing commit): the animation core MIT port — p.2.21.1 the engine.anim animation core (GeckoLib 4.8 MIT port, pure JDK: AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler deterministic sampling (linear / shortest-arc / fixed clamp) + AnimStateMachine deterministic state machine (registration order / duplicate rejection / linearly-eased transitions / tick wrap) + MIT notice + bundled-library notice materials under third-party/geckolib-4.8 (LICENSE + NOTICE, noting the 1.21.1 branch carries no LICENSE so the verbatim MIT text is taken from the main branch, no source bundled)) + p.2.21.2 runtime.anim AnimRuntime gated shell (subterra.probe.anim hook default no-op, the [Subterra anim] ok marker merged into the AsyncE2EProbe boot) + p.2.21.3 AnimProbe 30 checks (data-model fixed order / sampler bit-exact incl. clamp/NaN / state-machine transition determinism / wiring contract / license inventory) wired into probeAcceptance. Key path **engine.anim + runtime.anim + AnimProbe**. Conclusion: **the animation core MIT port landed — the engine.anim deterministic animation core + the runtime.anim gated shell + AnimProbe 30 checks**; probeAcceptance all green (92 pure-JVM + five E2E boots, incl. AnimProbe).
- **p.2.22 目标（ROAD p.2.22 行原文，本次承接）**：「UI / HUD core / engine.ui（自原 p.2.7 顺移）：AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）」。/ p.2.22 goal (the ROAD row verbatim, this handoff): “UI / HUD core / engine.ui (carried over from the original p.2.7): the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)”.
- **p.2.21 及更早里程碑（已落地，事实不变）**：p.2.21 动画核心 MIT 移植（上述，动画线）、p.2.27 自研渲染管线（engine.render + runtime.render 三 MC 钩子 + RenderFidelityProbe 62 checks）、p.2.27.1 Flywheel 实例化基座前哨（engine.render.instancing + RenderRuntime 门控壳 + FlywheelInstancingProbe 34 checks）、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移（六类迁入 + export 收编单核心）、p.2.18 导出枢纽（engine.export 七 form）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact，玩家侧世界内交互）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（td 直载 + `/subterra export` 范式）、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。UI 线现状：p.2.22 为 UI 线首项（p.2.23 In-game mod hub 在其后，ROAD 行序紧随），p.2.12.4 已预留 FormDataGen 的 kind→widget 固定映射（UI 渲染留 p.2.22 线），p.2.14 已落世界内交互规范（零 HUD 渲染规则，p.2.22 面板仅作管理视图）。/ p.2.21 and earlier milestones all landed (unchanged): p.2.21 the animation core MIT port (above, the animation line), p.2.27 the clean-room self-developed render pipeline (engine.render + runtime.render three-MC-hook injection + RenderFidelityProbe 62 checks), p.2.27.1 the Flywheel instancing-base guard (engine.render.instancing + RenderRuntime gated shell + FlywheelInstancingProbe 34 checks), p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration (six-category fold-in + export single-core fold-in), p.2.18 export hub (engine.export seven forms), p.2.17 rule system (engine.config.rules + `/subterra rule`), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 diegetic interaction (engine.interact, player-side in-world interaction), p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (td direct loading + the `/subterra export` pattern), p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. UI line: p.2.22 is the first UI-line item (p.2.23 In-game mod hub next ROAD row); p.2.12.4 already reserved the FormDataGen kind→widget fixed mapping (UI rendering deferred to the p.2.22 line); p.2.14 already landed the in-world interaction spec (zero-HUD rendering rules; p.2.22 panels are admin views only).
- **probeAcceptance 现状（p.2.21 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**92 个纯 JVM 探针**（含新增 `AnimProbe` 30 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.22 的 engine.ui 纯 JDK 落点自动纳入。p.2.22 验收基线 = **probeAcceptance 全绿（92 纯 JVM + 5 次 E2E，新增 AnimProbe）+ 本里程碑新增的 UI 确定性/接线探针**。/ probeAcceptance (p.2.21 closing): green — **92 pure-JVM probes** (incl. the new AnimProbe 30 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.22 pure-JDK engine.ui landing is auto-covered. The p.2.22 acceptance baseline = **probeAcceptance all green (92 pure-JVM + 5 E2E boots, incl. AnimProbe) + the new UI determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.22 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.21 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.22): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.21; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.22 承接 p.2.21（动画核心 MIT 移植落地为既有验收门，UI 确定性/接线探针叠加其上）；「AppleSkin 移植 / ModMenu 借鉴 / DataTip / Patchouli clean-room」复用既有 port 许可纪律（ROAD Port Allocation Map 第 77–78、89–90 行：AppleSkin —— HUD 数据层（饱食/饱和度覆盖、食物 tooltip）→ engine.ui，Unlicense 全量移植；ModMenu —— 模组列表交互模型与元数据 API 模式 → engine.ui + runtime.ui，MIT 借鉴（NeoForge 接线自研）；DataTip —— 数据驱动 tooltip（物品→tooltip 行）→ engine.ui，GPL-3.0 clean-room 参考（td 化，零 json）；Patchouli —— 数据驱动书籍/文档 GUI（手册/图鉴/教程）→ engine.ui.docbook，CC-BY-NC-SA 3.0 clean-room 参考（td 化，零 json），均 planned / 已立项）。**许可衔接**：p.2.21 GeckoLib 为 MIT 移植（保留原始版权与捆绑库声明）；p.2.22 为混合形态——**AppleSkin 为 Unlicense 全量移植（保留声明）**、**ModMenu 仅 MIT 借鉴（不移植其代码，NeoForge 接线自研）**、**DataTip（GPL-3.0）与 Patchouli（CC-BY-NC-SA 3.0）仅 clean-room 参考（不直接包含任何 copyleft / CC 代码）**；存疑按不允许处理（完整登记册见框架设计 §7）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.22 continues from p.2.21 (the animation core MIT port landing is the existing gate; the UI determinism/wiring probes stack on it); the “AppleSkin port / ModMenu borrow / DataTip / Patchouli clean-room” reuses the pinned port license discipline (ROAD Port Allocation Map rows 77–78, 89–90: AppleSkin — HUD data layer (hunger/saturation overlay, food tooltip) → engine.ui, Unlicense full port; ModMenu — mod-list interaction model and metadata API pattern → engine.ui + runtime.ui, MIT borrow (NeoForge wiring self-developed); DataTip — data-driven tooltips (item→tooltip lines) → engine.ui, GPL-3.0 clean-room reference (td-ized, zero json); Patchouli — data-driven book/doc GUI (handbook/atlas/tutorial) → engine.ui.docbook, CC-BY-NC-SA 3.0 clean-room reference (td-driven, zero json), all planned). **License handover**: p.2.21 GeckoLib was an MIT port (original copyright and bundled-library notices preserved); p.2.22 is a mixed shape — **AppleSkin is a full Unlicense port (notices preserved)**, **ModMenu is an MIT borrow only (no code ported; NeoForge wiring self-developed)**, **DataTip (GPL-3.0) and Patchouli (CC-BY-NC-SA 3.0) are clean-room reference only (no copyleft / CC code directly included)**; uncertain defaults to not allowed (full register in framework design §7). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.21 closing patterns. p.2.21 “Animation / 动画 engine.anim” fully landed and pushed (latest track **p.2.21.4**, key path **engine.anim + runtime.anim + AnimProbe**): the animation core MIT port — engine.anim pure-JDK animation core (AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler deterministic sampling (linear / shortest-arc / fixed clamp) + AnimStateMachine deterministic state machine (registration order / duplicate rejection / linearly-eased transitions / tick wrap), GeckoLib 4.8 MIT notice + bundled-library notice materials under third-party/geckolib-4.8, no source bundled) + runtime.anim AnimRuntime gated shell (subterra.probe.anim default no-op, [Subterra anim] ok marker merged into the AsyncE2EProbe boot) + AnimProbe 30 checks wired into probeAcceptance; conclusion: **the animation core MIT port landed**; probeAcceptance all green (**92 pure-JVM + five E2E boots**, incl. AnimProbe). p.2.22 defines the UI / HUD core milestone — per the ROAD row verbatim: **the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)** (engine.ui, carried over from the original p.2.7). Everything pinned by p.2.12–p.2.21 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. Acceptance baseline: **probeAcceptance all green (92 pure-JVM + 5 E2E boots, incl. AnimProbe) + the new UI determinism/wiring probes**. Related: p.2.23 In-game mod hub is the next ROAD row after p.2.22. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.22 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.22 =「UI / HUD core / engine.ui」——**AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：AppleSkin / ModMenu / DataTip / Patchouli 发行物与许可/声明材料（Unlicense 全量移植与 MIT 借鉴的材料形态与落位，参考 p.2.21 GeckoLib MIT 移植纪律与 p.2.27.1 Flywheel 直接包含纪律；GPL-3.0 / CC-BY-NC-SA 仅 clean-room 不直接包含）；p.2.21 动画线落点现状（engine.anim + runtime.anim 门控壳与 AnimRuntime 接线范式、AnimProbe）；runtime.ui 接线点现状（HUD/工具提示/书籍 GUI 接线面与门控壳范式，p.2.14 interact 的世界内语言消息/零 HUD 规则、p.2.12.4 FormDataGen 的 kind→widget 映射）；probeAcceptance 既有接线（92 纯 JVM + 5 E2E）与 FFM/依赖面），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.22 行原文）：「UI / HUD core / engine.ui（自原 p.2.7 顺移）：AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）」。示意子项（示例，可合并）：
1. **engine.ui 数据层（AppleSkin Unlicense 全量移植，纯 JDK）**：HUD 数据层确定性核心（饱食度/饱和度覆盖、食物 tooltip 数据行，固定序、逐字节一致）；Unlicense 移植保留声明（材料随库保留并注明来源与版本，形态参考 p.2.21 GeckoLib MIT 移植纪律）。
2. **engine.ui 数据驱动 tooltip + 文档书籍 GUI（clean-room，td 化零 json）**：DataTip 数据驱动 tooltip 数据层（物品→tooltip 行，td 格式，clean-room 参考 GPL-3.0 仅思想、不包含代码）+ Patchouli 数据驱动文档书籍 GUI 核心（手册/图鉴/教程，td 驱动，clean-room 参考 CC-BY-NC-SA 仅思想、不包含代码）。
3. **engine.ui 列表/详情/许可视图核心 + ModMenu 交互模型借鉴（MIT）**：模组列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）；ModMenu 元数据 API 模式借鉴（MIT 借鉴，NeoForge 接线自研）。
4. **runtime.ui 接线**：HUD 数据层 / 工具提示 / 书籍 GUI 消费接线（门控 -D/-P 范式，缺省 no-op，marker 并入既有 E2E 或独立确定性断言）。
5. **UI 确定性/接线探针**：核心确定性（数据层 / tooltip / 书籍 GUI / 视图核心固定序逐字节一致、往返恒等）+ 接线契约（runtime 面与既有 E2E markers 稳定性）探针接入 probeAcceptance（92 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）** 是 ROAD 原文的**交付面**，贯穿每次设计；**许可红线**：AppleSkin **Unlicense 全量移植保留声明**、ModMenu **MIT 借鉴**（不移植其代码）、DataTip（GPL-3.0）与 Patchouli（CC-BY-NC-SA）**仅 clean-room 参考不直接包含**；p.2.12–p.2.21 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.21: p.2.22 is the UI / HUD core milestone — **the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the AppleSkin / ModMenu / DataTip / Patchouli distributions and their license/notice material (the Unlicense full-port and MIT-borrow notice forms and placement, referencing the p.2.21 GeckoLib MIT-port discipline and the p.2.27.1 Flywheel direct-include discipline; GPL-3.0 / CC-BY-NC-SA are clean-room reference only, never directly included); the p.2.21 animation-line landing state (engine.anim + the runtime.anim gated shell and AnimRuntime wiring pattern, AnimProbe); the runtime.ui wiring points (the HUD / tooltip / book-GUI consumption surfaces and the gated-shell pattern; p.2.14 interact’s in-world-language messages / zero-HUD rules; p.2.12.4 FormDataGen’s kind→widget mapping); the existing probeAcceptance wiring (92 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.22 row verbatim: “UI / HUD core / engine.ui (carried over from the original p.2.7): the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)”. Illustrative directions (may merge): (1) **the engine.ui data layer (AppleSkin full Unlicense port, pure JDK)** — a deterministic HUD data-layer core (hunger/saturation overlay, food-tooltip data rows, fixed order, byte-identical); the Unlicense-port notices preserved (materials kept with the library citing source and version, the form referencing the p.2.21 GeckoLib MIT-port discipline); (2) **engine.ui data-driven tooltips + doc-book GUI (clean-room, td-ized, zero json)** — the DataTip data-driven tooltip data layer (item→tooltip lines, td format, clean-room reference of GPL-3.0 — ideas only, no code) + the Patchouli data-driven doc-book GUI core (handbook/atlas/tutorial, td-driven, clean-room reference of CC-BY-NC-SA — ideas only, no code); (3) **engine.ui list/detail/license view core + ModMenu interaction-model borrow (MIT)** — the mod-list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact); the ModMenu metadata-API pattern borrow (MIT borrow, NeoForge wiring self-developed); (4) **runtime.ui wiring** — HUD data layer / tooltip / book-GUI consumption wiring (gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts); (5) **the UI determinism/wiring probes** — core determinism (data layer / tooltip / book GUI / view core fixed-order byte-identical, round-trip stable) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 92 pure-JVM + 5 E2E). Note: **the AppleSkin data-layer port (Unlicense) + the ModMenu interaction-model borrow (MIT) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core (panels are admin views only; player-side interaction goes through p.2.14 interact)** is the ROAD delivery surface and runs through every design; **the license red lines**: AppleSkin is a **full Unlicense port with notices preserved**, ModMenu is an **MIT borrow** (no code ported), DataTip (GPL-3.0) and Patchouli (CC-BY-NC-SA) are **clean-room reference only — never directly included**; the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.12–p.2.21 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.22 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.22 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.22 子项一律 **p.2.22.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；UI 核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.22 红线）**：AppleSkin 为 **Unlicense 全量移植——保留声明**（形态参考 p.2.21 GeckoLib MIT 移植纪律：License/notice 材料原样随库保留并注明来源与版本）；ModMenu 为 **MIT 借鉴——不移植其代码**（交互模型与元数据 API 模式自研落地，NeoForge 接线自研）；**DataTip（GPL-3.0）与 Patchouli（CC-BY-NC-SA 3.0）仅 clean-room 参考——不直接包含任何 copyleft / CC 代码**（td 化、零 json）；permissive 可移植并**保留声明**；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。p.2.22 只做这四库/参考，不新增其他移植。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（UI 若涉 native/FFM 按需）。
- **验收门（p.2.22 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**92 纯 JVM + 五次 E2E（新增 AnimProbe 在内）**，**并新增本里程碑的 UI 确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.22.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the UI core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.22 red lines)**: AppleSkin is a **full Unlicense port — notices preserved** (the form references the p.2.21 GeckoLib MIT-port discipline: License/notice materials kept verbatim with the library citing source and version); ModMenu is an **MIT borrow — no code ported** (the interaction model and metadata API pattern are self-developed, NeoForge wiring self-developed); **DataTip (GPL-3.0) and Patchouli (CC-BY-NC-SA 3.0) are clean-room reference only — no copyleft / CC code directly included** (td-ized, zero json); permissive may be ported with notices preserved; copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. p.2.22 adds no other port beyond these four libraries/references. Acceptance gate (the p.2.22 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **92 pure-JVM + five E2E boots (incl. AnimProbe)**, **plus the new UI determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.22 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.22 各子项落地、最新 p 轨号 p.2.22.x、关键路径、UI 核心移植结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.23 In-game mod hub / 游戏内模组 Hub——td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.23-mod-hub.md`（bilingual），随当次提交一并提交。

**English**: After p.2.22 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.22 landing, latest track p.2.22.x, key path, the UI-core conclusion; scope pointing to the next milestone, per ROAD row order **p.2.23 In-game mod hub / 游戏内模组 Hub** — td-schema auto-generated forms, config editing + hot reload, entry wiring, server-side op config commands; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.23-mod-hub.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.22 工作区建议：engine.ui 数据层（AppleSkin Unlicense 全量移植，纯 JDK）+ engine.ui tooltip / 文档书籍 GUI（DataTip / Patchouli clean-room，td 化零 json）+ engine.ui 列表/详情/许可视图核心（ModMenu MIT 借鉴）+ runtime.ui 接线 + UI 确定性/接线探针。
- **p.2.21 动画核心 MIT 移植（已落地，既有验收门）**：engine.anim 确定性动画核心（AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler 确定性采样 + AnimStateMachine 确定性状态机，GeckoLib 4.8 MIT 声明与捆绑库声明材料 third-party/geckolib-4.8）+ runtime.anim AnimRuntime 门控壳（subterra.probe.anim 缺省 no-op、[Subterra anim] marker 并入 AsyncE2EProbe）+ AnimProbe 30 checks；probeAcceptance 全绿（**92 纯 JVM + 五次 E2E**）。
- **移植落点（ROAD Port Allocation Map 第 77–78、89–90 行）**：AppleSkin —— HUD 数据层 → **engine.ui**，Unlicense 全量移植；ModMenu —— 模组列表交互模型与元数据 API 模式 → **engine.ui + runtime.ui**，MIT 借鉴（NeoForge 接线自研）；DataTip —— 数据驱动 tooltip → **engine.ui**，GPL-3.0 clean-room 参考（td 化，零 json）；Patchouli —— 数据驱动书籍/文档 GUI → **engine.ui.docbook**，CC-BY-NC-SA 3.0 clean-room 参考（td 驱动，零 json）。
- **许可登记（p.2.22 为混合形态）**：AppleSkin 为 Unlicense（permissive）→ 可移植并**保留声明**（材料随库保留并注明来源与版本，形态参考 p.2.21 GeckoLib MIT 移植纪律）；ModMenu 为 MIT（permissive）→ **仅借鉴交互模型**（不移植其代码，NeoForge 接线自研）；DataTip（GPL-3.0）与 Patchouli（CC-BY-NC-SA）为 **copyleft / CC —— 仅 clean-room 参考，不直接包含**；存疑按不允许处理；完整登记册见框架设计 §7。
- **probeAcceptance 接线基准（p.2.21 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **92 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.22 在此之上新增 UI 确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.21 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.ui 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.21 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.21 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.22 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.21 动画核心 MIT 移植（已落地，既有验收门；动画线）/ p.2.23 In-game mod hub（ROAD 行序紧随 p.2.22，下一里程碑）；p.2.22 为 UI 线首项（承接 p.2.12.4 FormDataGen kind→widget 预留与 p.2.14 interact 零 HUD 规则）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.22 landing: the engine.ui data layer (AppleSkin full Unlicense port, pure JDK) + engine.ui tooltip / doc-book GUI (DataTip / Patchouli clean-room, td-ized, zero json) + engine.ui list/detail/license view core (ModMenu MIT borrow) + runtime.ui wiring + the UI determinism/wiring probes. p.2.21 the animation core MIT port (landed; the existing acceptance gate): engine.anim deterministic animation core (AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler deterministic sampling + AnimStateMachine deterministic state machine, GeckoLib 4.8 MIT notice + bundled-library notice materials under third-party/geckolib-4.8) + runtime.anim AnimRuntime gated shell (subterra.probe.anim default no-op, [Subterra anim] marker merged into AsyncE2EProbe) + AnimProbe 30 checks; probeAcceptance all green (**92 pure-JVM + five E2E boots**). Port landings (ROAD Port Allocation Map rows 77–78, 89–90): AppleSkin — HUD data layer → **engine.ui**, Unlicense full port; ModMenu — mod-list interaction model and metadata API pattern → **engine.ui + runtime.ui**, MIT borrow (NeoForge wiring self-developed); DataTip — data-driven tooltips → **engine.ui**, GPL-3.0 clean-room reference (td-ized, zero json); Patchouli — data-driven book/doc GUI → **engine.ui.docbook**, CC-BY-NC-SA 3.0 clean-room reference (td-driven, zero json). License (p.2.22 is a mixed shape): AppleSkin is Unlicense (permissive) → portable with notices preserved (materials kept with the library citing source and version, the form referencing the p.2.21 GeckoLib MIT-port discipline); ModMenu is MIT (permissive) → **interaction-model borrow only** (no code ported, NeoForge wiring self-developed); DataTip (GPL-3.0) and Patchouli (CC-BY-NC-SA) are **copyleft / CC — clean-room reference only, never directly included**; uncertain defaults to not allowed; the full register is in framework design §7. probeAcceptance wiring baseline (p.2.21 closing): **92 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.22 adds the UI determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.21 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.ui wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.21): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.21 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.22 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.21 the animation core MIT port (landed; the existing gate; the animation line) / p.2.23 In-game mod hub (next ROAD row after p.2.22); p.2.22 is the first UI-line item (building on the p.2.12.4 FormDataGen kind→widget reservation and the p.2.14 interact zero-HUD rules).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 92 纯 JVM + 五次 E2E 与新增 UI 探针）+ ROAD p.2.22 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 92 pure-JVM + five E2E boots plus the new UI probes), the “ROAD p.2.22 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
