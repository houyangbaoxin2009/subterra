# Subterra p.2.14 Diegetic Interaction 世界内交互 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.13 全轨 landing 后，按 p.2.13 交接单 §5 生成指向 p.2.14 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.13 fully landed, §5 of the p.2.13 handoff produces this p.2.14 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.14 里程碑**的交接单：p.2.14 =「Diegetic interaction 世界内交互」——**「无系统气味」的可落实交互层**：世界内交互规范与零 HUD 化渲染规则。核心原则：**无系统气味（一切机制用世界内语言表达，游戏内无 HUD 面板/进度条/数值计数/任务日志/等级图标/系统弹窗）+ 世界内交互规范模型（NPC 态度、碑文/卷轴/信件/遗物实体）+ 确定性交互规则引擎 + 零 HUD 渲染规则/表面映射 + 交互层可探针（InteractProbe 确定性 self-test）+ 与 engine.ui p.2.22 调和（panels 在 p.2.22 仅作管理视图，玩家侧交互走 p.2.14 interact）**；engine 纯 JDK 不碰 MC、交互核心可独立探针。执行模式与 p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.14.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 engine.pcg 的确定性生成范式、engine.ui p.2.22 的渲染/面板边界、engine.schema 的 td schema 模型、devkit 探针资产）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.14-milestone handoff: p.2.14 = “Diegetic interaction” — a **realizable interaction layer with “no system scent”**: an in-world interaction spec and zero-HUD rendering rules. Core principles: **no system scent (every mechanic expressed in in-world language; no HUD panels / progress bars / numeric counters / task logs / level icons / system popups in-game) + an in-world interaction-spec model (NPC attitudes, inscriptions / scrolls / letters / relic entities) + a deterministic interaction-rule engine + zero-HUD rendering rules / surface mapping + a probeable interaction layer (InteractProbe with a deterministic self-test) + reconciled with engine.ui p.2.22 (panels there are management views only; player-side interaction walks p.2.14 interact)**; engine pure JDK (no MC coupling), the interaction core independently probeable. Execution mode identical to the p.2.8 / p.2.9 / p.2.10 / p.2.11 / p.2.12 / p.2.13 closing runs: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.14.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm the deterministic generation paradigm of engine.pcg, the rendering/panel boundary of engine.ui p.2.22, the td-schema model of engine.schema, and the devkit probe assets), produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.13 里程碑「Deterministic PCG 确定性内容生成」已全部落地并已推 GitHub，最新 p 轨号 p.2.13.6（收尾提交）**：engine `subterra-engine\...\engine\pcg\`（纯 JDK）——确定性生成核心 `PcgSource`/`PcgPick`（于 XoroRandom PRNG 之上：种子扩散 / 可复现随机流，同种子→同序 picks、fork 隔离、pick 落因固定）+ 确定性文本 `PcgTextSample`（模板-槽采样，固定槽序）+ 确定性内容生成器 `PcgContentGen`（遗物/台账/配方/本地化/群系变异 td 文档，同种子→同 td 文本、可解析输出）+ schema 驱动生成计划 `PcgSchemaGen`（把 p.2.12 td Schema 填入确定性合法实例，同种子→同 validate 通过 td 文档）；纯 JVM 探针 `PcgProbe` 29 项确定性断言（seed/fork 确定性、加权 picks、text/content/schema-driven 逐字节一致、可 validate td）接入 probeAcceptance。关键路径 **engine.pcg**（纯 JDK）。结论：**确定性内容生成编排底座——PcgSource/PcgPick/PcgTextSample/PcgContentGen/PcgSchemaGen，同种子同输出、schema 驱动生成合法实例、PcgProbe 29 项断言接入 probeAcceptance**。/ p.2.13 “Deterministic PCG” fully landed and pushed, latest track **p.2.13.6** (closing commit); engine.pcg (pure JDK): the deterministic generation core PcgSource/PcgPick (over the XoroRandom PRNG — seed diffusion / reproducible random stream, same seed → same-order picks, fork isolation, fixed pick reasons) + PcgTextSample (template-slot sampling, fixed slot order) + PcgContentGen (relic/ledger/recipe/localization/biome-variant td documents, same seed → same td text, parseable output) + PcgSchemaGen (the schema-driven generation plan that fills a p.2.12 td Schema into a deterministic valid instance, same seed → same validate-passing td doc); PcgProbe 29 deterministic checks (seed/fork determinism, weighted picks, text/content/schema-driven byte-identical, validate-able td) wired into probeAcceptance. Key path **engine.pcg**. Conclusion: a deterministic content-generation orchestration base — PcgSource/PcgPick/PcgTextSample/PcgContentGen/PcgSchemaGen, same seed → same output, schema-driven valid instances, PcgProbe 29 checks wired into probeAcceptance.
- **p.2.12 Schema-first scaffolding（已落地，事实不变）**：engine.schema td schema 模型 + engine.scaffold 三生成器（同 schema 同字节、往返恒等），为 p.2.14 的交互规则字段/表面映射定型底座。/ p.2.12 (landed, unchanged): engine.schema td-schema model + engine.scaffold generators — the typing base for p.2.14's interaction-rule fields / surface mapping.
- **p.2.5 P2P / tink v2 / tsha1f / zd 栈（已落地，事实不变）**：engine.network + engine.p2p，语言无关 ABI。/ p.2.5 P2P / tink v2 / tsha1f / zd stack (landed, unchanged): language-agnostic ABI.
- **p.2.3 Save（已落地，数据底座，事实不变）**：统一 `SaveContainer` 六槽 + `engine.zd` zd v2 载体 + zdt 混合文档 + `SaveExportArchive` td 导出档案。/ p.2.3 Save rework (landed, unchanged).
- **p.2.4 server-interaction（已落地，事实不变）**：engine.network 五子引擎层 + runtime EnhancedChannelRuntime 门控壳。/ p.2.4 (landed, unchanged): five pure-JVM engine.network cores + the gated runtime shell.
- **p.2.8 sim / p.2.9 world / p.2.10 saveverify / p.2.11 session（已落地，事实不变）**：engine.sim / engine.world / engine.saveverify / engine.session 及其门控壳，「门控壳并入既有 boot」范式。/ p.2.8 / p.2.9 / p.2.10 / p.2.11 (landed, unchanged): the gate-shell-into-existing-boot paradigm.
- **probeAcceptance 现状（p.2.13 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**82 个纯 JVM 探针**（含新增 `PcgProbe` 29 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.13 closing): green — **82 pure-JVM probes** (incl. PcgProbe 29 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.14 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；td（tiedb-td 文档）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12/p.2.13 钉死并复用；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.14 为**革命·新增**，直接消费 p.2.13 的确定性内容生成底座、与世界内实体（遗物/卷轴/信件/碑文）衔接；p.2.22 engine.ui（HUD 核心）与其调和——**panels 在 p.2.22 仅作管理视图**，玩家侧交互走 p.2.14 interact；「无系统气味」的纯世界内表达正是其目标。（接 p.2.13 engine.pcg / p.2.22 engine.ui）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、engine/pcg 与 engine/schema 目录后再定，不臆造。）

**English**: See the Chinese block above — this handoff follows the exact p.2.9/p.2.10/p.2.11/p.2.12/p.2.13 closing patterns. p.2.13 “Deterministic PCG” fully landed and pushed (commits p.2.13.1–p.2.13.5, latest track **p.2.13.6**, key path **engine.pcg**): engine.pcg (PcgSource/PcgPick over XoroRandom, PcgTextSample, PcgContentGen, PcgSchemaGen — same seed → same output, schema-driven valid instances) + PcgProbe 29 deterministic checks wired into probeAcceptance is the p.2.14 handoff basis; p.2.14's interaction layer is expressed via in-world entities and the deterministic generation base. p.2.12 / p.2.5 / p.2.3 / p.2.4 / p.2.8 / p.2.9 / p.2.10 / p.2.11 all landed unchanged (see Chinese block). td is the framework's first-class data language; the deterministic byte-identical paradigm is proven by p.2.7/p.2.8 and pinned by p.2.12/p.2.13. probeAcceptance all green: **82 pure-JVM probes** (incl. PcgProbe 29 checks) + **5 E2E boots** (AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages. p.2.14 should land a no-system-scent in-world interaction layer (interaction-spec model, deterministic interaction-rule engine, zero-HUD rendering rules/surface mapping, reconciled with engine.ui p.2.22 where panels are management views only), per the ROAD p.2.14 milestone.

## 2. 本次范围（p.2.14 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 engine.pcg 的确定性生成范式、engine.ui p.2.22 的渲染/面板边界、engine.schema td schema 模型、devkit 探针资产），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 p.2.14 提炼由世界内语言表达的「无系统气味」交互层，承接 p.2.13 已钉死的确定性生成与 schema 驱动态势）：目标见 ROAD p.2.14 行：「engine.interact『无系统气味』的可落实交互层——世界内交互规范与零 HUD 化渲染规则（与 engine.ui p.2.22 调和，panels 在 p.2.22 仅作管理视图）」。**设计前提写死**：游戏内无 HUD 面板/进度条/数值计数/任务日志/等级图标/系统弹窗，一切机制用世界内语言表达——NPC 态度、碑文/卷轴/信件/遗物实体。示意子项（示例）：
1. **交互规范模型**：世界内交互的确定性规范模型——实体（NPC / 碑文 / 卷轴 / 信件 / 遗物）可交互面、交互动作、态度的确定性描述（td schema 定型，固定序、同输入同输出）。
2. **确定性交互规则引擎**：把交互规范欧拉到可执行规则——条件 / 前提 / 结果确定性求值（固定序、禁时序、可回放），复用 p.2.12 engine.schema td schema 的 validate 语义。
3. **零 HUD 渲染规则 / 表面映射**：游戏内交互如何以世界内语言表达——NPC 态度、实体上的文字（碑文/卷轴/信件）到表面的确定性映射规则（纯数据，渲染接 p.2.22 的若干处延后）；零 HUD 约束下无面板 / 进度条 / 数值 / 任务日志 / 等级图标 / 系统弹窗。
4. **确定性 InteractProbe**：InteractProbe 脚手架自身含确定性 self-test（给定交互规范 → 期望输出逐字节一致），纯 JVM 可独立跑，接入 probeAcceptance。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**无系统气味（一切机制用世界内语言表达）+ 世界内交互规范模型 + 确定性交互规则引擎 + 零 HUD 渲染规则/表面映射 + 交互层可探针 + p.2.22 调和（panels 仅管理视图）**贯穿每次设计；p.2.12/p.2.13 已钉死的「单 schema 单字节流」确定性生成范式与 p.2.7/p.2.8 逐字节一致范式必须被复用为默认语义。

**English**: Same principle as p.2.9–p.2.13: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm the deterministic generation paradigm of engine.pcg, the rendering/panel boundary of engine.ui p.2.22, the td-schema model, and the devkit probe assets), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.14 row: “engine.interact — a **”no system scent“ realizable interaction layer**: an in-world interaction spec and zero-HUD rendering rules (reconciled with engine.ui p.2.22, where panels are management views only).” **Design premise pinned**: no HUD panels / progress bars / numeric counters / task logs / level icons / system popups in-game; every mechanic is expressed in in-world language — NPC attitudes, inscriptions / scrolls / letters / relic entities. Illustrative directions: (1) an **interaction-spec model** — a deterministic spec for in-world interactions across entities (NPC / inscription / scroll / letter / relic): interactable surfaces, interaction actions, attitudes (td-schema-typed, fixed order, same input → same output); (2) a **deterministic interaction-rule engine** — resolves the interaction spec into executable rules — conditions / preconditions / outcomes evaluated deterministically (fixed order, no timing, replayable), reusing the p.2.12 engine.schema validate semantics; (3) **zero-HUD rendering rules / surface mapping** — how in-game interaction is carried by world language — NPC attitudes, surface-mapped text on entities (inscriptions/scrolls/letters), deterministic mapping rules (pure data; actual rendering deferred to p.2.22 seams); under zero-HUD constraints there are no panels / progress bars / numbers / task logs / level icons / system popups; (4) a **deterministic InteractProbe** carrying a deterministic self-test (given an interaction spec → expected byte-identical output), pure-JVM standalone, wired into probeAcceptance. Note: **no system scent (in-world-language-only) + an in-world interaction-spec model + a deterministic interaction-rule engine + zero-HUD rendering rules/surface mapping + a probeable interaction layer + p.2.22 reconciliation (panels as management views only)** runs through every design; the pinned “one schema → one byte stream” deterministic paradigm of p.2.12/p.2.13 and the byte-identical paradigm proven by p.2.7/p.2.8 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.14 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、devkit/脚手架子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/交互核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.14 runs as a cluster of sub-agents (scouting, engine, devkit/scaffold, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.14 子项一律 **p.2.14.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性规则求值**（固定序、禁时序；同输入同输出）；交互规则/表面映射无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.14 Diegetic interaction 为自研（clean-room），不新增抄录；td schema 能力复用 p.2.12 engine.schema 内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五次 E2E 开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.14.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic rule evaluation** (fixed order, no timing; same input → same output); the interaction-rule / surface-mapping core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.14 Diegetic interaction is self-developed (clean-room), no new copying; td schema capability reuses the p.2.12 engine.schema built-in. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.14 全轨完成后，必须生成 p.2.15 同格式提示词并纳入记录：更新上下文段（p.2.14 各子项落地、最新 p 轨号 p.2.14.x、关键路径、世界内交互抽象结论）、更新本次范围段指向 p.2.15（Engine migration + golden tests，见 ROAD p.2.15 行；现有 optim / config / log 纯 JDK 核心迁入 engine.* + vanilla 数值对照固化为 golden tests）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.15-migration-golden.md`（bilingual），随当次提交一并提交。

**English**: After p.2.14 fully lands, produce the p.2.15 prompt in the same format (context updated with p.2.14 landing, latest track p.2.14.x, key path, the in-world-interaction abstraction conclusion; scope pointing to p.2.15 Engine migration + golden tests, per ROAD p.2.15 — migrate the existing optim/config/log pure-JDK cores into engine.* + harden vanilla value comparisons as golden tests; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.15-migration-golden.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.14 工作区建议：engine 纯 JDK 落 `subterra-engine\...\engine\interact\`（交互规范模型 + 确定性规则引擎 + 零 HUD 表面映射）+ devkit 脚手架（接线 / InteractProbe）按需落 `subterra-devkit`（映射落点可探针）。
- **p.2.13 Deterministic PCG（已落地，p.2.13.6 收尾）**：engine.pcg（PcgSource/PcgPick 于 XoroRandom PRNG、PcgTextSample、PcgContentGen、PcgSchemaGen，同种子同输出）+ PcgProbe 29 checks 接入 probeAcceptance + probeAcceptance 82 纯 JVM + 5 次 E2E。p.2.14 交互层由该确定性生成底座表达世界内实体（遗物/卷轴/信件/碑文）。
- **确定性生成范式（p.2.7/p.2.8 已证明、p.2.12/p.2.13 钉死）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.14 交互规则求值与表面映射沿用同一约束；同输入 → 同输出。
- **td 一等语言（p.2.2/p.2.3/p.2.9 已确立）**：engine.datapack / engine.save / engine.export / engine.world——「td schema → 文档/注册」范式与 p.2.12/p.2.13 生成范式在 p.2.14 复用于交互规范的定型与表面映射规则。
- **engine.ui 调和（p.2.22，后续）**：panels 在 p.2.22 仅作管理视图；玩家侧交互、零 HUD 表达与表面映射走 p.2.14 interact；p.2.14 只出世界内语言的确定性映射规则（纯数据），真实 GUI/渲染接线留 p.2.22 若干处延后。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.13 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变（p.2.14 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 InteractProbe 落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.13 deterministic PCG / p.2.22 engine.ui（HUD 核心，panels 仅管理视图）/ p.2.15（接续后台）/ p.2.16 API 契约面；p.2.14 Diegetic interaction（engine.interact）接 p.2.13。「Diegetic interaction」为其种子驱动的确定性内容生成抽象的玩家侧交互落点。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.14 work dirs: engine pure JDK in `subterra-engine\...\engine\interact\` (interaction-spec model + deterministic rule engine + zero-HUD surface mapping) + devkit scaffold (wiring / InteractProbe) in `subterra-devkit` (mapping landing spots probeable). p.2.13 Deterministic PCG (landed, closing p.2.13.6): engine.pcg (PcgSource/PcgPick over XoroRandom, PcgTextSample, PcgContentGen, PcgSchemaGen — same seed → same output) + PcgProbe 29 checks wired into probeAcceptance + probeAcceptance 82 pure-JVM + 5 E2E boots; p.2.14's interaction layer expresses in-world entities (relic/scroll/letter/inscription) via this base. Deterministic paradigm (p.2.7/p.2.8 proven, p.2.12/p.2.13 pinned): byte-identical, fixed order, no timing — p.2.14's rule evaluation and surface mapping follow the same constraints. td first-class language (p.2.2/p.2.3/p.2.9): the “td schema → document/registration” and p.2.12/p.2.13 generation paradigms reused for interaction-spec typing and surface-mapping rules. engine.ui reconciliation (p.2.22, later): panels are management views only; player-side interaction, zero-HUD expression, and surface mapping go through p.2.14 interact; p.2.14 outputs only deterministic in-world-language mapping rules (pure data); real GUI/rendering wiring deferred to p.2.22 seams. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.13 proven): gate-off no-op, markers merged into the AsyncE2EProbe boot, staging via build/tmp, five boots unchanged. New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.13 deterministic PCG / p.2.22 engine.ui (HUD core, panels management-only) / p.2.15 (next) / p.2.16 API contract surface. Diegetic interaction (engine.interact) is the player-side interaction landing spot of the seed-driven deterministic content-generation abstraction.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.14 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.14 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.