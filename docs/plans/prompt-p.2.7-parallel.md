# Subterra p.2.7 engine.parallel 确定性并行执行底座 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.6 全轨 landing 后，按 p.2.6 交接单 §5 生成指向 p.2.7 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.6 fully landed, §5 of the p.2.6 handoff produces this p.2.7 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.7 里程碑**的交接单：p.2.7 =「engine.parallel 确定性并行执行底座」——面向多核并行且**绝不破种子确定性**的可复用执行基础设施（顺序保持、可 join/完成后归并、确定性分发与工作分配；每次运行同输入同产出，与串行/既有路径逐位一致）。p.2.6（C2ME 直接包含：异步区块生成/加载/I/O 核心 engine.worldgen.async，已落地）为其首个落地消费方，p.2.8 engine.sim 为后续下游；本里程碑把 p.2.6 已初现的确定性并行范式（per-chunk 隔离 + 确定性 Z-then-X 归并）提炼为底层通用底座，供后续引擎复用。核心原则：**确定性优先**（task 语义与执行器无关、并发结果与串行逐位一致）、engine 纯 JDK 不碰 MC、有界并行不放大主线程热路径阻塞。执行模式与 p.2.4 / p.2.5 / p.2.6 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.7.x**（≤三级），禁止「阶段X / 第N块 / R1 / block N」等表述。工作方式：先**摸底**（评估已落地的 engine.worldgen.async（p.2.6）与 engine.optim.sched（p.1.4.23 FlowSched）可提炼/并入的确定性并行原语、定位可复用的 join/define/ordered-merge 切面）并产出全轨子项拆分（6–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.7-milestone handoff: p.2.7 = “engine.parallel deterministic parallel execution foundation” — a reusable execution infrastructure for multi-core parallelism that **never breaks seed determinism** (order-preserving, joinable/merge-after-completion, deterministic dispatch and work assignment; same input → same output every run, byte-identical to serial / existing paths). p.2.6 (C2ME bundled directly: async chunk generation/loading/I-O core engine.worldgen.async — landed) is its first consuming deliverable, p.2.8 engine.sim is the downstream; this milestone distills the deterministic parallel paradigm that emerged in p.2.6 (per-chunk isolation + deterministic Z-then-X merge) into a lower-level reusable base for later engines. Core principles: **determinism-first** (task semantics independent of the executor; concurrent results byte-identical to serial), engine pure JDK (no MC coupling), bounded parallelism that does not amplify main-thread hot-path blocking. Execution mode identical to p.2.4 / p.2.5 / p.2.6 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.7.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (assess which deterministic parallel primitives in the landed engine.worldgen.async (p.2.6) and engine.optim.sched (p.1.4.23 FlowSched) can be distilled/incorporated, and locate the reusable join / define / ordered-merge surfaces) to produce the full-track sub-item split (6–8), then implement sub-item by sub-item, commit after each, and do a final unified review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.6 里程碑「C2ME 直接包含」已全部落地并推 GitHub**（提交见 git log；编号 p.2.6.1/2/3/4/6/8，最新 p 轨号 **p.2.6.8**）。关键路径 **engine.worldgen.async**（纯 JDK 核心，跨 `subterra-engine\...\engine\worldgen\async\`）：
  * **p.2.6.1** 确定性分发底座：`ChunkPartition`（区块坐标→bank 的纯确定性划分，上限 8 bank）`AsyncWorkerBank`（bank 内串行、bank 间并发、有界队列、exactly-once、awaitIdle）`DeterministicDispatcher`（提交→bank 路由→全部 join 后按 ChunkResult 自然序=先 Z 后 X 归并）。+ `AsyncDispatchProbe`。
  * **p.2.6.2** 每区块独立上下文 + 确定性每区块随机：`ctx\ChunkTaskContext`/`ctx\PerChunkRandom`（跨任务禁止共享易变状态）。+ `ChunkContextProbe`。
  * **p.2.6.3** 异步区块生成核心：`ChunkProducer`/`ChunkConsumer`/`AsyncChunkPipeline`（并行分发/归并框架，逐区块与同步路径一致）。+ `AsyncChunkGenProbe`（并行 vs 串行逐位 golden）。
  * **p.2.6.4** 异步加载/I-O 核心：`io\AsyncIoQueue`/`io\AsyncWrite`（确定性有序刷新队列，默认关）。+ `AsyncIoProbe`。
  * **p.2.6.6** runtime 异步壳：`runtime.worldgen.async.AsyncChunkRuntime`（`-Dsubterra.probe.async` 门控、默认全 no-op、ServerStarted 打开引擎 I/O 门、OSS 确定刷关）。+ **第 5 次 E2E 开服** `AsyncE2EProbe`。
  * **p.2.6.8** 收尾：C2ME MIT 声明（根 `NOTICE` + jar `META-INF/NOTICE` + README 第三方声明子块）、`C2meCoexistence` 共存文案重述（官方 C2ME 为受支持的兼容对等方；`[subterra_c2me]` marker 与公共 API 不变）、本 p.2.7 交接单。
- **异步确定性结论（p.2.6 核心产出）**：多核并行不破种子确定性——**每区块上下文隔离 + 确定性 Z-then-X 归并** 使并行结果与串行 golden **逐位一致**（AsyncChunkGenProbe 实证）；这为 p.2.7 的通用底座确立了「task 语义与执行器无关 + 完成后有序归并」的设计范式。
- **p.2.7 可复用的前置底座（已预落地）**：`engine.optim.sched` `StatusAdvancingScheduler` + p.1.4.23 `FlowSchedProbe`（调度原语，FlowSched/ScratchPool 已在优化集）；p.2.6 `engine.worldgen.async` 的 bank/join/ordered-merge 范式；既有 runtime 确定性 `-D`/`-P` 钩子范式（SameSeedCompareHook/WorldProfilerHook/EnhancedChannelRuntime 门控——默认 no-op、原版零接触）。
- **probeAcceptance 现状**：`gradlew :subterra-devkit:probeAcceptance` 通过——**67 个纯 JVM 探针**（含新增 `AsyncDispatch`/`ChunkContext`/`AsyncChunkGen`/`AsyncIo`）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（engine 纯 JDK 不碰 MC / 分层 api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；新增 engine 包（含 p.2.7 engine.parallel）由 iron law 常量池扫描自动覆盖。
- **ABI / 接线事实（已钉死，p.2.7 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；`engine.worldgen.*` 既有 ABI 与 runtime 世界生成壳装载范式；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链，不加新开服若可并入既有壳；staging 用 build/tmp）。
- **关联里程碑**：p.2.6 C2ME 直接包含（已落地，首个消费方）；p.2.8 engine.sim（下游）。p.2.7 应为 engine.parallel 落地一个通用、可在不改造 p.2.6/p.2.8 的前提下被复用的执行底座。

**English**: See the Chinese block above — this handoff follows the exact p.2.6 closing patterns. p.2.6 “C2ME bundled directly” fully landed and pushed (commits p.2.6.1/2/3/4/6/8, latest track **p.2.6.8**); the key path is **engine.worldgen.async** (pure JDK, in `subterra-engine\...\engine\worldgen\async\`). The p.2.6 sub-items: a deterministic dispatch base (ChunkPartition / AsyncWorkerBank / DeterministicDispatcher), per-chunk isolated context + deterministic per-chunk random (ctx), the async chunk-generation core (ChunkProducer / ChunkConsumer / AsyncChunkPipeline), the async load/I-O core with an ordered flush-queue default-off (io.AsyncIoQueue / AsyncWrite), the runtime async shell with a default-off gate and the 5th E2E boot (AsyncChunkRuntime / AsyncE2EProbe), and the p.2.6.8 closing (C2ME MIT notices, coexistence rework, this prompt). **Async-determinism conclusion**: parallel-per-chunk isolation + deterministic Z-then-X merge give byte-identical output vs a serial golden (proven by AsyncChunkGenProbe). The pre-landed reusable base: engine.optim.sched StatusAdvancingScheduler + FlowSched (p.1.4.23) plus the p.2.6 bank/join/ordered-merge paradigm. probeAcceptance is all green: 67 pure-JVM probes (incl. AsyncDispatch / ChunkContext / AsyncChunkGen / AsyncIo) + 5 E2E boots (Boot → Datapack → Save → Network → Async, mustRunAfter chain). Iron law auto-covers new engine packages. p.2.7 should land a general engine.parallel base reusable by p.2.6 (consumed) and p.2.8 (downstream) without rework.

## 2. 本次范围（p.2.7 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（评估 engine.worldgen.async（p.2.6）与 engine.optim.sched（p.1.4.23）可提炼/并入的确定性并行原语、确认通用性与确定性约束、定位可复用的 join/ordered-merge 切面），再定 6–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 engine.parallel 提炼通用的确定性并行执行底座，承接 p.2.6 范式 + 复用 engine.optim.sched）：
1. **通用确定性任务语义**：与执行器无关的 task 抽象（输入键 → 确定性 key → bank 归属；串行单线程可复现），同输入同产出。
2. **确定性分发/工作分配**：把 p.2.6 `ChunkPartition`/`DeterministicDispatcher` 的范式泛化为坐标/键无关的分发；join（awaitAll）后按确定性键序归并，与各执行器时序无关。
3. **顺序保持与乱序/重复键语义**：明确各 bank 内顺序保持、跨 bank 归并序、重复键处理（p.2.6 为 last-write-wins 非确定性保证——此处应给出确定性命中或显式拒绝）。
4. **有界并行 + 生命周期**：有界队列背压 + 可 join 的可复用执行器生命周期（创建/awaitIdle/shutdown/exactly-once），不放大主线程热路径。
5. **确定性探针（并行 vs 串行逐位一致 + 确定性分发序）**：任意 key 集并行 vs 串行结果逐位一致 + 固定分发序断言；保持既有 markers 稳定。
6. **引擎接线与复用验证**：engine.optim.sched 底座并入/组合验证 + iron law 覆盖 engine.parallel 包（常量池扫描）+ probeAcceptance 全量接线；如必要做一次 runtime 壳消费验证（默认关，另 E2E 尽量并入既有壳）。
提示：以上为示意（6–8 个子项以摸底后最小闭环为准）；**确定性优先**与「engine 纯 JDK 不碰 MC」贯穿每次设计；p.2.6 已证明的并行范式（per-chunk/per-key 隔离 + 确定性归并）必须被保留为默认语义。

**English**: Same principle as p.2.4/p.2.5/p.2.6: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first**, then produce a 6–8 sub-item split (adjustable, never expand scope unilaterally). Illustrative directions distilling a general engine.parallel base from the p.2.6 paradigm over engine.optim.sched: (1) executor-independent deterministic task semantics (input key → deterministic key → bank assignment; reproducible on a single serial thread); (2) deterministic dispatch / work assignment generalizing the p.2.6 ChunkPartition/DeterministicDispatcher pattern beyond chunk coords, with join-then-deterministic-order merge independent of executor timing; (3) order preservation plus out-of-order/duplicate-key semantics (p.2.6 used last-write-wins, a non-deterministic guarantee — here give a deterministic hit or explicit rejection); (4) bounded parallelism + lifecycle (bounded backpressure, reusable create/awaitIdle/shutdown/exactly-once, no main-thread hot-path amplification); (5) deterministic probes (parallel-vs-serial byte-identical + fixed dispatch order, stable markers); (6) engine wiring + reuse validation (engine.optim.sched base incorporation/combination + iron-law coverage of engine.parallel + full probeAcceptance wiring; one optional runtime-shell consumption check, default off, reusing an existing boot). Note: never break seed determinism; engine stays pure JDK; the p.2.6-proven parallel paradigm (per-key isolation + deterministic ordered merge) must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.7 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、runtime 子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 runtime/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/注入核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.7 runs as a cluster of sub-agents (scouting, engine, runtime, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before runtime/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.7 子项一律 **p.2.7.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；并行度有上限、不放大主线程热路径阻塞。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.6 C2ME（MIT）派生设计声明已在根 NOTICE，p.2.7 沿用既有派生头与声明，不新增抄录。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含 BootProbe / DatapackE2EProbe / SaveE2EProbe / NetworkE2EProbe / AsyncE2EProbe 五次开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.7.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound parallel core, don't block the main thread hot path. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: reuse/derive permissively with attribution only; p.2.6 C2ME (MIT) derived-design notice already lives in the root NOTICE — p.2.7 follows existing derived headers and declarations, no new copying. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.7 全轨完成后，必须生成 p.2.8 同格式提示词并纳入记录：更新上下文段（p.2.7 各子项落地、最新 p 轨号 p.2.7.x、关键路径 engine.parallel、确定性并行底座结论）、更新本次范围段指向 p.2.8（engine.sim 下游）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.8-sim.md`（bilingual），随当次提交一并提交。

**English**: After p.2.7 fully lands, produce the p.2.8 prompt in the same format (context updated with p.2.7 landing, latest track p.2.7.x, key path engine.parallel, the deterministic-parallel-base conclusion; scope pointing to p.2.8 engine.sim; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.8-sim.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.7 工作区建议：`subterra-engine\...\engine\parallel\`（通用确定性并行执行底座，纯 JDK）。
- **p.2.6 已落地（可复用/避免重复）**：`engine.worldgen.async` 全套（`ChunkPartition`/`AsyncWorkerBank`/`DeterministicDispatcher`/`ChunkKey`/`ChunkTask`/`ChunkProducer`/`ChunkConsumer`/`AsyncChunkPipeline`/`ctx\ChunkTaskContext`/`ctx\PerChunkRandom`/`io\AsyncIoQueue`/`io\AsyncWrite`）+ runtime 壳 `runtime.worldgen.async.AsyncChunkRuntime`。p.2.7 泛化其 bank/join/ordered-merge 范式，勿另起冲突命名。
- **p.2.7 复用前置**：`engine.optim.sched` `StatusAdvancingScheduler` + p.1.4.23 `FlowSchedProbe`（调度原语）；既有确定性随机载体 `engine.worldgen.pipeline/noise`（`XoroRandom`/`LegacyRandom`/`PositionalRand`）；runtime 门控范式（`EnhancedChannelRuntime`/`AsyncChunkRuntime`——默认 no-op、原版零接触）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 parallel 探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用 has mustRunAfter 既有开服门；staging 用 build/tmp）。
- 关联里程碑：p.2.6 C2ME 直接包含（已落地，消费方）；p.2.8 engine.sim（下游）；「engine.parallel 确定性并行执行底座」为其通用地基。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.7 work dir: `subterra-engine\...\engine\parallel\` (general deterministic parallel execution base, pure JDK). p.2.6 already landed (reuse/avoid duplication): the full engine.worldgen.async set plus the runtime shell async.AsyncChunkRuntime; p.2.7 generalizes its bank/join/ordered-merge paradigm without colliding naming. Reusable predecessors: engine.optim.sched StatusAdvancingScheduler + FlowSched (p.1.4.23), the deterministic random carriers in engine.worldgen.pipeline/noise, and the runtime gating style (EnhancedChannelRuntime / AsyncChunkRuntime — default no-op, zero vanilla impact). New parallel probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.6 C2ME (landed, consumer); p.2.8 engine.sim (downstream); engine.parallel is the general foundation.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.7 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.7 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.