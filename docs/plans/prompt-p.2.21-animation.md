# Subterra p.2.21 Animation / 动画 engine.anim · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.27 全轨 landing 后，按 p.2.27 交接单 §5 生成指向 p.2.21 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.27 fully landed, §5 of the p.2.27 handoff produces this p.2.21 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.21 里程碑**的交接单：p.2.21 =「Animation / 动画 engine.anim」（自原 p.2.6 顺移）——**GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding**（见 ROAD p.2.21 行）。**本里程碑为 MIT 移植形态**（形态参考 p.2.27.1 Flywheel 的 MIT 直接包含纪律）：把 GeckoLib 的 3D 关键帧动画核心（实体 / 方块 / 物品 / 盔甲）以纯 JDK 确定性核心移植进 engine.anim，并经 runtime.anim 接线到 MC 动画生命周期，以确定性探针验收；许可走 **MIT 移植纪律**（保留原始版权与捆绑库声明，声明材料随库保留并注明来源与版本；copyleft 仅 clean-room，存疑按不允许处理）。执行模式与 p.2.27 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.21.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 GeckoLib 发行物与许可/声明材料、p.2.27 渲染线落点（engine.render + runtime.render + RenderFidelityProbe）与 runtime 门控壳接线现状、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.21-milestone handoff: p.2.21 = “Animation / 动画 engine.anim” (carried over from the original p.2.6) — **the GeckoLib core port (MIT notice + bundled-library notice) + runtime anim binding** (per the ROAD p.2.21 row). **This milestone is an MIT-port shape** (form referencing the p.2.27.1 Flywheel MIT direct-include discipline): it ports GeckoLib's 3D keyframe-animation core (entities / blocks / items / armor) as a pure-JDK deterministic core in engine.anim, wired into the MC animation lifecycle via runtime.anim, accepted by deterministic probes; license follows the **MIT-port discipline** (original copyright and bundled-library notices preserved, notice materials kept with the library citing source and version; copyleft is clean-room reference only, uncertain defaults to not allowed). Execution mode identical to the p.2.27 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.21.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the GeckoLib distribution and its license/notice material, the p.2.27 render-line landings (engine.render + runtime.render + RenderFidelityProbe) and the runtime gated-shell wiring state, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.27 里程碑「Self-developed rendering / 自研渲染管线」已全部落地并已推 GitHub，最新 p 轨号 p.2.27.4（收尾提交）**：自研渲染管线 clean-room 落地——**p.2.27.1** engine.render 原版保真渲染面模型（clean-room：RenderStage 10 阶段固定序（form/opaque/sortOnCpu 语义，1.21.1 规范名对照）+ MaterialSurface 材质面规范 form 串 + VertexLayout BLOCK 32B / ENTITY 28B 字段序/偏移/对齐/byteSize 固定序逐字节一致，javadoc 注明对照 1.21.1 规范名、零 MC import、无 Sodium/Embeddium copyleft 包含）+ **p.2.27.2** runtime.render 三 MC 钩子注入（RenderHooks server 侧三面只读核对（RenderStage 固定序 / VertexLayout.BLOCK 布局 / BackendRegistry.defaultFor 缺省后端）+ RenderHooksClient 客户端 @EventBusSubscriber 事件注入——LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 真实对象一次性只读核对；subterra.probe.render 门控缺省 no-op、不改变渲染输出；hooks ok marker 并入 AsyncE2EProbe）+ **p.2.27.3** RenderFidelityProbe 62 checks（原版保真模型确定性 / 实例化基座互补（InstanceFormat 重建 BLOCK/ENTITY 同语义布局一致）/ 三钩子静态契约）接入 probeAcceptance。关键路径 **engine.render + runtime.render + RenderFidelityProbe**。结论：**自研渲染管线 clean-room 落地——engine.render 原版保真渲染面模型 + runtime.render 三 MC 钩子注入（门控 no-op）+ 与 p.2.27.1 实例化基座互补 + RenderFidelityProbe 62 checks**；probeAcceptance 全绿（91 纯 JVM + 五次 E2E，新增 RenderFidelityProbe）。/ p.2.27 “Self-developed rendering / 自研渲染管线” fully landed and pushed, latest track **p.2.27.4** (closing commit): the clean-room self-developed render pipeline — p.2.27.1 engine.render vanilla-fidelity surface model (clean-room: RenderStage 10-stage fixed order (form/opaque/sortOnCpu semantics, 1.21.1 canonical-name reference) + MaterialSurface canonical form strings + VertexLayout BLOCK 32B / ENTITY 28B field order/offsets/alignment/byteSize fixed-order byte-identical, javadoc citing the 1.21.1 canonical names, zero MC imports, no Sodium/Embeddium copyleft inclusion) + p.2.27.2 runtime.render three-MC-hook injection (RenderHooks server-side three-surface read-only check (RenderStage fixed order / VertexLayout.BLOCK layout / BackendRegistry.defaultFor default backend) + RenderHooksClient client-side @EventBusSubscriber event injection — one-shot read-only check of the real LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher objects; gated by subterra.probe.render default no-op, no render-output change; hooks-ok marker merged into AsyncE2EProbe) + p.2.27.3 RenderFidelityProbe 62 checks (vanilla-fidelity model determinism / instancing-base complementarity (InstanceFormat rebuilds BLOCK/ENTITY with the same-semantics layout) / three-hook static contract) wired into probeAcceptance. Key path **engine.render + runtime.render + RenderFidelityProbe**. Conclusion: **the clean-room self-developed render pipeline landed — engine.render vanilla-fidelity surface model + runtime.render three-MC-hook injection (gated no-op) + complementarity with the p.2.27.1 instancing base + RenderFidelityProbe 62 checks**; probeAcceptance all green (91 pure-JVM + five E2E boots, incl. RenderFidelityProbe).
- **p.2.21 目标（ROAD p.2.21 行原文，本次承接）**：「Animation / 动画 engine.anim（自原 p.2.6 顺移）：GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding」。/ p.2.21 goal (the ROAD row verbatim, this handoff): “Animation / 动画 engine.anim (carried over from the original p.2.6): the GeckoLib core port (MIT notice + bundled-library notice) + runtime anim binding”.
- **p.2.27 及更早里程碑（已落地，事实不变）**：p.2.27 自研渲染管线（上述，渲染线）、p.2.27.1 Flywheel 实例化基座前哨（engine.render.instancing 纯 JDK 核心 + RenderRuntime 门控壳 + FlywheelInstancingProbe 34 checks）、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移（六类迁入 + export 收编单核心）、p.2.18 导出枢纽（engine.export 七 form）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（td 直载 + `/subterra export` 范式）、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。动画线现状：p.2.21 为动画线首项，p.2.22 UI 在其后（ROAD 行序紧随）。/ p.2.27 and earlier milestones all landed (unchanged): p.2.27 self-developed rendering (above, the render line), p.2.27.1 the Flywheel instancing-base guard (engine.render.instancing pure-JDK core + RenderRuntime gated shell + FlywheelInstancingProbe 34 checks), p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration (six-category fold-in + export single-core fold-in), p.2.18 export hub (engine.export seven forms), p.2.17 rule system (engine.config.rules + `/subterra rule`), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (td direct loading + the `/subterra export` pattern), p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Animation line: p.2.21 is the first animation-line item, followed by p.2.22 UI (next ROAD row).
- **probeAcceptance 现状（p.2.27 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**91 个纯 JVM 探针**（含新增 `RenderFidelityProbe` 62 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.21 的 engine.anim 纯 JDK 落点自动纳入。p.2.21 验收基线 = **probeAcceptance 全绿（91 纯 JVM + 5 次 E2E，新增 RenderFidelityProbe）+ 本里程碑新增的动画确定性/接线探针**。/ probeAcceptance (p.2.27 closing): green — **91 pure-JVM probes** (incl. the new RenderFidelityProbe 62 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.21 pure-JDK engine.anim landing is auto-covered. The p.2.21 acceptance baseline = **probeAcceptance all green (91 pure-JVM + 5 E2E boots, incl. RenderFidelityProbe) + the new animation determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.21 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.27 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.21): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.27; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.21 承接 p.2.27（自研渲染管线 clean-room 落地为既有验收门，动画确定性/接线探针叠加其上）；「GeckoLib 移植」复用既有 port 许可纪律（ROAD Port Allocation Map 第 76 行：GeckoLib —— 3D 关键帧动画引擎（实体/方块/物品/盔甲）→ engine.anim + runtime.anim，MIT 移植，保留声明与捆绑库声明，planned / 已立项）。**许可衔接**：p.2.27.1 为 Flywheel MIT 直接包含（形态 A：保留声明、来源/版本注明）；p.2.21 GeckoLib 同为 MIT 移植——**保留原始版权与捆绑库声明**（形态可参考 p.2.27.1 的 MIT 直接包含纪律：声明材料随库保留并注明来源与版本）；copyleft 仅 clean-room，存疑按不允许处理（完整登记册见框架设计 §7）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.21 continues from p.2.27 (the clean-room self-developed render pipeline landing is the existing gate; the animation determinism/wiring probes stack on it); the “GeckoLib port” reuses the pinned port license discipline (ROAD Port Allocation Map row 76: GeckoLib — 3D keyframe animation engine (entities / blocks / items / armor) → engine.anim + runtime.anim, MIT port with notices and bundled-library notice preserved, planned). **License handover**: p.2.27.1 was a Flywheel MIT direct-include (form A: notices preserved, source/version cited); p.2.21 GeckoLib is likewise an MIT port — **original copyright and bundled-library notices preserved** (the form may reference the p.2.27.1 MIT direct-include discipline: notice materials kept with the library citing source and version); copyleft is clean-room reference only, uncertain defaults to not allowed (full register in framework design §7). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.27.1–p.2.27 closing patterns. p.2.27 “Self-developed rendering / 自研渲染管线” fully landed and pushed (latest track **p.2.27.4**, key path **engine.render + runtime.render + RenderFidelityProbe**): the clean-room self-developed render pipeline — engine.render vanilla-fidelity surface model (RenderStage 10-stage fixed order / MaterialSurface canonical forms / VertexLayout BLOCK 32B + ENTITY 28B, byte-identical, 1.21.1 canonical-name reference, no copyleft) + runtime.render three-MC-hook injection (RenderHooks server-side read-only check + RenderHooksClient client-side @EventBusSubscriber event injection, gated no-op, no render-output change) complementary to the p.2.27.1 instancing base + RenderFidelityProbe 62 checks wired into probeAcceptance; conclusion: **the clean-room self-developed render pipeline landed**; probeAcceptance all green (**91 pure-JVM + five E2E boots**, incl. RenderFidelityProbe). p.2.21 defines the Animation milestone — per the ROAD row verbatim: **the GeckoLib core port (MIT notice + bundled-library notice) + runtime anim binding** (engine.anim, carried over from the original p.2.6). Everything pinned by p.2.12–p.2.27 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. Acceptance baseline: **probeAcceptance all green (91 pure-JVM + 5 E2E boots, incl. RenderFidelityProbe) + the new animation determinism/wiring probes**. Related: p.2.22 UI / HUD core is the next ROAD row after p.2.21. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.21 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.21 =「Animation / 动画 engine.anim」（GeckoLib 核心移植，MIT 声明 + 捆绑库声明）；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：GeckoLib 发行物与许可/声明材料（MIT 移植保留声明的材料形态与落位，参考 p.2.27.1 Flywheel 直接包含纪律）；p.2.27 渲染线落点现状（engine.render + runtime.render 门控壳与 RenderRuntime 接线范式、RenderFidelityProbe）；runtime.anim 接线点现状（MC 动画生命周期接线面与门控壳范式）；probeAcceptance 既有接线（91 纯 JVM + 5 E2E）与 FFM/依赖面），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.21 行原文）：「Animation / 动画 engine.anim（自原 p.2.6 顺移）：GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding」。示意子项（示例，可合并）：
1. **engine.anim 动画核心（纯 JDK，GeckoLib MIT 移植）**：3D 关键帧动画确定性核心模型（动画数据模型 / 关键帧采样 / 状态机与过渡，固定序、逐字节一致）；MIT 移植保留声明（原始版权 + 捆绑库声明，材料随库保留并注明来源与版本，形态参考 p.2.27.1 Flywheel 直接包含纪律）。
2. **runtime.anim 接线（runtime anim binding）**：MC 动画生命周期接线（动画数据装载面 / 驱动与更新面 / 渲染消费面），门控 -D/-P 范式，缺省 no-op，marker 并入既有 E2E 或独立确定性断言。
3. **动画确定性/接线探针**：核心确定性（动画解析 / 采样 / 状态机固定序逐字节一致、往返恒等）+ 接线契约（runtimetable 与既有 E2E markers 稳定性）探针接入 probeAcceptance（91 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**engine.anim 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding** 是 ROAD 原文的**交付面**，贯穿每次设计；**MIT 移植保留声明（原始版权 + 捆绑库声明，参考 p.2.27.1 直接包含纪律）** 是许可红线；p.2.12–p.2.27 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.27: p.2.21 is the Animation milestone (the GeckoLib core port, MIT notice + bundled-library notice); the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the GeckoLib distribution and its license/notice material (the MIT-port notice form and placement, referencing the p.2.27.1 Flywheel direct-include discipline); the p.2.27 render-line landing state (engine.render + the runtime.render gated shell and RenderRuntime wiring pattern, RenderFidelityProbe); the runtime.anim wiring points (the MC animation-lifecycle wiring surfaces and the gated-shell pattern); the existing probeAcceptance wiring (91 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.21 row verbatim: “Animation / 动画 engine.anim (carried over from the original p.2.6): the GeckoLib core port (MIT notice + bundled-library notice) + runtime anim binding”. Illustrative directions (may merge): (1) **the engine.anim animation core (pure JDK, GeckoLib MIT port)** — a deterministic core model of 3D keyframe animation (animation data model / keyframe sampling / state machine with transitions, fixed order, byte-identical); the MIT-port notices preserved (original copyright + bundled-library notice, materials kept with the library citing source and version, the form referencing the p.2.27.1 Flywheel direct-include discipline); (2) **runtime.anim wiring (the runtime anim binding)** — MC animation-lifecycle wiring (animation data load surface / drive-and-update surface / render-consumption surface), gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts; (3) **the animation determinism/wiring probes** — core determinism (animation parse / sampling / state-machine fixed-order byte-identical, round-trip stable) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 91 pure-JVM + 5 E2E). Note: **the engine.anim core port (MIT notice + bundled-library notice) + runtime anim binding** is the ROAD delivery surface and runs through every design; **the MIT-port notice discipline (original copyright + bundled-library notice, referencing the p.2.27.1 direct-include discipline)** is the license red line; the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.12–p.2.27 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.21 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.21 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.21 子项一律 **p.2.21.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；动画核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.21 红线）**：GeckoLib 为 **MIT 移植——保留原始版权与捆绑库声明**（形态参考 p.2.27.1 Flywheel 的 MIT 直接包含纪律：License/notice 材料原样随库保留并注明来源与版本；捆绑库声明一并保留）；permissive 可移植并**保留声明**；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。p.2.21 只做 GeckoLib 一库移植，不新增其他移植。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（动画若涉 native/FFM 按需）。
- **验收门（p.2.21 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**91 纯 JVM + 五次 E2E（新增 RenderFidelityProbe 在内）**，**并新增本里程碑的动画确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.21.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the animation core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.21 red line)**: GeckoLib is an **MIT port — original copyright and bundled-library notices preserved** (the form references the p.2.27.1 Flywheel MIT direct-include discipline: License/notice materials kept verbatim with the library citing source and version; the bundled-library notice is preserved too); permissive may be ported with notices; copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. p.2.21 adds no other port beyond GeckoLib. Acceptance gate (the p.2.21 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **91 pure-JVM + five E2E boots (incl. RenderFidelityProbe)**, **plus the new animation determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.21 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.21 各子项落地、最新 p 轨号 p.2.21.x、关键路径、动画核心移植结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.22 UI / HUD core engine.ui——AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.22-ui-hud-core.md`（bilingual），随当次提交一并提交。

**English**: After p.2.21 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.21 landing, latest track p.2.21.x, key path, the animation-core-port conclusion; scope pointing to the next milestone, per ROAD row order **p.2.22 UI / HUD core engine.ui** — the AppleSkin data-layer port (Unlicense) + the ModMenu interaction model (MIT, borrowed) + data-driven tooltips (clean-room reference of DataTip GPL-3.0, td format) + doc-book GUI (clean-room reference of Patchouli CC-BY-NC-SA, td-driven) + list/detail/license view core; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.22-ui-hud-core.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.21 工作区建议：engine.anim 动画核心（GeckoLib MIT 移植，纯 JDK）+ runtime.anim 接线（runtime anim binding）+ 动画确定性/接线探针。
- **p.2.27 自研渲染管线（已落地，既有验收门）**：engine.render 原版保真渲染面模型（RenderStage 10 阶段固定序 / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B，clean-room 无 copyleft）+ runtime.render 三 MC 钩子注入（RenderHooks server 核对 + RenderHooksClient 客户端事件注入，subterra.probe.render 门控缺省 no-op、不改变渲染输出）+ RenderFidelityProbe 62 checks；probeAcceptance 全绿（**91 纯 JVM + 五次 E2E**）。
- **GeckoLib 移植落点（ROAD Port Allocation Map 第 76 行）**：3D 关键帧动画引擎（实体 / 方块 / 物品 / 盔甲）→ **engine.anim + runtime.anim**，MIT 移植，保留声明与捆绑库声明（planned / 已立项）。
- **许可登记（p.2.21 为 MIT 移植）**：GeckoLib 为 MIT（permissive）→ 可移植并**保留声明**（原始版权 + 捆绑库声明，材料随库保留并注明来源与版本，形态参考 p.2.27.1 Flywheel 的 MIT 直接包含纪律）；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。
- **probeAcceptance 接线基准（p.2.27 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **91 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.21 在此之上新增动画确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.27 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.anim 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.27 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.27 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.21 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.27 自研渲染管线（已落地，既有验收门；渲染线）/ p.2.22 UI / HUD core（ROAD 行序紧随 p.2.21，下一里程碑）；p.2.21 为动画线首项。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.21 landing: the engine.anim animation core (GeckoLib MIT port, pure JDK) + runtime.anim wiring (the runtime anim binding) + the animation determinism/wiring probes. p.2.27 the self-developed render pipeline (landed; the existing acceptance gate): engine.render vanilla-fidelity surface model (RenderStage 10-stage fixed order / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B, clean-room without copyleft) + runtime.render three-MC-hook injection (RenderHooks server-side check + RenderHooksClient client-side event injection, gated by subterra.probe.render default no-op, no render-output change) + RenderFidelityProbe 62 checks; probeAcceptance all green (**91 pure-JVM + five E2E boots**). GeckoLib port landing (ROAD Port Allocation Map row 76): 3D keyframe animation engine (entities / blocks / items / armor) → **engine.anim + runtime.anim**, MIT port with notices and bundled-library notice preserved (planned). License (p.2.21 is an MIT port): GeckoLib is MIT (permissive) → portable with notices preserved (original copyright + bundled-library notice, materials kept with the library citing source and version, the form referencing the p.2.27.1 Flywheel MIT direct-include discipline); copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. probeAcceptance wiring baseline (p.2.27 closing): **91 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.21 adds the animation determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.27 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.anim wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.27): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.27 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.21 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.27 the self-developed render pipeline (landed; the existing gate; the render line) / p.2.22 UI / HUD core (next ROAD row after p.2.21); p.2.21 is the first animation-line item.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 91 纯 JVM + 五次 E2E 与新增动画探针）+ ROAD p.2.21 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 91 pure-JVM + five E2E boots plus the new animation probes), the “ROAD p.2.21 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
