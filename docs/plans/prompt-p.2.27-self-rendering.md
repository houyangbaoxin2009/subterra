# Subterra p.2.27 Self-developed rendering / 自研渲染管线 · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.27.1 全轨 landing 后，按 p.2.27.1 交接单 §5 生成指向 p.2.27 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.27.1 fully landed, §5 of the p.2.27.1 handoff produces this p.2.27 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.27 里程碑**的交接单：p.2.27 =「Self-developed rendering / 自研渲染管线」（自原 p.4.2 顺移；2026-09-09 前移，clean-room，参考 Sodium/Embeddium 等登记项）——**原版保真块/实体渲染，与 p.2.27.1 实例化基座互补**（见 ROAD p.2.27 行）。**本里程碑为自研渲染管线（clean-room）**：把 p.2.27.1 落地的 engine.render.instancing 实例化基座（InstanceFormat / ShaderTemplate / RenderBackend+BackendRegistry，纯 JDK）接进 Minecraft 客户端渲染管线——承接 p.2.27.1.2 文档化的**三 MC 钩子**（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher）真实注入，以原版保真为判据、确定性探针验收；许可走 **clean-room 纪律**（参考 Sodium（LGPL）/ Embeddium 等登记项仅为边界参考，copyleft 不直接包含，存疑按不允许处理）。执行模式与 p.2.27.1 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.27.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 p.2.27.1 实例化基座与三 MC 钩子接线点现状、原版保真块/实体渲染的渲染面、probeAcceptance 既有接线）并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.27-milestone handoff: p.2.27 = “Self-developed rendering / 自研渲染管线” (carried over from the original p.4.2; advanced on 2026-09-09, clean-room, referencing the Sodium/Embeddium etc. registered items) — **vanilla-fidelity block/entity rendering, complementary to the p.2.27.1 instancing base** (per the ROAD p.2.27 row). **This milestone is the clean-room self-developed render pipeline**: it wires the p.2.27.1 instancing base (engine.render.instancing — InstanceFormat / ShaderTemplate / RenderBackend+BackendRegistry, pure JDK) into the Minecraft client render pipeline — taking over the **three MC hooks** documented by p.2.27.1.2 (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher) with real injection, judged by vanilla fidelity and accepted by deterministic probes; license follows the **clean-room discipline** (the registered items Sodium (LGPL) / Embeddium etc. are boundary references only — copyleft is never direct-included, uncertain defaults to not allowed). Execution mode identical to the p.2.27.1 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.27.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the p.2.27.1 instancing base and the three MC hook wiring points, the vanilla-fidelity block/entity render surfaces, and the existing probeAcceptance wiring), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.27.1 里程碑「Flywheel port / 飞轮移植前哨」已全部落地并已推 GitHub，最新 p 轨号 p.2.27.1.3（收尾提交）**：实例化基座前哨——**p.2.27.1.1** engine.render.instancing 纯 JDK 核心（InstanceFormat 实例布局确定性模型——字段序/标量类型/字节偏移/对齐/byteSize/固定序 layout()/逐字节一致规范编码；ShaderTemplate clean-room 固定序模板——部件拼装 + 固定序占位符替换，compile() 产出逐字节一致规范 GLSL；RenderBackend+BackendRegistry 后端 SPI——固定注册序、同名/空白名拒、确定性缺省选择；全零 MC/OpenGL import）+ 许可材料落位（META-INF/third-party/flywheel-1.0.6：MIT 原文 LICENSE + 双语 NOTICE.md——来源/版本 1.0.6 注明，ShaderTemplate 注明 clean-room 不宣称直接包含）+ **p.2.27.1.2** runtime.render RenderRuntime 门控壳（subterra.probe.render 钩子，缺省 no-op，[Subterra render] marker 并入 AsyncE2EProbe 同开服）+ FlywheelInstancingProbe 34 checks（装载确定性 / 后端 SPI / 实例格式模型 / 许可盘点）接入 probeAcceptance（95 任务 = 90 纯 JVM + 5 E2E）；**三 MC 钩子（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher）文档化为 p.2.27 接线点**。关键路径 **engine.render.instancing + runtime.render + FlywheelInstancingProbe**。结论：**Flywheel 1.0.6 实例化基座前哨落地——engine.render.instancing 纯 JDK 确定性核心（MIT 直接包含保留声明）+ runtime.render RenderRuntime 门控壳 + FlywheelInstancingProbe 34 checks**；probeAcceptance 全绿（90 纯 JVM + 五次 E2E）。/ p.2.27.1 “Flywheel port / 飞轮移植前哨” fully landed and pushed, latest track **p.2.27.1.3** (closing commit): the instancing-base guard — p.2.27.1.1 engine.render.instancing pure-JDK core (InstanceFormat deterministic instance-layout model — field order / scalar type / byte offset / alignment / byteSize / fixed-order layout() / byte-identical canonical encoding; ShaderTemplate clean-room fixed-order template — parts assembly + fixed-order placeholder substitution, compile() yielding byte-identical canonical GLSL; RenderBackend+BackendRegistry backend SPI — fixed registration order, duplicate/blank-name rejection, deterministic default selection; zero MC/OpenGL imports) + license materials in place (META-INF/third-party/flywheel-1.0.6: verbatim MIT LICENSE + bilingual NOTICE.md citing source and version 1.0.6, ShaderTemplate flagged clean-room, no direct-include claim) + p.2.27.1.2 runtime.render RenderRuntime gated shell (subterra.probe.render hook, default no-op, [Subterra render] markers merged into the AsyncE2EProbe boot) + FlywheelInstancingProbe 34 checks (load determinism / backend SPI / instance-format model / license inventory) wired into probeAcceptance (95 tasks = 90 pure-JVM + 5 E2E); **the three MC hooks (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher) documented as p.2.27 wiring points**. Key path **engine.render.instancing + runtime.render + FlywheelInstancingProbe**. Conclusion: **the Flywheel 1.0.6 instancing-base guard landed — engine.render.instancing pure-JDK deterministic core (MIT direct-include with notices preserved) + the runtime.render RenderRuntime gated shell + FlywheelInstancingProbe 34 checks**; probeAcceptance all green (90 pure-JVM + five E2E boots).
- **p.2.27 目标（ROAD p.2.27 行原文，本次承接）**：「Self-developed rendering / 自研渲染管线（自原 p.4.2 顺移；2026-09-09 前移，clean-room，参考 Sodium/Embeddium 等登记项）：原版保真块/实体渲染，与 p.2.27.1 实例化基座互补」。/ p.2.27 goal (the ROAD row verbatim, this handoff): “Self-developed rendering / 自研渲染管线 (carried over from the original p.4.2; advanced on 2026-09-09, clean-room, referencing the Sodium/Embeddium etc. registered items): vanilla-fidelity block/entity rendering, complementary to the p.2.27.1 instancing base”.
- **p.2.27.1 及更早里程碑（已落地，事实不变）**：p.2.27.1 Flywheel 实例化基座前哨（上述，渲染线首项）、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移（六类迁入 + export 收编单核心）、p.2.18 导出枢纽（engine.export 七 form）、p.2.17 规则系统（engine.config.rules + `/subterra rule`）、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 diegetic interaction（engine.interact）、p.2.13 deterministic PCG（engine.pcg）、p.2.12 schema-first（engine.schema + engine.scaffold）、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack（td 直载 + `/subterra export` 范式）、p.2.1 tie bridge（FFM）、p.2.0 模块重构、p.1 框架基线。渲染线现状：p.2.27.1 实例化基座已落地（渲染线首项），p.2.27 自研渲染管线为其后继（ROAD 行序紧随）。/ p.2.27.1 and earlier milestones all landed (unchanged): p.2.27.1 the Flywheel instancing-base guard (above, first render-line item), p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration (six-category fold-in + export single-core fold-in), p.2.18 export hub (engine.export seven forms), p.2.17 rule system (engine.config.rules + `/subterra rule`), p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim, p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack (td direct loading + the `/subterra export` pattern), p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Render line: the p.2.27.1 instancing base landed (first render-line item), followed by p.2.27 self-developed rendering (next ROAD row).
- **probeAcceptance 现状（p.2.27.1 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**90 个纯 JVM 探针**（含新增 `FlywheelInstancingProbe` 34 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。p.2.27 验收基线 = **probeAcceptance 全绿（90 纯 JVM + 5 次 E2E，新增 FlywheelInstancingProbe）+ 本里程碑新增的渲染确定性/接线探针**。/ probeAcceptance (p.2.27.1 closing): green — **90 pure-JVM probes** (incl. the new FlywheelInstancingProbe 34 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe. The p.2.27 acceptance baseline = **probeAcceptance all green (90 pure-JVM + 5 E2E boots, incl. FlywheelInstancingProbe) + the new render determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.27 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.27.1 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；**p.2.27 的 MC 渲染接线真实注入点已由 p.2.27.1.2 文档化（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher），p.2.27 承接**；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.27): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface; td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.27.1; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); **the real MC render-wiring injection points of p.2.27 were documented by p.2.27.1.2 (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher), taken over by p.2.27**; new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.27 承接 p.2.27.1（实例化基座前哨——engine.render.instancing 纯 JDK 确定性核心 + runtime.render 门控壳 + 三 MC 钩子接线点；本里程碑把实例化基座接进真实渲染管线）。**许可衔接**：p.2.27.1 为 Flywheel MIT 直接包含（形态 A）；p.2.27 自研渲染为 **clean-room**——参考 Sodium（LGPL-3.0）/ Embeddium（LGPL-3.0）等登记项时仅 clean-room 参考其思想/边界，copyleft 不直接包含任何源码/资产，存疑按不允许处理（完整登记册见框架设计 §7）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.27 continues from p.2.27.1 (the instancing-base guard — engine.render.instancing pure-JDK deterministic core + the runtime.render gated shell + the three MC hook wiring points; this milestone wires the instancing base into the real render pipeline). **License handover**: p.2.27.1 was a Flywheel MIT direct-include (form A); p.2.27 self-developed rendering is **clean-room** — when referencing the registered items Sodium (LGPL-3.0) / Embeddium (LGPL-3.0) etc., only their ideas/boundaries may inform the design, no copyleft source or asset is ever included, uncertain defaults to not allowed (full register in framework design §7). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.27.1–p.2.20 closing patterns. p.2.27.1 “Flywheel port / 飞轮移植前哨” fully landed and pushed (latest track **p.2.27.1.3**, key path **engine.render.instancing + runtime.render + FlywheelInstancingProbe**): the instancing-base guard — engine.render.instancing pure-JDK deterministic core (InstanceFormat / ShaderTemplate / RenderBackend+BackendRegistry, MIT direct-include with notices preserved under third-party/flywheel-1.0.6, ShaderTemplate flagged clean-room) + the runtime.render RenderRuntime gated shell (subterra.probe.render, [Subterra render] markers merged into the AsyncE2EProbe boot) + FlywheelInstancingProbe 34 checks wired into probeAcceptance; conclusion: **the Flywheel 1.0.6 instancing-base guard landed**; probeAcceptance all green (**90 pure-JVM + five E2E boots**, incl. FlywheelInstancingProbe). p.2.27 defines the clean-room self-developed render pipeline — per the ROAD row verbatim: **vanilla-fidelity block/entity rendering, complementary to the p.2.27.1 instancing base** (clean-room, referencing the Sodium/Embeddium etc. registered items; Sodium is LGPL-3.0 — copyleft boundary reference only, never direct-included; uncertain defaults to not allowed). The three MC hooks documented by p.2.27.1.2 (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher) are the real injection points this milestone takes over. Everything pinned by p.2.12–p.2.27.1 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. Acceptance baseline: **probeAcceptance all green (90 pure-JVM + 5 E2E boots, incl. FlywheelInstancingProbe) + the new render determinism/wiring probes**. Related: p.2.21 Animation is the next ROAD row after p.2.27. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.27 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.27 =「Self-developed rendering / 自研渲染管线」（clean-room，自研，非移植）；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：p.2.27.1 实例化基座（engine.render.instancing 的 InstanceFormat / ShaderTemplate / RenderBackend+BackendRegistry 消费面）与三 MC 钩子接线点现状（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 在 p.2.27.1.2 的文档化契约）；原版保真块/实体渲染的渲染面（渲染阶段/缓冲/材质面，以原版保真为判据）；probeAcceptance 既有接线（90 纯 JVM + 5 E2E）），再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.27 行原文）：「Self-developed rendering / 自研渲染管线（自原 p.4.2 顺移；2026-09-09 前移，clean-room，参考 Sodium/Embeddium 等登记项）：原版保真块/实体渲染，与 p.2.27.1 实例化基座互补」。示意子项（示例，可合并）：
1. **原版保真渲染核心（engine.render，clean-room）**：块/实体渲染的确定性核心模型（渲染面/缓冲分类/材质面固定序、原版保真对照），纯 JDK、零 MC 依赖；参考 Sodium/Embeddium 等登记项时仅 clean-room 参考思想/边界，copyleft 不直接包含。
2. **runtime.render 三 MC 钩子真实注入（p.2.27.1.2 接线点承接）**：LevelRenderer（区块级渲染面）/ ModelBlockRenderer（方块模型实例数据面，字段布局/偏移由 InstanceFormat 决定）/ EntityRenderDispatcher（按实体选择后端，确定性缺省经 BackendRegistry#defaultFor）；门控 -D/-P 范式，缺省 no-op，marker 并入既有 E2E 或独立确定性断言。
3. **渲染确定性/接线探针**：原版保真对照与实例化基座消费（格式/模板/后端注册表与真实注入的一致性）、三 MC 钩子接线契约探针接入 probeAcceptance（90 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**原版保真块/实体渲染 + 与 p.2.27.1 实例化基座互补 + 三 MC 钩子接线承接** 是 ROAD 原文的**交付面**，贯穿每次设计；**clean-room 许可纪律（Sodium（LGPL）/Embeddium 等登记项仅参考，copyleft 不直接包含，存疑不允许）** 是许可红线；p.2.12–p.2.27.1 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.27.1: p.2.27 is a clean-room self-developed milestone (not a port); the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the p.2.27.1 instancing base (the consumption surface of engine.render.instancing — InstanceFormat / ShaderTemplate / RenderBackend+BackendRegistry) and the three MC hook wiring points (the LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher contract documented by p.2.27.1.2); the vanilla-fidelity block/entity render surfaces (render stages/buffers/material surfaces, judged by vanilla fidelity); the existing probeAcceptance wiring (90 pure-JVM + 5 E2E)), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.27 row verbatim: “Self-developed rendering / 自研渲染管线 (carried over from the original p.4.2; advanced on 2026-09-09, clean-room, referencing the Sodium/Embeddium etc. registered items): vanilla-fidelity block/entity rendering, complementary to the p.2.27.1 instancing base”. Illustrative directions (may merge): (1) **the vanilla-fidelity render core (engine.render, clean-room)** — a deterministic core model of block/entity rendering (render-surface/buffer/material classification in fixed order, vanilla-fidelity comparison), pure JDK, zero MC dependency; referencing the Sodium/Embeddium etc. registered items only clean-room for ideas/boundaries, never direct-including copyleft; (2) **the runtime.render three MC hook real injection (taking over the p.2.27.1.2 wiring points)** — LevelRenderer (chunk-level render surface) / ModelBlockRenderer (block-model instance data surface, field layout/offsets from InstanceFormat) / EntityRenderDispatcher (per-entity backend selection, deterministic default via BackendRegistry#defaultFor); gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts; (3) **the render determinism/wiring probes** — vanilla-fidelity comparison + instancing-base consumption (format/template/backend-registry vs real injection consistency) + the three-MC-hook wiring contract probes wired into probeAcceptance (added on top of the 90 pure-JVM + 5 E2E). Note: **vanilla-fidelity block/entity rendering + complementarity with the p.2.27.1 instancing base + the three MC hook wiring takeover** is the ROAD delivery surface and runs through every design; **the clean-room license discipline (Sodium (LGPL)/Embeddium etc. registered items are references only, copyleft never direct-included, uncertain not allowed)** is the license red line; the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.12–p.2.27.1 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.27 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.27 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.27 子项一律 **p.2.27.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；渲染核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.27 红线）**：p.2.27 为**自研渲染管线（clean-room）**——参考 Sodium（LGPL-3.0）/ Embeddium（LGPL-3.0）等登记项时仅 clean-room 参考思想/边界（算法、数据结构、公共接口形态），**不直接包含任何 copyleft 源码或资产**；copyleft 仅 clean-room；存疑按不允许处理；完整登记册见框架设计 §7。p.2.27 不新增任何直接移植库（p.2.27.1 的 Flywheel MIT 直接包含材料保持原样不动）。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（渲染若涉 native/FFM 按需）。
- **验收门（p.2.27 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**90 纯 JVM + 五次 E2E（新增 FlywheelInstancingProbe 在内）**，**并新增本里程碑的渲染确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.27.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the render core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.27 red line)**: p.2.27 is a **clean-room self-developed render pipeline** — when referencing the registered items Sodium (LGPL-3.0) / Embeddium (LGPL-3.0) etc., only their ideas/boundaries inform the design (algorithms, data structures, public interface shape); **no copyleft source or asset is ever direct-included**; copyleft is clean-room reference only; uncertain defaults to not allowed; the full register is in framework design §7. p.2.27 adds no direct-included library (the p.2.27.1 Flywheel MIT direct-include materials stay untouched). Acceptance gate (the p.2.27 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **90 pure-JVM + five E2E boots (incl. FlywheelInstancingProbe)**, **plus the new render determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.27 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.27 各子项落地、最新 p 轨号 p.2.27.x、关键路径、自研渲染管线结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.21 Animation / 动画 engine.anim——GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding；若届时 ROAD 行序有变以 ROAD 为准）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.21-animation.md`（bilingual），随当次提交一并提交。

**English**: After p.2.27 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.27 landing, latest track p.2.27.x, key path, the self-developed-rendering conclusion; scope pointing to the next milestone, per ROAD row order **p.2.21 Animation / 动画 engine.anim** — the GeckoLib core port (MIT notice + bundled-library notice) + runtime anim binding; if the ROAD order changes by then, follow ROAD; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.21-animation.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.27 工作区建议：自研渲染管线（clean-room）——engine.render 原版保真渲染核心 + runtime.render 三 MC 钩子真实注入（承接 p.2.27.1.2 接线点）+ 渲染确定性/接线探针。
- **p.2.27.1 Flywheel 实例化基座前哨（已落地，p.2.27 的实例化基座基础）**：engine.render.instancing 纯 JDK（InstanceFormat 实例布局确定性模型 / ShaderTemplate clean-room 固定序模板 / RenderBackend+BackendRegistry 后端 SPI 固定注册序同名拒确定性缺省选择）+ 许可材料（META-INF/third-party/flywheel-1.0.6：MIT 原文 + 双语 NOTICE，ShaderTemplate 注明 clean-room）+ runtime.render RenderRuntime 门控壳（subterra.probe.render）+ FlywheelInstancingProbe 34 checks；probeAcceptance 全绿（**90 纯 JVM + 五次 E2E**）。
- **三 MC 钩子接线点（p.2.27.1.2 文档化，p.2.27 承接）**：LevelRenderer（区块级实例化渲染编译/装载面）、ModelBlockRenderer（方块模型实例数据面，字段布局/偏移由 InstanceFormat 决定）、EntityRenderDispatcher（按实体选择实例化后端，确定性缺省经 BackendRegistry#defaultFor）；各钩子以 p.2.27.1 门控壳的确定性装载/格式校验为前置。
- **许可登记（p.2.27 为 clean-room）**：ROAD p.2.27 行原文「clean-room，参考 Sodium/Embeddium 等登记项」——Sodium / Embeddium 均为 LGPL-3.0（copyleft）→ 仅 clean-room 参考思想/边界，不直接包含；存疑按不允许处理；完整登记册见框架设计 §7；p.2.27.1 的 Flywheel MIT 直接包含材料保持原样。
- **probeAcceptance 接线基准（p.2.27.1 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **90 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.27 在此之上新增渲染确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.27.1 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.render 真实注入沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.27.1 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.27.1 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.27 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.27.1 Flywheel 实例化基座前哨（已落地，实例化基座基础）/ p.2.21 Animation（ROAD 行序紧随 p.2.27，下一里程碑）；p.2.27 把实例化基座接进真实渲染管线并落渲染确定性/接线探针。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.27 landing: the clean-room self-developed render pipeline — the engine.render vanilla-fidelity render core + the runtime.render three MC hook real injection (taking over the p.2.27.1.2 wiring points) + the render determinism/wiring probes. p.2.27.1 the Flywheel instancing-base guard (landed; the instancing base for p.2.27): engine.render.instancing pure JDK (InstanceFormat deterministic instance-layout model / ShaderTemplate clean-room fixed-order template / RenderBackend+BackendRegistry backend SPI with fixed registration order, duplicate-name rejection, deterministic default selection) + license materials (META-INF/third-party/flywheel-1.0.6: verbatim MIT + bilingual NOTICE, ShaderTemplate flagged clean-room) + the runtime.render RenderRuntime gated shell (subterra.probe.render) + FlywheelInstancingProbe 34 checks; probeAcceptance all green (**90 pure-JVM + five E2E boots**). The three MC hook wiring points (documented by p.2.27.1.2, taken over by p.2.27): LevelRenderer (chunk-level instanced-render compile/load surface), ModelBlockRenderer (block-model instance data surface, field layout/offsets from InstanceFormat), EntityRenderDispatcher (per-entity instancing backend selection, deterministic default via BackendRegistry#defaultFor); each hook builds on the p.2.27.1 gated shell's deterministic load/format verification. License (p.2.27 is clean-room): ROAD p.2.27 row verbatim — “clean-room, referencing the Sodium/Embeddium etc. registered items” — Sodium / Embeddium are both LGPL-3.0 (copyleft) → clean-room reference of ideas/boundaries only, never direct-included; uncertain defaults to not allowed; full register in framework design §7; the p.2.27.1 Flywheel MIT direct-include materials stay untouched. probeAcceptance wiring baseline (p.2.27.1 closing): **90 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.27 adds the render determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.27.1 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.render real injection reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.27.1): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.27.1 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.27 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.27.1 the Flywheel instancing-base guard (landed; the instancing base) / p.2.21 Animation (next ROAD row after p.2.27); p.2.27 wires the instancing base into the real render pipeline and lands the render determinism/wiring probes.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 90 纯 JVM + 五次 E2E 与新增渲染探针）+ ROAD p.2.27 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 90 pure-JVM + five E2E boots plus the new render probes), the “ROAD p.2.27 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.
