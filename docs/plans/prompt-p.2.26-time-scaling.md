# Subterra p.2.26 Time Scaling / engine.time · 全轨一次性交接单 · 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.25 全轨 landing 后，按 p.2.25 交接单 §5 生成指向 p.2.26 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.25 fully landed, §5 of the p.2.25 handoff produces this p.2.26 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.26 里程碑**的交接单：p.2.26 =「Time scaling / 时间缩放 engine.time」（自原 p.3.3 前移入主线）——**clean-room 自研**（TimeScaleLib 因 PolyForm Shield 非标准变体（Noncompete）已评估剔除，自研替代）：**TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线**（见 ROAD p.2.26 行）。**本里程碑为 clean-room 自研形态**（形态参考 p.2.24 AI clean-room 纪律 / p.2.27 自研渲染纪律）：不引入任何第三方源码或按 Copy Dead 保留的库；capability 面以纯 JDK 确定性核心落 engine.*，并经 runtime 门控壳接线到 MC 逻辑 tick 生命周期，以确定性探针验收。执行模式与 p.2.25 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.26.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（盘点 engine 各纯 JDK 核心目录现状、runtime 门控壳接线现状、probeAcceptance 既有接线、p.2.8 engine.sim BudgetScheduler 语义与 TickBudget 预算能力面），并产出最小闭环拆分 → 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.26-milestone handoff: p.2.26 = “Time scaling / 时间缩放 engine.time” (moved up into the main line from the original p.3.3) — **clean-room self-developed** (TimeScaleLib was assessed and excluded for its PolyForm Shield non-standard variant (Noncompete); a self-developed replacement is used): **the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring** (per the ROAD p.2.26 row). **This milestone is a clean-room self-development shape** (form referencing the p.2.24 AI clean-room discipline / the p.2.27 self-render discipline): no third-party source or copy-dead-on-retained library is introduced; the capability lands as a pure-JDK deterministic core in engine.*, wired into the MC logic-tick lifecycle via a gated runtime shell, and accepted by deterministic probes. Execution mode identical to the p.2.25 closing run: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.26.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (inventory the current engine pure-JDK core directories, the runtime gated-shell wiring state, the existing probeAcceptance wiring, and the p.2.8 engine.sim BudgetScheduler / TickBudget budget capability surface), produce the minimal-closure split, then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.25 里程碑「Entity scale / engine.scale」已全部落地并已推 GitHub，最新 p 轨号 p.2.25.4（收尾提交）**：实体缩放核心 MIT 移植落地——**p.2.25.1** engine.scale 实体缩放核心（Pehkui-Rebuilt 3.8.5 MIT 移植，纯 JDK）——固定 9 维 ScaleType 序 / ScaleData / ScaleModifier / ScaledEntityData 按标签缩放 + 数据包缩放规则 td 化（ScaleRuleDocument），MIT 声明保留 Virtuoel 版权与重建声明（META-INF/third-party/pehkui-rebuilt-3.8.5：MIT 原文 LICENSE + 双语 NOTICE 注明来源/版本/适配范围，未捆绑源码）+ **p.2.25.2** runtime.scale ScaleRuntime 门控壳（subterra.probe.scale 缺省 no-op、[Subterra scale] ok marker 并入 AsyncE2EProbe 同开服）+ **p.2.25.3** ScaleProbe 23 checks（ScaleType 固定序 / ScaleData/ScaleModifier 逐位精确、rule-doc td 往返 + 覆盖、tag 合并、接线契约、pehkui-rebuilt 许可盘点）接入 probeAcceptance。关键路径 **engine.scale + runtime.scale + ScaleProbe**。结论：**实体缩放核心落地——engine.scale 确定性实体缩放模型（ScaleType 9 维固定序 / ScaleData / ScaleModifier / ScaledEntityData 按标签缩放 + td 化规则）+ runtime.scale 门控壳 + ScaleProbe 23 checks**；probeAcceptance 全绿（96 纯 JVM + 五次 E2E，新增 ScaleProbe）。/ p.2.25 “Entity scale / engine.scale” fully landed and pushed, latest track **p.2.25.4** (closing commit): the entity-scaling core MIT port landed — p.2.25.1 the engine.scale entity-scaling core (Pehkui-Rebuilt 3.8.5 MIT port, pure JDK) — fixed 9-dim ScaleType order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized datapack scale rules (ScaleRuleDocument), MIT notices preserving Virtuoel's copyright + the rebuild declaration (META-INF/third-party/pehkui-rebuilt-3.8.5: verbatim MIT LICENSE + bilingual NOTICE citing source/version/adaptation scope, no source bundled) + p.2.25.2 runtime.scale ScaleRuntime gated shell (subterra.probe.scale default no-op, the [Subterra scale] ok marker merged into the AsyncE2EProbe boot) + p.2.25.3 ScaleProbe 23 checks (ScaleType fixed order / ScaleData/ScaleModifier bit-exact, rule-doc td round-trip + override, tag merge, wiring contract, pehkui-rebuilt license inventory) wired into probeAcceptance. Key path **engine.scale + runtime.scale + ScaleProbe**. Conclusion: **the entity-scaling core landed — the engine.scale deterministic entity-scaling model (ScaleType 9-dim fixed order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized rules) + the runtime.scale gated shell + ScaleProbe 23 checks**; probeAcceptance all green (96 pure-JVM + five E2E boots, incl. ScaleProbe).
- **p.2.26 目标（ROAD p.2.26 行原文，本次承接）**：「Time scaling / 时间缩放 engine.time（自原 p.3.3 前移入主线，clean-room 自研）：TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线（接 engine.sim p.2.8）」。/ p.2.26 goal (the ROAD row verbatim, this handoff): “Time scaling / 时间缩放 engine.time (moved up into the main line from the original p.3.3, clean-room self-developed): the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring (over the p.2.8 engine.sim)”.
- **p.2.25 及更早里程碑（已落地，事实不变）**：p.2.25 实体缩放线（上述，最新 p 轨号 p.2.25.4）、p.2.24 AI behavior core（engine.ai + runtime.ai + AiProbe）、p.2.23 In-game mod hub、p.2.22 UI/HUD 核心、p.2.21 动画核心 MIT 移植、p.2.27 自研渲染管线、p.2.27.1 Flywheel 实例化基座前哨、p.2.20 devkit 完整化 + 官方示例模组脚手架、p.2.19 runtime 接线迁移、p.2.18 导出枢纽、p.2.17 规则系统、p.2.16 api 契约面、p.2.15 engine migration + golden tests、p.2.14 engine.interact、p.2.13 engine.pcg、p.2.12 engine.schema/scaffold、p.2.11 session、p.2.10 saveverify、p.2.9 world-as-artifact、p.2.8 sim（engine.sim 增量确定性模拟底座 + **BudgetScheduler/TickBudget 预算分级调度**）、p.2.7 parallel、p.2.6 C2ME、p.2.5 P2P 栈、p.2.4 server-interaction、p.2.3 Save、p.2.2 datapack、p.2.1 tie bridge、p.2.0 模块重构、p.1 框架基线。时间缩放线现状：**p.2.8 engine.sim 已落 BudgetScheduler/TickBudget 预算分级调度**（per/region/global 三级 cap、原子记账、确定性 key 序降级——p.2.26 tick 预算确定性调度的语义承接点）；**TimeScaleLib 已评估并剔除**（PolyForm Shield 非标准变体 Noncompete，不可移植，见 ROAD Progress Log），p.2.26 为 clean-room 自研替代。/ p.2.25 and earlier milestones all landed (unchanged): p.2.25 the entity-scaling line (above, latest track p.2.25.4), p.2.24 the AI behavior core (engine.ai + runtime.ai + AiProbe), p.2.23 the in-game mod hub, p.2.22 the UI/HUD core, p.2.21 the animation core MIT port, p.2.27 the self-developed render pipeline, p.2.27.1 the Flywheel instancing-base guard, p.2.20 devkit complete + official sample scaffold, p.2.19 runtime wiring migration, p.2.18 export hub, p.2.17 the rule system, p.2.16 api contract surface, p.2.15 engine migration + golden tests, p.2.14 engine.interact, p.2.13 engine.pcg, p.2.12 engine.schema/scaffold, p.2.11 session, p.2.10 saveverify, p.2.9 world-as-artifact, p.2.8 sim (the engine.sim incremental deterministic simulation base + **BudgetScheduler/TickBudget budget-tier scheduling**), p.2.7 parallel, p.2.6 C2ME, p.2.5 P2P stack, p.2.4 server-interaction, p.2.3 Save, p.2.2 datapack, p.2.1 tie bridge (FFM), p.2.0 module re-layout, the p.1 framework baseline. Time-scaling line: **p.2.8 engine.sim already landed the BudgetScheduler/TickBudget budget-tier scheduling** (per-simulant/region/global caps, atomic accounting, deterministic key-order degradation — the semantic anchor for the p.2.26 tick-budget determinism); **TimeScaleLib was assessed and excluded** (PolyForm Shield non-standard Noncompete variant, not portable, see the ROAD Progress Log); p.2.26 is the clean-room self-developed replacement.
- **probeAcceptance 现状（p.2.25 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**96 个纯 JVM 探针**（含新增 `ScaleProbe` 23 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖；p.2.26 的 engine.time 纯 JDK 落点自动纳入。p.2.26 验收基线 = **probeAcceptance 全绿（96 纯 JVM + 5 次 E2E，新增 ScaleProbe）+ 本里程碑新增的时间缩放确定性/接线探针**。/ probeAcceptance (p.2.25 closing): green — **96 pure-JVM probes** (incl. the new ScaleProbe 23 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain). The iron law (api←engine←runtime/migrate) stays covered by ironLawProbe; the p.2.26 pure-JDK engine.time landing is auto-covered. The p.2.26 acceptance baseline = **probeAcceptance all green (96 pure-JVM + 5 E2E boots, incl. ScaleProbe) + the new time-scaling determinism/wiring probes of this milestone**.
- **ABI / 接线事实（已钉死，p.2.26 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet 与 runtime.*；api 模块 `subterra-api\...\api\` 为对外契约宿主（**TimeScaleApi 落此**）；td（tiedb-td 文档）与 zd（通用载体）为本框架数据一等语言；确定性生成范式（逐字节一致、固定序、禁时序）由 p.2.7/p.2.8 证明、p.2.12–p.2.25 钉死并复用；**runtime 接线一律沿用门控 -D/-P 钩子范式**（默认 no-op、marker 并入既有 E2E、staging 用 build/tmp）；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链）。/ ABI / wiring facts (pinned, reused by p.2.26): engine pure JDK lands in engine.*, MC shells in the root sourceSet and runtime.*; the api module `subterra-api\...\api\` hosts the external contract surface (**TimeScaleApi lands here**); td (tiedb-td docs) and zd (generic carrier) are the framework’s first-class data languages; the deterministic paradigm (byte-identical, fixed order, no timing) was proven by p.2.7/p.2.8 and pinned/reused by p.2.12–p.2.25; runtime wiring always follows the gated -D/-P hook pattern (default no-op, markers merged into existing E2E, staging via build/tmp); new probes land in `subterra-devkit\src\main\java\io\toterra\subterra\probes\`, wired into probeAcceptance in `subterra-devkit\build.gradle` (the existing mustRunAfter E2E chain).
- **关联里程碑**：p.2.26 承接 p.2.25（实体缩放核心落地为既有验收门，时间缩放确定性/接线探针叠加其上）；p.2.26 直接复用 **p.2.8 engine.sim 的 BudgetScheduler/TickBudget 预算分级调度语义**（tick 预算确定性调度承接 p.2.8），并接 engine.sim p.2.8 的 runtime 接线；逻辑级节流的确定性范式与 p.2.24 BehaviorPriority/BrainScheduler 的预算跳过、TickBudget 三 cap 同源。**许可衔接（p.2.26 红线：clean-room 自研）**：本品不引用任何第三方代码；**TimeScaleLib 已评估剔除**（PolyForm Shield 非标准变体 Noncompete——含 Noncompete 条款的非标准 PolyForm Shield 变体，判定不可移植，自研替代，见 ROAD Progress Log）；copyleft 仅 clean-room 参考思想，**不按原样复制代码**；存疑按不允许处理（完整登记册见框架设计 §7）。（上下文段其余事实以仓库现状为准：读出 ROAD、git log、devkit build.gradle、runtime 各壳、engine 各核心目录后再定，不臆造。）
  Related tracks: p.2.26 continues from p.2.25 (the entity-scaling core landing is the existing gate; the time-scaling determinism/wiring probes stack on it); p.2.26 reuses the **p.2.8 engine.sim BudgetScheduler/TickBudget budget-tier scheduling semantics** (the tick-budget determinism carries over p.2.8) and wires into the p.2.8 engine.sim runtime; the logic-level-throttling determinism shares its origin with the p.2.24 BehaviorPriority/BrainScheduler budget-skip and TickBudget three-caps. **License handover (the p.2.26 red line: clean-room self-developed)**: no third-party code is referenced; **TimeScaleLib was assessed and excluded** (a non-standard PolyForm Shield variant containing a Noncompete clause — judged non-portable, self-developed replacement, see the ROAD Progress Log); copyleft is clean-room reference of ideas only, **no code copied verbatim**; uncertain defaults to not allowed (full register in framework design §7). Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

**English**: See the Chinese block above — this handoff follows the exact p.2.25 closing patterns. p.2.25 “Entity scale / engine.scale” fully landed and pushed (latest track **p.2.25.4**, key path **engine.scale + runtime.scale + ScaleProbe**): the entity-scaling core MIT port landed — p.2.25.1 the engine.scale core (Pehkui-Rebuilt 3.8.5 MIT port, fixed 9-dim ScaleType order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized datapack rules ScaleRuleDocument, MIT notices preserving Virtuoel's copyright + the rebuild declaration) + p.2.25.2 the runtime.scale ScaleRuntime gated shell (subterra.probe.scale default no-op, the [Subterra scale] ok marker in the AsyncE2EProbe boot) + ScaleProbe 23 checks (p.2.25.3, wired into probeAcceptance on top of the 95 pure-JVM + 5 E2E); conclusion: **the entity-scaling core landed**; probeAcceptance all green (**96 pure-JVM + five E2E boots**, incl. ScaleProbe). p.2.26 defines the Time scaling milestone — per the ROAD row verbatim: **Time scaling / 时间缩放 engine.time (moved up into the main line from the original p.3.3, clean-room self-developed): the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring (over the p.2.8 engine.sim)**. Everything pinned by p.2.12–p.2.25 (td-first, byte-identical deterministic, no-timing, gated -D/-P runtime hooks) stays the default semantics. **License red line: p.2.26 is clean-room self-developed — TimeScaleLib was assessed and excluded (PolyForm Shield non-standard Noncompete variant), the capability is self-developed clean-room; copyleft is reference-of-ideas only, no code copied verbatim.** Acceptance baseline: **probeAcceptance all green (96 pure-JVM + 5 E2E boots, incl. ScaleProbe) + the new time-scaling determinism/wiring probes**. Confirm remaining context facts against the repo state (ROAD, git log, the devkit build.gradle, the runtime shells, the engine cores) — never fabricate.

## 2. 本次范围（p.2.26 全轨，子项以摸底后最小闭环为准）· Scope (sub-item split after scouting minimal closure)

**中文**：总原则：p.2.26 =「Time scaling / engine.time」——**clean-room 自研**（TimeScaleLib 已剔除，自研替代）：**TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线**；子项拆分以**摸底后最小闭环**为准；每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（盘点：p.2.25 实体缩放线落点现状（engine.scale + runtime.scale + ScaleProbe）；既有确定性能力面（p.2.8 engine.sim BudgetScheduler/TickBudget 预算分级调度语义、p.2.24 BrainScheduler 预算跳过与 TickBudget 三 cap、p.2.13 XoroRandom/种子语义）；runtime 门控壳接线范式（subterra.probe.* 缺省 no-op + marker 并入既有 E2E）；probeAcceptance 既有接线（96 纯 JVM + 5 E2E）与 FFM/依赖面）,再定子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（目标见 ROAD p.2.26 行原文）：「Time scaling / 时间缩放 engine.time（自原 p.3.3 前移入主线，clean-room 自研）：TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线（接 engine.sim p.2.8）」。示意子项（示例，可合并）：
1. **engine.time 时间缩放核心（纯 JDK，clean-room 自研）**：TimeDomain 流速比例模型（全局/实体/区域/玩家四域）、流速比例确定性求值（固定域序、逐字节一致、禁时序）、逻辑级节流（tick 级 skip/降频，确定性 key 序）与感知级插值（同输入同输出、位模式一致）；TimeScaleApi（落 api 模块，对外契约）。
2. **runtime.time 接线（runtime time binding）**：tick 预算确定性调度（BudgetScheduler——语义承接 p.2.8）接入 MC 逻辑 tick 生命周期，门控 -D/-P 范式，缺省 no-op，marker 并入既有 E2E 或独立确定性断言；接 engine.sim p.2.8。
3. **时间缩放确定性/接线探针**：核心确定性（流速比例固定域序逐字节一致、插值往返恒等、预算调度确定性）+ 接线契约（runtime 面与既有 E2E markers 稳定性）探针接入 probeAcceptance（96 纯 JVM + 5 E2E 之上新增）。
提示：以上为示意；**clean-room 自研**是 ROAD 原文的**交付红线**，贯穿每次设计；**许可红线**：p.2.26 **clean-room 自研，不引用任何第三方源码；TimeScaleLib 已评估剔除（PolyForm Shield Noncompete 非标准变体，自研替代）；copyleft 仅参考思想，禁止原样复制**；若摸底发现需要参考其他第三方，先按既有纪律走（permissive 可移植保留声明、copyleft 仅 clean-room、存疑按不允许）；p.2.7–p.2.25 已钉死的「逐字节一致 / 固定序 / 禁时序」确定性范式必须被复用为默认语义。

**English**: Same principle as p.2.12–p.2.25: p.2.26 is the Time scaling milestone — **clean-room self-developed** (TimeScaleLib already excluded, a self-developed replacement): **the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring**; the sub-item split follows the post-scouting minimal closure; each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (inventory: the p.2.25 entity-scaling landing state (engine.scale + runtime.scale + ScaleProbe); the existing deterministic capability surfaces (the p.2.8 engine.sim BudgetScheduler/TickBudget budget-tier semantics, the p.2.24 BrainScheduler budget-skip + TickBudget three caps, the p.2.13 XoroRandom/seed semantics); the runtime gated-shell pattern (subterra.probe.* default no-op + markers merged into existing E2E); the existing probeAcceptance wiring (96 pure-JVM + 5 E2E) and the FFM/dependency surface), then produce the sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.26 row verbatim: “Time scaling / 时间缩放 engine.time (moved up into the main line from the original p.3.3, clean-room self-developed): the TimeDomain flow-rate ratio model (global/entity/zone/player domains), deterministic tick-budget scheduling (BudgetScheduler), logic-level throttling + perception-level interpolation, TimeScaleApi + runtime wiring (over the p.2.8 engine.sim)”. Illustrative directions (may merge): (1) **the engine.time time-scaling core (pure JDK, clean-room self-developed)** — the TimeDomain flow-rate ratio model (global/entity/zone/player four domains), deterministic ratio evaluation (fixed domain order, byte-identical, no timing), logic-level throttling (tick-level skip/downsampling, deterministic key order) + perception-level interpolation (same input → same output, bit-pattern identical); TimeScaleApi (lands in the api module, the external contract); (2) **runtime.time wiring (the runtime time binding)** — the tick-budget deterministic scheduling drivers (BudgetScheduler — semantics carrying over p.2.8) wired into the MC logic-tick lifecycle, gated -D/-P pattern, default no-op, markers merged into existing E2E or standalone deterministic asserts; over the p.2.8 engine.sim; (3) **the time-scaling determinism/wiring probes** — core determinism (fixed-domain-order byte-identical flow-rate, interpolation round-trip identity, budget-scheduling determinism) + wiring contract (runtime surface and existing E2E marker stability) probes wired into probeAcceptance (added on top of the 96 pure-JVM + 5 E2E). Note: **clean-room self-developed** is the ROAD **delivery red line** and runs through every design; **the license red line**: p.2.26 **references no third-party source; TimeScaleLib was assessed and excluded (PolyForm Shield Noncompete non-standard variant, self-developed replacement); copyleft is reference-of-ideas only, verbatim copying is forbidden**; if scouting finds another reference, follow the pinned discipline first (permissive portable with notices, copyleft clean-room only, uncertain defaults to not allowed); the pinned “byte-identical / fixed-order / no-timing” deterministic paradigm of p.2.7–p.2.25 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.26 由子代理集群执行**（摸底（只读 + 基准探针，产出全轨任务拆分）、实现子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送））。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine/契约先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/runtime 接线）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.26 runs as a cluster of sub-agents (scouting, per-wiring, probe, closing; the shape may be slimmed accordingly). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine/contract before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.26 子项一律 **p.2.26.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性求值**（固定序、禁时序；同输入同输出）；时间缩放核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落 runtime.* 与根 sourceSet；migrate 仅依赖 api；api 模块不得依赖 engine。
- **许可纪律（p.2.26 红线）**：p.2.26 **clean-room 自研，不引用任何第三方源码**——**TimeScaleLib 已评估剔除**（PolyForm Shield 非标准变体（Noncompete）→ 不可移植，自研替代）；copyleft 仅参考思想、禁原样复制；permissive 亦不搬运（本品无需任何移植）；存疑按不允许处理；完整登记册见框架设计 §7。p.2.26 不新增任何移植项。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（时间缩放若涉 native/FFM 按需）。
- **验收门（p.2.26 基线）**：`gradlew :subterra-devkit:probeAcceptance` 全绿——**96 纯 JVM + 五次 E2E（新增 ScaleProbe 在内）**，**并新增本里程碑的时间缩放确定性/接线探针** 接入后才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.26.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic evaluation** (fixed order, no timing; same input → same output); the time-scaling core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in runtime.* and the root sourceSet; migrate depends only on api; the api module must not depend on engine. **License discipline (the p.2.26 red line)**: p.2.26 is **clean-room self-developed with no third-party source** — **TimeScaleLib was assessed and excluded** (PolyForm Shield non-standard variant (Noncompete) → not portable, self-developed replacement); copyleft is reference-of-ideas only, no verbatim copying; permissive is likewise not ported here (this item needs no port); uncertain defaults to not allowed; the full register is in framework design §7. p.2.26 adds no port. Acceptance gate (the p.2.26 baseline): `gradlew :subterra-devkit:probeAcceptance` all green — **96 pure-JVM + five E2E boots (incl. ScaleProbe)**, **plus the new time-scaling determinism/wiring probes wired in**, before closing.

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.26 全轨完成后，必须生成下一个里程碑同格式提示词并纳入记录：更新上下文段（p.2.26 各子项落地、最新 p 轨号 p.2.26.x、关键路径、时间缩放自研结论）、更新本次范围段指向下一里程碑（按 ROAD 行序为 p.2.26 之后的下一个里程碑；**若届时 ROAD 行序有变以 ROAD 为准**）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.<next>.md`（bilingual），随当次提交一并提交。

**English**: After p.2.26 fully lands, produce the next-milestone prompt in the same format (context updated with the p.2.26 landing, latest track p.2.26.x, key path, the time-scaling clean-room conclusion; scope pointing to the next milestone after p.2.26 per the then-current ROAD row order; **if the ROAD order changes by then, follow ROAD**; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.<next>.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.26 工作区建议：engine.time 时间缩放核心（clean-room 自研，纯 JDK：TimeDomain 流速比例模型 + BudgetScheduler + 逻辑级节流 + 感知级插值）+ TimeScaleApi（api 模块）+ runtime.time 接线 + 时间缩放确定性/接线探针。
- **p.2.25 实体缩放线（已落地，既有验收门）**：engine.scale 实体缩放核心（Pehkui-Rebuilt 3.8.5 MIT 移植，ScaleType 9 维固定序 / ScaleData / ScaleModifier / ScaledEntityData 按标签缩放 + td 化规则 ScaleRuleDocument）+ runtime.scale ScaleRuntime 门控壳（subterra.probe.scale 缺省 no-op、[Subterra scale] ok marker 并入 AsyncE2EProbe）+ ScaleProbe 23 checks；probeAcceptance 全绿（**96 纯 JVM + 五次 E2E**）。
- **p.2.8 engine.sim 预算分级调度（p.2.26 的 BudgetScheduler 语义承接点）**：BudgetScheduler/TickBudget——per/region/global 三级 cap、原子记账、确定性 key 序降级；p.2.26 的 tick 预算确定性调度复用该语义。
- **TimeScaleLib 许可登记（p.2.26 clean-room 自研红线）**：TimeScaleLib 为 PolyForm Shield 非标准变体（Noncompete）→ **不可移植，已评估剔除**；p.2.26 时间缩放能力 clean-room 自研替代（自原 p.3.3 前移入主线）。
- **probeAcceptance 接线基准（p.2.25 收官）**：`gradlew :subterra-devkit:probeAcceptance` —— **96 纯 JVM + 五次 E2E**（Boot → Datapack → Save → Network → Async，mustRunAfter 链；staging 用 build/tmp）。p.2.26 在此之上新增时间缩放确定性/接线探针。
- **门控 -D/-P 钩子确定性触发范式（p.2.2.7 已证明、p.2.19–p.2.25 沿用）**：gradle `-P` 属性经 runServer system property 转发，startup 即跑同一核心（规避 forked server JVM 的 stdin 双层中转不可靠）；runtime.time 接线沿用（缺省 no-op）。
- **依赖铁律（p.2.0/p.2.15–p.2.25 已确立）**：api ← engine ← runtime/migrate；api 模块不得依赖 engine；runtime 为 MC 壳层、可依赖 engine 与 api；devkit 可依赖全部并承载探针。
- **运行时壳并入既有 boot 范式（p.2.8–p.2.25 已证明）**：默认 no-op（-D/-P 门控）、marker 并入既有 E2E 探针、staging 用 build/tmp、保持五次开服不变（p.2.26 仅按需加确定性 markers）。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.25 实体缩放线（已落地，既有验收门）/ p.2.26 Time scaling（本里程碑，ROAD 行序紧随 p.2.25）；p.2.26 为时间缩放线首项（承接 p.2.8 engine.sim 预算调度语义与 runtime 门控壳范式、p.2.24 BrainScheduler 预算跳过）。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.26 landing: the engine.time time-scaling core (clean-room self-developed, pure JDK: the TimeDomain flow-rate ratio model + BudgetScheduler + logic-level throttling + perception-level interpolation) + TimeScaleApi (in the api module) + runtime.time wiring + the time-scaling determinism/wiring probes. p.2.25 the entity-scaling line (landed; the existing acceptance gate): the engine.scale entity-scaling core (Pehkui-Rebuilt 3.8.5 MIT port, ScaleType 9-dim fixed order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized rules ScaleRuleDocument) + the runtime.scale ScaleRuntime gated shell (subterra.probe.scale default no-op, the [Subterra scale] ok marker merged into AsyncE2EProbe) + ScaleProbe 23 checks; probeAcceptance all green (**96 pure-JVM + five E2E boots**). The p.2.8 engine.sim budget-tier scheduling (the BudgetScheduler semantic anchor for p.2.26): BudgetScheduler/TickBudget — per/region/global caps, atomic accounting, deterministic key-order degradation; p.2.26's tick-budget determinism reuses that. The TimeScaleLib license register (the p.2.26 clean-room red line): TimeScaleLib is a PolyForm Shield non-standard variant (Noncompete) → **not portable, assessed and excluded**; the p.2.26 time-scaling capability is the clean-room self-developed replacement (moved up from the original p.3.3). probeAcceptance wiring baseline (p.2.25 closing): **96 pure-JVM + five E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; staging via build/tmp); p.2.26 adds the time-scaling determinism/wiring probes on top. The gated -D/-P hook pattern (p.2.2.7 proven, p.2.19–p.2.25 reused): a gradle `-P` property forwarded to runServer as a system property runs the same core at startup; the runtime.time wiring reuses it (default no-op). Dependency iron law (p.2.0/p.2.15–p.2.25): api ← engine ← runtime/migrate; the api module must not depend on engine; runtime is the MC shell layer and may depend on engine and api; the devkit may depend on all and hosts the probes. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.25 proven) still applies (gate-off no-op, markers merged into existing E2E probes, staging via build/tmp, five boots unchanged — p.2.26 adds deterministic markers only as needed). New probes under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle. Related tracks: p.2.25 the entity-scaling line (landed; the existing gate) / p.2.26 Time scaling (this milestone, next ROAD row after p.2.25); p.2.26 is the first time-scaling-line item (building on the p.2.8 engine.sim budget-scheduling semantics and the runtime gated-shell pattern, plus the p.2.24 BrainScheduler budget-skip).

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含既有 96 纯 JVM + 五次 E2E 与新增时间缩放探针）+ ROAD p.2.26 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (the existing 96 pure-JVM + five E2E boots plus the new time-scaling probes), the “ROAD p.2.26 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.