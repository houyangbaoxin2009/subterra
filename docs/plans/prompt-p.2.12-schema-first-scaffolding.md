# Subterra p.2.12 Schema-first scaffolding 模式先行脚手架 · 全轨一次性交接单（摸底定子项）· 开发任务提示词

> 本文件由契约保持的双语交接单延续机制自动生成：p.2.11 全轨 landing 后，按 p.2.11 交接单 §5 生成指向 p.2.12 的同结构提示词。
> This file is the bilingual handoff prompt auto-generated by the continued handoff mechanism: after p.2.11 fully landed, §5 of the p.2.11 handoff produces this p.2.12 prompt with the same 0–7 section structure.

## 0. 角色与目标

**中文**：你是 Subterra（NeoForge 模组开发框架，服务 Toterra 主模组）的开发代理。本提示词是一个**单会话一次性做完整个 p.2.12 里程碑**的交接单：p.2.12 =「Schema-first scaffolding 模式先行脚手架」——**以 td schema 为一等输入，自动生成注册码 / 探针 / 文档 / 表单**：模组编写从「写代码」变成「写数据」，开发者声明一个 td schema，脚手架据此生成注册表接线代码、确定性探针、开发文档与（游戏内 / 工具）表单。核心原则：**td schema 为单一事实源 + 确定性生成（同 schema 同输出）+ 生成物可探针（脚手架自身含确定性 self-test）+ 生成产物接入既有注册机制**；engine 纯 JDK 不碰 MC、schema 模型与生成核心可独立探针。执行模式与 p.2.5 / p.2.7 / p.2.8 / p.2.9 / p.2.10 / p.2.11 收官完全一致：主代理只做任务下发与 trust-but-verify；摸底/设计、实现、探针、验收、文档、推送全部由子代理执行；每个子代理领取一个自足小任务、完成即提交一次；全部完成后由收尾子代理统一 review+清理+推送（GitHub，仅 origin）。编号全程用 **p.2.12.x**（≤三级），禁止「阶段X / 第N块 / block N」等表述。工作方式：先**摸底**（确认 td 的 schema 能力现状、引擎既有注册机制/生成范式、devkit 探针资产、form/文档生成该接的既有形态）并产出全轨子项拆分（5–8 个）→ 逐子项实现 → 每个小任务完成即提交 → 全部子项完成后统一 review+清理+推送。

**English**: You are the development agent of Subterra (a NeoForge mod dev framework serving the Toterra main mod). This prompt is a single-session, run-the-whole-p.2.12-milestone handoff: p.2.12 = “Schema-first scaffolding” — **take a td schema as the first-class input and auto-generate registration code / probes / docs / forms**: mod authoring shifts from “writing code” to “writing data”, where a developer declares one td schema and the scaffolder produces the registry-wiring code, deterministic probes, dev docs and (in-game/tool) forms from it. Core principles: **td schema as the single source of truth + deterministic generation (same schema → same output) + generated artifacts probeable (the scaffold itself carries a deterministic self-test) + generated products plug into the existing registration mechanism**; engine pure JDK (no MC coupling), the schema model and the generation core independently probeable. Execution mode identical to p.2.5 / p.2.7 / p.2.8 / p.2.9 / p.2.10 / p.2.11 closing: the main agent only dispatches and trust-but-verifies; scouting/design, implementation, probes, acceptance, docs, and push are all done by sub-agents; each sub-agent takes one self-contained task and commits on completion; the closing sub-agent does the final review + cleanup + push (GitHub, origin only). Numbers use **p.2.12.x** (≤3 levels); never say “phase X / block N / R1”. Workflow: first **scout** (confirm td's schema capability, the engine registration mechanisms / generation paradigms, the devkit probe assets, and the existing form/doc generation shapes to hook) to produce the full-track sub-item split (5–8), then implement sub-item by sub-item, commit after each, and do final review + cleanup + push when all sub-items are done.

## 1. 项目上下文（必读）· Project context (read first)

**中文**：
- 系列聚合根 F:\Projects\Toterra-Repo\Subterra：Subterra（框架，开源 TPL 2.0，origin=github.com/houyangbaoxin2009/subterra）+ Toterra（github.com/houyangbaoxin2009/toterra）。
- **p.2.11 里程碑「Session programmable 会话可编程」已全部落地并已推 GitHub，最新 p 轨号 p.2.11.6（收尾提交）**：engine `subterra-engine\...\engine\session\`（纯 JDK）——命令协议核心 `SessionCommand`/`SessionCommandKind`/`SessionEnvelopeCodec`（确定性命令信封 + 固定命令动词语义，载荷走 p.2.5 zd 载荷通道）+ 指令化会话状态机 `SessionMachine`/`SessionState`/`SessionMachineResult`（连接/鉴权/命令派发/状态回读/断开确定性转移 + 单调序号门，乱序/重放均拒）+ 外部进程帧协议入口 `SessionFrame`/`SessionFrameCodec`（会话命令经 p.2.5 tink v2 帧 ABI，ext TLV session/seq/key9 meta，坏帧拒绝）；runtime 壳 `runtime.session.SessionRuntime`（-Dsubterra.probe.session 门控默认 no-op、`[Subterra session]` marker 并入 AsyncE2EProbe boot）；纯 JVM 探针 `SessionProbe` 35 项确定性断言。关键路径 **engine.session**（纯 JDK）。结论：**会话可编程运行时抽象——命令/状态机/帧入口复用 p.2.5 tink v2/tsha1f/zd ABI，runtime 门控壳并入 AsyncE2EProbe、五次开服不变**。/ p.2.11 “Session programmable” fully landed and pushed (commits p.2.11.1→…→p.2.11.5; closing commit p.2.11.6); engine.session (pure JDK) — command-protocol core SessionCommand/SessionCommandKind/SessionEnvelopeCodec (deterministic command envelope + fixed verbs over the p.2.5 zd payload channel) + command-ized session state machine SessionMachine/SessionState/SessionMachineResult (deterministic connect/auth/dispatch/read-back/disconnect with a monotonic-seq gate, reorder/replay rejected) + external-process frame-protocol entry SessionFrame/SessionFrameCodec (session commands over the tink v2 frame ABI, ext TLV session/seq/key9 meta, bad frames rejected); the runtime shell runtime.session.SessionRuntime (-Dsubterra.probe.session gate-off no-op, [Subterra session] marker merged into the AsyncE2EProbe boot); SessionProbe 35 deterministic pure-JVM checks. Key path engine.session. Conclusion: a session-programmable runtime abstraction — command/state-machine/frame entry reuse the p.2.5 tink v2/tsha1f/zd ABI, the gated runtime shell merged into the AsyncE2EProbe boot, five boots unchanged.
- **p.2.5 P2P / tink v2 / tsha1f / zd 栈（已落地）**：engine.network + engine.p2p，语言无关 ABI；p.2.11 会话入口已复用为其实体承载。/ p.2.5 P2P / tink v2 / tsha1f / zd stack (landed): engine.network + engine.p2p, language-agnostic ABI; p.2.11's session entry already reuses it as the concrete carrier.
- **p.2.3 Save（已落地，数据底座，事实不变）**：统一 `SaveContainer` 六槽 + `engine.zd` zd v2 载体 + zdt 混合文档 + `SaveExportArchive` td 导出档案。/ p.2.3 Save rework (landed, unchanged): the SaveContainer six-slot unified form, engine.zd carrier + zdt hybrid docs, the SaveExportArchive td export archive.
- **p.2.4 server-interaction（已落地，事实不变）**：engine.network 五子引擎层 + runtime EnhancedChannelRuntime 门控壳。/ p.2.4 server-interaction (landed, unchanged): five pure-JVM engine.network cores + the gated runtime shell.
- **p.2.9 World-as-artifact（已落地，事实不变）**：engine.world WorldPack + engine.export 端口预留 + runtime WorldRuntime 门控壳 + 确定性归档/往返范式。/ p.2.9 World-as-artifact (landed, unchanged): engine.world WorldPack + engine.export port cut + runtime WorldRuntime + the deterministic-archival/round-trip paradigm.
- **p.2.8 sim / p.2.10 saveverify（已落地，事实不变）**：engine.sim + runtime SimRuntime、engine.saveverify + runtime SaveVerifyRuntime，「门控壳并入既有 boot」范式。/ p.2.8 sim / p.2.10 saveverify (landed, unchanged): the gate-shell-into-existing-boot paradigm proven by these tracks.
- **probeAcceptance 现状（p.2.11 收官）**：`gradlew :subterra-devkit:probeAcceptance` 通过——**80 个纯 JVM 探针**（含新增 `SessionProbe` 35 checks + 既有全部）+ **5 次 E2E 开服**（BootProbe → DatapackE2EProbe → SaveE2EProbe → NetworkE2EProbe → AsyncE2EProbe，mustRunAfter 链；AsyncE2EProbe 同开服并入 `[Subterra async]`/`[Subterra sim]`/`[Subterra world]`/`[Subterra saveverify]`/`[Subterra session]` 五壳联合断言）。铁律（api←engine←runtime/migrate）由 ironLawProbe 恒定覆盖。/ probeAcceptance (p.2.11 closing): green — **80 pure-JVM probes** (incl. SessionProbe 35 checks + all existing) + **5 E2E boots** (Boot → Datapack → Save → Network → Async, mustRunAfter chain; AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages.
- **ABI / 接线事实（已钉死，p.2.12 复用）**：engine 纯 JDK 落 engine.*、MC 壳落根 sourceSet；td（tiedb-td 文档）为本框架数据一等语言，既有 engine.datapack / engine.save / engine.export / engine.world 均已确立「td schema → 文档/注册」范式；新探针落 `subterra-devkit\src\main\java\io\toterra\subterra\probes\`，probeAcceptance 接线于 `subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 链；staging 用 build/tmp）。
- **关联里程碑**：p.2.12 为**革命·新增**，直接衔接 p.2.2 datapack / p.2.3 save / p.2.16 API 契约面 / p.2.20 devkit 完整化 / p.2.23 游戏内 Hub 表单单生成；p.2.13 Deterministic PCG（engine.pcg）接 p.2.12 的生成底座。p.2.12 应为 engine / devkit 落地一个由 td schema 驱动的确定性脚手架（生成注册码 / 探针 / 文档 / 表单），把模组编写从「写代码」推向「写数据」。

**English**: See the Chinese block above — this handoff follows the exact p.2.5/p.2.9/p.2.10/p.2.11 closing patterns. p.2.11 “Session programmable” fully landed and pushed (commits p.2.11.1–p.2.11.5, latest track **p.2.11.6**); engine.session (command protocol + command-ized state machine + external-process frame entry reusing the p.2.5 tink v2/tsha1f/zd ABI) + runtime.session.SessionRuntime gated shell ([Subterra session] marker merged into the AsyncE2EProbe boot) is the p.2.12 handoff basis. p.2.5 (landed) is the frame-stack foundation; p.2.3 / p.2.4 / p.2.8 / p.2.9 / p.2.10 all landed unchanged (see Chinese block). td is the framework's first-class data language, with an established “td schema → document/registration” paradigm across engine.datapack / engine.save / engine.export / engine.world. probeAcceptance all green: **80 pure-JVM probes** (incl. SessionProbe 35 checks) + **5 E2E boots** (AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). Iron law auto-covers new engine packages. p.2.12 should land a deterministic, td-schema-driven scaffolding for engine/devkit that auto-generates registration code / probes / docs / forms — moving mod authoring from “writing code” to “writing data” — building on the ROAD p.2.12 milestone.

## 2. 本次范围（p.2.12 全轨，子项以摸底后最小闭环为准）· Scope (final sub-items after scouting minimal closure)

**中文**：总原则：每子项=一个自足最小闭环（纯 JDK 优先、探针确定性命中验收；E2E 仅在真实生命周期必要处加确定性 `-D`/`-P` 钩子 marker，**保持既有 markers 稳定**）；每子项独立提交。**先摸底**（确认 td 的 schema 能力现状、引擎既有注册机制/生成范式、devkit 探针资产、form/文档生成该接的既有形态），再定 5–8 个子项拆分——本段为示意，最终以摸底后最小闭环为准，可微调，**不擅自扩大范围**；不确定处先列出提问再动手。示意方向（为 p.2.12 提炼由 td schema 驱动的确定性脚手架，承接前述各轨已确立的 td 文档/注册范式 + 确定性生成范式）：目标见 ROAD p.2.12 行：「td schema → 自动生成注册码 / 探针 / 文档 / 表单——模组编写从「写代码」变「写数据」」。示意子项（示例）：
1. **schema 模型**：td schema 的一等解析/校验/遍历模型（确定性 schema 定型，接 engine 纯 JDK），作为单一事实源。
2. **注册码生成器**：由已定型 schema 确定性生成注册表接线代码（同 schema 同字节输出，接入既有注册机制）。
3. **探针/文档生成器**：由 schema 确定性生成对应探针（self-test it 于脚手架自身）与开发文档。
4. **表单生成**：由 schema 确定性生成（游戏内 / 工具）表单结构 / 渲染数据（接 p.2.23 游戏内 Hub 的 td schema 表单自动生成，见 ROAD p.2.23）。
5. **确定性脚手架探针**：脚手架自身含确定性 self-test（给定 schema → 期望输出逐字节一致），纯 JVM 可独立跑。
提示：以上为示意（5–8 个子项以摸底后最小闭环为准）；**td schema 单一事实源 + 确定性生成（同 schema 同输出）+ 生成物可探针 + 接入既有注册机制**贯穿每次设计；p.2.2/p.2.3/p.2.9 已确立的 td 文档/注册范式与 p.2.7/p.2.8 已证明的确定性（逐字节一致）生成范式必须被复用为默认语义。

**English**: Same principle as p.2.7/p.2.8/p.2.9/p.2.10/p.2.11: each sub-item is a self-contained minimal closure (pure JDK preferred, deterministic probe acceptance; E2E only where a real lifecycle marker is required via `-D`/`-P`, keeping existing markers stable); each sub-item is its own commit. **Scout first** (confirm td's schema capability, the engine registration mechanisms / generation paradigms, the devkit probe assets, and the existing form/doc generation shapes to hook), then produce a 5–8 sub-item split (adjustable, never expand scope unilaterally). Goal per the ROAD p.2.12 row: “td schema → auto-generate registration code / probes / docs / forms — mod authoring from writing code to writing data.” Illustrative directions: (1) td schema first-class parse/validate/walk model (deterministic schema typing, engine pure JDK) as the single source of truth; (2) a registration-code generator — deterministic wiring output (same schema → same bytes) plugged into the existing registration mechanism; (3) probe/doc generators — deterministic probe + dev-doc output from the schema (the scaffold itself self-tests); (4) form generation — deterministic (in-game/tool) form structure/render data from the schema (hooks p.2.23's in-game-Hub td-schema auto-generated forms, per ROAD p.2.23); (5) a deterministic scaffold probe — the scaffolder carries a deterministic self-test (given schema → expected byte-identical output), pure-JVM standalone. Note: **td schema as single source of truth + deterministic generation (same schema → same output) + probeable generated artifacts + plugged into the existing registration mechanism** runs through every design; the td document/registration paradigm proven by p.2.2/p.2.3/p.2.9 and the deterministic byte-identical generation paradigm proven by p.2.7/p.2.8 must remain the default semantics.

## 3. 子代理纪律（大任务必须用，禁止主代理硬扛全量）· Sub-agent discipline

**中文**：
- **整个 p.2.12 由子代理集群执行**：摸底（只读 + 基准探针，产出全轨任务拆分）、engine 子代理、devkit/脚手架子代理、探针子代理、收尾子代理（门禁 + 文档 + 推送）。按文件归属拆分，保证**同一文件同一时刻只有一个子代理编辑**；有编译/API 依赖时严格串行（engine 先于 devkit/探测编译）。
- **子代理节奏**：完成一个小任务即提交一次并报告；全部完成后由收尾子代理统一 review+清理+提交一次 push。
- 主代理 trust but verify：关键改动（build.gradle/新探针/生成核心）必须读实际文件核对，必要时重跑探针。
- **同文件多处修改禁止并行下发多个子代理**（并行 Edit 互覆盖丢内容，已踩坑多次）；同文件编辑串行单发，改后读文件核对；一次消息内同文件只允许一个 Edit。

**English**: The whole p.2.12 runs as a cluster of sub-agents (scouting, engine, devkit/scaffold, probe, closing). Split by file ownership so that only one sub-agent edits a given file at a time; strictly serialize on compile/API dependencies (engine before devkit/probe compile). Commit after each completed sub-task; final unified review + push by the closing sub-agent. Never dispatch parallel edits to the same file (mutual overwrite); serialize same-file edits, one per message, and re-read to verify.

## 4. 纪律（硬性，违反即返工）· Discipline (hard; violation = rework)

**中文**：
- 主代理/子代理均：小任务逐个提交；全部完成后统一 review+清理+推送（GitHub）。
- 提交信息一律英文；作者保持本地 jiro；**只推 GitHub**（subterra origin），严禁 git.franj2.top。
- 版本 p.x.x.x 最多三级；禁用「阶段X / 第N块 / R1/R2 / block N」标签，p.2.12 子项一律 **p.2.12.x**。
- 文档双语（中英）；本任务尽量不新建文档，优先既有 docs/plans（本文件即全轨交接单）。
- 性能纪律：禁 O(n²)；热路径先建模；**确定性生成**（固定序、禁时序；同 schema 同输出）；schema/生成核心无全局扫描级依赖。
- 确定性：验收一律确定性探针；禁时序断言；断言前先做前置动作；失败计数只在失败路径自增。E2E 沿用事件驱动标记 + **`-D`/`-P` 转发属性钩子**（Done 后装载完成），不赌 sleep、不依赖 stdin 命令往返。
- 编译纪律：改探针源后必须重编（compileJava/探针任务），否则 JavaExec 用旧字节码。
- 文件编辑纪律：同文件连续编辑必须串行单发、改后读文件核对。
- 依赖铁律：api ← engine ← runtime/migrate；engine 纯 JDK 不碰 MC；纯 JDK 落 engine.*，MC 壳落根 sourceSet；migrate 仅依赖 api。
- 许可：permissive 可带声明复用/沿用；copyleft 仅 clean-room；存疑按不允许处理。p.2.12 Schema-first scaffolding 为自研（clean-room），不新增抄录；td schema 能力复用引擎既有内建实现。
- 铁律接线：settings.gradle include 与 iron law 探针覆盖；FFM 探针 JavaExec 需 `--enable-native-access=ALL-UNNAMED`（若涉及 tie）。
- 验收门：`gradlew :subterra-devkit:probeAcceptance` 全绿（含五次 E2E 开服）才算完成。

**English**: Commit per small task, English messages, author stays local `jiro`, **push GitHub origin only** (never git.franj2.top). Track numbering p.2.12.x (≤3 levels); no “phase X / block N / R1”. Bilingual docs; prefer existing docs/plans. No O(n²); bound hot paths; **deterministic generation** (fixed order, no timing; same schema → same output); the schema/generation core is free of global-scan-style dependencies. Deterministic probes everywhere; event-driven markers + `-D`/`-P` hooks; no sleep, no stdin. Recompile probe sources after edits. api ← engine ← runtime/migrate; engine pure JDK, MC only in the runtime shell; migrate depends only on api. Licensing: permissive reuse/derive with attribution only; p.2.12 Schema-first scaffolding is self-developed (clean-room), no new copying; td schema capability reuses the engine built-in implementation. Acceptance gate: `gradlew :subterra-devkit:probeAcceptance` all green (the five E2E boots included).

## 5. 模块完成 → 同格式提示词（延续机制，强制）· Handoff continuation (mandatory)

**中文**：p.2.12 全轨完成后，必须生成 p.2.13 同格式提示词并纳入记录：更新上下文段（p.2.12 各子项落地、最新 p 轨号 p.2.12.x、关键路径、td-schema 脚手架抽象结论）、更新本次范围段指向 p.2.13（Deterministic PCG，见 ROAD p.2.13 行；engine.pcg 名称/文本/配方/遗物/群系变种/本地化**生成器编排底座**——纯函数、同种子一致）、保持第 0/3/4 段结构不变、存放 `docs/plans/prompt-p.2.13-deterministic-pcg.md`（bilingual），随当次提交一并提交。

**English**: After p.2.12 fully lands, produce the p.2.13 prompt in the same format (context updated with p.2.12 landing, latest track p.2.12.x, key path, the td-schema scaffolding conclusion; scope pointing to p.2.13 Deterministic PCG, per ROAD p.2.13; engine.pcg generator-orchestration base for names/text/recipes/relics/biome-variants/localization — pure functions, same-seed reproducibility; sections 0/3/4 structure unchanged; store at docs/plans/prompt-p.2.13-deterministic-pcg.md, bilingual), committed with that change.

## 6. 关键路径速查 · Key-path quick reference

**中文**：
- 仓库根：F:\Projects\Toterra-Repo\Subterra；p.2.12 工作区建议：engine 纯 JDK 落 `subterra-engine\...\engine\schema\`（td schema 模型与生成核心）+ devkit 脚手架生成器（注册码 / 探针 / 文档 / 表单）按需落 `subterra-devkit`（生成物落点可探针）。
- **p.2.11 会话可编程（已落地，p.2.11.6）**：engine.session（SessionCommand/SessionMachine/SessionFrame 等，见 §1）+ runtime.session.SessionRuntime + SessionProbe（35 checks）+ probeAcceptance 80 纯 JVM + 5 次 E2E。
- **td 一等语言（p.2.2/p.2.3/p.2.9 已确立）**：engine.datapack（td 直载 / export 档案 / DatapackExportArchive）、engine.save（zdt + SaveContainer / SaveExportArchive）、engine.world（WorldPack td 文档）、engine.export（ExportKind/ExportPort/ExportHub）——「td schema → 文档/注册」范式在此复用。
- **确定性生成范式（p.2.7/p.2.8 已证明）**：逐字节一致（byte-identical）、固定序、禁时序——p.2.12 生成核心沿用同一约束。
- **运行时壳并入既有 boot 范式（p.2.8/p.2.9/p.2.10/p.2.11 已证明）**：默认 no-op（-D/-P 门控）、marker 并入 AsyncE2EProbe、staging 用 build/tmp、保持五次开服不变。
- 探针资产：`subterra-devkit\src\main\java\io\toterra\subterra\probes\`；新 schema/脚手架探针落同处；probeAcceptance 接线：`subterra-devkit\build.gradle`（E2E 沿用既有 mustRunAfter 开服链；staging 用 build/tmp）。
- 关联里程碑：p.2.2 datapack / p.2.3 save / p.2.16 API 契约面 / p.2.20 devkit 完整化 / p.2.23 游戏内 Hub 表单自动生成；p.2.13 Deterministic PCG（engine.pcg，生成器编排底座）接 p.2.12。「Schema-first scaffolding」为其 td-schema 驱动确定性脚手架抽象。

**English**: Repo root F:\Projects\Toterra-Repo\Subterra. p.2.12 work dirs: engine pure JDK in `subterra-engine\...\engine\schema\` (td schema model + generation core) + devkit scaffold generators (registration code / probes / docs / forms) in `subterra-devkit` (generated-artifact landing spots probeable). p.2.11 session programmable (landed, closing p.2.11.6): engine.session + runtime.session.SessionRuntime + SessionProbe (35 checks) + probeAcceptance 80 pure-JVM + 5 E2E boots. td first-class language (p.2.2/p.2.3/p.2.9 proven): engine.datapack / engine.save / engine.world / engine.export — the “td schema → document/registration” paradigm reused here. Deterministic generation paradigm (p.2.7/p.2.8 proven): byte-identical, fixed order, no timing — p.2.12's generation core follows the same constraints. The runtime-shell-into-existing-boot paradigm (p.2.8–p.2.11 proven): gate-off no-op, markers merged into the AsyncE2EProbe boot, staging via build/tmp, five boots unchanged. New probes go under the devkit probes; wire into probeAcceptance in subterra-devkit/build.gradle (E2E reuses the existing mustRunAfter boot chain; stage via build/tmp). Related tracks: p.2.2 datapack / p.2.3 save / p.2.16 API contract surface / p.2.20 devkit completion / p.2.23 in-game-Hub auto-generated forms; p.2.13 Deterministic PCG (engine.pcg, generator-orchestration base) builds on p.2.12. Schema-first scaffolding is the td-schema-driven deterministic scaffolding abstraction.

## 7. 输出与汇报 · Output & reporting

**中文**：主代理与各子代理均：双语说明（做了什么 + 结论）后立即提交；收尾子代理报 probeAcceptance 全量结果（含 E2E markers 数与五次开服）+ ROAD p.2.12 landed + 新提示词文件路径 + push 结果；最终给用户汇报完整子项清单与验收证据。

**English**: Every agent (main and sub) reports bilingually (what was done + conclusion) and commits immediately. The closing agent reports the full probeAcceptance result (E2E marker counts + the five boots), the “ROAD p.2.12 landed” note, the new prompt path, and the push result; the final user report lists the complete sub-item breakdown and acceptance evidence.