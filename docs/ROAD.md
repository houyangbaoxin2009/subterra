# Subterra Roadmap / Subterra 路线图

**Status / 状态:** Active / 生效中
**Date / 日期:** 2026-09-08（自系列 ROAD 拆分 / split from series ROAD）
**Version discipline / 编号纪律:** Development uses the pre-release p-track only — `p.<release>.<module>.<sub-item>` (max three levels, no "stage/session" grouping). The release track `r.x.y.z` is cut from a specific p version at stabilization time and introduces no new features. Planning never uses milestone-style labels such as R1/R2. / 开发计划一律只用 p 轨 `p.<发布档>.<模块>.<子项>`（最多三级，禁用「阶段X」分组与 R1/R2 式标签）；`r.x.y.z` 为正式发行轨，在基于某个 p 版本时切出，只做优化与稳定、不引新功能。

## Frame / 框架基线

* Subterra 为全栈开发框架模组（NeoForge 21.1.x / MC 1.21.1），设计见聚合仓库 `docs/2026-09-08-subterra-framework-design.md`。 / Subterra is a full-stack development framework mod (NeoForge 21.1.x / MC 1.21.1); design in aggregate-repo `docs/2026-09-08-subterra-framework-design.md`.
* p.1 线（骨架 / L1 启动 / L2 兼容 / L3 API / 优化集 / td 配置 / 日志 / 世界生成管线）全部落地，构成框架化基线；vanilla 保真工作（p.1.8.20–32）保留为默认预设并固化为 golden tests。 / The p.1 line is fully landed as the framework baseline; vanilla-fidelity work stays as the default preset and is frozen into golden tests.
* p.2 线起为框架化：模块重构为 api / engine / runtime / migrate / devkit，服务大型、颠覆原版、不考虑兼容性的模组（首位消费方 Toterra）。 / From p.2 the line is the framework: modules re-laid as api / engine / runtime / migrate / devkit, serving large, vanilla-overturning, compatibility-agnostic mods (first consumer Toterra).

## Milestone Map / 里程碑映射

* Every milestone below is a p-track item; sub-items continue the track per "one library = one sub-item". / 以下里程碑均为 p 轨条目；子项沿 p 轨推进，一库一子项。
* Acceptance is deterministic probes only (server boots / client enters world / probes PASS), never timing assertions. / 验收一律确定性探针：服务端开服、客户端进世界、探针断言全 PASS；禁时序断言。
* 2026-09-08 — 新增三项数据层重构：**数据包重构**（td 为数据包内容一等语言，数据包逻辑可直接以 tie 编写，替代手写 JSON）+ **存档重构**（zd+td 混合替代原版 NBT `.dat`，减小体积 / 加快加载）+ **服务端交互重构**（增强通道：tink v2 帧 + zd 载荷 + 增量差分，性能更好带宽更小）。 / Added three data-layer reworks: datapack (td first-class, logic in tie), save (zd+td hybrid), and server-interaction (enhanced channel: tink v2 frames + zd payloads + delta sync).
* 2026-09-09 — p.2.2.4 落地（loot 真合并 + worldgen/structure 真注册）：**recipe 规范形往返**（td→ShapedRecipe/SmokingRecipe→规范串，canonical markers 逐字符一致）、**loot_table 真合并进 LOOT_TABLE datapack registry**（1.21.1 无 LootDataManager；`ReloadableServerResources.fullRegistries().get()` RELOADABLE 层，经 `DatapackRegistryInjector` 公开 unfreeze→register→freeze 窗口注入）、**worldgen→CONFIGURED_FEATURE**（configured_feature 最小 schema：simple_block + block）、**structure→STRUCTURE/STRUCTURE_SET**（DesertPyramidStructure 体 + biomes holder）；E2E 16 标记全绿。 / p.2.2.4 landed: loot merge into the LOOT_TABLE datapack registry via a public un-freeze window + worldgen/structure real registration; E2E 16 markers green.
* 2026-09-09 — p.2.2.5 落地（DataPack→td 导出往返）：**RecipeManager → td → 回灌 → 再导出逐字节一致**——engine.datapack `RecipeDatum`（record，read/write 规范 td，count/cooking_time clamp≥1、experience 经 cleanExperience float 往返归一化，镜像 buildShaped/buildSmoking 语义）+ `DatapackExporter` 导出前门；runtime `DatapackRecipeExporter`（holder→datum，pattern 行按格首次出现确定性重建字符 A,B,C…）→ `DatapackRegistrar.runExportRoundTrip`（导出→buildRecipe 回灌→再导出，字节级比对，marker ok）+ 类 javadoc；纯 JVM 探针：导出→DatapackLoader 文件重载→再导出逐字节一致 + 三连稳定 + 导出字段无损（=源 payload 序列化，6 新断言）；E2E 标记 16→18（`export roundtrip toterra:example/smoke ok`）；probeAcceptance 全绿。 / p.2.2.5 landed: DataPack→td export round-trip byte-exact (holder→td→buildRecipe→td), engine RecipeDatum schema + mc-shell exporter + registrar round-trip markers + JVM byte-identical probe; E2E 18 markers green.
* 2026-09-09 — p.2.2.6 落地（导出全种类 + 导出档案 Hub）：**导出器覆盖全部七类**（RECIPE 对象级逐字节往返；TAG/LANG 归一化 TagDatum/LangDatum；LOOT_TABLE/WORLDGEN/STRUCTURE/FUNCTION canonical pass-through、schema 由注册器消费方重建验收）+ **内容级导出档案 DatapackExportArchive**（`type tie<data>` 单文档 `export=[version,entries]`，export∘rehydrate 逐字节恒等，DatapackPack 文件级之外的第二档案形态，p.2.18 engine.export 前哨）；`DatapackRegistrar` 统一 marker `export roundtrip <kind> <id> ok` + `export archive roundtrip ok (entries=10)`；纯 JVM 探针 10 条目文件重载往返逐字节一致 + 档案往返 + 字段无损；E2E 标记 18→27；probeAcceptance 全绿。 / p.2.2.6 landed: export covers all seven kinds + content-level export archive (byte-exact rehydrate identity); E2E 27 markers, full gate green.
* 2026-09-09 — p.2.2.7 落地：/subterra export 指令接通（p.2.18 engine.export 前哨）——`/subterra export [<path>]`（op 级 2）把已装载 pack 逐包经 DatapackExportArchive 导出为内容级 td 档案文档到指定目录（缺省 subterra-export），并对每份文档立即 export∘rehydrate 逐字节回灌校验；E2E 经确定性属性钩子触发（`subterra.probe.export` 经 gradle -P 可靠转发到 `runServer`，startup 即跑同一导出核心，规避 gradle-forked server JVM 的 stdin 双层中转不可靠），新增 2 marker；probeAcceptance 全绿。 / p.2.2.7 landed: /subterra export command writes content-level export-archive docs per pack with byte-identical rehydrate check, driven deterministically at server startup via a forwarded gradle property (stdin double-hop through the forked server JVM is unreliable); 2 new markers.
* 2026-09-09 — p.2.2.8 落地：数据包双接口（td + API）——engine DatapackLookup（entry/byKind/tdText）定型类型化读 API 与 td 文本路由并存：API 直达同一 DatapackEntry 实例（身份保持）、td 路由逐字节等于导出规范形（tie 消费路径=导出 td + tiec parse_data 同构 schema，FFM 桥接见 p.2.1）；纯 JVM 探针断言双路径逐字节一致 + Td 可解析。 / p.2.2.8 landed: datapack dual interface — typed DatapackLookup API route and td-text route agree byte-for-byte; tie consumption documented (exported td + tiec parse_data, FFM via p.2.1).
* 2026-09-09 — p.2.2.9 落地：数据包级规则 + 存档覆盖优先级——pack.td `rules`（`[ [ k, v ] ]`，任意 td 值）→ DatapackRules.fromManifest/resolve/render（确定性；later pack 覆盖 earlier、save override 覆盖全部）；runtime 经 `subterra.override` 属性或覆盖文件读入存档覆盖，logRules 输出 `rules <k> = <v>` 确定性 marker；JVM 断言提交序（无覆盖 wild=true、覆盖后 wild=false、未覆盖键保持）+ E2E 2 marker；接 p.2.17 规则系统前哨。 / p.2.2.9 landed: pack-level rules with save-override precedence (pack order, then save overrides win), deterministic markers — forward hook to p.2.17.
* 备注 / Note：p.2.2 全部子项均沿用 p.2.2.x 三级编号（p.2.2.1…p.2.2.9），无阶段式标签（阶段X / R1/R2）。 / All p.2.2 sub-items use the p.2.2.x three-level numbering only; no stage/session-style labels.
* 2026-09-09 — p.2.2.1 落地：**纯 td 直载**（不产出 JSON）——engine.datapack 容器 + 注册表 + tie 逻辑链，DatapackProbe 26 断言全绿；顺带修复共享 Td 解析器 header 误杀 `type = "..."` 数据键与注册表排序丢失两 bug。 / p.2.2.1 landed: pure td direct loading, engine.datapack registry + tie logic chain.
* 2026-09-09 — p.2.2.2 落地（MC 壳注册接线）：runtime.datapack——**recipe 编译为原版 ShapedRecipe → RecipeManager.replaceRecipes 合并**（crafting_shaped），tag/lang 索引进内存 + 确定性 markers，**function 经 TieLogicBundle → engine.tie 0 参调用**；DatapackE2EProbe 开服断言全绿；两开服探针 mustRunAfter 串行（并行抢 run/world session.lock）；E2E staging 隔离 build/tmp（避开 bootProbe 的 Windows dll 句柄）。 / p.2.2.2 landed: MC-shell registration wiring — recipes into RecipeManager, tag/lang indexes, tie functions called; E2E gate green; boot gates serialized.
* 2026-09-09 — p.2.2.3 落地（打包往返 + 配方类型扩展 + 剩余三类 staging）：**DatapackPack**（目录↔单一 td 文档，base64 content，解包重载注册表/包名/td 载荷逐字节一致）+ **smoking→SmokingRecipe**（buildRecipe 按 type 分派）+ loot/worldgen/structure 三类的 staged 索引与 markers（LootDataManager/RegisterEvent 真注入留 p.2.2.4，不在本块硬塞）；独立探针 31 断言、E2E 10 标记全绿。 / p.2.2.3 landed: DatapackPack round-trip, smoking recipe type, remaining kinds staged with markers; deep vanilla injection deferred to p.2.2.4.
* 2026-09-09 — p.2.4 落地（服务端交互重构-增强通道，革命）：engine.network 五子引擎层（frame tink v1 crc32 载体 + v2 ABI 常量 + zd 载荷桥；integrity tsha1f 便携 Java + FrameV2 快/强校验槽与流式分块；strategy 三级载荷策略；bandwidth 削减优化器；crypto x25519+AEAD）为 p.2.5 P2P 同栈基座（语言无关 ABI）+ runtime.network.EnhancedChannelRuntime 门控渐进增强壳（原版握手零接触），NetworkE2EProbe 第四次开服；六探针接入 probeAcceptance，全量验收全绿。 / p.2.4 landed: five pure-JVM engine.network cores (frame/integrity/strategy/bandwidth/crypto, the language-agnostic p.2.5 same-stack base) + EnhancedChannelRuntime gated progressive shell (vanilla handshake untouched), six probes wired into the acceptance gate, all-green.

| p-track / p 轨 | Milestone / 里程碑 | Status / 状态 |
| --- | --- | --- |
| p.1.0–p.1.8 | Framework baseline / 框架化基线：模块骨架、L1 启动、L2 Java25 兼容修补、L3 API 库、优化集（ScratchPool + 移植核心）、td 配置、日志、世界生成管线（九维模型 / 密度函数 / router / surface / 默认 vanilla 预设 / 探针） | landed / 已落地 |
| p.2.0 | Module re-layout / 模块重构（收尾中）：api / engine / runtime / migrate / devkit 分层落地，依赖铁律探针化，探针全绿保持 | landed / 已落地（收尾） |
| p.2.1 | tie bridge / tie 桥（**革命基座**，自原 p.4.0 前移）：tiec → DLL → FFM 加载调用，热点路径 tie 化 + 性能验证 | landed / 已落地 |
| p.2.2 | Datapack rework / 数据包重构（**革命**，自原 p.2.10 前移）：td 为数据包内容一等语言——函数 / 配方 / 战利品表 / 世界生成 / 结构 / 标签 / 本地化 td 声明 + **纯 td 直载**（engine.datapack 注册表 + tie 逻辑链，探针闭环已落地）；数据包逻辑可直接以 tie 编写（tiec 编译 → tie bridge 装载，接 p.2.1），热点路径 tie 化；数据包打包分发（接 config 包）；td + API（含 tie）双接口、`/subterra export` 指令、数据包级规则 + 存档覆盖（接 p.2.17）均已落地 | landed / 已落地 |
| p.2.3 | Save rework / 存档重构（**革命**，自原 p.2.11 前移）：现代化存档形态——**zd + td 混合替代原版 NBT `.dat`**（level / player / 侧数据），减小体积、加快加载（zd 压缩变体 / 字典 / 零拷贝）；统一 SaveContainer（世界 + 配置 + 藏录 Ledger + 领域档案 + 遗物条目 + 台账）；双层配置（全局 + 存档覆盖，接 p.2.17）；迁移走 subterra-migrate；元数据可导出（接 p.2.18 export）。落地小结 / landing notes（p.2.3.1–p.2.3.6）：**p.2.3.1** engine.zd 通用 zd v2 载体（头部规范抽自 profiler ZdWriter，10 字节头精确保持 + 固定六字段 wire2 记录读写 + 树平铺往返）+ 六槽 SaveContainer 骨架（规则槽叠加 = DatapackRules 语义）；**p.2.3.2** level.dat 语义迁移 → zdt 混合文档（td 元数据 + base64 zd 载荷）+ runtime.save 壳（ServerStarted 旁路，`-Dsubterra.probe.save` 门控 no-op，E2E 标记）；**p.2.3.3** player 迁移（NBT 根复合 → PlayerDatum → zdt，复用通用 ZdtTransfer 载体）；**p.2.3.4** 藏录 / 领域 / 遗物 / 台账四槽类型化文档（确定性排序 + 线性构建防 O(n²)，字段级往返）；**p.2.3.5** 双层配置（DatapackRules.resolveTiered 全局 → 存档胜出）+ subterra-migrate 首块迁移管线（api SPI + engine 实现 + ServiceLoader 运行时发现，migrate 仅依赖 api）；**p.2.3.6** SaveExportArchive td 导出档案（与 DatapackExportArchive 同构，export∘rehydrate 逐字节；/subterra export 的 save 接线留 p.2.18）。/ p.2.3.1 generic engine.zd carrier (pinned 10-byte header, six-field wire2 records, tree round-trip) + six-slot SaveContainer; p.2.3.2 level.dat→zdt hybrid doc + gated runtime.save shell (E2E marker); p.2.3.3 player→zdt (shared ZdtTransfer); p.2.3.4 four typed slot docs; p.2.3.5 two-tier config + first migrate SPI pipeline (api-only); p.2.3.6 td save export archive (byte-identical rehydrate; save cmd wiring in p.2.18). | landed / 已落地 |
| p.2.4 | Server-interaction rework / 服务端交互重构（**革命**，自原 p.4.3 前移）：客户端 ⇄ 服务端**增强通道**——tink v2 帧 + tsha1f 帧级强校验 + zd 载荷序列化（与 p.2.5 P2P 同栈，语言无关 ABI）；三级载荷策略（L1 高频增量差分 / 兴趣域订阅 · L2 中频快照 + 变更流 · L3 低频加密）；带宽削减 = 增量同步 / 状态降频插值 / 按需订阅；默认强加密（x25519 + AEAD，局域网可信可关）；原版协议路径保留（渐进增强，原版客户端仍可连）。落地小结 / landing notes（p.2.4.1–p.2.4.6）：**p.2.4.1** engine.network.frame——tink v1 crc32 帧载体（FrameV1/Iterator/Crc32Ieee）+ v2 ABI 常量（FrameConst）+ zd 载荷桥（ZdFrameBridge）；语言无关 ABI，p.2.5 同栈基座；**p.2.4.2** engine.network.integrity——tsha1f 便携 Java 实现（tie-main 金标向量 KAT 全中，含 n=8 快 8B 与 n=48 强档 32B）+ tink v2 帧编解码（FrameV2：快/强校验槽、ext TLV 未知键跳过、v1 兼容读、保留位拒绝、流式分块 split/join）+ 单字节篡改即拒；**p.2.4.3** engine.network.strategy——三级载荷策略（L1 高频增量差分+兴趣域订阅 / L2 中频快照+变更流 / L3 低频加密标记）：确定性选择器、增量 diff 往返、变更流有序回放；**p.2.4.4** engine.network.bandwidth——带宽削减优化器：增量同步/状态降频插值/按需订阅三技术量化权重 + argmax 选型（固定枚举序破平局），确定性命中计数，L3 加密适用性读局域网可信开关；**p.2.4.5** engine.network.crypto——x25519 协商 + SHA-256 KDF → AES-256-GCM（12B nonce = 8B BE seq + 4B 零，同密钥永不重复），往返/篡改拒绝/错钥拒绝，局域网可信可关（默认开，L3 策略接入）；**p.2.4.6** runtime.network.EnhancedChannelRuntime——ServerStartedEvent 旁路壳（-Dsubterra.probe.network 门控，默认 no-op、原版握手零接触）；接线断言组件装载 + original-path-preserved + E2E 第四次开服 NetworkE2EProbe。/ p.2.4.1 engine.network.frame: tink v1 crc32 carrier (FrameV1/Iterator/Crc32Ieee) + v2 ABI consts (FrameConst) + zd payload bridge (ZdFrameBridge); language-agnostic, the p.2.5 same-stack base. p.2.4.2 engine.network.integrity: portable tsha1f (tie-main golden-vector KAT all-pass, incl. n=8 fast 8B + n=48 strong 32B) + tink v2 frame codec (FrameV2: fast/strong slots, ext TLV unknown-skip, v1-compat read, reserved-bit reject, streaming split/join) + single-byte tamper rejection. p.2.4.3 engine.network.strategy: three-tier payload strategy (L1 high-freq incremental diff + interest subscribe / L2 mid-freq snapshot + change stream / L3 low-freq encrypted marker) — deterministic selector, incremental diff round-trip, ordered change-stream replay. p.2.4.4 engine.network.bandwidth: bandwidth-cuts optimizer — incremental sync / decimation-interpolation / on-demand subscribe quantified weights + argmax selection (fixed-enum order breaks ties), deterministic hit counts, L3-fit reads the LAN-trustable toggle. p.2.4.5 engine.network.crypto: x25519 + SHA-256 KDF → AES-256-GCM (12B nonce = 8B BE seq + 4B zero, same key never repeats), round-trip / tamper-reject / wrong-key-reject, LAN-trustable on by default (L3 hookup). p.2.4.6 runtime.network.EnhancedChannelRuntime: ServerStartedEvent shell (gated by -Dsubterra.probe.network, default no-op, vanilla handshake untouched); wiring asserts component load + original-path-preserved + fourth-boot NetworkE2EProbe. / 诚实披露 / honest disclosures：(a) p.2.4.6 壳为门控生命周期自练（增强帧在 ServerStartedEvent 时**进程内往返**）；真实 client⇄server 封包接线与延迟载荷通道在 p.2.5 落地；(b) 强 32B 完整性移植使用通用 state-per-n 核心，在 tie 金标 n=48 向量上验证相等（dual9/11h 为按需生成的对等性能等价项，非照抄）。 | landed / 已落地 |
| p.2.5 | P2P decentralized networking / P2P 去中心化网络（自原 p.4.1 前移）：tink v2 + tsha1f 帧级强校验；zd 作为自定义载荷通道通信介质（先可行性基准）。落地小结 / landing notes（p.2.5.1–p.2.5.8，全部纯 JDK、复用 p.2.4 engine.network 同栈、ABI 对齐 p.2.4）：**p.2.5.1** 可行性基准 `P2pChannelBenchmarkProbe`——**zd 作载荷通道可行**（raw-vs-zd 体积比 ≈0.47–0.51，即 ≈2× 以内；medium-table encode 5.1 / decode 19.7 ops/ms 满足吞吐；1MiB 帧强校验表头恒定 42B）；**p.2.5.2** engine.p2p `NodeId/NodeAddr(direct/viaRelay)/AddressBook` 寻址；**p.2.5.3** engine.p2p.handshake 确定性握手 + x25519 密钥双向绑定（篡改/错钥/伪造/乱序/重放拒）；**p.2.5.4** engine.p2p.transport 帧级传输（会话序号、有序重排、去重、确定性丢包/篡改/重放拒）；**p.2.5.5** engine.p2p.payload zd 载荷通道 ABI（payload==zd doc + 类型信封，meta 走 tink v2 ext key9）；**p.2.5.6** engine.p2p.plan 通道规划（策略 + 带宽编排）；**p.2.5.7** engine.p2p.discovery `KademliaLite` DHT 无中心发现（结构核心，RPC/线上层留 runtime）；**p.2.5.8** engine.p2p.relay 志愿 relay（登记→转发→投递、未登记 REJECT、超跳 DROPPED、shutdown 清表）+ **关停钩子接 MC 壳** `EnhancedChannelRuntime`（ServerStoppingEvent 清表，`[Subterra relay]` marker 新行、既有 network markers 原样保留）。去中心化原则全量落实（直连 + DHT + relay 无中心）。probeAcceptance 全绿（61 纯 JVM + 四次 E2E，原 NetworkE2EProbe 第四服、original-path-preserved 不变）。/ p.2.5.1 feasibility benchmark: **zd-as-channel works** (raw-vs-zd ≈0.47–0.51, i.e. ≈within 2×; medium-table encode 5.1/decode 19.7 ops/ms; 1MiB strong-frame header constant 42B). p.2.5.2 engine.p2p addressing (NodeId/NodeAddr direct|viaRelay/AddressBook). p.2.5.3 engine.p2p.handshake deterministic handshake + x25519 key binding (tamper/wrong-key/forged/reorder/replay reject). p.2.5.4 engine.p2p.transport frame-level channel (session seq, ordered re-assembly, dedup, deterministic loss/tamper/replay reject). p.2.5.5 engine.p2p.payload zd payload-channel ABI (payload==zd doc + type envelope, meta via tink v2 ext key9). p.2.5.6 engine.p2p.plan channel planner (strategy+bandwidth wiring). p.2.5.7 engine.p2p.discovery KademliaLite (structural core; RPC/wire layer deferred). p.2.5.8 engine.p2p.relay voluntary relay (register→forward→deliver, unregistered REJECT, over-hop DROPPED, shutdown clear) + teardown hook wired in the MC shell EnhancedChannelRuntime (new [Subterra relay] line; existing network markers untouched). Decentralization implemented (direct + DHT + relay, no central account/session). probeAcceptance all green (61 pure-JVM + four E2E boots, NetworkE2EProbe fourth boot, original-path-preserved unchanged). / 诚实披露 / honest disclosures：(a) p.2.5 各核心（寻址/握手/传输/载荷/规划/发现/relay）为**纯 JDK 确定性结构核心**，线上 RPC/真实 socket 传输层与 relay 线上端点留 runtime 后续（p.2.5.8 的 relay 为内存端点语义，仅 MC 壳做关停接线）；(b) E2E **不新增开服探针**，relay 关停由纯 JVM P2pRelayProbe（含 shutdown clear）覆盖，NetworkE2EProbe 强杀进程故不触发 ServerStoppingEvent（符合设计）。 | landed / 已落地 |
| p.2.6 | C2ME bundled / C2ME 直接包含（自原 p.3.0 前移）：engine.worldgen.async 核心 + runtime 异步壳，随框架发布（MIT 声明） | landed / 已落地 |
| p.2.7 | Deterministic parallel / 确定性并行执行 engine.parallel（**革命·新增**）：多核并行（sim / AI / 区块）**不破种子确定性**——通用键分区 KeyPartition（双 long 路径与 p.2.6 ChunkPartition 逐位一致）+ 有界串行 WorkerBank + 泛型确定性分发器 DeterministicDispatcher（**重复键显式拒绝**，替代 p.2.6 last-write-wins 非确定性保证）+ ParallelRunner 门面（并行 run / 串行 runSerial **逐位一致**、批次生命周期、无线程泄漏、输入序无关）；五探针全接 probeAcceptance，ChunkKey 域归并序与 p.2.6 一致，optim.sched 组合不破确定性；engine.sim / C2ME 的地基 | landed / 已落地 |
| p.2.8 | World sim / 世界推演 engine.sim（**革命·新增**）：时间 / 物理 / 生态 / 经济 / 势力的**增量确定性模拟底座**——预算分级、分区空间索引、无全局扫描（接 engine.parallel p.2.7）；服务 Toterra 生态层（食物链 / 季节 / 迁徙）、势力扩张与贸易网络、自建聚落；与 engine.time（p.2.26）衔接。落地小结 / landing notes（p.2.8.1–p.2.8.6）：**p.2.8.1** engine.sim.budget 预算分级调度（BudgetTier/RegionKey/TickBudget/BudgetScheduler——per-simulant/region/global 三级 cap、原子记账、超预算确定性 key 序降级，p.2.26 BudgetScheduler 前哨）；**p.2.8.2** engine.sim.spatial 分区空间索引（CellCoord/SpatialIndex——半径邻域只访问 (2r+1)² 局部窗口 cell、无全局扫描，cell 键经 p.2.7 KeyPartition 双 long 路径入 bank）；**p.2.8.3** engine.sim.core 增量确定性模拟核心（SimKey/PerSimRandom/Simulator/SimWorld——advance(dirty) 只推进变化局部、逐 key 顺序无关、diff/回放与全量重算逐字节一致）；**p.2.8.4** engine.sim.run per-simulant 隔离 + 确定性归并门面（SimRunner——直接消费 p.2.7 ParallelRunner，run/runSerial 逐字节一致、重复键显式拒绝）；**p.2.8.5** SimCombineProbe 全轨组合简景（budget+spatial+core+parallel）；**p.2.8.6** runtime.sim.SimRuntime（-D/-Psubterra.probe.sim 门控默认 no-op、[Subterra sim] marker + 四微型确定性校验，AsyncE2EProbe 同开服并入）。/ p.2.8.1 engine.sim.budget budget-tier scheduling (per-simulant/region/global caps, atomic accounting, deterministic key-order degradation — the p.2.26 BudgetScheduler precursor); p.2.8.2 engine.sim.spatial partitioned spatial index (local-window neighborhood queries, no global scan, p.2.7 partition reuse); p.2.8.3 engine.sim.core incremental deterministic core (dirty-set advance, order-independent per-key ticks, diff/replay byte-identical to full recompute); p.2.8.4 engine.sim.run per-simulant isolation + deterministic merge facade (SimRunner over p.2.7 ParallelRunner, parallel/serial byte-identical, duplicate-key rejection); p.2.8.5 SimCombineProbe full-stack combination; p.2.8.6 runtime.sim.SimRuntime (gate-off no-op shell, [Subterra sim] markers + four micro-checks, merged into the AsyncE2EProbe boot). | landed / 已落地 |
| p.2.9 | World-as-artifact / 世界即产物（**革命·新增**）：export + save + 数据包 + 藏录一键打包为「世界包」，可搬移、可再水化（接 p.2.3 / p.2.18）；服务 Toterra 镜像层与存档互导。落地小结 / landing notes（p.2.9.1–p.2.9.6）：**p.2.9.1** engine.world.WorldPack 统一世界包档案形态（单一 td 文档 `world=[version,meta,save,datapack]`，save/datapack 为 base64 载荷，meta TreeMap 排序，段序固定 version→meta→save→datapack）；**p.2.9.2** 确定性归档 + `export(rehydrate(export(w))).equals(export(w))` 逐字节往返契约 + p.2.8 稳态衔接 meta 键；**p.2.9.3** WorldPackPacker 世界工作树目录↔单 td 打包/还原（只扫六固定 SaveSlot 目录、无全局遍历、路径穿越防护，可搬移互导）；**p.2.9.4** probes.WorldPackProbe 20 项确定性断言 + 接入 probeAcceptance；**p.2.9.5** engine.export 导出端口预留（ExportKind/ExportPort/ExportHub，preload 确定性注册 WORLD_PACK + SAVE 两内建端口，接 p.2.18）；**p.2.9.6** runtime.world.WorldRuntime（@EventBusSubscriber ServerStartedEvent 钩子、-Dsubterra.probe.world 门控默认 no-op、[Subterra world] marker、staging 隔离不写服务器、AsyncE2EProbe 并入 world 两槽断言）。/ p.2.9.1 engine.world.WorldPack uniform world-pack archive form (single td doc `world=[version,meta,save,datapack]`, base64 payloads, TreeMap-sorted meta, fixed version→meta→save→datapack section order); p.2.9.2 deterministic archival + byte-exact export∘rehydrate∘export identity + p.2.8 steady-state meta keys; p.2.9.3 WorldPackPacker world-work-tree dir↔single-td pack/restore (scans only the six fixed SaveSlot dirs, no global scan, path-traversal guards, movable/interchangeable); p.2.9.4 probes.WorldPackProbe 20 deterministic checks wired into probeAcceptance; p.2.9.5 engine.export port cut reserved — ExportKind/ExportPort/ExportHub, WORLD_PACK+SAVE built-ins deterministically preloaded (p.2.18 seam); p.2.9.6 runtime.world.WorldRuntime (@EventBusSubscriber ServerStartedEvent hook, -Dsubterra.probe.world default no-op, [Subterra world] markers, staging-isolated no server write, world two-slot asserts merged into AsyncE2EProbe). | landed / 已落地 |
| p.2.10 | Save verifiability / 存档可验证（**革命·新增**）：zd 区块 + ed25519 / tsha1f 签名，损坏自检与防篡改——存档 = 可验证账本（接 p.2.3）。落地小结 / landing notes（p.2.10.1–p.2.10.6）：**p.2.10.1** engine.saveverify.chunk——zd 区块划分 + 逐块 tsha1f 快/强校验元数据（Chunk/ChunkPlan/ChunkVerifier，确定性、固定序）；**p.2.10.2** engine.saveverify.sig——Ed25519 强签名核心，确定性密钥对注入（JDK 内置，RFC 8032 确定性签名，Ed25519Keys/SaveSigner）；**p.2.10.3** 可验证存档案（engine.saveverify 根包）——**单一 td 文档 saveverify=[version,meta,config,payload,chunks,sig]**（接 p.2.3 文档形态）：ed25519 头签名 + tsha1f 双档块标 + seq 重放门 + **五类损坏确定性拒绝**（SaveVerifyArchive/SaveVerifyLedger/VerifyResult）；**p.2.10.4** SaveVerifyProbe 15 项确定性断言（flip/truncate/reorder/wrong-key/replay 五类损坏均拒、完好档案与往返接受）；**p.2.10.5** runtime.saveverify.SaveVerifyRuntime 门控壳（-Dsubterra.probe.saveverify 默认 no-op、[Subterra saveverify] marker 并入 AsyncE2EProbe、服务器启动即验账本）；**p.2.10.6** SaveVerifyProbe 接入 probeAcceptance。/ p.2.10.1 engine.saveverify.chunk: zd-chunk partition + per-chunk tsha1f fast/strong verify metadata (Chunk/ChunkPlan/ChunkVerifier, deterministic fixed-order). p.2.10.2 engine.saveverify.sig: Ed25519 strong-signing core with deterministic keypair injection (JDK built-in, RFC 8032 deterministic signatures — Ed25519Keys/SaveSigner). p.2.10.3 verifiable-save archive (engine.saveverify root pkg): **single td doc saveverify=[version,meta,config,payload,chunks,sig]** (over the p.2.3 doc form) — Ed25519 head signature + tsha1f dual-chunk marks + seq replay gate + **five corruption classes deterministically rejected** (SaveVerifyArchive/SaveVerifyLedger/VerifyResult). p.2.10.4 SaveVerifyProbe 15 deterministic checks (flip/truncate/reorder/wrong-key/replay rejected, intact archive + round-trip accepted). p.2.10.5 runtime.saveverify.SaveVerifyRuntime gated shell (-Dsubterra.probe.saveverify default no-op, [Subterra saveverify] marker merged into the AsyncE2EProbe boot, ledger verified on server start). p.2.10.6 SaveVerifyProbe wired into probeAcceptance. / probeAcceptance 全绿（79 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world+saveverify marker） / probeAcceptance all green (79 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world+saveverify markers together). | landed / 已落地 |
| p.2.11 | Session programmable / 会话可编程（**革命·新增**）：外部进程（调试器 / 编辑器 / tie 脚本 / 工具）经 tink 帧驱动游戏运行时——tink hub 形态 B（接 p.2.5）；游戏成为可编程运行时。落地小结 / landing notes（p.2.11.1–p.2.11.5，收尾 p.2.11.6）：**p.2.11.1** engine.session 会话/命令协议核心——确定性命令信封 + 固定命令动词语义，载荷走 p.2.5 zd 载荷通道（SessionCommand/SessionCommandKind/SessionEnvelopeCodec）；**p.2.11.2** 指令化会话状态机——连接/鉴权/命令派发/状态回读/断开确定性转移 + 单调序号门（乱序/重放均拒，SessionMachine/SessionState/SessionMachineResult）；**p.2.11.3** 外部进程帧协议入口——会话命令经 p.2.5 tink v2 帧 ABI（ext TLV session/seq/key9 meta，坏帧拒绝，SessionFrame/SessionFrameCodec）；**p.2.11.4** SessionProbe 35 项确定性断言（命令信封/帧往返、状态机脚本序列、坏帧/乱序/重放拒绝，纯 JVM 接入 probeAcceptance）；**p.2.11.5** runtime.session.SessionRuntime 门控壳（-Dsubterra.probe.session 默认 no-op、[Subterra session] marker 并入 AsyncE2EProbe、五次开服不变）。/ p.2.11.1 engine.session session/command-protocol core over the p.2.5 zd payload channel (deterministic command envelope + fixed command verbs — SessionCommand/SessionCommandKind/SessionEnvelopeCodec); p.2.11.2 command-ized session state machine — deterministic connect/auth/dispatch/read-back/disconnect with a monotonic-seq gate (reorder/replay both rejected — SessionMachine/SessionState/SessionMachineResult); p.2.11.3 external-process frame-protocol entry — session commands over the p.2.5 tink v2 frame ABI (ext TLV session/seq/key9 meta, bad frames rejected — SessionFrame/SessionFrameCodec); p.2.11.4 SessionProbe 35 deterministic checks (envelope/frame round-trips, scripted state-machine sequences, bad-frame/reorder/replay rejection, pure-JVM wired into probeAcceptance); p.2.11.5 runtime.session.SessionRuntime gated shell (-Dsubterra.probe.session default no-op, [Subterra session] marker merged into the AsyncE2EProbe boot, five boots unchanged). / 结论：会话可编程运行时抽象——命令/状态机/帧入口复用 p.2.5 tink v2/tsha1f/zd ABI，runtime 门控壳并入 AsyncE2EProbe、五次开服不变。probeAcceptance 全绿（80 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world+saveverify+session marker）。/ Conclusion: a session-programmable runtime abstraction — command/state-machine/frame entry reuse the p.2.5 tink v2/tsha1f/zd ABI, the gated runtime shell merged into the AsyncE2EProbe boot, five boots unchanged. probeAcceptance all green (80 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). | landed / 已落地 |
| p.2.12 | Schema-first scaffolding（**革命·新增**）：td schema → 自动生成注册码 / 探针 / 文档 / 表单——模组编写从「写代码」变「写数据」。落地小结 / landing notes（p.2.12.1–p.2.12.5，收尾 p.2.12.6）：**p.2.12.1** engine.schema td schema 模型——确定性定型 / 校验 / 遍历（Schema / SchemaField / SchemaKind / SchemaCodec / SchemaReport / SchemaViolationException，td 为单一事实源、固定字段序）；**p.2.12.2** engine.scaffold.RegisterCodeGen——schema→注册接线代码，同 schema 同字节、往返恒等；**p.2.12.3** engine.scaffold.SchemaStubGen——schema→探针骨架 + 开发文档文本，同 schema 同字节、往返恒等；**p.2.12.4** engine.scaffold.FormDataGen——schema→表单结构 / 渲染数据纯数据，kind→widget 固定映射（UI 留 p.2.23）；**p.2.12.5** ScaffoldProbe 40 项确定性断言（schema 定型 / 违约 / validate + 三生成器逐字节一致与往返恒等，接入 probeAcceptance）；顺带修复 **TdTable 首现去重吞掉重复键**的死代码缺陷（duplicates() 附加 API）。/ p.2.12.1 engine.schema td-schema model — deterministic parse / validate / walk (Schema / SchemaField / SchemaKind / SchemaCodec / SchemaReport / SchemaViolationException, td as the single source of truth, fixed field order); p.2.12.2 engine.scaffold RegisterCodeGen — schema→register-wiring code, same schema → same bytes, round-trip stable; p.2.12.3 engine.scaffold SchemaStubGen — schema→probe skeleton + dev-doc text, same schema → same bytes, round-trip stable; p.2.12.4 engine.scaffold FormDataGen — schema→form-structure / render-data pure data, fixed kind→widget map (UI in p.2.23); p.2.12.5 ScaffoldProbe 40 deterministic checks (schema type / validate / reject + three generators byte-identical and round-trip stable, wired into probeAcceptance); incidental fix: the **TdTable first-occurs dedup that swallowed duplicate keys** dead-code defect (duplicates() added). / 结论：td schema 为单一事实源的确定性脚手架——定型 / 注册码 / 探针文档 / 表单四生成器同 schema 同字节，ScaffoldProbe 40 项断言接入 probeAcceptance。probeAcceptance 全绿（81 纯 JVM + 五次 E2E）。/ Conclusion: a td-schema-as-single-source-of-truth deterministic scaffold — typing / register-code / probe-doc / form-data four generators share one schema and one byte stream; ScaffoldProbe 40 checks wired into probeAcceptance. probeAcceptance all green (81 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.13 | Deterministic PCG / 确定性内容生成 engine.pcg（**革命·新增**）：名称 / 文本 / 配方 / 遗物 / 群系变种 / 本地化的**生成器编排底座**——纯函数、同种子一致（接 schema-first p.2.12 / worldgen）；「无尽探索」的框架化底座 | pending / 待开工 |
| p.2.14 | Diegetic interaction / engine.interact（**革命·新增**）：「无系统气味」的可落实交互层——世界内交互规范与零 HUD 化渲染规则（与 engine.ui p.2.22 调和，panels 在 p.2.22 仅作管理视图） | pending / 待开工 |
| p.2.15 | Engine migration + golden tests / engine 迁移（自原 p.2.2 顺移）：现有 optim / config / log 纯 JDK 核心迁入 engine.*；vanilla 数值对照固化为 golden tests 随改动回归 | pending / 待开工 |
| p.2.16 | API contract surface / api 契约面（自原 p.2.1 顺移）：worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API | pending / 待开工 |
| p.2.17 | Rule system / 规则系统 engine.config.rules（自原 p.2.4 顺移，参考 RollingGate 模型）：类型化规则、双层配置（全局 + 存档覆盖）、校验、热重载、`/subterra rule` 命令 | pending / 待开工 |
| p.2.18 | Export hub / 导出器 engine.export（自原 p.2.3 顺移）：language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令 | pending / 待开工 |
| p.2.19 | Runtime wiring migration / runtime 接线迁移（自原 p.2.5 顺移）：launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入，export 指令接线（接 p.2.18） | pending / 待开工 |
| p.2.20 | devkit complete / devkit 完整化 + 官方示例模组脚手架（自原 p.3.1 前移，**可用判据**）：probes 全量接线 + 最小「颠覆性模组」示例（兼作框架自举验收） | pending / 待开工 |
| p.2.27.1 | Flywheel port / 飞轮移植前哨（2026-09-09 新增，形态 A）：Flywheel 1.0.6（1.21.1/NeoForge 线，MIT 直接包含，保留声明）→ engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 钩子）+ 装载与后端确定性探针；作为 p.2.27 实例化基座前哨（一库一子项） | pending / 待开工 |
| p.2.27 | Self-developed rendering / 自研渲染管线（自原 p.4.2 顺移；2026-09-09 前移，clean-room，参考 Sodium/Embeddium 等登记项）：原版保真块/实体渲染，与 p.2.27.1 实例化基座互补 | pending / 待开工 |
| p.2.21 | Animation / 动画 engine.anim（自原 p.2.6 顺移）：GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding | pending / 待开工 |
| p.2.22 | UI / HUD core / engine.ui（自原 p.2.7 顺移）：AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact） | pending / 待开工 |
| p.2.23 | In-game mod hub / 游戏内模组 Hub（自原 p.2.8 顺移）：td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令 | pending / 待开工 |
| p.2.24 | AI behavior core / engine.ai（自原 p.2.9 顺移）：clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding | pending / 待开工 |
| p.2.25 | Entity scale / 实体缩放 engine.scale（自原 p.3.2 前移入主线）：Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）：ScaleType/ScaleData/ScaleModifier 模型、按维度/标签缩放、数据包缩放规则 + runtime scale binding | pending / 待开工 |
| p.2.26 | Time scaling / 时间缩放 engine.time（自原 p.3.3 前移入主线，clean-room 自研）：TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线（接 engine.sim p.2.8） | pending / 待开工 |

## Sequence Notes / 顺序说明

* 小任务逐个提交：每完成一个小任务即报告并提交一次，得确认后再做下一个。 / Small tasks are submitted one at a time; each is reported and committed before the next starts.
* 所有子任务完成后统一 review + 清理 + 推送。 / After all sub-tasks complete: one review, cleanup, and push.
* 正式发行 r.x.y.z 从对应 p 版本切出，只做优化与稳定性验证，不引新功能。 / A release `r.x.y.z` is cut from its p version and only stabilizes.
* 许可纪律：permissive（MIT / Apache-2.0 / BSD / Unlicense / ISC）可移植并保留声明；copyleft（GPL / LGPL / AGPL / MPL）仅 clean-room 参考；存疑按不允许处理；完整登记册见框架设计 §7。 / License discipline: permissive may be ported with notices; copyleft is clean-room reference only; uncertain defaults to not allowed; full register in framework design §7.

## Port Allocation Map / 移植落点分配表

* 功能分类（非来源分类）：所有移植按功能域落进 engine.*（纯 JDK 核心）与 runtime.*（MC 层接线）；纯逻辑核心独立纯 JDK 包并可探针。 / Functional organization: every port lands in a functional domain under engine.* (pure-JDK core) or runtime.* (MC wiring); pure-logic cores stay standalone pure-JDK with probes.

| Pending target / 待移植目标 | Function / 功能 | Landing / 落点 | License path / 许可路径 | Status / 状态 |
| --- | --- | --- | --- | --- |
| GeckoLib | 3D 关键帧动画引擎（实体/方块/物品/盔甲） | engine.anim + runtime.anim | MIT 移植，保留声明与捆绑库声明 | planned / 已立项 |
| AppleSkin | HUD 数据层（饱食/饱和度覆盖、食物 tooltip） | engine.ui | Unlicense 全量移植 | planned / 已立项 |
| ModMenu | 模组列表交互模型与元数据 API 模式 | engine.ui + runtime.ui | MIT 借鉴（NeoForge 接线自研） | planned / 已立项 |
| SmartBrainLib | Brain 编排模型 | engine.ai | MPL-2.0 clean-room 参考 | planned / 已立项 |
| C2ME | 异步区块生成/加载/I/O + FlowSched | engine.worldgen.async + runtime | MIT 直接包含（NeoForge ver/1.21.1） | planned / 已立项（p.1.4.13 可选采纳 → 升级为直接包含） |
| Export-Language-Keys-for-Compasses | 语言键全量导出 | engine.export | MIT 借鉴 | planned / 已立项 |
| RollingGate | 规则系统模型（类型化规则 / 双层配置 / 命令面板） | engine.config.rules | LGPL-3.0 clean-room 模型参考；资产 CC-BY-NC-ND 不用 | planned / 已立项 |
| ServerCore 系列 | 实体激活范围 / mobcap / 村民脑死亡 / 同步加载 / 区块 tick 缓存 / 动态距离 / 繁殖合并 | engine.optim + runtime.optim-shell | MIT 部分直接 + GPL 部分 clean-room | landed / 已落地（p.1.4 全线） |
| JEC 读音搜索 | 语言无关读音搜索（zh/ja/ko/gr） | engine.optim.logic.pronounce + runtime.optim-shell | MIT | landed / 已落地（p.1.4.8-.10） |
| InvAdvOpt / SmoothBoot | 库存推进加速 / 线程调优 | runtime.optim-shell | MIT | landed / 已落地（p.1.4.4/.5） |
| Traveler Title / Weather / Super Resolution / 皮肤补丁 | 标题提示 / 天气 / 超分 / 皮肤（自研化） | engine.* + runtime.* | LGPL/GPL clean-room | planned / 已立项 |
| Physics Mod | 方块/生物物理 | — | All Rights Reserved，不适用 | dropped / 剔除 |
| TimeScaleLib | 时间缩放（子弹时间） | — | PolyForm Shield 非标准变体（Noncompete，存疑）→ 不适用；能力 clean-room 自研 | dropped / 剔除（自研替代） |
| DataTip | 数据驱动 tooltip（物品→tooltip 行） | engine.ui | GPL-3.0 clean-room 参考（td 化，零 json） | planned / 已立项 |
| Patchouli | 数据驱动书籍/文档 GUI（手册/图鉴/教程） | engine.ui.docbook | CC-BY-NC-SA 3.0 clean-room 参考（td 化，零 json） | planned / 已立项 |
| Feature Recycler / VoxelBridge / OptiCores | 待定义 | — | All Rights Reserved 或语义不匹配 | dropped / 剔除 |
| StellarRTP | 随机传送 | engine.optim.server.teleport | GPLv3 clean-room 参考 | planned / 已立项 |
| Itemban | 物品使用控制 | engine.optim.server.item_control | Apache-2.0 | pending / 待开工 |
| mcwifipnp | LAN 打洞 | runtime.network.holepunch | 移植为原生模块 | pending / 待开工 |
| Pehkui-Rebuilt | 实体尺寸缩放（ScaleType / ScaleData / ScaleModifier / ScaleRegistries、按维度/标签、数据包规则） | engine.scale + runtime.scale | MIT 移植，保留 Virtuoel 原始版权与重建声明 | planned / 已立项 |
| Flywheel | GPU 实例化渲染 + 着色器模板系统（实体 / 方块实体路径） | engine.render.instancing + runtime.render | MIT 直接包含（形态 A，保留声明；p.2.27.1 前哨） | planned / 已立项 |

## Progress Log / 进度记录

* 2026-09-08 — p.2.0 landed: module re-layout api / engine / runtime / migrate / devkit + IronLawProbe dependency iron law, probes green / 2026-09-08 — p.2.0 落地：api / engine / runtime / migrate / devkit 模块重构 + IronLawProbe 依赖铁律，探针全绿。
* 2026-09-08 — Patchouli assessed / Patchouli 评估：CC-BY-NC-SA 3.0 → 仅思想参考；文档书籍 GUI clean-room 自研（engine.ui.docbook，td 驱动零 json）。 / 2026-09-08 — Patchouli 评估：CC-BY-NC-SA 3.0 → 仅思想参考；文档书籍 GUI clean-room 自研（engine.ui.docbook，td 驱动零 json）。
* 2026-09-08 — DataTip assessed / DataTip 评估：GPL-3.0 → 仅思想参考；数据驱动 tooltip 数据层 clean-room 自研（engine.ui，td 化，零 json，与 AppleSkin 协同）。 / 2026-09-08 — DataTip 评估：GPL-3.0 → 仅思想参考；数据驱动 tooltip 数据层 clean-room 自研（engine.ui，td 化，零 json，与 AppleSkin 协同）。
* 2026-09-08 — TimeScaleLib assessed / TimeScaleLib 评估：PolyForm Shield 非标准变体（Noncompete）→ 不可移植，框架时间缩放能力 clean-room 自研（engine.time，p.3.3）。 / 2026-09-08 — TimeScaleLib 评估：PolyForm Shield 非标准变体（Noncompete）→ 不可移植，时间缩放能力 clean-room 自研（engine.time，p.3.3）。
* 2026-09-08 — Pehkui-Rebuilt registered / Pehkui-Rebuilt 入册：实体缩放能力纳入框架（engine.scale，MIT 移植，保留 Virtuoel 原始版权）。 / 2026-09-08 — Pehkui-Rebuilt 入册：实体缩放能力纳入框架（engine.scale，MIT 移植，保留 Virtuoel 原始版权）。
* 2026-09-08 — Roadmap split per repo / 路线图按仓库拆分：Subterra 框架线迁入本文件，系列 ROAD 仅保留总览。 / 2026-09-08 — 路线图按仓库拆分：Subterra 框架线迁入本文件，系列 ROAD 仅保留总览。
* 2026-09-08 — **Framework design finalized / 框架设计定稿**: Subterra promoted to a full-stack development framework mod (design `docs/2026-09-08-subterra-framework-design.md`); port register extended (GeckoLib / AppleSkin / ModMenu / SmartBrainLib / C2ME / Export-Language-Keys-for-Compasses / RollingGate; Physics Mod dropped as All Rights Reserved). / 2026-09-08 — **框架设计定稿**：Subterra 升格为全栈开发框架模组；移植登记册扩展（…；Physics Mod 因 All Rights Reserved 剔除）。
* 2026-09-08 — p.1.8.32 landed (perf: dedup climate-spline/sloped-cheese evals, CachedDensity single-slot ThreadLocal) — pre-framework line last item. / 2026-09-08 — p.1.8.32 落地（性能：气候样条/乳酪去重缓存）——框架化前最后一项。
* 2026-09-09 — Flywheel assessed / Flywheel 评估：MIT → 形态 A「直接包含」（1.0.6，1.21.1/NeoForge 线）入册，划为 p.2.27 自研渲染管线的前哨子项 p.2.27.1（engine.render.instancing + runtime.render）；自研渲染管线前移至 p.2.20 之后、p.2.21 动画之前（渲染线提前，实例化基座供后续 runtime 渲染接线共用）。 / 2026-09-09 — Flywheel assessed: MIT → direct-include 1.0.6 (1.21.1/NeoForge line) registered as p.2.27.1 (advance guard for p.2.27); the self-developed render line was advanced to right after p.2.20.
* 2026-09-09 — p.2.7 landed: engine.parallel 确定性并行执行底座（p.2.7.1-5，全量验收全绿）——KeyPartition 通用确定性键分区（与 p.2.6 ChunkPartition 双 long 路径逐位一致）、WorkerBank 有界串行 lane、DeterministicDispatcher 重复键显式拒绝（契约升级：last-write-wins → IllegalArgumentException）、ParallelRunner 并行/串行逐位一致 + 批次生命周期、optim.sched 组合探针（池化暂存不破确定性）；ChunkKey 域与 p.2.6 归并序一致；probeAcceptance 全绿（72 纯 JVM + 五次 E2E）。/ 2026-09-09 — p.2.7 landed: engine.parallel deterministic parallel execution base (p.2.7.1-5, acceptance all green) — generic KeyPartition (two-long path bit-identical to p.2.6 ChunkPartition), bounded serial WorkerBank, DeterministicDispatcher with explicit duplicate-key rejection (contract upgrade: last-write-wins → IllegalArgumentException), ParallelRunner with byte-identical serial golden path + batch lifecycle, optim.sched combination probe (pooled scratch stays byte-identical); ChunkKey-domain merge order matches p.2.6; probeAcceptance all green (72 pure-JVM + five E2E boots).
* 2026-09-09 — p.2.8 landed: engine.sim 增量确定性模拟底座（p.2.8.1-6，全量验收全绿）——budget 预算分级调度（per/region/global 三级 cap、原子记账、确定性 key 序降级，p.2.26 前哨）、spatial 分区空间索引（半径邻域只访问局部窗口、无全局扫描，cell 键经 p.2.7 双 long 路径入 bank）、core 增量确定性核心（advance(dirty) 只推进变化局部、逐 key 顺序无关、diff/回放与全量重算逐字节一致）、run per-simulant 隔离 + 确定性归并门面（SimRunner 直接消费 p.2.7 ParallelRunner，并行/串行逐字节一致）、SimCombineProbe 全轨组合简景、runtime.sim.SimRuntime 门控默认 no-op 壳（[Subterra sim] marker + 四微型确定性校验，并入 AsyncE2EProbe 同开服，五次开服总数不变）；probeAcceptance 全绿（77 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim marker）。p.2.9 前哨提示词见 docs/plans/prompt-p.2.9-artifact.md。/ 2026-09-09 — p.2.8 landed: engine.sim incremental deterministic simulation base (p.2.8.1-6, acceptance all green) — budget-tier scheduling (per-simulant/region/global caps, atomic accounting, deterministic key-order degradation, the p.2.26 precursor), partitioned spatial index (local-window neighborhood only, no global scan, cell keys through the p.2.7 two-long path into banks), incremental deterministic core (dirty-set advance, order-independent per-key ticks, diff/replay byte-identical to full recompute), per-simulant isolation + deterministic merge facade (SimRunner over the p.2.7 ParallelRunner), the SimCombineProbe full-stack caricature, and the gate-off runtime.sim.SimRuntime shell ([Subterra sim] markers + four micro-checks, merged into the AsyncE2EProbe boot — five boots unchanged); probeAcceptance all green (77 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim markers together). p.2.9 preview prompt: docs/plans/prompt-p.2.9-artifact.md.
* 2026-09-09 — p.2.9 landed: World-as-artifact 世界即产物（p.2.9.1-6，全量验收全绿）——世界包**统一档案形态**（engine.world.WorldPack 单一 td 文档 world=[version,meta,save,datapack]，save/datapack 为 base64 载荷、meta TreeMap 排序、段序固定）+ export(rehydrate(export(w))) 逐字节往返契约 + p.2.8 稳态 meta 键衔接；六槽/藏录经 SaveContainer 槽复用、DatapackPack 数据包并入；WorldPackPacker 世界工作树目录↔单 td 打包/还原（只扫六固定 SaveSlot 目录、无全局遍历、路径穿越防护，可搬移互导）；engine.export 导出端口预留（p.2.18 前哨，ExportKind/ExportPort/ExportHub，WORLD_PACK + SAVE 两内建端口确定性 preload）；runtime.world.WorldRuntime 门控壳并入既有开服（-Dsubterra.probe.world 默认 no-op、[Subterra world] marker、staging 隔离不写服务器，五次开服不变）；WorldPackProbe 20 项确定性断言；probeAcceptance 全绿（78 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world marker）。p.2.10 前哨提示词见 docs/plans/prompt-p.2.10-save-verifiability.md。/ 2026-09-09 — p.2.9 landed: World-as-artifact (p.2.9.1-6, acceptance all green) — uniform world-pack archive form (engine.world.WorldPack, single td doc world=[version,meta,save,datapack], base64 payloads, TreeMap-sorted meta, fixed section order) + byte-exact export(rehydrate(export(w))) round-trip + p.2.8 steady-state meta keys; six-slots/Ledger reused via the SaveContainer slots, DatapackPack datapacks folded in; WorldPackPacker world-work-tree dir↔single-td pack/restore (only the six fixed SaveSlot dirs scanned, no global scan, path-traversal guards, movable/interchangeable); engine.export port cut reserved (p.2.18 seam — ExportKind/ExportPort/ExportHub, WORLD_PACK + SAVE built-ins deterministically preloaded); the gated runtime.world.WorldRuntime shell merged into the existing boots (-Dsubterra.probe.world default no-op, [Subterra world] markers, staging-isolated no server write — five boots unchanged); WorldPackProbe 20 deterministic checks; probeAcceptance all green (78 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world markers together). p.2.10 preview prompt: docs/plans/prompt-p.2.10-save-verifiability.md.
