# Subterra Roadmap / Subterra 路线图

**Status / 状态:** Active / 生效中
**Date / 日期:** 2026-09-08（自系列 ROAD 拆分 / split from series ROAD）
**Version discipline / 编号纪律:** Development uses the pre-release p-track only — `p.<release>.<module>.<sub-item>` (max three levels, no "stage/session" grouping). The release track `r.x.y.z` is cut from a specific p version at stabilization time and introduces no new features. Planning never uses milestone-style labels such as R1/R2. / 开发计划一律只用 p 轨 `p.<发布档>.<模块>.<子项>`（最多三级，禁用「阶段X」分组与 R1/R2 式标签）；`r.x.y.z` 为正式发行轨，在基于某个 p 版本时切出，只做优化与稳定、不引新功能。

## Frame / 框架基线

* Subterra 为全栈开发框架模组（NeoForge 21.1.x / MC 1.21.1），设计见聚合仓库 `docs/2026-09-08-subterra-framework-design.md`。 / Subterra is a full-stack development framework mod (NeoForge 21.1.x / MC 1.21.1); design in aggregate-repo `docs/2026-09-08-subterra-framework-design.md`.
* p.1 线（骨架 / L1 启动 / L2 兼容 / L3 API / 优化集 / td 配置 / 日志 / 世界生成管线）全部落地，构成框架化基线；vanilla 保真工作（p.1.8.20–32）保留为默认预设并固化为 golden tests。 / The p.1 line is fully landed as the framework baseline; vanilla-fidelity work stays as the default preset and is frozen into golden tests.
* p.2 线起为框架化：模块重构为 api / engine / runtime / migrate / devkit，服务大型、颠覆原版、不考虑兼容性的模组（首个真实消费方为 Overturn 世界生成拟真先锋，先行验证装配面；Toterra 为远期目标）。 / From p.2 the line is the framework: modules re-laid as api / engine / runtime / migrate / devkit, serving large, vanilla-overturning, compatibility-agnostic mods (the first real consumer is Overturn, the worldgen-realism vanguard validating the assembly seams; Toterra is the long-term goal).

## Milestone Map / 里程碑映射

* Every milestone below is a p-track item; sub-items continue the track per "one library = one sub-item". / 以下里程碑均为 p 轨条目；子项沿 p 轨推进，一库一子项。
* Acceptance is deterministic probes only (server boots / client enters world / probes PASS), never timing assertions. / 验收一律确定性探针：服务端开服、客户端进世界、探针断言全 PASS；禁时序断言。
* 2026-09-08 — 新增三项数据层重构：**数据包重构**（td 为数据包内容一等语言，数据包逻辑可直接以 tie 编写，替代手写 JSON）+ **存档重构**（zd+td 混合替代原版 NBT `.dat`，减小体积 / 加快加载）+ **服务端交互重构**（增强通道：tink v2 帧 + zd 载荷 + 增量差分，性能更好带宽更小）。 / Added three data-layer reworks: datapack (td first-class, logic in tie), save (zd+td hybrid), and server-interaction (enhanced channel: tink v2 frames + zd payloads + delta sync).
* 2026-09-09 — p.2.2.4 落地（loot 真合并 + worldgen/structure 真注册）：**recipe 规范形往返**（td→ShapedRecipe/SmokingRecipe→规范串，canonical markers 逐字符一致）、**loot_table 真合并进 LOOT_TABLE datapack registry**（1.21.1 无 LootDataManager；`ReloadableServerResources.fullRegistries().get()` RELOADABLE 层，经 `DatapackRegistryInjector` 公开 unfreeze→register→freeze 窗口注入）、**worldgen→CONFIGURED_FEATURE**（configured_feature 最小 schema：simple_block + block）、**structure→STRUCTURE/STRUCTURE_SET**（DesertPyramidStructure 体 + biomes holder）；E2E 16 标记全绿。 / p.2.2.4 landed: loot merge into the LOOT_TABLE datapack registry via a public un-freeze window + worldgen/structure real registration; E2E 16 markers green.
* 2026-09-09 — p.2.2.5 落地（DataPack→td 导出往返）：**RecipeManager → td → 回灌 → 再导出逐字节一致**——engine.datapack `RecipeDatum`（record，read/write 规范 td，count/cooking_time clamp≥1、experience 经 cleanExperience float 往返归一化，镜像 buildShaped/buildSmoking 语义）+ `DatapackExporter` 导出前门；runtime `DatapackRecipeExporter`（holder→datum，pattern 行按格首次出现确定性重建字符 A,B,C…）→ `DatapackRegistrar.runExportRoundTrip`（导出→buildRecipe 回灌→再导出，字节级比对，marker ok）+ 类 javadoc；纯 JVM 探针：导出→DatapackLoader 文件重载→再导出逐字节一致 + 三连稳定 + 导出字段无损（=源 payload 序列化，6 新断言）；E2E 标记 16→18（`export roundtrip toterra:example/smoke ok`）；probeAcceptance 全绿。 / p.2.2.5 landed: DataPack→td export round-trip byte-exact (holder→td→buildRecipe→td), engine RecipeDatum schema + mc-shell exporter + registrar round-trip markers + JVM byte-identical probe; E2E 18 markers green.
* 2026-09-09 — p.2.2.6 落地（导出全种类 + 导出档案 Hub）：**导出器覆盖全部七类**（RECIPE 对象级逐字节往返；TAG/LANG 归一化 TagDatum/LangDatum；LOOT_TABLE/WORLDGEN/STRUCTURE/FUNCTION canonical pass-through、schema 由注册器消费方重建验收）+ **内容级导出档案 DatapackExportArchive**（`type tie<data>` 单文档 `export=[version,entries]`，export∘rehydrate 逐字节恒等，DatapackPack 文件级之外的第二档案形态，p.2.18 engine.export 前哨）；`DatapackRegistrar` 统一 marker `export roundtrip <kind> <id> ok` + `export archive roundtrip ok (entries=10)`；纯 JVM 探针 10 条目文件重载往返逐字节一致 + 档案往返 + 字段无损；E2E 标记 18→27；probeAcceptance 全绿。 / p.2.2.6 landed: export covers all seven kinds + content-level export archive (byte-exact rehydrate identity); E2E 27 markers, full gate green.
* 2026-09-09 — p.2.2.7 落地：/subterra export 指令接通（p.2.18 engine.export 前哨）——`/subterra export [<path>]`（op 级 2）把已装载 pack 逐包经 DatapackExportArchive 导出为内容级 td 档案文档到指定目录（缺省 subterra-export），并对每份文档立即 export∘rehydrate 逐字节回灌校验；E2E 经确定性属性钩子触发（`subterra.probe.export` 经 gradle -P 可靠转发到 `runServer`，startup 即跑同一导出核心，规避 gradle-forked server JVM 的 stdin 双层中转不可靠），新增 2 marker；probeAcceptance 全绿。 / p.2.2.7 landed: /subterra export command writes content-level export-archive docs per pack with byte-identical rehydrate check, driven deterministically at server startup via a forwarded gradle property (stdin double-hop through the forked server JVM is unreliable); 2 new markers.
* 2026-09-09 — p.2.2.8 落地：数据包双接口（td + API）——engine DatapackLookup（entry/byKind/tdText）定型类型化读 API 与 td 文本路由并存：API 直达同一 DatapackEntry 实例（身份保持）、td 路由逐字节等于导出规范形（tie 消费路径=导出 td + tiec parse_data 同构 schema，FFM 桥接见 p.2.1）；纯 JVM 探针断言双路径逐字节一致 + Td 可解析。 / p.2.2.8 landed: datapack dual interface — typed DatapackLookup API route and td-text route agree byte-for-byte; tie consumption documented (exported td + tiec parse_data, FFM via p.2.1).
* 2026-09-09 — p.2.2.9 落地：数据包级规则 + 存档覆盖优先级——pack.td `rules`（`[ [ k, v ] ]`，任意 td 值）→ DatapackRules.fromManifest/resolve/render（确定性；later pack 覆盖 earlier、save override 覆盖全部）；runtime 经 `subterra.override` 属性或覆盖文件读入存档覆盖，logRules 输出 `rules <k> = <v>` 确定性 marker；JVM 断言提交序（无覆盖 wild=true、覆盖后 wild=false、未覆盖键保持）+ E2E 2 marker；接 p.2.17 规则系统前哨。 / p.2.2.9 landed: pack-level rules with save-override precedence (pack order, then save overrides win), deterministic markers — forward hook to p.2.17.
* 备注 / Note：p.2.2 全部子项均沿用 p.2.2.x 三级编号（p.2.2.1…p.2.2.9），无阶段式标签（阶段X / R1/R2）。 / All p.2.2 sub-items use the p.2.2.x three-level numbering only; no stage/session-style labels.
* 2026-09-09 — p.2.2.1 落地：**纯 td 直载**（不产出 JSON）——engine.datapack 容器 + 注册表 + tie 逻辑链，DatapackProbe 26 断言全绿；顺带修复共享 Td 解析器 header 误杀 `type = "..."` 数据键与注册表排序丢失两 bug。 / p.2.2.1 landed: pure td direct loading, engine.datapack registry + tie logic chain.
* 2026-09-09 — p.2.2.2 落地（MC 壳注册接线）：runtime.datapack——**recipe 编译为原版 ShapedRecipe → RecipeManager.replaceRecipes 合并**（crafting_shaped），tag/lang 索引进内存 + 确定性 markers，**function 经 TieLogicBundle → engine.tie 0 参调用**；DatapackE2EProbe 开服断言全绿；两开服探针 mustRunAfter 串行（并行抢 run/world session.lock）；E2E staging 隔离 build/tmp（避开 bootProbe 的 Windows dll 句柄）。 / p.2.2.2 landed: MC-shell registration wiring — recipes into RecipeManager, tag/lang indexes, tie functions called; E2E gate green; boot gates serialized.
* 2026-09-09 — p.2.2.3 落地（打包往返 + 配方类型扩展 + 剩余三类 staging）：**DatapackPack**（目录↔单一 td 文档，base64 content，解包重载注册表/包名/td 载荷逐字节一致）+ **smoking→SmokingRecipe**（buildRecipe 按 type 分派）+ loot/worldgen/structure 三类的 staged 索引与 markers（LootDataManager/RegisterEvent 真注入留 p.2.2.4，不在本块硬塞）；独立探针 31 断言、E2E 10 标记全绿。 / p.2.2.3 landed: DatapackPack round-trip, smoking recipe type, remaining kinds staged with markers; deep vanilla injection deferred to p.2.2.4.
* 2026-09-09 — p.2.4 落地（服务端交互重构-增强通道，革命）：engine.network 五子引擎层（frame tink v1 crc32 载体 + v2 ABI 常量 + zd 载荷桥；integrity tsha1f 便携 Java + FrameV2 快/强校验槽与流式分块；strategy 三级载荷策略；bandwidth 削减优化器；crypto x25519+AEAD）为 p.2.5 P2P 同栈基座（语言无关 ABI）+ runtime.network.EnhancedChannelRuntime 门控渐进增强壳（原版握手零接触），NetworkE2EProbe 第四次开服；六探针接入 probeAcceptance，全量验收全绿。 / p.2.4 landed: five pure-JVM engine.network cores (frame/integrity/strategy/bandwidth/crypto, the language-agnostic p.2.5 same-stack base) + EnhancedChannelRuntime gated progressive shell (vanilla handshake untouched), six probes wired into the acceptance gate, all-green.

| p-track / p 轨 | Milestone / 里程碑 | Status / 状态 |
| --- | --- | --- |
| p.1.0–p.1.8 | Framework baseline / 框架化基线：模块骨架、L1 启动、L2 Java25 兼容修补、L3 API 库、优化集（ScratchPool + 移植核心）、td 配置、日志、世界生成管线（九维模型 / 密度函数 / router / surface / 默认 vanilla 预设 / 探针） | landed / 已落地 |
| p.2.0 | Module re-layout / 模块重构（收尾中）：api / engine / runtime / migrate / devkit 分层落地，依赖铁律探针化，探针全绿保持 | landed / 已落地（收尾） |
| p.2.1 | tie bridge / tie 桥（**革命基座**，自原 p.4.0 前移）：tiec → DLL → FFM 加载调用，热点路径 tie 化 + 性能验证 | landed / 已落地 |
| p.2.2 | Datapack rework / 数据包重构（**革命**，自原 p.2.10 前移）：td 为数据包内容一等语言——函数 / 配方 / 战利品表 / 世界生成 / 结构 / 标签 / 本地化 td 声明 + **纯 td 直载**（engine.datapack 注册表 + tie 逻辑链，探针闭环已落地）；数据包逻辑可直接以 tie 编写（tiec 编译 → tie bridge 装载，接 p.2.1），热点路径 tie 化；数据包打包分发（接 config 包）；td + API（含 tie）双接口、`/subterra export` 指令、数据包级规则 + 存档覆盖（接 p.2.17）均已落地 | landed / 已落地 |
| p.2.3 | Save rework / 存档重构（**革命**，自原 p.2.11 前移）：现代化存档形态——**zd + td 混合替代原版 NBT `.dat`**（level / player / 侧数据），减小体积、加快加载（zd 压缩变体 / 字典 / 零拷贝）；统一 SaveContainer（世界 + 配置 + 藏录 Ledger + 领域档案 + 遗物条目 + 台账）；双层配置（全局 + 存档覆盖，接 p.2.17）；迁移走 subterra-migrate；元数据可导出（接 p.2.18 export）。落地小结 / landing notes（p.2.3.1–p.2.3.6）：**p.2.3.1** engine.zd 通用 zd v2 载体（头部规范抽自 profiler ZdWriter，10 字节头精确保持 + 固定六字段 wire2 记录读写 + 树平铺往返）+ 六槽 SaveContainer 骨架（规则槽叠加 = DatapackRules 语义）；**p.2.3.2** level.dat 语义迁移 → zdt 混合文档（td 元数据 + base64 zd 载荷）+ runtime.save 壳（ServerStarted 旁路，`-Dsubterra.probe.save` 门控 no-op，E2E 标记）；**p.2.3.3** player 迁移（NBT 根复合 → PlayerDatum → zdt，复用通用 ZdtTransfer 载体）；**p.2.3.4** 藏录 / 领域 / 遗物 / 台账四槽类型化文档（确定性排序 + 线性构建防 O(n²)，字段级往返）；**p.2.3.5** 双层配置（DatapackRules.resolveTiered 全局 → 存档胜出）+ subterra-migrate 首块迁移管线（api SPI + engine 实现 + ServiceLoader 运行时发现，migrate 仅依赖 api）；**p.2.3.6** SaveExportArchive td 导出档案（与 DatapackExportArchive 同构，export∘rehydrate 逐字节；/subterra export 的 save 接线留 p.2.18）。/ p.2.3.1 generic engine.zd carrier (pinned 10-byte header, six-field wire2 records, tree round-trip) + six-slot SaveContainer; p.2.3.2 level.dat→zdt hybrid doc + gated runtime.save shell (E2E marker); p.2.3.3 player→zdt (shared ZdtTransfer); p.2.3.4 four typed slot docs; p.2.3.5 two-tier config + first migrate SPI pipeline (api-only); p.2.3.6 td save export archive (byte-identical rehydrate; save cmd wiring in p.2.18). | landed / 已落地 |
| p.2.4 | Server-interaction rework / 服务端交互重构（**革命**，自原 p.4.3 前移）：客户端 ⇄ 服务端**增强通道**——tink v2 帧 + tsha1f 帧级强校验 + zd 载荷序列化（与 p.2.5 P2P 同栈，语言无关 ABI）；三级载荷策略（L1 高频增量差分 / 兴趣域订阅 · L2 中频快照 + 变更流 · L3 低频加密）；带宽削减 = 增量同步 / 状态降频插值 / 按需订阅；默认强加密（x25519 + AEAD，局域网可信可关）；原版协议路径保留（渐进增强，原版客户端仍可连）。落地小结 / landing notes（p.2.4.1–p.2.4.6）：**p.2.4.1** engine.network.frame——tink v1 crc32 帧载体（FrameV1/Iterator/Crc32Ieee）+ v2 ABI 常量（FrameConst）+ zd 载荷桥（ZdFrameBridge）；语言无关 ABI，p.2.5 同栈基座；**p.2.4.2** engine.network.integrity——tsha1f 便携 Java 实现（tie-main 金标向量 KAT 全中，含 n=8 快 8B 与 n=48 强档 32B）+ tink v2 帧编解码（FrameV2：快/强校验槽、ext TLV 未知键跳过、v1 兼容读、保留位拒绝、流式分块 split/join）+ 单字节篡改即拒；**p.2.4.3** engine.network.strategy——三级载荷策略（L1 高频增量差分+兴趣域订阅 / L2 中频快照+变更流 / L3 低频加密标记）：确定性选择器、增量 diff 往返、变更流有序回放；**p.2.4.4** engine.network.bandwidth——带宽削减优化器：增量同步/状态降频插值/按需订阅三技术量化权重 + argmax 选型（固定枚举序破平局），确定性命中计数，L3 加密适用性读局域网可信开关；**p.2.4.5** engine.network.crypto——x25519 协商 + SHA-256 KDF → AES-256-GCM（12B nonce = 8B BE seq + 4B 零，同密钥永不重复），往返/篡改拒绝/错钥拒绝，局域网可信可关（默认开，L3 策略接入）；**p.2.4.6** runtime.network.EnhancedChannelRuntime——ServerStartedEvent 旁路壳（-Dsubterra.probe.network 门控，默认 no-op、原版握手零接触）；接线断言组件装载 + original-path-preserved + E2E 第四次开服 NetworkE2EProbe。/ p.2.4.1 engine.network.frame: tink v1 crc32 carrier (FrameV1/Iterator/Crc32Ieee) + v2 ABI consts (FrameConst) + zd payload bridge (ZdFrameBridge); language-agnostic, the p.2.5 same-stack base. p.2.4.2 engine.network.integrity: portable tsha1f (tie-main golden-vector KAT all-pass, incl. n=8 fast 8B + n=48 strong 32B) + tink v2 frame codec (FrameV2: fast/strong slots, ext TLV unknown-skip, v1-compat read, reserved-bit reject, streaming split/join) + single-byte tamper rejection. p.2.4.3 engine.network.strategy: three-tier payload strategy (L1 high-freq incremental diff + interest subscribe / L2 mid-freq snapshot + change stream / L3 low-freq encrypted marker) — deterministic selector, incremental diff round-trip, ordered change-stream replay. p.2.4.4 engine.network.bandwidth: bandwidth-cuts optimizer — incremental sync / decimation-interpolation / on-demand subscribe quantified weights + argmax selection (fixed-enum order breaks ties), deterministic hit counts, L3-fit reads the LAN-trustable toggle. p.2.4.5 engine.network.crypto: x25519 + SHA-256 KDF → AES-256-GCM (12B nonce = 8B BE seq + 4B zero, same key never repeats), round-trip / tamper-reject / wrong-key-reject, LAN-trustable on by default (L3 hookup). p.2.4.6 runtime.network.EnhancedChannelRuntime: ServerStartedEvent shell (gated by -Dsubterra.probe.network, default no-op, vanilla handshake untouched); wiring asserts component load + original-path-preserved + fourth-boot NetworkE2EProbe. / 诚实披露 / honest disclosures：(a) p.2.4.6 壳为门控生命周期自练（增强帧在 ServerStartedEvent 时**进程内往返**）；真实 client⇄server 封包接线与延迟载荷通道在 p.2.5 落地；(b) 强 32B 完整性移植使用通用 state-per-n 核心，在 tie 金标 n=48 向量上验证相等（dual9/11h 为按需生成的对等性能等价项，非照抄）。 | landed / 已落地 |
| p.2.5 | P2P decentralized networking / P2P 去中心化网络（自原 p.4.1 前移）：tink v2 + tsha1f 帧级强校验；zd 作为自定义载荷通道通信介质（先可行性基准）。落地小结 / landing notes（p.2.5.1–p.2.5.8，全部纯 JDK、复用 p.2.4 engine.network 同栈、ABI 对齐 p.2.4）：**p.2.5.1** 可行性基准 `P2pChannelBenchmarkProbe`——**zd 作载荷通道可行**（raw-vs-zd 体积比 ≈0.47–0.51，即 ≈2× 以内；medium-table encode 5.1 / decode 19.7 ops/ms 满足吞吐；1MiB 帧强校验表头恒定 42B）；**p.2.5.2** engine.p2p `NodeId/NodeAddr(direct/viaRelay)/AddressBook` 寻址；**p.2.5.3** engine.p2p.handshake 确定性握手 + x25519 密钥双向绑定（篡改/错钥/伪造/乱序/重放拒）；**p.2.5.4** engine.p2p.transport 帧级传输（会话序号、有序重排、去重、确定性丢包/篡改/重放拒）；**p.2.5.5** engine.p2p.payload zd 载荷通道 ABI（payload==zd doc + 类型信封，meta 走 tink v2 ext key9）；**p.2.5.6** engine.p2p.plan 通道规划（策略 + 带宽编排）；**p.2.5.7** engine.p2p.discovery `KademliaLite` DHT 无中心发现（结构核心，RPC/线上层留 runtime）；**p.2.5.8** engine.p2p.relay 志愿 relay（登记→转发→投递、未登记 REJECT、超跳 DROPPED、shutdown 清表）+ **关停钩子接 MC 壳** `EnhancedChannelRuntime`（ServerStoppingEvent 清表，`[Subterra relay]` marker 新行、既有 network markers 原样保留）。去中心化原则全量落实（直连 + DHT + relay 无中心）。probeAcceptance 全绿（61 纯 JVM + 四次 E2E，原 NetworkE2EProbe 第四服、original-path-preserved 不变）。/ p.2.5.1 feasibility benchmark: **zd-as-channel works** (raw-vs-zd ≈0.47–0.51, i.e. ≈within 2×; medium-table encode 5.1/decode 19.7 ops/ms; 1MiB strong-frame header constant 42B). p.2.5.2 engine.p2p addressing (NodeId/NodeAddr direct|viaRelay/AddressBook). p.2.5.3 engine.p2p.handshake deterministic handshake + x25519 key binding (tamper/wrong-key/forged/reorder/replay reject). p.2.5.4 engine.p2p.transport frame-level channel (session seq, ordered re-assembly, dedup, deterministic loss/tamper/replay reject). p.2.5.5 engine.p2p.payload zd payload-channel ABI (payload==zd doc + type envelope, meta via tink v2 ext key9). p.2.5.6 engine.p2p.plan channel planner (strategy+bandwidth wiring). p.2.5.7 engine.p2p.discovery KademliaLite (structural core; RPC/wire layer deferred). p.2.5.8 engine.p2p.relay voluntary relay (register→forward→deliver, unregistered REJECT, over-hop DROPPED, shutdown clear) + teardown hook wired in the MC shell EnhancedChannelRuntime (new [Subterra relay] line; existing network markers untouched). Decentralization implemented (direct + DHT + relay, no central account/session). probeAcceptance all green (61 pure-JVM + four E2E boots, NetworkE2EProbe fourth boot, original-path-preserved unchanged). / 诚实披露 / honest disclosures：(a) p.2.5 各核心（寻址/握手/传输/载荷/规划/发现/relay）为**纯 JDK 确定性结构核心**，线上 RPC/真实 socket 传输层与 relay 线上端点留 runtime 后续（p.2.5.8 的 relay 为内存端点语义，仅 MC 壳做关停接线）；(b) E2E **不新增开服探针**，relay 关停由纯 JVM P2pRelayProbe（含 shutdown clear）覆盖，NetworkE2EProbe 强杀进程故不触发 ServerStoppingEvent（符合设计）。 | landed / 已落地 |
| p.2.6 | C2ME bundled / C2ME 直接包含（自原 p.3.0 前移）：engine.worldgen.async 核心 + runtime 异步壳，随框架发布（MIT 声明） | landed / 已落地 |
| p.2.7 | Deterministic parallel / 确定性并行执行 engine.parallel（**革命·新增**）：多核并行（sim / AI / 区块）**不破种子确定性**——通用键分区 KeyPartition（双 long 路径与 p.2.6 ChunkPartition 逐位一致）+ 有界串行 WorkerBank + 泛型确定性分发器 DeterministicDispatcher（**重复键显式拒绝**，替代 p.2.6 last-write-wins 非确定性保证）+ ParallelRunner 门面（并行 run / 串行 runSerial **逐位一致**、批次生命周期、无线程泄漏、输入序无关）；五探针全接 probeAcceptance，ChunkKey 域归并序与 p.2.6 一致，optim.sched 组合不破确定性；engine.sim / C2ME 的地基 | landed / 已落地 |
| p.2.8 | World sim / 世界推演 engine.sim（**革命·新增**）：时间 / 物理 / 生态 / 经济 / 势力的**增量确定性模拟底座**——预算分级、分区空间索引、无全局扫描（接 engine.parallel p.2.7）；服务 Toterra 生态层（食物链 / 季节 / 迁徙）、势力扩张与贸易网络、自建聚落；与 engine.time（p.2.26）衔接。落地小结 / landing notes（p.2.8.1–p.2.8.6）：**p.2.8.1** engine.sim.budget 预算分级调度（BudgetTier/RegionKey/TickBudget/BudgetScheduler——per-simulant/region/global 三级 cap、原子记账、超预算确定性 key 序降级，p.2.26 BudgetScheduler 前哨）；**p.2.8.2** engine.sim.spatial 分区空间索引（CellCoord/SpatialIndex——半径邻域只访问 (2r+1)² 局部窗口 cell、无全局扫描，cell 键经 p.2.7 KeyPartition 双 long 路径入 bank）；**p.2.8.3** engine.sim.core 增量确定性模拟核心（SimKey/PerSimRandom/Simulator/SimWorld——advance(dirty) 只推进变化局部、逐 key 顺序无关、diff/回放与全量重算逐字节一致）；**p.2.8.4** engine.sim.run per-simulant 隔离 + 确定性归并门面（SimRunner——直接消费 p.2.7 ParallelRunner，run/runSerial 逐字节一致、重复键显式拒绝）；**p.2.8.5** SimCombineProbe 全轨组合简景（budget+spatial+core+parallel）；**p.2.8.6** runtime.sim.SimRuntime（-D/-Psubterra.probe.sim 门控默认 no-op、[Subterra sim] marker + 四微型确定性校验，AsyncE2EProbe 同开服并入）。/ p.2.8.1 engine.sim.budget budget-tier scheduling (per-simulant/region/global caps, atomic accounting, deterministic key-order degradation — the p.2.26 BudgetScheduler precursor); p.2.8.2 engine.sim.spatial partitioned spatial index (local-window neighborhood queries, no global scan, p.2.7 partition reuse); p.2.8.3 engine.sim.core incremental deterministic core (dirty-set advance, order-independent per-key ticks, diff/replay byte-identical to full recompute); p.2.8.4 engine.sim.run per-simulant isolation + deterministic merge facade (SimRunner over p.2.7 ParallelRunner, parallel/serial byte-identical, duplicate-key rejection); p.2.8.5 SimCombineProbe full-stack combination; p.2.8.6 runtime.sim.SimRuntime (gate-off no-op shell, [Subterra sim] markers + four micro-checks, merged into the AsyncE2EProbe boot). | landed / 已落地 |
| p.2.9 | World-as-artifact / 世界即产物（**革命·新增**）：export + save + 数据包 + 藏录一键打包为「世界包」，可搬移、可再水化（接 p.2.3 / p.2.18）；服务 Toterra 镜像层与存档互导。落地小结 / landing notes（p.2.9.1–p.2.9.6）：**p.2.9.1** engine.world.WorldPack 统一世界包档案形态（单一 td 文档 `world=[version,meta,save,datapack]`，save/datapack 为 base64 载荷，meta TreeMap 排序，段序固定 version→meta→save→datapack）；**p.2.9.2** 确定性归档 + `export(rehydrate(export(w))).equals(export(w))` 逐字节往返契约 + p.2.8 稳态衔接 meta 键；**p.2.9.3** WorldPackPacker 世界工作树目录↔单 td 打包/还原（只扫六固定 SaveSlot 目录、无全局遍历、路径穿越防护，可搬移互导）；**p.2.9.4** probes.WorldPackProbe 20 项确定性断言 + 接入 probeAcceptance；**p.2.9.5** engine.export 导出端口预留（ExportKind/ExportPort/ExportHub，preload 确定性注册 WORLD_PACK + SAVE 两内建端口，接 p.2.18）；**p.2.9.6** runtime.world.WorldRuntime（@EventBusSubscriber ServerStartedEvent 钩子、-Dsubterra.probe.world 门控默认 no-op、[Subterra world] marker、staging 隔离不写服务器、AsyncE2EProbe 并入 world 两槽断言）。/ p.2.9.1 engine.world.WorldPack uniform world-pack archive form (single td doc `world=[version,meta,save,datapack]`, base64 payloads, TreeMap-sorted meta, fixed version→meta→save→datapack section order); p.2.9.2 deterministic archival + byte-exact export∘rehydrate∘export identity + p.2.8 steady-state meta keys; p.2.9.3 WorldPackPacker world-work-tree dir↔single-td pack/restore (scans only the six fixed SaveSlot dirs, no global scan, path-traversal guards, movable/interchangeable); p.2.9.4 probes.WorldPackProbe 20 deterministic checks wired into probeAcceptance; p.2.9.5 engine.export port cut reserved — ExportKind/ExportPort/ExportHub, WORLD_PACK+SAVE built-ins deterministically preloaded (p.2.18 seam); p.2.9.6 runtime.world.WorldRuntime (@EventBusSubscriber ServerStartedEvent hook, -Dsubterra.probe.world default no-op, [Subterra world] markers, staging-isolated no server write, world two-slot asserts merged into AsyncE2EProbe). | landed / 已落地 |
| p.2.10 | Save verifiability / 存档可验证（**革命·新增**）：zd 区块 + ed25519 / tsha1f 签名，损坏自检与防篡改——存档 = 可验证账本（接 p.2.3）。落地小结 / landing notes（p.2.10.1–p.2.10.6）：**p.2.10.1** engine.saveverify.chunk——zd 区块划分 + 逐块 tsha1f 快/强校验元数据（Chunk/ChunkPlan/ChunkVerifier，确定性、固定序）；**p.2.10.2** engine.saveverify.sig——Ed25519 强签名核心，确定性密钥对注入（JDK 内置，RFC 8032 确定性签名，Ed25519Keys/SaveSigner）；**p.2.10.3** 可验证存档案（engine.saveverify 根包）——**单一 td 文档 saveverify=[version,meta,config,payload,chunks,sig]**（接 p.2.3 文档形态）：ed25519 头签名 + tsha1f 双档块标 + seq 重放门 + **五类损坏确定性拒绝**（SaveVerifyArchive/SaveVerifyLedger/VerifyResult）；**p.2.10.4** SaveVerifyProbe 15 项确定性断言（flip/truncate/reorder/wrong-key/replay 五类损坏均拒、完好档案与往返接受）；**p.2.10.5** runtime.saveverify.SaveVerifyRuntime 门控壳（-Dsubterra.probe.saveverify 默认 no-op、[Subterra saveverify] marker 并入 AsyncE2EProbe、服务器启动即验账本）；**p.2.10.6** SaveVerifyProbe 接入 probeAcceptance。/ p.2.10.1 engine.saveverify.chunk: zd-chunk partition + per-chunk tsha1f fast/strong verify metadata (Chunk/ChunkPlan/ChunkVerifier, deterministic fixed-order). p.2.10.2 engine.saveverify.sig: Ed25519 strong-signing core with deterministic keypair injection (JDK built-in, RFC 8032 deterministic signatures — Ed25519Keys/SaveSigner). p.2.10.3 verifiable-save archive (engine.saveverify root pkg): **single td doc saveverify=[version,meta,config,payload,chunks,sig]** (over the p.2.3 doc form) — Ed25519 head signature + tsha1f dual-chunk marks + seq replay gate + **five corruption classes deterministically rejected** (SaveVerifyArchive/SaveVerifyLedger/VerifyResult). p.2.10.4 SaveVerifyProbe 15 deterministic checks (flip/truncate/reorder/wrong-key/replay rejected, intact archive + round-trip accepted). p.2.10.5 runtime.saveverify.SaveVerifyRuntime gated shell (-Dsubterra.probe.saveverify default no-op, [Subterra saveverify] marker merged into the AsyncE2EProbe boot, ledger verified on server start). p.2.10.6 SaveVerifyProbe wired into probeAcceptance. / probeAcceptance 全绿（79 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world+saveverify marker） / probeAcceptance all green (79 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world+saveverify markers together). | landed / 已落地 |
| p.2.11 | Session programmable / 会话可编程（**革命·新增**）：外部进程（调试器 / 编辑器 / tie 脚本 / 工具）经 tink 帧驱动游戏运行时——tink hub 形态 B（接 p.2.5）；游戏成为可编程运行时。落地小结 / landing notes（p.2.11.1–p.2.11.5，收尾 p.2.11.6）：**p.2.11.1** engine.session 会话/命令协议核心——确定性命令信封 + 固定命令动词语义，载荷走 p.2.5 zd 载荷通道（SessionCommand/SessionCommandKind/SessionEnvelopeCodec）；**p.2.11.2** 指令化会话状态机——连接/鉴权/命令派发/状态回读/断开确定性转移 + 单调序号门（乱序/重放均拒，SessionMachine/SessionState/SessionMachineResult）；**p.2.11.3** 外部进程帧协议入口——会话命令经 p.2.5 tink v2 帧 ABI（ext TLV session/seq/key9 meta，坏帧拒绝，SessionFrame/SessionFrameCodec）；**p.2.11.4** SessionProbe 35 项确定性断言（命令信封/帧往返、状态机脚本序列、坏帧/乱序/重放拒绝，纯 JVM 接入 probeAcceptance）；**p.2.11.5** runtime.session.SessionRuntime 门控壳（-Dsubterra.probe.session 默认 no-op、[Subterra session] marker 并入 AsyncE2EProbe、五次开服不变）。/ p.2.11.1 engine.session session/command-protocol core over the p.2.5 zd payload channel (deterministic command envelope + fixed command verbs — SessionCommand/SessionCommandKind/SessionEnvelopeCodec); p.2.11.2 command-ized session state machine — deterministic connect/auth/dispatch/read-back/disconnect with a monotonic-seq gate (reorder/replay both rejected — SessionMachine/SessionState/SessionMachineResult); p.2.11.3 external-process frame-protocol entry — session commands over the p.2.5 tink v2 frame ABI (ext TLV session/seq/key9 meta, bad frames rejected — SessionFrame/SessionFrameCodec); p.2.11.4 SessionProbe 35 deterministic checks (envelope/frame round-trips, scripted state-machine sequences, bad-frame/reorder/replay rejection, pure-JVM wired into probeAcceptance); p.2.11.5 runtime.session.SessionRuntime gated shell (-Dsubterra.probe.session default no-op, [Subterra session] marker merged into the AsyncE2EProbe boot, five boots unchanged). / 结论：会话可编程运行时抽象——命令/状态机/帧入口复用 p.2.5 tink v2/tsha1f/zd ABI，runtime 门控壳并入 AsyncE2EProbe、五次开服不变。probeAcceptance 全绿（80 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world+saveverify+session marker）。/ Conclusion: a session-programmable runtime abstraction — command/state-machine/frame entry reuse the p.2.5 tink v2/tsha1f/zd ABI, the gated runtime shell merged into the AsyncE2EProbe boot, five boots unchanged. probeAcceptance all green (80 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world+saveverify+session markers together). | landed / 已落地 |
| p.2.12 | Schema-first scaffolding（**革命·新增**）：td schema → 自动生成注册码 / 探针 / 文档 / 表单——模组编写从「写代码」变「写数据」。落地小结 / landing notes（p.2.12.1–p.2.12.5，收尾 p.2.12.6）：**p.2.12.1** engine.schema td schema 模型——确定性定型 / 校验 / 遍历（Schema / SchemaField / SchemaKind / SchemaCodec / SchemaReport / SchemaViolationException，td 为单一事实源、固定字段序）；**p.2.12.2** engine.scaffold.RegisterCodeGen——schema→注册接线代码，同 schema 同字节、往返恒等；**p.2.12.3** engine.scaffold.SchemaStubGen——schema→探针骨架 + 开发文档文本，同 schema 同字节、往返恒等；**p.2.12.4** engine.scaffold.FormDataGen——schema→表单结构 / 渲染数据纯数据，kind→widget 固定映射（UI 留 p.2.23）；**p.2.12.5** ScaffoldProbe 40 项确定性断言（schema 定型 / 违约 / validate + 三生成器逐字节一致与往返恒等，接入 probeAcceptance）；顺带修复 **TdTable 首现去重吞掉重复键**的死代码缺陷（duplicates() 附加 API）。/ p.2.12.1 engine.schema td-schema model — deterministic parse / validate / walk (Schema / SchemaField / SchemaKind / SchemaCodec / SchemaReport / SchemaViolationException, td as the single source of truth, fixed field order); p.2.12.2 engine.scaffold RegisterCodeGen — schema→register-wiring code, same schema → same bytes, round-trip stable; p.2.12.3 engine.scaffold SchemaStubGen — schema→probe skeleton + dev-doc text, same schema → same bytes, round-trip stable; p.2.12.4 engine.scaffold FormDataGen — schema→form-structure / render-data pure data, fixed kind→widget map (UI in p.2.23); p.2.12.5 ScaffoldProbe 40 deterministic checks (schema type / validate / reject + three generators byte-identical and round-trip stable, wired into probeAcceptance); incidental fix: the **TdTable first-occurs dedup that swallowed duplicate keys** dead-code defect (duplicates() added). / 结论：td schema 为单一事实源的确定性脚手架——定型 / 注册码 / 探针文档 / 表单四生成器同 schema 同字节，ScaffoldProbe 40 项断言接入 probeAcceptance。probeAcceptance 全绿（81 纯 JVM + 五次 E2E）。/ Conclusion: a td-schema-as-single-source-of-truth deterministic scaffold — typing / register-code / probe-doc / form-data four generators share one schema and one byte stream; ScaffoldProbe 40 checks wired into probeAcceptance. probeAcceptance all green (81 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.13 | Deterministic PCG / 确定性内容生成 engine.pcg（**革命·新增**）：名称 / 文本 / 配方 / 遗物 / 群系变种 / 本地化的**生成器编排底座**——纯函数、同种子一致（接 schema-first p.2.12 / worldgen）；「无尽探索」的框架化底座。落地小结 / landing notes（p.2.13.1–p.2.13.5，收尾 p.2.13.6）：**p.2.13.1** engine.pcg 确定性生成核心——`PcgSource`/`PcgPick` 于 XoroRandom PRNG 之上（种子扩散 / 可复现随机流，同种子→同序 picks、fork 隔离、pick 落因固定，`PcgException`）；**p.2.13.2** `PcgTextSample` 确定性名称/文本生成（模板-槽采样，固定槽序，同种子→同文本）；**p.2.13.3** `PcgContentGen` 确定性内容生成器——遗物 / 台账 / 配方 / 本地化 / 群系变种的 td 文档（同种子→同 td 文本、可解析输出）；**p.2.13.4** `PcgSchemaGen` schema 驱动生成计划——把 p.2.12 td Schema 填入确定性合法实例（同种子→同 validate 通过 td 文档）；**p.2.13.5** PcgProbe 29 项确定性断言（seed/fork 确定性、加权 picks、text/content/schema-driven 逐字节一致、可 validate td），接入 probeAcceptance。结论：确定性内容生成编排底座——PcgSource/PcgPick/PcgTextSample/PcgContentGen/PcgSchemaGen，同种子同输出、schema 驱动生成合法实例、PcgProbe 29 项断言接入 probeAcceptance。probeAcceptance 全绿（82 纯 JVM + 五次 E2E）。/ p.2.13.1 engine.pcg deterministic generation core — `PcgSource`/`PcgPick` over the XoroRandom PRNG (seed diffusion / reproducible random stream; same seed → same-order picks, fork isolation, fixed pick reasons; `PcgException`). p.2.13.2 `PcgTextSample` deterministic name/text generation (template-slot sampling, fixed slot order, same seed → same text). p.2.13.3 `PcgContentGen` deterministic content generators — relic / ledger / recipe / localization / biome-variant td documents (same seed → same td text, parseable output). p.2.13.4 `PcgSchemaGen` schema-driven generation plan — fills a p.2.12 td Schema into a deterministic valid instance (same seed → same validate-passing td doc). p.2.13.5 PcgProbe 29 deterministic checks (seed/fork determinism, weighted picks, text/content/schema-driven byte-identical, validate-able td), wired into probeAcceptance. Conclusion: a deterministic content-generation orchestration base — PcgSource/PcgPick/PcgTextSample/PcgContentGen/PcgSchemaGen, same seed → same output, schema-driven valid instances, PcgProbe 29 checks wired into probeAcceptance. probeAcceptance all green (82 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.14 | Diegetic interaction / engine.interact（**革命·新增**）：「无系统气味」的可落实交互层——世界内交互规范与零 HUD 化渲染规则（与 engine.ui p.2.22 调和，panels 在 p.2.22 仅作管理视图）。落地小结 / landing notes（p.2.14.1–p.2.14.4，收尾 p.2.14.5）：**p.2.14.1** 世界内交互规范模型（engine.interact——InteractKind/Attitude/InteractAction/InteractSurface/InteractSpec + InteractSpecParser + InteractViolationException：td schema 定型、固定序、无系统气味词汇面；顺带修复 schemaTd 缺方括号成无效 td 文档的缺陷）；**p.2.14.2** 确定性交互规则引擎（InteractState/InteractResult/InteractRuleEngine：固定序表面/动作/态度/状态规则、可回放、世界内语言消息、零 HUD）；**p.2.14.3** 零 HUD 渲染规则/表面映射（SurfaceTextMapper+ZeroHudGuard+SurfaceText：世界内语言表面文本确定性映射、系统气味黑名单守卫，纯数据、渲染留 p.2.22）；**p.2.14.4** InteractProbe 30 项确定性断言（spec 往返/违约、规则引擎脚本化可回放、零 HUD 表面映射与系统气味黑名单）接入 probeAcceptance。结论：世界内交互抽象——td schema 定型的交互规范模型 + 确定性规则引擎（固定序可回放、世界内语言消息、零 HUD）+ 表面映射与系统气味黑名单守卫，InteractProbe 30 项断言接入 probeAcceptance。probeAcceptance 全绿（83 纯 JVM + 五次 E2E）。/ p.2.14.1 in-world interaction-spec model (engine.interact — InteractKind/Attitude/InteractAction/InteractSurface/InteractSpec + InteractSpecParser + InteractViolationException: td-schema typed, fixed order, no-system-scent vocabulary; incidental fix of the schemaTd missing-square-brackets defect that yielded invalid td docs). p.2.14.2 deterministic interaction-rule engine (InteractState/InteractResult/InteractRuleEngine: fixed-order surface/action/attitude/state rules, replayable, in-world-language messages, zero HUD). p.2.14.3 zero-HUD rendering rules / surface mapping (SurfaceTextMapper+ZeroHudGuard+SurfaceText: deterministic in-world-language surface text, system-scent blacklist guard, pure data, rendering deferred to p.2.22). p.2.14.4 InteractProbe 30 deterministic checks (spec round-trip/violations, scripted replayable rule-engine scenarios, zero-HUD surface mapping + system-scent guard) wired into probeAcceptance. Conclusion: the in-world interaction abstraction — a td-schema-typed interaction-spec model + a deterministic rule engine (fixed-order, replayable, in-world-language messages, zero HUD) + surface mapping guarded by a system-scent blacklist; InteractProbe 30 checks wired into probeAcceptance. probeAcceptance all green (83 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.15 | Engine migration + golden tests / engine 迁移（自原 p.2.2 顺移）：现有 optim / config / log 纯 JDK 核心迁入 engine.*；vanilla 数值对照固化为 golden tests 随改动回归。落地小结 / landing notes（p.2.15.1–p.2.15.3，收尾 p.2.15.4）：**p.2.15.1** engine 落点审计——optim/config/log 纯 JDK 核心全部确认落于 engine.*（17 处双语 package-info 稳定化、零行为变化、旧 FQCN 确认缺席）；**p.2.15.2** vanilla 数值对照黄金化——`VanillaGoldenGen` 冻结 seed=44905237 的 NoiseRouter 镜像 **12 点×15 场共 180 个位模式**为 td golden 资产（`subterra-devkit\src\main\resources\golden\vanilla-router-seed44905237.td`，golden_version=1 锚点、int64 位模式逐位无损往返；一次性生成器刻意不接 probeAcceptance）；**p.2.15.3** `VanillaGoldenProbe` 黄金门禁回归探针——180 项逐位回归 / 确定性再入 / 文档往返 / 元数据锚定，接入 probeAcceptance。结论：engine 迁移落点稳定（零行为变化）+ vanilla 数值对照 golden-test 化回归。probeAcceptance 全绿（84 纯 JVM + 五次 E2E）。/ p.2.15.1 engine landing audit — optim/config/log pure-JDK cores all confirmed in engine.* (17 bilingual package-infos stabilized, zero behavior change, old FQCNs confirmed absent). p.2.15.2 vanilla-fidelity golden asset — VanillaGoldenGen freezes the seed-44905237 NoiseRouter mirror, **12 points × 15 fields = 180 bit patterns**, into a td golden doc (golden_version=1 anchor, int64 bit patterns round-trip losslessly; the one-time generator deliberately NOT wired into probeAcceptance). p.2.15.3 VanillaGoldenProbe golden-gate regression — 180 bit-exact regressions / determinism re-entry / doc round-trip / metadata anchor, wired into probeAcceptance. Conclusion: engine migration landed stably (zero behavior change) + vanilla value comparisons hardened as golden-test regressions. probeAcceptance all green (84 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.16 | API contract surface / api 契约面（自原 p.2.1 顺移）：worldgen / config(rules) / export / event 契约 + Exporter 注册表 + 规则注册 API。落地小结 / landing notes（p.2.16.1–p.2.16.5，收尾 p.2.16.6）：**p.2.16.1** worldgen 契约——`NoiseRouterContract`（15 字段规范名与 engine 镜像 `NoiseRouter.FIELD_NAMES` 逐字同序 + golden 锚点 seed=44905237 / golden_version=1 / Y∈[-64,320) + 12 固定采样点 + 位模式布局，p.2.15 金标的数据底座化为契约形状，数值底座以数据注入、不反向依赖 engine）；**p.2.16.2** config(rules) 契约 + 规则注册 API——`Rule`/`RuleSet`/`RuleRegistry`（固定注册序、同 key 重复拒绝、双层解析与 render 语义对齐 `DatapackRules`，p.2.17 消费方前哨）；**p.2.16.3** export 契约 + Exporter 注册表——`ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry`（注册序查表、重复拒绝、form=world/save/datapack 与 engine `ExportKind` 逐一一致，p.2.18 前哨）；**p.2.16.4** event 契约——`EventKind`/`EventEnvelope`/`EventStream`（monotonic-seq 门拒乱序/重放、类型化载荷可回放，承接 p.2.4/2.5/2.11）；**p.2.16.5** `ContractProbe` 38 checks（worldgen 锚点桥接 / 规则注册 vs DatapackRules / 注册表固定序与重复拒 / 事件单调序门）接入 probeAcceptance。结论：API 契约面定型——四契约面 + 双注册表全纯 JDK、确定性可探针、金标底座数据化注入。probeAcceptance 全绿（85 纯 JVM + 五次 E2E）。/ p.2.16.1 worldgen contract — `NoiseRouterContract` (15 canonical field names verbatim-ordered against the engine mirror `NoiseRouter.FIELD_NAMES` + golden anchor seed=44905237 / golden_version=1 / Y∈[-64,320) + 12 fixed sample points + bit-pattern layout; the p.2.15 golden value base shaped into a contract, numbers injected as data, never reverse-depending on engine). p.2.16.2 config(rules) contract + rule-registration API — `Rule`/`RuleSet`/`RuleRegistry` (fixed registration order, duplicate-key rejection, two-tier resolve/render aligned with `DatapackRules`, the p.2.17 consumer precursor). p.2.16.3 export contract + Exporter registry — `ExportKindSpec`/`ExportPortSpec`/`ExporterRegistry` (registration-order lookup, duplicate rejection, form=world/save/datapack 1:1 with engine `ExportKind`, the p.2.18 precursor). p.2.16.4 event contract — `EventKind`/`EventEnvelope`/`EventStream` (monotonic-seq gate rejecting reorder/replay, typed replayable payloads, over p.2.4/2.5/2.11). p.2.16.5 `ContractProbe` 38 checks (worldgen anchor bridge / rule registry vs DatapackRules / registry fixed-order + dup-reject / event monotonic-seq gate) wired into probeAcceptance. Conclusion: the API contract surface is typed — four contract surfaces + two registries all pure JDK, deterministic and probeable, the golden base injected as data. probeAcceptance all green (85 pure-JVM + five E2E boots). | landed / 已落地 |
| p.2.17 | Rule system / 规则系统 engine.config.rules（自原 p.2.4 顺移，参考 RollingGate 模型）：类型化规则、双层配置（全局 + 存档覆盖）、校验、热重载、`/subterra rule` 命令。落地小结 / landing notes（p.2.17.1–p.2.17.5，收尾 p.2.17.6）：**p.2.17.1** engine.config.rules 类型化规则核心——RuleType/RuleKey/RuleSpec + RuleValidator/RuleReport/RuleViolation（UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER 四类确定性违约、固定序输出），值载体复用 p.2.16.2 api.config.Rule 契约、td rule-doc 文档形态（对齐 DatapackRules.fromManifest）；**p.2.17.2** RuleStore 双层配置——全局档 + 存档覆盖档，全量校验违约抛 RuleViolationException（全有或全无装载），resolve=global→overrides 胜出、render TreeMap 字典序，语义对齐 api RuleRegistry.resolveTiered 与 p.2.3.5；**p.2.17.3** RuleReloader 热重载——逐字节变更检测、原子 apply-or-keep-old、重入幂等、revision 单调（禁时序）；**p.2.17.4** `/subterra rule` 命令（view/set/override 共享确定性核心 runVerb）+ RulesRuntime 门控壳（subterra.probe.rule 钩子、ServerStarted 接线）+ DatapackE2EProbe 32 项断言（含新 rule marker，既有断言不动）；**p.2.17.5** RuleSystemProbe 43 checks（注册序/重复拒/双层覆盖 vs RuleRegistry 与 DatapackRules/校验违约/渲染对齐/热重载重入/文档往返）接入 probeAcceptance。结论：规则系统引擎侧落地——类型化规则模型 + 确定性校验 + 双层配置 + 热重载 + `/subterra rule` 命令，全部确定性可探针。probeAcceptance 全绿（86 纯 JVM + 五次 E2E，新增 RuleSystemProbe）。/ p.2.17.1 engine.config.rules typed rule core — RuleType/RuleKey/RuleSpec + RuleValidator/RuleReport/RuleViolation (four deterministic violation kinds UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER, fixed-order output), value carrier reusing the p.2.16.2 api.config.Rule contract, td rule-doc form (aligned with DatapackRules.fromManifest). p.2.17.2 RuleStore two-tier configuration — global tier + save-override tier, all-or-nothing load raising RuleViolationException on any violation, resolve = global→overrides wins, TreeMap-ordered render, aligned with api RuleRegistry.resolveTiered and p.2.3.5. p.2.17.3 RuleReloader hot reload — byte-identical change detection, atomic apply-or-keep-old, re-entrant idempotent replay, monotonic revision (no timing). p.2.17.4 `/subterra rule` command (view/set/override over a shared deterministic core) + RulesRuntime gated shell (subterra.probe.rule hook, ServerStarted wiring) + DatapackE2EProbe 32 asserts incl. the new rule marker (existing asserts untouched). p.2.17.5 RuleSystemProbe 43 checks (registration order / dup-reject / two-tier override vs RuleRegistry and DatapackRules / validation violations / render alignment / hot-reload re-entry / doc round-trip) wired into probeAcceptance. Conclusion: the rule system landed engine-side — typed rule model + deterministic validation + two-tier configuration + hot reload + the `/subterra rule` command, all deterministic and probeable. probeAcceptance all green (86 pure-JVM + five E2E boots, incl. RuleSystemProbe). | landed / 已落地 |
| p.2.18 | Export hub / 导出器 engine.export（自原 p.2.3 顺移）：language-keys / config / world / registries / migrate-maps，td/zd 双格式，`/subterra export` 指令。落地小结 / landing notes（p.2.18.1–p.2.18.6，收尾 p.2.18.7）：**p.2.18.1** engine.export 正式导出枢纽——功能性 ExportProducer（td/zd 双格式 + export∘rehydrate∘export 逐字节恒等契约）、ExportKind 扩为七 form（world/save/datapack/language_keys/config/registries/migrate_maps）、ExportHub 注册序/重复拒对齐 p.2.16.3 api.ExporterRegistry（preload() 保持 p.2.9.5 语义、preloadFormal() 全量注册）；**p.2.18.2** LanguageKeysExporter/Producer（字典序 td + zd + 回水化恒等）；**p.2.18.3** ConfigExporter/Producer（global+overrides+effective 三块字典序、恒等、对齐 RuleStore.resolve）；**p.2.18.4** RegistriesExporter/MigrateMapsExporter + producers；**p.2.18.5** /subterra export hub 接线（exportForm 共享核心、ExportHubCommand/ExportHubRuntime、subterra.probe.exportHub 门控钩子 + DatapackE2EProbe 追加 3 断言、既有 32 项不动）；**p.2.18.6** ExportHubProbe 41 checks；另修复 ContractProbe 导出 form 断言改前缀语义（枚举扩七后过时断言修复）。结论：导出枢纽七 form 双格式落地——五个确定性导出器（language-keys / config / world / registries / migrate-maps，各字典序固定字段序禁时间戳）+ 指令接线 + 探针全绿。probeAcceptance 全绿（87 纯 JVM + 五次 E2E，新增 ExportHubProbe）。/ p.2.18.1 the engine.export formal export hub — functional ExportProducer (td/zd dual formats + byte-identical export∘rehydrate∘export identity contract), ExportKind grown to seven forms (world/save/datapack/language_keys/config/registries/migrate_maps), ExportHub registration-order/dup-reject aligned with the p.2.16.3 api.ExporterRegistry (preload() keeps p.2.9.5 semantics, preloadFormal() registers all seven); p.2.18.2 LanguageKeysExporter/Producer (key-sorted td + zd + rehydrate identity); p.2.18.3 ConfigExporter/Producer (global+overrides+effective three layers key-sorted, identity, aligned with RuleStore.resolve); p.2.18.4 RegistriesExporter/MigrateMapsExporter + producers; p.2.18.5 /subterra export hub wiring (shared exportForm core, ExportHubCommand/ExportHubRuntime, subterra.probe.exportHub gated hook + three new DatapackE2EProbe asserts, existing 32 untouched); p.2.18.6 ExportHubProbe 41 checks; plus the ContractProbe exporter-form assertion fixed to prefix semantics. Conclusion: the export hub landed in seven forms dual-format — five deterministic exporters (each key-sorted, fixed field order, no timestamps) + command wiring + all probes green. probeAcceptance all green (87 pure-JVM + five E2E boots, incl. ExportHubProbe). | landed / 已落地 |
| p.2.19 | Runtime wiring migration / runtime 接线迁移（自原 p.2.5 顺移）：launch / runtime-fix / worldgen / optim-shell / tie / cfglog 迁入，export 指令接线（接 p.2.18）。落地小结 / landing notes（p.2.19.1–p.2.19.6，收尾 p.2.19.7）：**p.2.19.1** 统一接线总表 docs/runtime-wiring.md（双语盘点 20 行壳/命令表 + 六类缺口 + export 收编目标）；**p.2.19.2** LaunchRuntime（subterra.probe.launch 门控 JVM 健康检查，L1 日志文本逐字保留）+ FixRuntime（subterra.probe.fix 门控 Java25Gaps boot 收编）；**p.2.19.3** ConfigRuntime（subterra.probe.cfglog 门控 td 双层样例装载 + RuleStore/RuleReloader 热重载门）+ LogRuntime（同门控 engine.log MC 壳装载验证）；**p.2.19.4** TieRuntime（subterra.probe.tie 门控 FFM tie-bridge MC 壳，真实 ABI 调用 tiefib$add(2,3)==5 打 ok、无 tie lib 确定性 skip）；**p.2.19.5** /subterra export 指令收编（ExportCommandCore 单核心，export cmd / export hub markers 逐字不变，DatapackExportCommand 收缩委托壳，ExportHubCommand/ExportHubRuntime 删除，ExportRuntime 双门控）；**p.2.19.6** RuntimeWiringProbe 52 checks（六类落点 + 门控常量 + marker 清单）接入 probeAcceptance；worldgen / optim-shell 落点确认（既有壳、本次盘点入表）。结论：runtime 统一接线迁移——接线总表 + 六类迁入（launch/fix/cfglog/tie 四新壳 + worldgen/optim-shell 落点确认）+ export 指令收编单核心，全部确定性可探针。probeAcceptance 全绿（88 纯 JVM + 五次 E2E，新增 RuntimeWiringProbe，AsyncE2EProbe 同开服断言 launch/fix/cfglog/tie marker）。/ p.2.19.1 unified wiring inventory docs/runtime-wiring.md (bilingual 20-row shell/command table + six-category gaps + export fold-in target); p.2.19.2 LaunchRuntime (subterra.probe.launch-gated JVM health check, L1 log text kept verbatim) + FixRuntime (subterra.probe.fix-gated Java25Gaps boot fold-in); p.2.19.3 ConfigRuntime (subterra.probe.cfglog-gated td two-tier sample load + RuleStore/RuleReloader hot-reload gate) + LogRuntime (same gate, engine.log MC-shell load verification); p.2.19.4 TieRuntime (subterra.probe.tie-gated FFM tie-bridge MC shell, real ABI call tiefib$add(2,3)==5 → ok, deterministic skip without a tie lib); p.2.19.5 /subterra export command fold-in (single ExportCommandCore, export cmd / export hub markers character-for-character unchanged, DatapackExportCommand shrunk to a delegating shell, ExportHubCommand/ExportHubRuntime removed, ExportRuntime dual-gated); p.2.19.6 RuntimeWiringProbe 52 checks (six-category landing presence + gated-hook property constants + marker checklist) wired into probeAcceptance; worldgen / optim-shell landing confirmed (existing shells, inventoried this milestone). Conclusion: unified runtime wiring migration — inventory table + six-category fold-in (four new shells launch/fix/cfglog/tie + worldgen/optim-shell landing confirmation) + single-core export command fold-in, all deterministic and probeable. probeAcceptance all green (88 pure-JVM + five E2E boots, incl. RuntimeWiringProbe, AsyncE2EProbe asserting launch/fix/cfglog/tie markers in the same boot). | landed / 已落地 |
| p.2.20 | devkit complete / devkit 完整化 + 官方示例模组脚手架（自原 p.3.1 前移，**可用判据**）：probes 全量接线 + 最小「颠覆性模组」示例（兼作框架自举验收）。落地小结 / landing notes（p.2.20.1–p.2.20.5，收尾 p.2.20.6）：**p.2.20.1** devkit 接线盘点——probeAcceptance 任务面全量审计（93 任务 = 88 纯 JVM + 5 E2E 全接线、零缺口、E2E 链完整）；**p.2.20.2** devkit 任务面完整化——探针任务统一收编 verification 分组、一次性生成器隔离 devkit 分组（VanillaGoldenGen / WorldProfileAcceptance 不入 probeAcceptance）、验收任务描述更新 93 任务（零行为变化）；**p.2.20.3** 官方示例模组骨架 samples/overturn-minimal（双语 README + 最小 NeoForge 装配清单 neoforge.mods.toml + 纯 JDK 可编译入口 OverturnMinimal + 最小 td 规则包 pack.td + settings include；模块内 FML 不消费、装配元数据声明形态）；**p.2.20.4** 示例能力面——OverturnMinimal 逐一显式消费框架纯 JDK 能力面（规则双层 RuleStore 覆盖档胜出 / td 数据包直载 / export td+zd 逐字节恒等 / tie skip-or-ok），确定性报告行、同输入同字节；**p.2.20.5** SampleBootstrapProbe 13 checks「示例即验收」——跑 OverturnMinimal.main（System.out 捕获还原），断言固定序报告行（规则双层 / td 数据包 / export 恒等 / tie skip-or-ok / PASS）+ 从 classpath 盘点示例结构（pack.td 规则表 + neoforge.mods.toml），接入 probeAcceptance（94 任务 = 89 纯 JVM + 5 E2E）。结论：devkit 完整化 + 官方示例模组脚手架——probeAcceptance 93/94 任务全量接线零缺口 + devkit 任务面完整化（verification 分组 + 生成器隔离）+ 最小颠覆性模组示例 samples/overturn-minimal（双语 README + 清单式结构 + 能力面：规则双层 / td 数据包直载 / export td/zd 恒等 / tie skip-or-ok，确定性报告）+ SampleBootstrapProbe 13 checks「示例即验收」接入 probeAcceptance。probeAcceptance 全绿（89 纯 JVM + 五次 E2E，新增 SampleBootstrapProbe）。/ p.2.20.1 devkit wiring inventory — full probeAcceptance task-surface audit (all probes wired, zero gaps, E2E chain intact; 93 tasks = 88 pure-JVM + 5 E2E). p.2.20.2 devkit task-surface completion — probe tasks grouped under verification, one-time generators isolated to the devkit group (VanillaGoldenGen / WorldProfileAcceptance stay out of probeAcceptance), acceptance description updated to 93 tasks (no behavior change). p.2.20.3 official sample scaffold — samples/overturn-minimal skeleton (bilingual README, minimal NeoForge assembly manifest, compilable pure-JDK entry OverturnMinimal referencing the framework, minimal td rules pack, settings include; FML does not consume it inside the module — declared assembly metadata). p.2.20.4 sample capability surfaces — OverturnMinimal explicitly exercises each framework pure-JDK surface (two-tier RuleStore overrides win / td datapack direct load / export td+zd byte-identical identity / tie skip-or-ok), deterministic report lines, same input → same bytes. p.2.20.5 SampleBootstrapProbe 13 checks — “sample-is-the-acceptance”: runs OverturnMinimal.main (System.out captured/restored), asserts the fixed-order report lines (rules two-tier / td datapack / export identity / tie skip-or-ok / PASS) + inventories the sample structure from classpath (pack.td rules table + neoforge.mods.toml), wired into probeAcceptance (94 tasks = 89 pure-JVM + 5 E2E). Conclusion: devkit complete + official sample mod scaffold — probeAcceptance 93/94 tasks fully wired with zero gaps + devkit task surface completed (verification grouping + generator isolation) + the minimal vanilla-overturning sample samples/overturn-minimal (bilingual README + manifest structure + capability surfaces: two-tier rules / td datapack direct load / export td/zd identity / tie skip-or-ok, deterministic report) + SampleBootstrapProbe 13 checks “sample-is-the-acceptance” wired into probeAcceptance. probeAcceptance all green (89 pure-JVM + five E2E boots, incl. SampleBootstrapProbe). | landed / 已落地 |
| p.2.27.1 | Flywheel port / 飞轮移植前哨（2026-09-09 新增，形态 A）：Flywheel 1.0.6（1.21.1/NeoForge 线，MIT 直接包含，保留声明）→ engine.render.instancing（实例格式 / 着色器模板 / 后端 SPI，纯 JDK）+ runtime.render 接线（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 钩子）+ 装载与后端确定性探针；作为 p.2.27 实例化基座前哨（一库一子项）。落地小结 / landing notes（p.2.27.1.1–p.2.27.1.2，收尾 p.2.27.1.3）：**p.2.27.1.1** engine.render.instancing 纯 JDK 核心（InstanceFormat 实例布局确定性模型——字段序/标量类型/字节偏移/对齐/byteSize/固定序 layout()/逐字节一致规范编码；ShaderTemplate clean-room 固定序模板——部件拼装 + 固定序占位符替换，compile() 产出逐字节一致规范 GLSL；RenderBackend+BackendRegistry 后端 SPI——固定注册序、同名/空白名拒、确定性缺省选择；全零 MC/OpenGL import）+ 许可材料落位（META-INF/third-party/flywheel-1.0.6：MIT 原文 LICENSE + 双语 NOTICE.md——来源/版本 1.0.6 注明，ShaderTemplate 注明 clean-room 不宣称直接包含）；**p.2.27.1.2** runtime.render RenderRuntime 门控壳（subterra.probe.render 钩子，缺省 no-op，[Subterra render] marker 并入 AsyncE2EProbe 同开服）+ FlywheelInstancingProbe 34 checks（装载确定性 / 后端 SPI / 实例格式模型 / 许可盘点）接入 probeAcceptance（95 任务 = 90 纯 JVM + 5 E2E）；三 MC 钩子（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher）文档化为 p.2.27 接线点。结论：Flywheel 1.0.6 实例化基座前哨——engine.render.instancing 纯 JDK 确定性核心 + MIT 直接包含保留声明 + RenderRuntime 门控壳 + FlywheelInstancingProbe 34 checks；probeAcceptance 全绿（90 纯 JVM + 五次 E2E，新增 FlywheelInstancingProbe）。/ p.2.27.1.1 engine.render.instancing pure-JDK core (InstanceFormat deterministic instance-layout model — field order / scalar type / byte offset / alignment / byteSize / fixed-order layout() / byte-identical canonical encoding; ShaderTemplate clean-room fixed-order template — parts assembly + fixed-order placeholder substitution, compile() yielding byte-identical canonical GLSL; RenderBackend+BackendRegistry backend SPI — fixed registration order, duplicate/blank-name rejection, deterministic default selection; zero MC/OpenGL imports) + license materials in place (META-INF/third-party/flywheel-1.0.6: verbatim MIT LICENSE + bilingual NOTICE.md citing source and version 1.0.6, ShaderTemplate flagged clean-room, no direct-include claim). p.2.27.1.2 runtime.render RenderRuntime gated shell (subterra.probe.render hook, default no-op, [Subterra render] markers merged into the AsyncE2EProbe boot) + FlywheelInstancingProbe 34 checks (load determinism / backend SPI / instance-format model / license inventory) wired into probeAcceptance (95 tasks = 90 pure-JVM + 5 E2E); the three MC hooks (LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher) documented as p.2.27 wiring points. Conclusion: the Flywheel 1.0.6 instancing-base guard — engine.render.instancing pure-JDK deterministic core + MIT direct-include with notices preserved + the RenderRuntime gated shell + FlywheelInstancingProbe 34 checks; probeAcceptance all green (90 pure-JVM + five E2E boots, incl. FlywheelInstancingProbe). | landed / 已落地 |
| p.2.27 | Self-developed rendering / 自研渲染管线（自原 p.4.2 顺移；2026-09-09 前移，clean-room，参考 Sodium/Embeddium 等登记项）：原版保真块/实体渲染，与 p.2.27.1 实例化基座互补。落地小结 / landing notes（p.2.27.1–p.2.27.3，收尾 p.2.27.4）：**p.2.27.1** engine.render 原版保真渲染面模型（clean-room）——RenderStage 10 阶段固定序（form/opaque/sortOnCpu 语义，1.21.1 规范名对照）+ MaterialSurface 材质面规范 form 串 + VertexLayout BLOCK 32B / ENTITY 28B 字段序/偏移/对齐/byteSize 固定序逐字节一致（javadoc 注明对照 1.21.1 规范名，零 MC import、无 Sodium/Embeddium copyleft 包含）；**p.2.27.2** runtime.render 三 MC 钩子注入——RenderHooks server 侧三面只读核对（RenderStage 固定序 / VertexLayout.BLOCK 布局 / BackendRegistry.defaultFor 缺省后端）+ RenderHooksClient 客户端 @EventBusSubscriber 事件注入（LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher 真实对象一次性只读核对），subterra.probe.render 门控缺省 no-op、不改变渲染输出，hooks ok marker 并入 AsyncE2EProbe；**p.2.27.3** RenderFidelityProbe 62 checks（原版保真模型确定性 / 实例化基座互补（InstanceFormat 重建 BLOCK/ENTITY 同语义布局一致）/ 三钩子静态契约）接入 probeAcceptance。结论：自研渲染管线 clean-room 落地——engine.render 原版保真渲染面模型 + runtime.render 三 MC 钩子注入（门控 no-op）+ 与 p.2.27.1 实例化基座互补 + RenderFidelityProbe 62 checks；probeAcceptance 全绿（91 纯 JVM + 五次 E2E，新增 RenderFidelityProbe）。/ p.2.27.1 engine.render vanilla-fidelity surface model (clean-room) — RenderStage 10-stage fixed order (form/opaque/sortOnCpu semantics, 1.21.1 canonical-name reference) + MaterialSurface canonical form strings + VertexLayout BLOCK 32B / ENTITY 28B field order/offsets/alignment/byteSize fixed-order byte-identical (javadoc cites the 1.21.1 canonical names; zero MC imports, no Sodium/Embeddium copyleft inclusion). p.2.27.2 runtime.render three-MC-hook injection — RenderHooks server-side three-surface read-only check (RenderStage fixed order / VertexLayout.BLOCK layout / BackendRegistry.defaultFor default backend) + RenderHooksClient client-side @EventBusSubscriber event injection (one-shot read-only check of the real LevelRenderer / ModelBlockRenderer / EntityRenderDispatcher objects), gated by subterra.probe.render default no-op, no render-output change, hooks-ok marker merged into AsyncE2EProbe. p.2.27.3 RenderFidelityProbe 62 checks (vanilla-fidelity model determinism / instancing-base complementarity (InstanceFormat rebuilds BLOCK/ENTITY with the same-semantics layout) / three-hook static contract) wired into probeAcceptance. Conclusion: the clean-room self-developed render pipeline landed — engine.render vanilla-fidelity surface model + runtime.render three-MC-hook injection (gated no-op) + complementarity with the p.2.27.1 instancing base + RenderFidelityProbe 62 checks; probeAcceptance all green (91 pure-JVM + five E2E boots, incl. RenderFidelityProbe). | landed / 已落地 |
| p.2.21 | Animation / 动画 engine.anim（自原 p.2.6 顺移）：GeckoLib 核心移植（MIT 声明 + 捆绑库声明）+ runtime anim binding。落地小结 / landing notes（p.2.21.1–p.2.21.3，收尾 p.2.21.4）：**p.2.21.1** engine.anim 动画核心（GeckoLib 4.8 MIT 移植，纯 JDK）——AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler 确定性采样（线性 / 最短弧 / 固定 clamp）+ AnimStateMachine 确定性状态机（注册序 / 重复拒 / 过渡线性缓动 / tick 回绕）+ MIT 声明与捆绑库声明材料 third-party/geckolib-4.8（LICENSE + NOTICE，注明 1.21.1 分支未带 LICENSE、MIT 原文取自 main 分支、未捆绑源码）；**p.2.21.2** runtime.anim AnimRuntime 门控壳（subterra.probe.anim 钩子缺省 no-op、[Subterra anim] ok marker 并入 AsyncE2EProbe 同开服）；**p.2.21.3** AnimProbe 30 checks（数据模型固定序 / 采样逐位一致含 clamp/NaN / 状态机过渡确定性 / 接线契约 / 许可盘点）接入 probeAcceptance。结论：动画核心 MIT 移植落地——engine.anim 确定性动画核心 + runtime.anim 门控壳 + AnimProbe 30 checks；probeAcceptance 全绿（92 纯 JVM + 五次 E2E，新增 AnimProbe）。/ p.2.21.1 the engine.anim animation core (GeckoLib 4.8 MIT port, pure JDK) — AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler deterministic sampling (linear / shortest-arc / fixed clamp) + AnimStateMachine deterministic state machine (registration order / duplicate rejection / linearly-eased transitions / tick wrap) + MIT notice + bundled-library notice materials under third-party/geckolib-4.8 (LICENSE + NOTICE, noting the 1.21.1 branch carries no LICENSE so the verbatim MIT text is taken from the main branch, no source bundled); p.2.21.2 runtime.anim AnimRuntime gated shell (subterra.probe.anim hook default no-op, the [Subterra anim] ok marker merged into the AsyncE2EProbe boot); p.2.21.3 AnimProbe 30 checks (data-model fixed order / sampler bit-exact incl. clamp/NaN / state-machine transition determinism / wiring contract / license inventory) wired into probeAcceptance. Conclusion: the animation core MIT port landed — the engine.anim deterministic animation core + the runtime.anim gated shell + AnimProbe 30 checks; probeAcceptance all green (92 pure-JVM + five E2E boots, incl. AnimProbe). | landed / 已落地 |
| p.2.22 | UI / HUD core / engine.ui（自原 p.2.7 顺移）：AppleSkin 数据层移植（Unlicense）+ ModMenu 交互模型借鉴（MIT）+ 数据驱动 tooltip（clean-room 参考 DataTip GPL-3.0，td 格式）+ 文档书籍 GUI（clean-room 参考 Patchouli CC-BY-NC-SA，td 驱动）+ 列表/详情/许可视图核心（面板仅作管理视图，玩家侧交互走 p.2.14 interact）。落地小结 / landing notes（p.2.22.1–p.2.22.5，收尾 p.2.22.6）：**p.2.22.1** engine.ui 数据层——AppleSkin HUD 数据层 Unlicense 全量移植（纯 JDK 确定性 FoodValues/FoodTooltipRow/HudData/FoodOverlay，许可材料 third-party/appleskin-3.0.6：Unlicense 原文 + 双语 NOTICE 注明来源/版本/适配范围，未捆绑源码）；**p.2.22.2** engine.ui tooltip + 文档书籍核心——数据驱动 TooltipBook/TooltipRule（td 化零 json，clean-room 参考 DataTip GPL-3.0 仅思想）+ DocBookTd/BookDef/BookPage 文档书籍（td 驱动，clean-room 参考 Patchouli CC-BY-NC-SA 仅思想），固定序逐字节往返；**p.2.22.3** engine.ui 列表/详情/许可视图核心——ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore（ModMenu MIT 借鉴不复制代码，td 化固定序确定性；面板仅管理视图、玩家侧交互走 p.2.14 interact）；**p.2.22.4** runtime.ui UiRuntime 门控壳（subterra.probe.ui 缺省 no-op、[Subterra ui] ok marker 并入 AsyncE2EProbe 同开服；HUD/tooltip/书籍消费接线面文档化为 p.2.23 接线点）；**p.2.22.5** UiProbe 30 checks（AppleSkin 数据层 / tooltip+doc-book td 往返 / 视图核心 / 接线契约 / 许可盘点）接入 probeAcceptance（98 任务 = 93 纯 JVM + 5 E2E）。结论：UI/HUD 核心落地——engine.ui 确定性数据面（AppleSkin Unlicense 移植 + DataTip/Patchouli clean-room td 化 + ModMenu 借鉴视图核心）+ runtime.ui 门控壳 + UiProbe 30 checks；probeAcceptance 全绿（93 纯 JVM + 五次 E2E，新增 UiProbe）。/ p.2.22.1 the engine.ui data layer — AppleSkin HUD data-layer full Unlicense port (pure-JDK deterministic FoodValues/FoodTooltipRow/HudData/FoodOverlay, license materials under third-party/appleskin-3.0.6: verbatim Unlicense + bilingual NOTICE citing source/version/adaptation scope, no source bundled); p.2.22.2 engine.ui tooltip + doc-book cores — data-driven TooltipBook/TooltipRule (td-ized zero-json, clean-room reference of DataTip GPL-3.0 ideas only) + the DocBookTd/BookDef/BookPage doc book (td-driven, clean-room reference of Patchouli CC-BY-NC-SA ideas only), fixed-order byte-identical round-trip; p.2.22.3 engine.ui list/detail/license view cores — ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore (ModMenu MIT pattern borrow without copying code, td-ized fixed-order deterministic; panels are admin views only, player interaction via p.2.14 interact); p.2.22.4 runtime.ui UiRuntime gated shell (subterra.probe.ui default no-op, the [Subterra ui] ok marker merged into the AsyncE2EProbe boot; the HUD/tooltip/book consumption surfaces documented as p.2.23 wiring points); p.2.22.5 UiProbe 30 checks (AppleSkin data layer / tooltip+doc-book td round-trip / view cores / wiring contract / license inventory) wired into probeAcceptance (98 tasks = 93 pure-JVM + 5 E2E). Conclusion: the UI/HUD core landed — the engine.ui deterministic data plane (AppleSkin Unlicense port + DataTip/Patchouli clean-room td-ization + ModMenu-borrowed view cores) + the runtime.ui gated shell + UiProbe 30 checks; probeAcceptance all green (93 pure-JVM + five E2E boots, incl. UiProbe). | landed / 已落地 |
| p.2.23 | In-game mod hub / 游戏内模组 Hub（自原 p.2.8 顺移）：td schema 表单自动生成、配置编辑 + 热重载、入口接线、服务器端 op 配置命令。落地小结 / landing notes（p.2.23.1–p.2.23.4，收尾 p.2.23.5）：**p.2.23.1** engine.hub 表单自动生成核心（纯 JDK）——FormField/FormStructure/FormRenderData/HubFormGen，复用 p.2.12.4 FormDataGen 的 kind→widget 固定映射（string→text、int/float→number、bool→check、table→group、list→repeat），td schema → 类型化表单结构，toTd/fromTd 往返恒等、render 与 FormDataGen 输出去头注释逐字节一致；**p.2.23.2** engine.hub 配置编辑 + 热重载核心（纯 JDK）——HubConfigEditor/HubConfigReloader/HubFormValues，复用 p.2.17 RuleStore/RuleReloader 语义（双层写 global+overrides、resolve 胜出、逐字节变更检测、原子 apply-or-keep-old、revision 单调、重入幂等；Reloader 直接委托 RuleReloader 不重复实现）；**p.2.23.3** runtime.hub HubRuntime 门控壳（subterra.probe.hub 缺省 no-op、[Subterra hub] ok marker 并入 AsyncE2EProbe 同开服）+ /subterra hub op 配置命令（view/form/set/override 共享确定性核心 runVerb，承接 p.2.17 /subterra rule 范式）；**p.2.23.4** HubProbe 27 checks（表单自动生成固定序 + FormDataGen 对齐、配置编辑双层 + 热重载 revision 单调 + 原子保旧、td 往返恒等、runtime 接线契约）接入 probeAcceptance（99 任务 = 94 纯 JVM + 5 E2E）。结论：In-game mod hub 落地——engine.hub 确定性表单/配置数据面（复用 p.2.12.4 + p.2.17 能力面）+ runtime.hub 门控壳 + /subterra hub op 配置命令 + HubProbe 27 checks；probeAcceptance 全绿（94 纯 JVM + 五次 E2E，新增 HubProbe）。玩家侧表单 UI 接线为 p.2.23 后接线点（见 HubRuntime javadoc）。/ p.2.23.1 the engine.hub auto-generated-forms core (pure JDK) — FormField/FormStructure/FormRenderData/HubFormGen over the p.2.12.4 FormDataGen kind→widget fixed map (string→text, int/float→number, bool→check, table→group, list→repeat), td schema → typed form structure, toTd/fromTd round-trip identity, render byte-identical to FormDataGen minus its header comments; p.2.23.2 the engine.hub config-edit + hot-reload core (pure JDK) — HubConfigEditor/HubConfigReloader/HubFormValues over the p.2.17 RuleStore/RuleReloader semantics (two-tier global+overrides writes, resolve-wins, byte-exact change detection, atomic apply-or-keep-old, monotonic revision, re-entrant idempotent; the Reloader delegates to RuleReloader, no re-implementation); p.2.23.3 the runtime.hub HubRuntime gated shell (subterra.probe.hub default no-op, the [Subterra hub] ok marker merged into the AsyncE2EProbe boot) + the /subterra hub op config command (view/form/set/override over the shared deterministic runVerb core, the p.2.17 /subterra rule pattern); p.2.23.4 HubProbe 27 checks (form auto-gen fixed order + FormDataGen alignment, config-edit two-tier + hot-reload monotonic revision + atomic keep-old, td round-trip identity, runtime wiring contract) wired into probeAcceptance (99 tasks = 94 pure-JVM + 5 E2E). Conclusion: the in-game mod hub landed — the engine.hub deterministic form/config data plane (reusing the p.2.12.4 + p.2.17 capability surfaces) + the runtime.hub gated shell + the /subterra hub op config command + HubProbe 27 checks; probeAcceptance all green (94 pure-JVM + five E2E boots, incl. HubProbe). The player-side form-UI wiring is a post-p.2.23 wiring point (see the HubRuntime javadoc). | landed / 已落地 |
| p.2.24 | AI behavior core / engine.ai（自原 p.2.9 顺移）：clean-room Brain 编排核心（参考 SmartBrainLib 模型）+ runtime ai binding。落地小结 / landing notes（p.2.24.1–p.2.24.4，收尾 p.2.24.5）：**p.2.24.1** engine.ai Brain 编排核心（纯 JDK，clean-room）——Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask（类型化记忆槽、固定注册序、重复拒、确定性 sense→decide→execute 编排、禁时序同输入同输出，SmartBrainLib MPL-2.0 仅模型参考零代码包含，声明在 package-info）；**p.2.24.2** engine.ai 行为选择 + 调度核心——BehaviorPriority（优先级降序裁决 / 同优先级注册序打破平局 / 预算跳过）、BrainScheduler（TickBudget 三 cap 对齐 p.2.8 budget 语义、超限确定性跳过）、AiRandom（委托 p.2.13 PcgSource、同种子同序列、fork 隔离）；**p.2.24.3** runtime.ai AiRuntime 门控壳（subterra.probe.ai 缺省 no-op、[Subterra ai] ok marker 并入 AsyncE2EProbe 同开服）；**p.2.24.4** AiProbe 29 checks（编排固定序 / 行为选择 + 预算调度 / AiRandom 同种子同序列 + fork 隔离 / 接线契约 / clean-room 盘点）接入 probeAcceptance（100 任务 = 95 纯 JVM + 5 E2E）。结论：AI behavior core 落地——engine.ai 确定性 Brain 编排 + 行为选择/调度 + runtime.ai 门控壳 + AiProbe 29 checks；probeAcceptance 全绿（95 纯 JVM + 五次 E2E，新增 AiProbe）。/ p.2.24.1 the engine.ai Brain orchestration core (pure JDK, clean-room) — Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask (typed memory slots, fixed registration order, duplicate rejection, deterministic sense→decide→execute orchestration, no timing same-input-same-output, SmartBrainLib MPL-2.0 model reference only zero code inclusion, declared in package-info); p.2.24.2 the engine.ai behavior-selection + scheduling core — BehaviorPriority (priority-descending arbitration / same-priority registration-order tie-break / budget skip), BrainScheduler (TickBudget three caps over the p.2.8 budget semantics, deterministic over-cap skip), AiRandom (delegates to the p.2.13 PcgSource, same-seed-same-sequence, fork isolation); p.2.24.3 runtime.ai AiRuntime gated shell (subterra.probe.ai default no-op, the [Subterra ai] ok marker merged into the AsyncE2EProbe boot); p.2.24.4 AiProbe 29 checks (orchestration fixed order / behavior-selection + budget scheduling / AiRandom same-seed-same-sequence + fork isolation / wiring contract / clean-room inventory) wired into probeAcceptance (100 tasks = 95 pure-JVM + 5 E2E). Conclusion: the AI behavior core landed — the engine.ai deterministic Brain orchestration + behavior-selection/scheduling + the runtime.ai gated shell + AiProbe 29 checks; probeAcceptance all green (95 pure-JVM + five E2E boots, incl. AiProbe). | landed / 已落地 |
| p.2.25 | Entity scale / 实体缩放 engine.scale（自原 p.3.2 前移入主线）：Pehkui-Rebuilt 核心移植（MIT，保留 Virtuoel 原始版权与重建声明）：ScaleType/ScaleData/ScaleModifier 模型、按维度/标签缩放、数据包缩放规则 + runtime scale binding。落地小结 / landing notes（p.2.25.1–p.2.25.3，收尾 p.2.25.4）：**p.2.25.1** engine.scale 实体缩放核心（Pehkui-Rebuilt 3.8.5 MIT 移植，纯 JDK）——ScaleType 固定 9 维序 / ScaleData / ScaleModifier / ScaledEntityData 按标签缩放 + 数据包缩放规则 td 化（ScaleRuleDocument），MIT 声明保留 Virtuoel 版权与重建声明（third-party/pehkui-rebuilt-3.8.5）；**p.2.25.2** runtime.scale ScaleRuntime 门控壳（subterra.probe.scale 缺省 no-op、[Subterra scale] ok marker 并入 AsyncE2EProbe 同开服，接 engine.sim p.2.8）；**p.2.25.3** ScaleProbe 23 checks（ScaleType 固定序 / ScaleData/ScaleModifier 逐位精确、rule-doc td 往返 + 覆盖、tag 合并、接线契约、pehkui-rebuilt 许可盘点）接入 probeAcceptance（101 任务 = 96 纯 JVM + 5 E2E）。结论：实体缩放核心 MIT 移植落地——ScaleType 9 维固定序 / ScaleData / ScaleModifier / ScaledEntityData 标签缩放 + td 化规则（ScaleRuleDocument）+ runtime.scale 门控壳 + ScaleProbe 23 checks；probeAcceptance 全绿（96 纯 JVM + 五次 E2E，新增 ScaleProbe）。/ p.2.25.1 the engine.scale entity-scaling core (Pehkui-Rebuilt 3.8.5 MIT port, pure JDK) — fixed 9-dim ScaleType order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized datapack scale rules (ScaleRuleDocument), MIT notices preserving Virtuoel's copyright + the rebuild declaration (third-party/pehkui-rebuilt-3.8.5)); p.2.25.2 runtime.scale ScaleRuntime gated shell (subterra.probe.scale default no-op, the [Subterra scale] ok marker merged into the AsyncE2EProbe boot, over engine.sim p.2.8); p.2.25.3 ScaleProbe 23 checks (ScaleType fixed order / ScaleData/ScaleModifier bit-exact, rule-doc td round-trip + override, tag merge, wiring contract, pehkui-rebuilt license inventory) wired into probeAcceptance (101 tasks = 96 pure-JVM + 5 E2E). Conclusion: the entity-scaling core MIT port landed — ScaleType fixed 9-dim order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized rules (ScaleRuleDocument) + the runtime.scale gated shell + ScaleProbe 23 checks; probeAcceptance all green (96 pure-JVM + five E2E boots, incl. ScaleProbe). | landed / 已落地 |
| p.2.26 | Time scaling / 时间缩放 engine.time（自原 p.3.3 前移入主线，clean-room 自研）：TimeDomain 流速比例模型（全局/实体/区域/玩家域）、tick 预算确定性调度（BudgetScheduler）、逻辑级节流 + 感知级插值，TimeScaleApi + runtime 接线（接 engine.sim p.2.8）。落地小结 / landing notes（p.2.26.1–p.2.26.5，收尾 p.2.26.6）：**p.2.26.1** engine.time 时间缩放核心（clean-room 自研，纯 JDK）——TimeDomain 四域固定序（全局/实体/区域/玩家）/ FlowRate 流速模型 / TimeScale GLOBAL 回退链 / TimeThrottle 逻辑级节流 / TimeInterpolation 感知级插值 / TimeScaleDoc td 往返（TimeScaleLib 评估剔除、clean-room 自研替代声明在 package-info）；**p.2.26.2** api.time TimeScaleApi 对外契约（TimeDomain/FlowRateSpec/TimeScaleSpec/TimeScaleApi：四域固定序 + GLOBAL 回退链 + 确定性节流/插值；api 纯 JDK 不依赖 engine，engine 为镜像实现）；**p.2.26.3** TimeBudgetScheduler/TimeBudgetKey tick 预算确定性调度（复用 p.2.8 engine.sim.budget 内核：每域 BudgetScheduler 承载、固定键序、原子记账、超预算确定性跳过）；**p.2.26.4** runtime.time TimeRuntime 门控壳（subterra.probe.time 缺省 no-op、[Subterra time] ok marker 并入 AsyncE2EProbe 同开服，MC 逻辑 tick 接线面文档化不注入）；**p.2.26.5** TimeProbe 43 checks 接入 probeAcceptance。结论：时间缩放核心 clean-room 落地——engine.time 四域时间缩放 + api.time 契约面 + tick 预算调度 + runtime.time 门控壳 + TimeProbe 43 checks；probeAcceptance 全绿（97 纯 JVM + 五次 E2E，新增 TimeProbe）。/ p.2.26.1 the engine.time time-scaling core (clean-room, pure JDK) — TimeDomain four-domain fixed order (global/entity/region/player) / FlowRate / TimeScale GLOBAL fallback chain / TimeThrottle logic-level throttling / TimeInterpolation perception-level interpolation / TimeScaleDoc td round-trip (TimeScaleLib excluded, the self-developed replacement declared in package-info); p.2.26.2 api.time TimeScaleApi contract surface (TimeDomain/FlowRateSpec/TimeScaleSpec/TimeScaleApi: four-domain fixed order + GLOBAL fallback chain + deterministic throttling/interpolation; api pure JDK with no engine dependency, engine.time mirrors as implementation); p.2.26.3 TimeBudgetScheduler/TimeBudgetKey tick-budget deterministic scheduling over the p.2.8 engine.sim.budget kernel (per-domain BudgetScheduler backing, fixed key order, atomic accounting, deterministic over-budget skip); p.2.26.4 runtime.time TimeRuntime gated shell (subterra.probe.time default no-op, the [Subterra time] ok marker merged into the AsyncE2EProbe boot, MC logic-tick wiring surfaces documented, not injected); p.2.26.5 TimeProbe 43 checks wired into probeAcceptance. Conclusion: the clean-room time-scaling core landed — engine.time four-domain scaling + the api.time contract + tick-budget scheduling + the runtime.time gated shell + TimeProbe 43 checks; probeAcceptance all green (97 pure-JVM + five E2E boots, incl. TimeProbe). | landed / 已落地 |
| p.2.28 | LOD / 多级细节渲染 engine.render.lod（**革命·新增**，紧接 p.2.27 自研渲染线；clean-room 自研 + tie 化热点）。落地小结 / landing notes（p.2.28.1–p.2.28.6）：**p.2.28.1** 数据模型（LodLevel L0..L5 阶梯 / LodColumnStack+LodQuad / LodSection 固定序字节=zd ABI 载荷钉死 / LodVertexLayout QUAD+COLUMN byteSize 8 / LodConfigDoc td 往返）；**p.2.28.2** 生成管线（LodPolygonizer 列柱+水平合并固定扫描序 / LodMerger mip 合并 / LodDirtyTracker+LodPipeline 预算批次重建（p.2.7 语义）/ **tie 标量原语下沉** subterra_lod_pipeline.tie→dll（tiec --shared；DLL 进程内边界=标量/string（FFM 事实）；**zd 数据载体交换可行**（tiec --compress-data + engine.zd/zdjava），LodCache 升级为标准 zd v2 载体）+ LodTieAccelerator FFM 缺 lib 确定性 skip）；**p.2.28.3** 视景管理（LodDistanceSelector 固定分档 / LodViewCuller Q8.24 整数视锥+朝向剔除 / LodBudgetScheduler 复用 p.2.8 预算内核 / ViewPlan）；**p.2.28.4** 渲染后端（LodRenderBackend 复用 p.2.27.1 BackendRegistry/InstanceFormat / LodShaderTemplate clean-room GLSL / LodSeamRules 接缝+雾）；**p.2.28.5** 持久化（LodCache 落盘+逐条目 tsha1f 双档，篡改/截断/版本/坏魔数四类拒，可再生物）；**p.2.28.6** api.lod 契约面（LodQualitySpec/LodDistanceSpec/LodApi，api 纯 JDK 零 engine 依赖 + LodApiMirror 镜像）+ runtime.render.lod LodRuntime 门控壳（subterra.probe.lod、[Subterra lod] marker 并入 AsyncE2EProbe）+ RenderHooksClient 注入点钩子（门控 no-op）+ RuleStore 档位读取 + LodProbe 接入 probeAcceptance。结论：LOD 多级细节渲染 clean-room 自研 + tie 化全轨落地——Distant Horizons（LGPL-3.0）仅思想参考、零源码包含；probeAcceptance 全绿（98 纯 JVM + 五次 E2E，新增 LodProbe）。/ p.2.28 LOD multi-level-of-detail rendering engine.render.lod (clean-room self-developed + tie-ized hot paths). Landing notes (p.2.28.1–p.2.28.6): p.2.28.1 the data model (LodLevel L0..L5 ladder / LodColumnStack+LodQuad / LodSection fixed-order bytes = the pinned zd-ABI payload / LodVertexLayout QUAD+COLUMN byteSize 8 / LodConfigDoc td round-trip); p.2.28.2 the generation pipeline (LodPolygonizer column+horizontal-merge fixed scan order / LodMerger mip merge / LodDirtyTracker+LodPipeline budgeted batch rebuild (p.2.7 semantics) / **tie-ized scalar primitives** subterra_lod_pipeline.tie→dll (tiec --shared; measured DLL boundary = scalars/string only (in-process FFM fact); zd data-carrier interchange feasible (tiec --compress-data + engine.zd/zdjava), LodCache upgraded to a standard zd v2 carrier) + LodTieAccelerator FFM deterministic skip without a lib); p.2.28.3 view management (LodDistanceSelector fixed tiers / LodViewCuller Q8.24 integer frustum+facing culling / LodBudgetScheduler over the p.2.8 budget kernel / ViewPlan); p.2.28.4 the render backend (LodRenderBackend over the p.2.27.1 BackendRegistry/InstanceFormat / LodShaderTemplate clean-room GLSL / LodSeamRules seams+fog); p.2.28.5 persistence (LodCache on-disk + per-entry tsha1f dual-tier, corrupt/truncate/version/magic four-way rejection, regenerable); p.2.28.6 the api.lod contract (LodQualitySpec/LodDistanceSpec/LodApi, api pure JDK zero engine dependency + the LodApiMirror) + the runtime.render.lod LodRuntime gated shell (subterra.probe.lod, the [Subterra lod] marker into AsyncE2EProbe) + the RenderHooksClient injection-point hook (gated no-op) + RuleStore tier readback + LodProbe wired into probeAcceptance. Conclusion: the LOD line landed clean-room self-developed + tie-ized — Distant Horizons (LGPL-3.0) reference-of-ideas only, zero source included; probeAcceptance all green (98 pure-JVM + five E2E boots, incl. LodProbe). | landed / 已落地 |
| p.2.29 | MC worldgen assembly completion / MC worldgen 装配面补齐（**新增**，框架补缺驱动型）：把「规则 → 真实生成路径」的接线补完（缺口由 samples/overturn-minimal 言明：规则接入实际密度场的 MC 装配面待根工程提供）——**p.2.29.1** 规则→密度场接线（世界预设启用路径 + `subterra:density` 实读 RuleStore，启动/热重载生效）；**p.2.29.2** 规则→表面规则接线（engine `OverworldSurfaceRules` 自定义材质面 palette 接入实际区块表面生成）；**p.2.29.3** 规则→特征/成矿接线（矿物/植被 placed feature 按规则挂载）；**p.2.29.4** 三缝联合验收 + 接线文档（runtime-wiring 表更新）。服务首位真实消费模组 **Overturn**（世界生成拟真先锋：litho 地层 / 气候植被 / 矿物母岩，设计文档聚合仓 `docs/2026-09-15-overturn-mod-design.md`）；装配面未落地前 Overturn 内容先行沉淀为纯数据 td 表 + 纯 JVM 确定性探针。验收全部确定性、禁时序。/ p.2.29 MC worldgen assembly completion (**new**, framework-gap-driven): complete rules→real-worldgen wiring — p.2.29.1 rules→density field (world-preset enablement path + `subterra:density` reading the RuleStore live, start/hot-reload effective); p.2.29.2 rules→surface rules (engine custom surface palette into real chunk surface generation); p.2.29.3 rules→feature/ore (mineral/vegetation placed features mounted by rule); p.2.29.4 joint acceptance + wiring doc (runtime-wiring table refresh). Serves the first real consumer mod **Overturn** (worldgen-realism vanguard: litho strata / climate vegetation / mineral-by-rock; design doc in the aggregate repo); before the seams land, Overturn content is pre-built as pure-data td tables + pure-JVM deterministic probes. Acceptance all deterministic, no timing. 落地小结 / landing notes（**p.2.2 td-plus 全闭环 p.2.29 全落地**，2026-09-15）：
  * p.2.2 "td = JSON datapack plus" 全闭环：noise_settings td 化 + JSON ↔ td 往返 + JSON 剥离 tie_ 前缀导出 + 探针 NoiseSettingsTdProbe（16 断言）接入 probeAcceptance——完成 "数据包内容 td 一等公民"承诺，`subterra:density` 规则类型可在 JSON 噪声设置中引用子terra密度。
  * p.2.29.1 规则→密度场接线：DensityAssembly（确定性：规则键 `subterra.worldgen.density_offset/.scale/.surface_palette/.ore_density`，解析/渲染/apply 同输入同字节，非法值确定性拒绝，先 scale 后 offset）+ AssemblyApiMirror（不 import api，逐字导出）；api 契约 `api.worldgen.assembly.WorldgenAssemblyApi`（纯 JDK 无 engine 依赖）；探针 AssemblyProbe 6 checks（api↔mirror 逐位一致 / 再入 / 缺省 / applyDensity / 非法拒绝）接入 probeAcceptance。
  * p.2.29.2 规则→表面规则接线：engine `OverworldSurfaceRules` 自定义材质面 palette 接入实际区块表面生成。
  * p.2.29.3 规则→特征/成矿接线：矿物/植被放置特征按规则挂载。
  * p.2.29.4 三缝联合验收：probeAcceptance 全接入，`subterra:density` 在 vanilla JSON 噪声设置中可直接引用，default 预设调整 `height/min_y` 匹配新版 vanilla 高度，vanilla 保真 golden tests 同步更新基准。
  * runtime 启动快照：`SubterraDensity.setAssembly` 缺省恒等 = 默认预设零行为变化 + `SubterraWorldgen.onServerStarting` 捕获 [Subterra assembly] marker；tie 桥 `TieTerrainDensityBridge` DLL 注入密度求值，缺 DLL 静默降级为纯 Java 树，不改变真值。
  * 2026-09-16 — p.2.29.2 表面真接缝落地：`subterra:surface_palette` rule-source 类型经 `RegisterEvent` 注册进 `Registries.MATERIAL_RULE`（runtime `SubterraSurfaceRules`/`PaletteRuleSource`，随附 noise_settings `surface_rule` 包裹引用进入真实区块表面路径），engine `SurfacePaletteResolution` 解析 `subterra.worldgen.surface_palette` 规则键（缺省 `vanilla`=恒等、非法值确定性拒绝、渲染按状态名定序）；`PaletteRuleSource extends SurfaceRules` 取得 protected 嵌套类型访问（无 mixin、无 access-transformer，主理人裁定为最小侵入框架补缝）。
  * 2026-09-16 — p.2.29.3 特征/成矿真接缝落地：engine `FeatureAssembly` 规则核心 + runtime `SubterraFeatures`（`RegisterEvent` 注册 `subterra:rule_ore` → `Registries.FEATURE`、`subterra:rule_vein` → `Registries.PLACEMENT_MODIFIER_TYPE`）+ 随附数据包（configured/placed_feature + `neoforge:add_features` biome modifier 挂到 `#minecraft:is_overworld`）；矿物×母岩前置 / 矿脉走向 / 结构护栏 td 键面 `subterra.worldgen.feature.*`，缺省 enable=false 恒等；样本 `samples/overturn-minimal`「按母岩矿石」最小可见效果。RCA 修复：`RegisterEvent` 逐注册表触发而共享幂等守卫跳过了 `rule_vein` 注册（marker 照打、类型未进表），改逐类型守卫（提交 07c6c48）后开服绿。
  * 2026-09-16 — p.2.29.4 三缝联合验收：`probeAcceptance` 114→119 任务全绿（新增 `SurfaceSeamProbe` 24 / `FeatureSeamProbe` 42 / `FeatureAssemblyProbe` 8 / `TrimandSeamProbe` 40）；`ServerBootProbe` 开服真 marker 全命中（assembly 快照 / trimand ok-skip / surface reg+id+gate / feature reg+plan）；`VanillaGoldenProbe` 360 位逐位零回归；`runtime-wiring.md` 补 worldgen · surface / worldgen · feature 两行。遗留（另行发号）：`ore_density` 标量接入 count 倍率（p.2.29.3.x）、表面 palette 热重载再快照（p.2.29.2.x）、特征挂载面缺省零增量最小化（p.2.29.3.x）——**同日三小项全部落地**（p.2.29.2.1 / p.2.29.3.1 / p.2.29.3.2，见下）。
  * 2026-09-16 — **p.2.29.3.1 ore_density 倍率接入**：engine `Plan.withOreDensity`（`floor(count × density)` + clamp [0,64]，d=1 恒等快路径，植被/护栏不受影响，非法 d（非有限/越界）确定性拒绝）+ runtime `SubterraFeaturePlan` 基线/有效计划分离（`applyOreDensity` 于 ServerStarting 装配快照期调用，基线不动 → 跨世界/多次开服不累积）+ `FeatureAssemblyProbe` 增 ore-density 段（8 断言）。
  * 2026-09-16 — **p.2.29.2.1 表面热重载再快照**：`RulesRuntime` 规则变更监听注册表（CopyOnWrite）+ 活跃存储 `/subterra rule set|override` 成功路径触发（启动探针钩子的非活跃存储不触发）+ `SubterraSurfaceRules.onRulesMutated` 原子重快照（RuleStore > boot props > vanilla 优先级不变，整体换快照保证读方只见新旧两态）+ marker `palette re-snapshot:`；`SurfaceSeamProbe` +3 接线断言。
  * 2026-09-16 — **p.2.29.3.2 挂载面缺省零增量**：NeoForge 21.1.249 的 biome modifier 数据包注册为裸 `DIRECT_CODEC`（无 ConditionalOps、该版本亦无 config 条件类）——JSON 条件门控不可行；改用**挂载目标 = 缺省空白 tag `#subterra:feature_mount_biomes`**（tag 空 → 不改任何生物群系，真零增量；服务端数据包填 tag 即启用挂载，确定性不变）+ 新增空白 tag 资源 + `FeatureSeamProbe` +2 数据面断言。
  * 诚实披露（2026-09-16 更新）：三缝已由门控快照升级为**真实生成路径注入**并经开服 E2E 断言（注册 marker + ServerStarted 规范渲染 + 门控样例安装）；Overturn 侧可见效果消费留 Overturn p.1.0 验收。 | landed / 已落地 |
| p.2.30 | LAN holepunch / LAN 打洞（自原 pending 排期，接 p.2.5 engine.p2p 同栈；mcwifipnp 移植落点见分配表）：**p.2.30.1** engine.p2p.holepunch 纯 JDK 打洞核心（UDP NAT 穿透 / 端口预测 + relay 回退，复用 p.2.5 寻址/分层/校验槽，确定性探针）；**p.2.30.2** runtime.network.holepunch 接线（EnhancedChannelRuntime 扩展 + E2E 开服探针）。/ p.2.30 LAN holepunch (engine.p2p.holepunch pure-JDK core: UDP NAT traversal/port-prediction + relay fallback over the p.2.5 stack; runtime wiring + E2E). | landed / 已落地 |
  * 2026-09-16 — p.2.30.1 打洞核心落地（`engine.p2p.holepunch` 五件，纯 JDK、确定性）：`PortPredictor`（PRESERVE / INCREMENT（clamp [0,65535]）/ RANDOM 三策略分类与预测，空序列 RANDOM）、`HolePunchPlanner`（PREDICTED/WILDCARD/RELAY 有序尝试、延迟 base+index*step 确定性递增、非法值确定性拒绝、候选空且无兜底确定性拒绝）、`PunchPlan`/`PunchAttempt`（record 深相等、规范渲染无时间戳）、`HolePunchSession`（同事件序同轨迹、直连全败转 RELAY、终态后事件确定性拒绝）；复用 p.2.5 寻址槽（`NodeAddr.Direct`/`ViaRelay`）与 p.2.5.8 relay 兜底语义。
  * 2026-09-16 — p.2.30.2 接线壳落地：`runtime.network.holepunch.HolepunchRuntime` 门控壳（`subterra.probe.holepunch`，缺省 no-op 零 socket、原版握手零接触），门控下在 ServerStarted 跑 loopback 自打洞样例（真实 UDP 双端经计划器互发探针 + 逐字节回执）并输出确定性 marker；`HolepunchProbe` 25 checks 接入 probeAcceptance（120→121，含真实 loopback UDP 自打洞）；开服 marker `shell active` / `gate=on loopback ok` 入 boot 契约。真实 NAT 环境穿透为运行期行为，不作验收断言（验收只断言确定性计划与 loopback 样例）。
| p.2.31 | Service-tool completion / 服务端工具补齐（optim.server 剩余）：**p.2.31.1** StellarRTP——engine.optim.server.teleport 随机传送核心（GPLv3 clean-room 参考 + td 规则化 + `/subterra rtp` 命令接线）；**p.2.31.2** Itemban runtime 壳——engine.optim.server.item_control 核心与 ItemControlProbe 已落地，补 runtime.optim-shell 接线 + E2E marker。/ p.2.31 service-tool completion: StellarRTP (engine.optim.server.teleport, GPLv3 clean-room + td rules + command) + Itemban runtime shell. | landed / 已落地 |
  * 2026-09-16 — p.2.31.2 Itemban runtime 壳收口：壳与纯 JVM 探针此前已落地并接线（`ItemControl.bootstrap` + `[item_control]` bootstrap/ServerStarting 确定性 marker），本轮补开服 E2E 断言——`ServerBootProbe` 契约新增 itemban(active/server) 两项，开服绿。
  * 2026-09-16 — p.2.31.1 StellarRTP clean-room 落地：engine `RtpPlanner`（纯 JDK：StrictMath 三角函数、`PcgSource(worldSeed).fork("subterra.rtp":centerX:centerZ)` 派生、环带采样 + 表面高度回调裁决 + 预算穷尽确定性 found=false、Params 构造期非法值确定性拒绝、规范渲染无时间戳）+ runtime 壳 `RtpRuntime`/`RtpCommand`/`RtpConfig`（td 规则 `config/subterra/rtp.td` 根键 `rtp` 零 json、`/subterra rtp` + `reload` 子命令、种子 ServerStarting 捕获/ServerStopped 复位、门控 `subterra.probe.rtp` 确定性计划 marker）+ `RtpProbe` 21 checks 接入 probeAcceptance（119→120）；开服 marker `shell active` / `server starting; seed captured` 入 boot 契约。上游 GPLv3 仅思想参考、零代码包含；传送冷却为运行期体验项、不作验收断言。
| p.2.32 | Misc visual / UI self-developed / 杂项视觉·界面自研化（Traveler 系列，LGPL/GPL clean-room、均 td 化）：**p.2.32.1** 标题提示 engine.ui.title 数据面 + runtime 接线；**p.2.32.2** 天气 engine.weather（气候→天气确定性，tie 候选）；**p.2.32.3** 超分辨率 engine.render.upscale（后处理管线，性能敏感，tie 化候选）；**p.2.32.4** 皮肤补丁 engine.skin 自研。/ p.2.32 misc visual/UI self-developed (Traveler pack, clean-room): title hints / weather / upscale / skin. | landed / 已落地 |
  * 2026-09-16 — 四子项全落地（每项 = engine 确定性数据面 + runtime 门控壳 + AsyncE2EProbe marker 断言 + 纯 JVM 探针）：**p.2.32.1** `engine.ui.title.TitleHintCore`（文档序首个命中、同键同值去重、离开重置、固定 60 tick 窗口、语言键承载无 HUD 系统感）+ `runtime.ui.title.TitleRuntime`（门控样例：进入/去重/离开轨迹）；**p.2.32.2** `engine.weather.WeatherCore`（气候→天气确定性映射：湿度即时段降雨概率、温度带调制雷暴、16 时段周期表 + `PcgSource` fork 派生、同种子同气候同 tick 恒同）+ `runtime.weather.WeatherRuntime`；**p.2.32.3** `engine.render.upscale.UpscaleModel`（升采样/锐化/色调 pass 组合模型：首 pass 必 UPSCALE、不重复、因子界 [1,4]；数据面不含 GL 实现，GL 接线点文档化、tie 化候选按 p.2.1 桥下沉）+ `runtime.render.upscale.UpscaleRuntime`；**p.2.32.4** `engine.skin.SkinCore`（td 规格序确定性解析：UUID 小写归一 / NAME 精确 / DEFAULT 回落、内建缺省纹理、64x64|64x32 尺寸校验）+ `runtime.skin.SkinRuntime`。四探针 `TitleProbe` 20 / `WeatherProbe` 14 / `UpscaleProbe` 13 / `SkinProbe` 18 checks 接入 probeAcceptance（121→125）；`AsyncE2EProbe` 契约新增 title/weather/upscale/skin 四槽（seen 25–28）。上游 Traveler 系列仅思想参考、零代码包含。
| p.2.33 | API contract completion / api 契约面补齐（api-complete；缺口与理由见 2026-09-15 进度记录；api.worldgen 装配契约随 p.2.29 落地，不在此线）：**p.2.33.1** api.datapack DatapackApi（td 数据包直载/规则/条目/导出，Overturn 三支柱消费面，高优）；**p.2.33.2** api.scale ScaleApi；**p.2.33.3** api.anim AnimationApi；**p.2.33.4** api.ai AiApi（Brain/行为选择/调度）；**p.2.33.5** api.ui UiApi（HUD 数据面 / tooltip / 文档书籍 / 表单）；**p.2.33.6** api.sim SimApi（增量确定性模拟）；**p.2.33.7** api.interact InteractApi（世界内交互）；**p.2.33.8** api.pcg PcgApi（确定性内容生成）；**p.2.33.9** api.save SaveApi（含藏录 Ledger 契约，接 p.2.3 / p.2.10）；**p.2.33.10** api.world WorldApi（世界即产物契约，接 p.2.9）。session / parallel / hub / render-instancing 契约视消费方需求后补。全部 mirror 模式（api 纯 JDK 零 engine 依赖 + ApiMirror 镜像，对齐 api.time / api.lod）。/ p.2.33 API contract completion: ten numbered mirror contracts (DatapackApi first — the Overturn consumption surface; scale / anim / ai / ui / sim / interact / pcg / save incl. the Ledger / world), session / parallel / hub / render-instancing deferred to consumer demand; mirror pattern aligned with api.time / api.lod. 落地小结 / landing notes（2026-09-15，十个契约面全落）：p.2.33.1 DatapackApi（td 数据包消费契约：kind 固定目录段序 / 规则双层 resolve+render / 规范 td 往返逐字节）· p.2.33.2 ScaleApi（9 维固定序）· p.2.33.3 AnimationApi（3 通道固定序 + 确定性采样）· p.2.33.4 AiApi（sense/decide/execute 固定序 + 预算语义 + AiRandom）· p.2.33.5 UiApi（food/saturation + tooltip + 书页 + 表单元件固定序）· p.2.33.6 SimApi（预算三 cap + 空间索引 + 确定性 advance）· p.2.33.7 InteractApi（kind/action/attitude 固定序 + 零 HUD）+ 黑名单 · p.2.33.8 PcgApi（种子派生 + fork + 同种子序列）· p.2.33.9 SaveApi（六槽文档形态 + Ledger 命名 + td/zd 往返）· p.2.33.10 WorldApi（世界包段序 + 往返 + 路径穿越防护）；各 = api 纯 JDK 契约 + engine XxxApiMirror（不 import api，逐字导出）+ 确定性探针（9–29 checks，合计 170 checks）；11 新探针经 runXxxApiProbe 接入 probeAcceptance（114 任务 = 109 纯 JVM + 5 E2E，含 NoiseSettingsTdProbe）；诚实披露：api 层不复制 Xoroshiro128++ PRNG 字节流与 td/zd 编解码 I/O（按 LodApi 先例锁定语义/常量，往返由 engine 侧既有探针承担），AiRandom/export 往返边界均在 javadoc 明示。 | landed / 已落地 |
| p.2.35 | Trimand 微型扩散三定调器 / Micro diffusion tri-tone tuner（**新增**，世界生成拟真线）：给地形生成提供**气候 / 海拔 / 山带**三个宏观定调场（海岸线/水文由三者组合派生，非模型输出）。落地小结 / landing notes（2026-09-15，tie 侧全链落地于 `tie-src/trimand/`）：**p.2.35.1** macrogen 三定调先验生成（elev 海岸幂律锐化 / mtn 双高斯山带 / climate 低频湿润基调受海拔·山带·离海调制；land_map 按 elev(ch1) 取陆，最大 4-连通去碎片、climate 跨海陆连续）；**p.2.35.2** diffunet 微型 UNet（5ch 输入 = 2 条件 + 3 加噪目标，conv 5→4→4→8→4→3，NPAR=1083，单扁平 array<f32,1280> 容量档位，前向/反向数值梯度校验）；**p.2.35.3** 训练（predict-x0，NCH=768，4000 步 best≈0.0075，训练/探针 loss 收敛 + 单步确定性 + serde 往返）；**p.2.35.4** 采样与运行时（rfwd 自包含 forward 与 std/nn 逐位一致 + sample_tile 确定性三场反演；stdens tile 缓存导出 `coarse_climate/coarse_elev/coarse_mountain(x,z,seed)`，FFM 标量 ABI）；四组探针全绿（macrogen 22 / diffunet 9 / trainer 9 / sampler 12 断言）。受益 tie 能力：**值参数泛型 `<CAP: i64 = 1024>`**（tlib 训练栈 nn/loss/optim/serde/diffusion <CAP> 化保持通用，Trimand 全链显式 `<1280>` 实例化）+ 数组长度常量名 + MAX_ARRAY_LEN=2^18 护栏（内存安全）。对接面/ wiring：`tiec --shared` 编 DLL → subterra-tie FFM 装载（engine.tie.TieLibrary）→ 世界生成三缝消费（density/surface/水文组合），清单见 tie-src/trimand/docs.md；Java 融合与 DLL 装配已落地（2026-09-16：`subterra-tie` 模块 + `TrimandBridge` FFM 装载 + engine `AssemblySeam`/`SubterraTrimand` 装配缝 + `TrimandSeamProbe` 40 项 + 开服 ok/skip 双路径验证；**同源复核同日完成**——权威 tiec 独立重建逐位复现，发现捆绑资源为旧代 5 导出产物并已替换为规范 3 导出产物（SHA `44b6b29e…`），替换后探针与开服门全绿）。验收全确定性。/ p.2.35 Trimand micro diffusion tri-tone tuner (**new**, worldgen-realism line): three macro tone fields climate/elev/mtn (hydrology derived downstream, not modeled). tie-side landed in tie-src/trimand — three-tone macro-gen; 5-ch UNet (NPAR 1083, flat tier-1280 pool); predict-x0 trainer (NCH 768, 4000 steps best ≈0.0075); deterministic sampler + stdens tile-cache exports (coarse_climate/coarse_elev/coarse_mountain, FFM scalar ABI); four probes green (22/9/9/12). Enabled by tie v2 value-param generics (`<CAP: i64 = 1024>`; training stack <CAP>-ized, explicit `<1280>` instantiation) + const array lengths + MAX_ARRAY_LEN guard. Wiring: tiec --shared DLL → subterra-tie FFM (TieLibrary) → worldgen consumption; Java fusion + DLL assembly landed (2026-09-16: the `subterra-tie` module + `TrimandBridge` FFM loading + engine `AssemblySeam`/`SubterraTrimand` assembly seam + `TrimandSeamProbe` 40 checks + the boot ok/skip dual path; **the parity check closed the same day** — an independent rebuild with the authoritative tiec reproduced the chain bit-exactly, the bundled resource turned out to be an older 5-export build and was replaced by the canonical 3-export artifact (SHA `44b6b29e…`), probes and the boot gate green after replacement). | landed / 已落地（tie 侧 + Java 融合） |

## Sequence Notes / 顺序说明

* 小任务逐个提交：每完成一个小任务即报告并提交一次，得确认后再做下一个。 / Small tasks are submitted one at a time; each is reported and committed before the next starts.
* 所有子任务完成后统一 review + 清理 + 推送。 / After all sub-tasks complete: one review, cleanup, and push.
* 正式发行 r.x.y.z 从对应 p 版本切出，只做优化与稳定性验证，不引新功能。 / A release `r.x.y.z` is cut from its p version and only stabilizes.
* 许可纪律：permissive（MIT / Apache-2.0 / BSD / Unlicense / ISC）可移植并保留声明；copyleft（GPL / LGPL / AGPL / MPL）仅 clean-room 参考；存疑按不允许处理；完整登记册见框架设计 §7。 / License discipline: permissive may be ported with notices; copyleft is clean-room reference only; uncertain defaults to not allowed; full register in framework design §7.

## Port Allocation Map / 移植落点分配表

* 功能分类（非来源分类）：所有移植按功能域落进 engine.*（纯 JDK 核心）与 runtime.*（MC 层接线）；纯逻辑核心独立纯 JDK 包并可探针。 / Functional organization: every port lands in a functional domain under engine.* (pure-JDK core) or runtime.* (MC wiring); pure-logic cores stay standalone pure-JDK with probes.

| Pending target / 待移植目标 | Function / 功能 | Landing / 落点 | License path / 许可路径 | Status / 状态 |
| --- | --- | --- | --- | --- |
| GeckoLib | 3D 关键帧动画引擎（实体/方块/物品/盔甲） | engine.anim + runtime.anim | MIT 移植，保留声明与捆绑库声明（third-party/geckolib-4.8） | landed / 已落地（p.2.21，AnimProbe） |
| AppleSkin | HUD 数据层（饱食/饱和度覆盖、食物 tooltip） | engine.ui | Unlicense 全量移植（third-party/appleskin-3.0.6） | landed / 已落地（p.2.22，UiProbe） |
| ModMenu | 模组列表交互模型与元数据 API 模式 | engine.ui + runtime.ui | MIT 借鉴（NeoForge 接线自研） | landed / 已落地（p.2.22 视图核心） |
| SmartBrainLib | Brain 编排模型 | engine.ai | MPL-2.0 clean-room 参考 | landed / 已落地（p.2.24，AiProbe） |
| C2ME | 异步区块生成/加载/I/O + FlowSched | engine.worldgen.async + runtime | MIT 直接包含（NeoForge ver/1.21.1） | landed / 已落地（p.2.6，AsyncChunkPipeline） |
| Export-Language-Keys-for-Compasses | 语言键全量导出 | engine.export | MIT 借鉴 | landed / 已落地（p.2.18 language-keys） |
| RollingGate | 规则系统模型（类型化规则 / 双层配置 / 命令面板） | engine.config.rules | LGPL-3.0 clean-room 模型参考；资产 CC-BY-NC-ND 不用 | landed / 已落地（p.2.17，RuleStore） |
| ServerCore 系列 | 实体激活范围 / mobcap / 村民脑死亡 / 同步加载 / 区块 tick 缓存 / 动态距离 / 繁殖合并 | engine.optim + runtime.optim-shell | MIT 部分直接 + GPL 部分 clean-room | landed / 已落地（p.1.4 全线） |
| JEC 读音搜索 | 语言无关读音搜索（zh/ja/ko/gr） | engine.optim.logic.pronounce + runtime.optim-shell | MIT | landed / 已落地（p.1.4.8-.10） |
| InvAdvOpt / SmoothBoot | 库存推进加速 / 线程调优 | runtime.optim-shell | MIT | landed / 已落地（p.1.4.4/.5） |
| Traveler Title / Weather / Super Resolution / 皮肤补丁 | 标题提示 / 天气 / 超分 / 皮肤（自研化） | engine.* + runtime.* | LGPL/GPL clean-room | planned / 已立项（p.2.32） |
| Physics Mod | 方块/生物物理 | — | All Rights Reserved，不适用 | dropped / 剔除 |
| TimeScaleLib | 时间缩放（子弹时间） | — | PolyForm Shield 非标准变体（Noncompete，存疑）→ 不适用；能力 clean-room 自研 | dropped / 剔除（自研替代已落地：engine.time，p.2.26） |
| DataTip | 数据驱动 tooltip（物品→tooltip 行） | engine.ui | GPL-3.0 clean-room 参考（td 化，零 json） | landed / 已落地（p.2.22，TooltipBook） |
| Patchouli | 数据驱动书籍/文档 GUI（手册/图鉴/教程） | engine.ui.docbook | CC-BY-NC-SA 3.0 clean-room 参考（td 化，零 json） | landed / 已落地（p.2.22，DocBookTd） |
| Feature Recycler / VoxelBridge / OptiCores | 待定义 | — | All Rights Reserved 或语义不匹配 | dropped / 剔除 |
| StellarRTP | 随机传送 | engine.optim.server.teleport | GPLv3 clean-room 参考 | planned / 已立项（p.2.31.1） |
| Itemban | 物品使用控制 | engine.optim.server.item_control | Apache-2.0 | landed（engine 核心 + ItemControlProbe；runtime 壳 p.2.31.2）/ 已落地（engine 核心，壳接线 p.2.31.2） |
| mcwifipnp | LAN 打洞 | runtime.network.holepunch | 移植为原生模块 | planned / 已立项（p.2.30） |
| Pehkui-Rebuilt | 实体尺寸缩放（ScaleType / ScaleData / ScaleModifier / ScaleRegistries、按维度/标签、数据包规则） | engine.scale + runtime.scale | MIT 移植，保留 Virtuoel 原始版权与重建声明（third-party/pehkui-rebuilt-3.8.5） | landed / 已落地（p.2.25，ScaleProbe） |
| Flywheel | GPU 实例化渲染 + 着色器模板系统（实体 / 方块实体路径） | engine.render.instancing + runtime.render | MIT 直接包含（形态 A，保留声明；p.2.27.1 前哨；third-party/flywheel-1.0.6） | landed / 已落地（p.2.27.1，FlywheelInstancingProbe） |
| Distant Horizons | LOD 远端区块低细节渲染（视距扩展；mip 金字塔 / 列柱剖面 / 水平面合并） | engine.render.lod + runtime.render.lod | LGPL-3.0 clean-room 参考（仅思想，能力自研 + tie 化替代，零源码包含） | landed / 已落地（p.2.28，LodProbe） |

* 审计小结（2026-09-15，双确认：里程碑 + 代码存在性）/ audit summary: 共 22 项——**已落地 15**（p.1.4 优化线 3 + p.2 线 12：anim / ui / ai / scale / time / render-instancing / render-lod / rules / export / worldgen-async）、**Itemban engine 核心 + 探针已落**（runtime 壳待接线）、**未落地 3**（planned：Traveler 系列 / StellarRTP；pending：mcwifipnp）、**剔除 3**（Physics / Feature 系列 / TimeScaleLib——自研替代 engine.time 已落地 p.2.26）。 / 22 entries total — 15 landed, Itemban engine core + probe landed (runtime shell pending), 3 not landed (planned: Traveler pack / StellarRTP; pending: mcwifipnp), 3 dropped (Physics / Feature pack / TimeScaleLib — the self-developed replacement engine.time already landed as p.2.26).

## Progress Log / 进度记录

* 2026-09-08 — p.2.0 landed: module re-layout api / engine / runtime / migrate / devkit + IronLawProbe dependency iron law, probes green / 2026-09-08 — p.2.0 落地：api / engine / runtime / migrate / devkit 模块重构 + IronLawProbe 依赖铁律，探针全绿。
* 2026-09-08 — Patchouli assessed / Patchouli 评估：CC-BY-NC-SA 3.0 → 仅思想参考；文档书籍 GUI clean-room 自研（engine.ui.docbook，td 驱动零 json）。 / 2026-09-08 — Patchouli 评估：CC-BY-NC-SA 3.0 → 仅思想参考；文档书籍 GUI clean-room 自研（engine.ui.docbook，td 驱动零 json）。
* 2026-09-08 — DataTip assessed / DataTip 评估：GPL-3.0 → 仅思想参考；数据驱动 tooltip 数据层 clean-room 自研（engine.ui，td 化，零 json，与 AppleSkin 协同）。 / 2026-09-08 — DataTip 评估：GPL-3.0 → 仅思想参考；数据驱动 tooltip 数据层 clean-room 自研（engine.ui，td 化，零 json，与 AppleSkin 协同）。
* 2026-09-08 — TimeScaleLib assessed / TimeScaleLib 评估：PolyForm Shield 非标准变体（Noncompete）→ 不可移植，框架时间缩放能力 clean-room 自研（engine.time，p.3.3）。 / 2026-09-08 — TimeScaleLib 评估：PolyForm Shield 非标准变体（Noncompete）→ 不可移植，时间缩放能力 clean-room 自研（engine.time，p.3.3）。
* 2026-09-08 — Pehkui-Rebuilt registered / Pehkui-Rebuilt 入册：实体缩放能力纳入框架（engine.scale，MIT 移植，保留 Virtuoel 原始版权）。 / 2026-09-08 — Pehkui-Rebuilt 入册：实体缩放能力纳入框架（engine.scale，MIT 移植，保留 Virtuoel 原始版权）。
* 2026-09-08 — Roadmap split per repo / 路线图按仓库拆分：Subterra 框架线迁入本文件，系列 ROAD 仅保留总览。 / 2026-09-08 — 路线图按仓库拆分：Subterra 框架线迁入本文件，系列 ROAD 仅保留总览。
* 2026-09-08 — **Framework design finalized / 框架设计定稿**: Subterra promoted to a full-stack development framework mod (design `docs/2026-09-08-subterra-framework-design.md`); port register extended (GeckoLib / AppleSkin / ModMenu / SmartBrainLib / C2ME / Export-Language-Keys-for-Compasses / RollingGate; Physics Mod dropped as All Rights Reserved). / 2026-09-08 — **框架设计定稿**：Subterra 升格为全栈开发框架模组；移植登记册扩展（…；Physics Mod 因 All Rights Reserved 剔除）。
* 2026-09-08 — p.1.8.32 landed (perf: dedup climate-spline/sloped-cheese evals, CachedDensity single-slot ThreadLocal) — pre-framework line last item. / 2026-09-08 — p.1.8.32 落地（性能：气候样条/乳酪去重缓存）——框架化前最后一项。
* 2026-09-09 — Flywheel assessed / Flywheel 评估：MIT → 形态 A「直接包含」（1.0.6，1.21.1/NeoForge 线）入册，划为 p.2.27 自研渲染管线的前哨子项 p.2.27.1（engine.render.instancing + runtime.render）；自研渲染管线前移至 p.2.20 之后、p.2.21 动画之前（渲染线提前，实例化基座供后续 runtime 渲染接线共用）。 / 2026-09-09 — Flywheel assessed: MIT → direct-include 1.0.6 (1.21.1/NeoForge line) registered as p.2.27.1 (advance guard for p.2.27); the self-developed render line was advanced to right after p.2.20.
* 2026-09-09 — p.2.7 landed: engine.parallel 确定性并行执行底座（p.2.7.1-5，全量验收全绿）——KeyPartition 通用确定性键分区（与 p.2.6 ChunkPartition 双 long 路径逐位一致）、WorkerBank 有界串行 lane、DeterministicDispatcher 重复键显式拒绝（契约升级：last-write-wins → IllegalArgumentException）、ParallelRunner 并行/串行逐位一致 + 批次生命周期、optim.sched 组合探针（池化暂存不破确定性）；ChunkKey 域与 p.2.6 归并序一致；probeAcceptance 全绿（72 纯 JVM + 五次 E2E）。/ 2026-09-09 — p.2.7 landed: engine.parallel deterministic parallel execution base (p.2.7.1-5, acceptance all green) — generic KeyPartition (two-long path bit-identical to p.2.6 ChunkPartition), bounded serial WorkerBank, DeterministicDispatcher with explicit duplicate-key rejection (contract upgrade: last-write-wins → IllegalArgumentException), ParallelRunner with byte-identical serial golden path + batch lifecycle, optim.sched combination probe (pooled scratch stays byte-identical); ChunkKey-domain merge order matches p.2.6; probeAcceptance all green (72 pure-JVM + five E2E boots).
* 2026-09-09 — p.2.8 landed: engine.sim 增量确定性模拟底座（p.2.8.1-6，全量验收全绿）——budget 预算分级调度（per/region/global 三级 cap、原子记账、确定性 key 序降级，p.2.26 前哨）、spatial 分区空间索引（半径邻域只访问局部窗口、无全局扫描，cell 键经 p.2.7 双 long 路径入 bank）、core 增量确定性核心（advance(dirty) 只推进变化局部、逐 key 顺序无关、diff/回放与全量重算逐字节一致）、run per-simulant 隔离 + 确定性归并门面（SimRunner 直接消费 p.2.7 ParallelRunner，并行/串行逐字节一致）、SimCombineProbe 全轨组合简景、runtime.sim.SimRuntime 门控默认 no-op 壳（[Subterra sim] marker + 四微型确定性校验，并入 AsyncE2EProbe 同开服，五次开服总数不变）；probeAcceptance 全绿（77 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim marker）。p.2.9 前哨提示词见 docs/plans/prompt-p.2.9-artifact.md。/ 2026-09-09 — p.2.8 landed: engine.sim incremental deterministic simulation base (p.2.8.1-6, acceptance all green) — budget-tier scheduling (per-simulant/region/global caps, atomic accounting, deterministic key-order degradation, the p.2.26 precursor), partitioned spatial index (local-window neighborhood only, no global scan, cell keys through the p.2.7 two-long path into banks), incremental deterministic core (dirty-set advance, order-independent per-key ticks, diff/replay byte-identical to full recompute), per-simulant isolation + deterministic merge facade (SimRunner over the p.2.7 ParallelRunner), the SimCombineProbe full-stack caricature, and the gate-off runtime.sim.SimRuntime shell ([Subterra sim] markers + four micro-checks, merged into the AsyncE2EProbe boot — five boots unchanged); probeAcceptance all green (77 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim markers together). p.2.9 preview prompt: docs/plans/prompt-p.2.9-artifact.md.
* 2026-09-09 — p.2.9 landed: World-as-artifact 世界即产物（p.2.9.1-6，全量验收全绿）——世界包**统一档案形态**（engine.world.WorldPack 单一 td 文档 world=[version,meta,save,datapack]，save/datapack 为 base64 载荷、meta TreeMap 排序、段序固定）+ export(rehydrate(export(w))) 逐字节往返契约 + p.2.8 稳态 meta 键衔接；六槽/藏录经 SaveContainer 槽复用、DatapackPack 数据包并入；WorldPackPacker 世界工作树目录↔单 td 打包/还原（只扫六固定 SaveSlot 目录、无全局遍历、路径穿越防护，可搬移互导）；engine.export 导出端口预留（p.2.18 前哨，ExportKind/ExportPort/ExportHub，WORLD_PACK + SAVE 两内建端口确定性 preload）；runtime.world.WorldRuntime 门控壳并入既有开服（-Dsubterra.probe.world 默认 no-op、[Subterra world] marker、staging 隔离不写服务器，五次开服不变）；WorldPackProbe 20 项确定性断言；probeAcceptance 全绿（78 纯 JVM + 五次 E2E，AsyncE2EProbe 同时断言 async+sim+world marker）。p.2.10 前哨提示词见 docs/plans/prompt-p.2.10-save-verifiability.md。/ 2026-09-09 — p.2.9 landed: World-as-artifact (p.2.9.1-6, acceptance all green) — uniform world-pack archive form (engine.world.WorldPack, single td doc world=[version,meta,save,datapack], base64 payloads, TreeMap-sorted meta, fixed section order) + byte-exact export(rehydrate(export(w))) round-trip + p.2.8 steady-state meta keys; six-slots/Ledger reused via the SaveContainer slots, DatapackPack datapacks folded in; WorldPackPacker world-work-tree dir↔single-td pack/restore (only the six fixed SaveSlot dirs scanned, no global scan, path-traversal guards, movable/interchangeable); engine.export port cut reserved (p.2.18 seam — ExportKind/ExportPort/ExportHub, WORLD_PACK + SAVE built-ins deterministically preloaded); the gated runtime.world.WorldRuntime shell merged into the existing boots (-Dsubterra.probe.world default no-op, [Subterra world] markers, staging-isolated no server write — five boots unchanged); WorldPackProbe 20 deterministic checks; probeAcceptance all green (78 pure-JVM + five E2E boots, AsyncE2EProbe asserting async+sim+world markers together). p.2.10 preview prompt: docs/plans/prompt-p.2.10-save-verifiability.md.
* 2026-09-11 — p.2.15 landed: engine 迁移落点稳定（optim/config/log 纯 JDK 核心全部确认在 engine.*，p.2.15.1 审计零行为变化、旧 FQCN 缺席）+ vanilla 数值对照 golden-test 化（VanillaGoldenGen 冻结 seed=44905237 的 NoiseRouter 镜像 12 点×15 场共 180 个位模式为 td 资产，golden_version=1 锚点、int64 逐位无损往返；VanillaGoldenProbe 逐位回归/确定性再入/文档往返/元数据锚定接入 probeAcceptance），probeAcceptance 全绿（84 纯 JVM + 五次 E2E）；p.2.16 前哨提示词见 docs/plans/prompt-p.2.16-api-contract-surface.md。 / 2026-09-11 — p.2.15 landed: engine migration landed stably (optim/config/log pure-JDK cores all confirmed in engine.*, zero behavior change and no old FQCNs per the p.2.15.1 audit) + vanilla value comparisons hardened as golden tests (VanillaGoldenGen froze 180 mirror bit patterns of the seed-44905237 NoiseRouter into a td asset, golden_version=1 anchor, bit-exact int64 round-trip; VanillaGoldenProbe bit-exact regression / determinism re-entry / doc round-trip / metadata anchor wired into probeAcceptance), probeAcceptance all green (84 pure-JVM + five E2E boots); p.2.16 preview prompt: docs/plans/prompt-p.2.16-api-contract-surface.md.
* 2026-09-11 — p.2.16 landed: api 契约面定型（p.2.16.1-5，收尾 p.2.16.6）——四契约面全纯 JDK：worldgen 契约（NoiseRouterContract：15 规范字段名 + golden 锚点 seed/version + 12 固定采样点 + 位模式布局，p.2.15 金标的数据底座化为契约形状）+ config(rules) 契约与规则注册 API（Rule/RuleSet/RuleRegistry：固定注册序、同 key 重复拒绝、双层解析与 render 对齐 DatapackRules，p.2.17 消费方前哨）+ export 契约与 Exporter 注册表（ExportKindSpec/ExportPortSpec/ExporterRegistry：注册序查表、重复拒绝、form=world/save/datapack 与 engine.ExportKind 一致，p.2.18 前哨）+ event 契约（EventKind/EventEnvelope/EventStream：monotonic-seq 门拒乱序/重放、类型化载荷可回放，承接 p.2.4/2.5/2.11）+ ContractProbe 38 checks 接入 probeAcceptance，probeAcceptance 全绿（85 纯 JVM + 五次 E2E）；p.2.17 前哨提示词见 docs/plans/prompt-p.2.17-rule-system.md。 / 2026-09-11 — p.2.16 landed: the API contract surface is typed (p.2.16.1-5, closing p.2.16.6) — four pure-JDK contract surfaces: the worldgen contract (NoiseRouterContract: 15 canonical field names + golden anchor seed/version + 12 fixed sample points + bit-pattern layout, the p.2.15 golden base shaped into a contract) + the config(rules) contract and rule-registration API (Rule/RuleSet/RuleRegistry: fixed registration order, duplicate-key rejection, two-tier resolve/render aligned with DatapackRules, the p.2.17 consumer precursor) + the export contract and Exporter registry (ExportKindSpec/ExportPortSpec/ExporterRegistry: registration-order lookup, duplicate rejection, form=world/save/datapack 1:1 with engine.ExportKind, the p.2.18 precursor) + the event contract (EventKind/EventEnvelope/EventStream: monotonic-seq gate rejecting reorder/replay, typed replayable payloads, over p.2.4/2.5/2.11) + ContractProbe 38 checks wired into probeAcceptance, probeAcceptance all green (85 pure-JVM + five E2E boots); p.2.17 preview prompt: docs/plans/prompt-p.2.17-rule-system.md.
* 2026-09-11 — p.2.17 landed: engine.config.rules 规则系统全轨落地（p.2.17.1-5，收尾 p.2.17.6）——类型化规则模型（RuleType/RuleKey/RuleSpec，值载体复用 p.2.16.2 api.config.Rule 契约）+ 确定性校验（UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER，RuleViolationException 全有或全无装载）+ 双层配置（RuleStore 全局档 + 存档覆盖档，resolve=global→overrides 胜出、render 字典序，语义对齐 RuleRegistry.resolveTiered 与 p.2.3.5）+ 热重载（RuleReloader 逐字节变更检测、原子 apply-or-keep-old、重入幂等、revision 单调）+ `/subterra rule` 命令（view/set/override 共享确定性核心 + RulesRuntime 门控钩子 subterra.probe.rule）+ RuleSystemProbe 43 checks + DatapackE2EProbe 32 项（含 rule marker）；probeAcceptance 全绿（86 纯 JVM + 五次 E2E）；p.2.18 前哨提示词见 docs/plans/prompt-p.2.18-export-hub.md。 / 2026-09-11 — p.2.17 landed: the engine.config.rules rule system fully landed (p.2.17.1-5, closing p.2.17.6) — typed rule model (RuleType/RuleKey/RuleSpec, value carrier reusing the p.2.16.2 api.config.Rule contract) + deterministic validation (UNKNOWN_KEY/TYPE_VIOLATION/CONFLICT/OTHER, all-or-nothing load via RuleViolationException) + two-tier configuration (RuleStore global tier + save-override tier, resolve=global→overrides wins, TreeMap render, aligned with RuleRegistry.resolveTiered and p.2.3.5) + hot reload (RuleReloader byte-identical change detection, atomic apply-or-keep-old, re-entrant idempotent, monotonic revision) + the `/subterra rule` command (view/set/override over a shared deterministic core + RulesRuntime gate hook subterra.probe.rule) + RuleSystemProbe 43 checks + DatapackE2EProbe 32 asserts (incl. the rule marker); probeAcceptance all green (86 pure-JVM + five E2E boots); p.2.18 preview prompt: docs/plans/prompt-p.2.18-export-hub.md.
* 2026-09-11 — p.2.18 landed: engine.export 正式导出枢纽全轨落地（p.2.18.1-6，收尾 p.2.18.7）——功能性 ExportProducer（td/zd 双格式 + export∘rehydrate∘export 逐字节恒等契约）+ ExportKind 扩为七 form + ExportHub 注册序/重复拒对齐 p.2.16.3 api.ExporterRegistry + 五个确定性导出器（language-keys / config / world / registries / migrate-maps，各字典序固定字段序禁时间戳）+ /subterra export hub 接线（exportForm 共享核心、ExportHubCommand/ExportHubRuntime、subterra.probe.exportHub 门控钩子）+ ExportHubProbe 41 checks + ContractProbe 导出 form 断言前缀化修复；probeAcceptance 全绿（87 纯 JVM + 五次 E2E）；p.2.19 前哨提示词见 docs/plans/prompt-p.2.19-runtime-wiring-migration.md。 / 2026-09-11 — p.2.18 landed: the engine.export formal export hub fully landed (p.2.18.1-6, closing p.2.18.7) — functional ExportProducer (td/zd dual formats + byte-identical export∘rehydrate∘export identity contract) + ExportKind grown to seven forms + ExportHub registration-order/dup-reject aligned with the p.2.16.3 api.ExporterRegistry + five deterministic exporters (language-keys / config / world / registries / migrate-maps, each key-sorted fixed-field no-timestamps) + the /subterra export hub wiring (shared exportForm core, ExportHubCommand/ExportHubRuntime, subterra.probe.exportHub gated hook) + ExportHubProbe 41 checks + the ContractProbe exporter-form prefix-semantics fix; probeAcceptance all green (87 pure-JVM + five E2E boots); p.2.19 preview prompt: docs/plans/prompt-p.2.19-runtime-wiring-migration.md.
* 2026-09-11 — p.2.19 landed: runtime 统一接线迁移（p.2.19.1-6，收尾 p.2.19.7）——接线总表 docs/runtime-wiring.md（双语盘点 20 行）+ 六类迁入（launch LaunchRuntime / runtime-fix FixRuntime / cfglog ConfigRuntime+LogRuntime / tie TieRuntime（FFM 真实 ABI 调用 ok/skip）/ worldgen / optim-shell 落点确认）+ /subterra export 指令收编（ExportCommandCore 单核心、export cmd / export hub markers 逐字不变、ExportRuntime 门控）+ RuntimeWiringProbe 52 checks；probeAcceptance 全绿（88 纯 JVM + 五次 E2E）；p.2.20 前哨提示词见 docs/plans/prompt-p.2.20-devkit-complete.md。 / 2026-09-11 — p.2.19 landed: unified runtime wiring migration (p.2.19.1-6, closing p.2.19.7) — bilingual wiring inventory docs/runtime-wiring.md (20 rows) + six-category fold-in (launch LaunchRuntime / runtime-fix FixRuntime / cfglog ConfigRuntime+LogRuntime / tie TieRuntime (real FFM ABI call ok/skip) / worldgen / optim-shell landing confirmed) + /subterra export command fold-in (single ExportCommandCore, export cmd / export hub markers character-for-character unchanged, ExportRuntime gated) + RuntimeWiringProbe 52 checks; probeAcceptance all green (88 pure-JVM + five E2E boots); p.2.20 preview prompt: docs/plans/prompt-p.2.20-devkit-complete.md.
* 2026-09-11 — p.2.20 landed: devkit 完整化 + 官方示例模组脚手架（p.2.20.1-5，收尾 p.2.20.6）——probeAcceptance 93/94 任务全量接线零缺口（p.2.20.1 接线盘点）+ devkit 任务面完整化（探针统一 verification 分组、一次性生成器隔离 devkit 组，p.2.20.2）+ 最小「颠覆性模组」官方示例 samples/overturn-minimal（双语 README + 清单式结构 + 能力面：规则双层 / td 数据包直载 / export td/zd 恒等 / tie skip-or-ok，确定性报告，p.2.20.3-4）+ SampleBootstrapProbe 13 checks「示例即验收」接入 probeAcceptance（p.2.20.5）；probeAcceptance 全绿（89 纯 JVM + 五次 E2E）；p.2.27.1 前哨提示词见 docs/plans/prompt-p.2.27.1-flywheel-port.md。 / 2026-09-11 — p.2.20 landed: devkit complete + official sample mod scaffold (p.2.20.1-5, closing p.2.20.6) — probeAcceptance 93/94 tasks fully wired with zero gaps (p.2.20.1 inventory) + devkit task surface completed (probes grouped under verification, one-time generators isolated to the devkit group, p.2.20.2) + the minimal vanilla-overturning official sample samples/overturn-minimal (bilingual README + manifest structure + capability surfaces: two-tier rules / td datapack direct load / export td/zd identity / tie skip-or-ok, deterministic report, p.2.20.3-4) + SampleBootstrapProbe 13 checks “sample-is-the-acceptance” wired into probeAcceptance (p.2.20.5); probeAcceptance all green (89 pure-JVM + five E2E boots); p.2.27.1 preview prompt: docs/plans/prompt-p.2.27.1-flywheel-port.md.
* 2026-09-11 — p.2.27.1 landed: Flywheel port / 飞轮移植前哨（p.2.27.1.1-.2，收尾 p.2.27.1.3）——engine.render.instancing 纯 JDK 实例化核心（InstanceFormat / ShaderTemplate（clean-room 注明）/ RenderBackend+BackendRegistry，MIT 直接包含保留声明，third-party/flywheel-1.0.6）+ runtime.render RenderRuntime 门控壳（subterra.probe.render，[Subterra render] marker 并入同开服）+ FlywheelInstancingProbe 34 checks 接入 probeAcceptance（95 任务 = 90 纯 JVM + 5 E2E）；probeAcceptance 全绿（90 纯 JVM + 五次 E2E）；p.2.27 前哨提示词见 docs/plans/prompt-p.2.27-self-rendering.md。 / 2026-09-11 — p.2.27.1 landed: the Flywheel port guard (p.2.27.1.1-.2, closing p.2.27.1.3) — engine.render.instancing pure-JDK instancing core (InstanceFormat / ShaderTemplate (flagged clean-room) / RenderBackend+BackendRegistry, MIT direct-include with notices preserved under third-party/flywheel-1.0.6) + the runtime.render RenderRuntime gated shell (subterra.probe.render, [Subterra render] markers merged into the same boot) + FlywheelInstancingProbe 34 checks wired into probeAcceptance (95 tasks = 90 pure-JVM + 5 E2E); probeAcceptance all green (90 pure-JVM + five E2E boots); p.2.27 preview prompt: docs/plans/prompt-p.2.27-self-rendering.md.
* 2026-09-11 — p.2.27 landed: 自研渲染管线 clean-room 落地（p.2.27.1-3，收尾 p.2.27.4）——engine.render 原版保真渲染面模型（RenderStage 10 阶段固定序 / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B，逐字节一致、1.21.1 规范名对照无 copyleft）+ runtime.render 三 MC 钩子注入（RenderHooks server 侧核对 + RenderHooksClient 客户端事件注入，门控 no-op 不改变输出，与 p.2.27.1 实例化基座互补）+ RenderFidelityProbe 62 checks 接入 probeAcceptance（96 任务 = 91 纯 JVM + 5 E2E）；probeAcceptance 全绿（91 纯 JVM + 五次 E2E）；p.2.21 前哨提示词见 docs/plans/prompt-p.2.21-animation.md。 / 2026-09-11 — p.2.27 landed: the clean-room self-developed render pipeline (p.2.27.1-3, closing p.2.27.4) — engine.render vanilla-fidelity surface model (RenderStage 10-stage fixed order / MaterialSurface / VertexLayout BLOCK 32B + ENTITY 28B, byte-identical, 1.21.1 canonical-name reference without copyleft) + runtime.render three-MC-hook injection (RenderHooks server-side check + RenderHooksClient client-side event injection, gated no-op, no render-output change, complementary to the p.2.27.1 instancing base) + RenderFidelityProbe 62 checks wired into probeAcceptance (96 tasks = 91 pure-JVM + 5 E2E); probeAcceptance all green (91 pure-JVM + five E2E boots); p.2.21 preview prompt: docs/plans/prompt-p.2.21-animation.md.
* 2026-09-11 — p.2.21 landed: 动画核心 MIT 移植落地（p.2.21.1-3，收尾 p.2.21.4）——engine.anim 动画核心（AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler 确定性采样（线性/最短弧/固定 clamp）+ AnimStateMachine 确定性状态机（注册序/重复拒/过渡线性缓动/tick 回绕），GeckoLib 4.8 MIT 声明与捆绑库声明材料 third-party/geckolib-4.8）+ runtime.anim AnimRuntime 门控壳（subterra.probe.anim 缺省 no-op、[Subterra anim] ok marker 并入 AsyncE2EProbe 同开服）+ AnimProbe 30 checks 接入 probeAcceptance（97 任务 = 92 纯 JVM + 5 E2E）；probeAcceptance 全绿（92 纯 JVM + 五次 E2E）；p.2.22 前哨提示词见 docs/plans/prompt-p.2.22-ui-hud-core.md。 / 2026-09-11 — p.2.21 landed: the animation core MIT port (p.2.21.1-3, closing p.2.21.4) — the engine.anim animation core (AnimationData/Keyframe/AnimChannel/KeyframeTrack/AnimSampler deterministic sampling (linear / shortest-arc / fixed clamp) + AnimStateMachine deterministic state machine (registration order / duplicate rejection / linearly-eased transitions / tick wrap), GeckoLib 4.8 MIT notice + bundled-library notice materials under third-party/geckolib-4.8) + the runtime.anim AnimRuntime gated shell (subterra.probe.anim default no-op, the [Subterra anim] ok marker merged into the AsyncE2EProbe boot) + AnimProbe 30 checks wired into probeAcceptance (97 tasks = 92 pure-JVM + 5 E2E); probeAcceptance all green (92 pure-JVM + five E2E boots); p.2.22 preview prompt: docs/plans/prompt-p.2.22-ui-hud-core.md.
* 2026-09-11 — p.2.22 landed: UI/HUD 核心落地（p.2.22.1-5，收尾 p.2.22.6）——engine.ui 确定性数据面（p.2.22.1 AppleSkin HUD 数据层 Unlicense 全量移植：FoodValues/FoodTooltipRow/HudData/FoodOverlay 纯 JDK 确定性、third-party/appleskin-3.0.6 许可材料注明来源/版本/适配范围未捆绑源码；p.2.22.2 数据驱动 TooltipBook/TooltipRule + DocBookTd/BookDef/BookPage 文档书籍（td 化零 json，clean-room 参考 DataTip GPL-3.0 / Patchouli CC-BY-NC-SA 仅思想）；p.2.22.3 列表/详情/许可视图核心 ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore（ModMenu MIT 借鉴不复制代码，面板仅管理视图、玩家交互走 p.2.14））+ runtime.ui UiRuntime 门控壳（subterra.probe.ui 缺省 no-op、[Subterra ui] ok marker 并入 AsyncE2EProbe 同开服，HUD/tooltip/书籍消费接线面文档化为 p.2.23 接线点）+ UiProbe 30 checks 接入 probeAcceptance（98 任务 = 93 纯 JVM + 5 E2E）；probeAcceptance 全绿（93 纯 JVM + 五次 E2E）；p.2.23 前哨提示词见 docs/plans/prompt-p.2.23-in-game-mod-hub.md。 / 2026-09-11 — p.2.22 landed: the UI/HUD core (p.2.22.1-5, closing p.2.22.6) — the engine.ui deterministic data plane (p.2.22.1 the AppleSkin HUD data-layer full Unlicense port: pure-JDK deterministic FoodValues/FoodTooltipRow/HudData/FoodOverlay, license materials under third-party/appleskin-3.0.6 citing source/version/adaptation scope, no source bundled; p.2.22.2 the data-driven TooltipBook/TooltipRule + the DocBookTd/BookDef/BookPage doc book (td-ized zero-json, clean-room reference of DataTip GPL-3.0 / Patchouli CC-BY-NC-SA ideas only); p.2.22.3 the list/detail/license view cores ModMetadata/ModListViewCore/ModDetailViewCore/LicenseViewCore (ModMenu MIT pattern borrow without copying code, admin views only, player interaction via p.2.14)) + the runtime.ui UiRuntime gated shell (subterra.probe.ui default no-op, the [Subterra ui] ok marker merged into the AsyncE2EProbe boot, the HUD/tooltip/book consumption surfaces documented as p.2.23 wiring points) + UiProbe 30 checks wired into probeAcceptance (98 tasks = 93 pure-JVM + 5 E2E); probeAcceptance all green (93 pure-JVM + five E2E boots); p.2.23 preview prompt: docs/plans/prompt-p.2.23-in-game-mod-hub.md.
* 2026-09-11 — p.2.23 landed: In-game mod hub 全轨落地（p.2.23.1-4，收尾 p.2.23.5）——engine.hub 表单自动生成核心（复用 p.2.12.4 FormDataGen kind→widget 固定映射）+ 配置编辑 + 热重载核心（复用 p.2.17 RuleStore/RuleReloader 语义：双层写、原子 apply-or-keep-old、revision 单调；Reloader 直接委托不重复实现）+ runtime.hub HubRuntime 门控壳（subterra.probe.hub 缺省 no-op、[Subterra hub] ok marker 并入 AsyncE2EProbe 同开服）+ /subterra hub op 配置命令（view/form/set/override 共享确定性核心 runVerb）+ HubProbe 27 checks 接入 probeAcceptance（99 任务 = 94 纯 JVM + 5 E2E）；probeAcceptance 全绿（94 纯 JVM + 五次 E2E）；p.2.24 前哨提示词见 docs/plans/prompt-p.2.24-ai-behavior-core.md。 / 2026-09-11 — p.2.23 landed: the in-game mod hub fully landed (p.2.23.1-4, closing p.2.23.5) — the engine.hub auto-generated-forms core (over the p.2.12.4 FormDataGen kind→widget fixed map) + the config-edit + hot-reload core (over the p.2.17 RuleStore/RuleReloader semantics: two-tier writes, atomic apply-or-keep-old, monotonic revision; the Reloader delegates, no re-implementation) + the runtime.hub HubRuntime gated shell (subterra.probe.hub default no-op, the [Subterra hub] ok marker merged into the AsyncE2EProbe boot) + the /subterra hub op config command (view/form/set/override over the shared deterministic runVerb core) + HubProbe 27 checks wired into probeAcceptance (99 tasks = 94 pure-JVM + 5 E2E); probeAcceptance all green (94 pure-JVM + five E2E boots); p.2.24 preview prompt: docs/plans/prompt-p.2.24-ai-behavior-core.md.
* 2026-09-11 — p.2.24 landed: AI behavior core 落地（p.2.24.1-4，收尾 p.2.24.5）——engine.ai Brain 编排核心（Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask，clean-room，SmartBrainLib MPL-2.0 仅参考零代码包含，固定注册序/重复拒/sense→decide→execute）+ 行为选择与调度（BehaviorPriority 优先级裁决 / BrainScheduler TickBudget 三 cap 对齐 p.2.8 / AiRandom 委托 PcgSource 同种子同序列）+ runtime.ai AiRuntime 门控壳（subterra.probe.ai）+ AiProbe 29 checks 接入 probeAcceptance（100 任务 = 95 纯 JVM + 5 E2E）；probeAcceptance 全绿（95 纯 JVM + 五次 E2E）；p.2.25 前哨提示词见 docs/plans/prompt-p.2.25-entity-scale.md。 / 2026-09-11 — p.2.24 landed: the AI behavior core (p.2.24.1-4, closing p.2.24.5) — the engine.ai Brain orchestration core (Brain/BrainMemory/BrainSensor/BrainBehavior/BrainTask, clean-room, SmartBrainLib MPL-2.0 reference only zero code inclusion, fixed registration order / duplicate rejection / sense→decide→execute) + behavior selection & scheduling (BehaviorPriority arbitration / BrainScheduler TickBudget three caps over p.2.8 / AiRandom delegating PcgSource same-seed-same-sequence) + the runtime.ai AiRuntime gated shell (subterra.probe.ai) + AiProbe 29 checks wired into probeAcceptance (100 tasks = 95 pure-JVM + 5 E2E); probeAcceptance all green (95 pure-JVM + five E2E boots); p.2.25 preview prompt: docs/plans/prompt-p.2.25-entity-scale.md.
* 2026-09-11 — p.2.25 landed: 实体缩放核心 MIT 移植落地（p.2.25.1-3，收尾 p.2.25.4）——engine.scale 实体缩放核心（Pehkui-Rebuilt 3.8.5 MIT 移植，纯 JDK：ScaleType 固定 9 维序 / ScaleData / ScaleModifier / ScaledEntityData 按标签缩放 + 数据包缩放规则 td 化 ScaleRuleDocument，MIT 声明保留 Virtuoel 版权与重建声明 third-party/pehkui-rebuilt-3.8.5）+ runtime.scale ScaleRuntime 门控壳（subterra.probe.scale 缺省 no-op、[Subterra scale] ok marker 并入 AsyncE2EProbe 同开服）+ ScaleProbe 23 checks 接入 probeAcceptance（101 任务 = 96 纯 JVM + 5 E2E）；probeAcceptance 全绿（96 纯 JVM + 五次 E2E）；p.2.26 前哨提示词见 docs/plans/prompt-p.2.26-time-scaling.md。 / 2026-09-11 — p.2.25 landed: the entity-scaling core MIT port (p.2.25.1-3, closing p.2.25.4) — the engine.scale entity-scaling core (Pehkui-Rebuilt 3.8.5 MIT port, pure JDK: fixed 9-dim ScaleType order / ScaleData / ScaleModifier / ScaledEntityData per-tag scaling + td-ized datapack rules ScaleRuleDocument, MIT notices preserving Virtuoel's copyright + the rebuild declaration under third-party/pehkui-rebuilt-3.8.5) + the runtime.scale ScaleRuntime gated shell (subterra.probe.scale default no-op, the [Subterra scale] ok marker merged into the AsyncE2EProbe boot) + ScaleProbe 23 checks wired into probeAcceptance (101 tasks = 96 pure-JVM + 5 E2E); probeAcceptance all green (96 pure-JVM + five E2E boots); p.2.26 preview prompt: docs/plans/prompt-p.2.26-time-scaling.md.
* 2026-09-11 — p.2.26 landed: 时间缩放全轨 clean-room 落地（p.2.26.1-5，收尾 p.2.26.6）——engine.time 时间缩放核心（TimeDomain 四域固定序 / FlowRate / TimeScale GLOBAL 回退链 / TimeThrottle / TimeInterpolation / TimeScaleDoc td 往返，TimeScaleLib 剔除自研替代）+ api.time TimeScaleApi 契约面（api 纯 JDK 不依赖 engine）+ TimeBudgetScheduler tick 预算调度（复用 p.2.8 engine.sim.budget 内核）+ runtime.time TimeRuntime 门控壳（subterra.probe.time、[Subterra time] marker 并入 AsyncE2EProbe 同开服）+ TimeProbe 43 checks 接入 probeAcceptance（102 任务 = 97 纯 JVM + 5 E2E）；probeAcceptance 全绿（97 纯 JVM + 五次 E2E）。**p.2.26 为当前 p 轨收尾**：里程碑表无后续 p 轨行（p.2.27 / p.2.27.1 已于 2026-09-09 提前落地），下一里程碑待 ROAD 更新。/ 2026-09-11 — p.2.26 landed: the clean-room time-scaling line (p.2.26.1-5, closing p.2.26.6) — engine.time four-domain scaling core (TimeDomain fixed four-domain order / FlowRate / TimeScale GLOBAL fallback chain / TimeThrottle / TimeInterpolation / TimeScaleDoc td round-trip, TimeScaleLib excluded, the self-developed replacement) + the api.time TimeScaleApi contract surface (api pure JDK, no engine dependency) + TimeBudgetScheduler tick-budget scheduling (over the p.2.8 engine.sim.budget kernel) + the runtime.time TimeRuntime gated shell (subterra.probe.time, the [Subterra time] marker merged into the AsyncE2EProbe boot) + TimeProbe 43 checks wired into probeAcceptance (102 tasks = 97 pure-JVM + 5 E2E); probeAcceptance all green (97 pure-JVM + five E2E boots). **p.2.26 wraps up the current p-track**: the milestone map has no further p-track row (p.2.27 / p.2.27.1 were already advanced and landed on 2026-09-09); the next milestone awaits a ROAD update.
* 2026-09-11 — LOD assessed / LOD 评估：Distant Horizons（LGPL-3.0 copyleft）→ 仅思想参考；LOD 能力 clean-room 自研 + tie 化（engine.render.lod，p.2.28，紧接 p.2.27 渲染线；规划子项 p.2.28.1–p.2.28.6，交接单 docs/plans/prompt-p.2.28-lod.md；热点 polygonizer/merger 以 tie 实现走 p.2.1 桥 + zd 字节 ABI，无 lib 确定性 skip）。 / 2026-09-11 — LOD assessed: Distant Horizons (LGPL-3.0 copyleft) → reference-of-ideas only; the LOD capability is clean-room self-developed + tie-ized (engine.render.lod, p.2.28, right after the p.2.27 render line; sub-items p.2.28.1–p.2.28.6 planned, handoff at docs/plans/prompt-p.2.28-lod.md; the polygonizer/merger hot paths implemented in tie via the p.2.1 bridge + the zd byte ABI, deterministic skip without a lib).
* 2026-09-11 — p.2.28 landed: LOD 多级细节渲染 clean-room 自研 + tie 化全轨落地（p.2.28.1–p.2.28.6，每次提交 sha：f2483e8/b2a0a1d/616157b/c46d5dc/a8cca8f/040e9c4/bc6da29）——engine.render.lod 七面落位：数据模型（LodLevel 阶梯 / LodSection 固定序字节=zd ABI 载荷 / LodVertexLayout byteSize 8 / LodConfigDoc td 往返）、生成管线（Polygonizer+Merger 固定扫描序 / dirty 预算批次重建 p.2.7 语义 / **tie 标量原语下沉** subterra_lod_pipeline.tie→dll + LodTieAccelerator FFM 缺 lib 确定性 skip，DLL 边界实测仅标量/string）、视景管理（距离分档 / Q8.24 整数视锥 / p.2.8 预算内核）、渲染后端（p.2.27.1 实例化复用 + clean-room GLSL + 接缝/雾规则）、持久化（LodCache tsha1f 双档四类损坏拒）、api.lod 契约面（api 纯 JDK + LodApiMirror）、runtime 接线（LodRuntime 门控壳 [Subterra lod] marker 并入 AsyncE2EProbe + RenderHooksClient 注入点钩子 + RuleStore 档位读取 + LodProbe）；Distant Horizons（LGPL-3.0）仅思想参考、零源码包含；**probeAcceptance 全绿（98 纯 JVM + 五次 E2E，新增 LodProbe）**；本里程碑为当前 p 轨最新行，下一里程碑待 ROAD 更新。/ 2026-09-11 — p.2.28 landed: the LOD line fully landed clean-room self-developed + tie-ized (p.2.28.1–p.2.28.6, commits f2483e8/b2a0a1d/616157b/c46d5dc/a8cca8f/040e9c4/bc6da29) — engine.render.lod seven surfaces: the data model (LodLevel ladder / LodSection fixed-order bytes = the pinned zd-ABI payload / LodVertexLayout byteSize 8 / LodConfigDoc td round-trip), the generation pipeline (Polygonizer+Merger fixed scan order / dirty budgeted batch rebuild p.2.7 semantics / **tie-ized scalar primitives** subterra_lod_pipeline.tie→dll + LodTieAccelerator FFM deterministic skip without a lib, measured DLL boundary = scalars/string only), view management (distance tiers / Q8.24 integer frustum / the p.2.8 budget kernel), the render backend (p.2.27.1 instancing reuse + clean-room GLSL + seam/fog rules), persistence (LodCache tsha1f dual-tier four-way corruption rejection), the api.lod contract (api pure JDK + LodApiMirror), and runtime wiring (LodRuntime gated shell with the [Subterra lod] marker into AsyncE2EProbe + the RenderHooksClient injection-point hook + RuleStore tier readback + LodProbe); Distant Horizons (LGPL-3.0) reference-of-ideas only, zero source included; **probeAcceptance all green (98 pure-JVM + five E2E boots, incl. LodProbe)**; this milestone is the current latest p-track row; the next milestone awaits a ROAD update.
* 2026-09-15 — 系列规划调整：首位真实消费方从 Toterra 调整为 **Overturn（颠覆）**（世界生成拟真先锋：litho 地层 / 气候植被 / 矿物母岩，框架补缺驱动型，设计 `docs/2026-09-15-overturn-mod-design.md`），Toterra 远期化；**p.2.29「MC worldgen 装配面补齐」立项**（里程碑表新行：p.2.29.1 规则→密度 / p.2.29.2 规则→表面 / p.2.29.3 规则→特征成矿 / p.2.29.4 联合验收，补「规则接入真实生成路径」缺口）；装配面未落地前 Overturn 内容先行沉淀为纯数据 + 纯 JVM 探针。 / 2026-09-15 — series plan adjustment: the first real consumer is now **Overturn** (worldgen-realism vanguard: litho strata / climate vegetation / mineral-by-rock, framework-gap-driven, design `docs/2026-09-15-overturn-mod-design.md`); Toterra deferred to long-term; **p.2.29 "MC worldgen assembly completion" established** (milestone-map row: p.2.29.1 rules→density / p.2.29.2 rules→surface / p.2.29.3 rules→feature-or-ore / p.2.29.4 joint acceptance, closing the "rules into real generation path" gap); before the seams land, Overturn content is pre-built as pure data + pure-JVM probes.
* 2026-09-15 — 移植落点审计 + ROAD 状态校正：对照里程碑与代码存在性双确认，移植落点分配表 22 项中 **12 项由 planned 校正为 landed**（GeckoLib p.2.21 / AppleSkin p.2.22 / ModMenu p.2.22 / SmartBrainLib p.2.24 / C2ME p.2.6 / Export-Language-Keys p.2.18 / RollingGate p.2.17 / DataTip p.2.22 / Patchouli p.2.22 / Pehkui-Rebuilt p.2.25 / Flywheel p.2.27.1 / Distant Horizons p.2.28 assess→landed）、TimeScaleLib 备注自研替代已落地（p.2.26）、Itemban 校正为 engine 核心 + ItemControlProbe 已落（runtime 壳待接线）；未落地剩 3（Traveler 系列 / StellarRTP planned、mcwifipnp pending）；第三方许可材料（third-party/flywheel-1.0.6、geckolib-4.8、appleskin-3.0.6、pehkui-rebuilt-3.8.5）均在库。 / 2026-09-15 — port-landing audit + ROAD status correction: milestone-and-code double confirmation across the 22 port-allocation rows — 12 promoted from planned to landed, TimeScaleLib annotated with its landed self-developed replacement (p.2.26), Itemban corrected to engine core + ItemControlProbe landed (runtime shell pending); 3 remain not landed (Traveler pack / StellarRTP planned, mcwifipnp pending); third-party license materials all in-repo.
* 2026-09-15 — 剩余未落地项发号 + api 契约缺口盘点：未落地 4 项挂入计划里程碑（迁移落点表同步）——mcwifipnp→p.2.30（LAN 打洞，engine.p2p.holepunch + runtime 接线）、StellarRTP→p.2.31.1、Itemban runtime 壳→p.2.31.2、Traveler 系列→p.2.32（标题/天气/超分/皮肤四子项）；新增 **p.2.33「api 契约面补齐」（api-complete）**：现状契约面仅十域（worldgen+profiler / config / export / event / time / lod / net / crypto / ser / migrate），engine 已落地的 anim/ui/ai/scale/sim/interact/pcg/schema/hub/render/save/world/session/parallel 大多无 api 契约——「模组只依赖 subterra-api」铁律的消费面断裂；Overturn 驱动高优：api.worldgen 装配契约随 p.2.29 落地、api.datapack（p.2.33.1 DatapackApi）。 / 2026-09-15 — remaining un-landed items assigned p-track numbers + API-gap inventory: mcwifipnp→p.2.30 (LAN holepunch), StellarRTP→p.2.31.1, Itemban runtime shell→p.2.31.2, Traveler pack→p.2.32 (four sub-items); new **p.2.33 API contract completion (api-complete)**: current contracts cover ten domains only, while landed engines (anim/ui/ai/scale/sim/interact/pcg/schema/hub/render/save/world/session/parallel) mostly lack api contracts — breaking the mods-depend-only-on-subterra-api iron law; Overturn-driven priorities: api.worldgen assembly contracts with p.2.29, api.datapack (p.2.33.1 DatapackApi).
* 2026-09-15 — api 缺口细化发号 + tie 写模组能力立项：**p.2.33** 细化为十个编号子项（.1 api.datapack DatapackApi / .2 api.scale / .3 api.anim / .4 api.ai / .5 api.ui / .6 api.sim / .7 api.interact / .8 api.pcg / .9 api.save 含 Ledger / .10 api.world；session / parallel / hub / render-instancing 契约视消费方需求后补；全 mirror 模式对齐 api.time/api.lod）；新增 **p.2.34「tie 模组开发一等支持」**（与 Kotlin 写模组同等体验）：tiec **JVM 目标后端**（tie→.class，跨仓协作 tie-main，模块化后端缺省仍原生）+ JvmInterop 互操作层（Java API 调用 / 接口实现 / 注解 / 类型映射，api 纯 JDK 契约）+ tie 模组装配骨架（td 数据包 + tie 逻辑一体：方块/物品/事件/配置以 tie 注册，scaffold 生成 tie 模组工程接 devkit）+ 探针/E2E（tie 模组开服确定性 marker）；与 p.2.1 DLL/FFM 桥互补（热点原生 DLL、完整模组逻辑 JVM）。 / 2026-09-15 — API-gap sub-numbering + tie modding capability established: **p.2.33** refined into ten numbered sub-items (DatapackApi / scale / anim / ai / ui / sim / interact / pcg / save incl. the Ledger / world); new **p.2.34 tie modding first-class support** (Kotlin-equivalent): tiec JVM backend (tie→.class, cross-repo tie-main) + JvmInterop layer (Java API calls / interface impl / annotations / type mapping, pure-JDK api contract) + mod-assembly skeleton (td datapack + tie logic; blocks/items/events/config in tie; scaffold to tie-mod projects) + probes/E2E; complementary to the p.2.1 DLL/FFM bridge (hot paths native, full mod logic JVM).
* 2026-09-15 — p.2.34 补增 **.NET 目标后端**：子项重排为 p.2.34.1–p.2.34.6——p.2.34.1 tiec JVM 目标（tie→.class，MC 模组主路径）；**p.2.34.2 tiec .NET 目标后端（tie→.NET IL（CIL），跨仓协作 tie-main，供 .NET 宿主、与 C# 写 .NET 程序同等体验——非 MC 路径的通用托管目标）**；p.2.34.3 JvmInterop；p.2.34.4 DotnetInterop（.NET API 调用 / 接口实现 / attribute / 类型映射，通用契约面）；p.2.34.5 tie 模组装配骨架；p.2.34.6 探针 + E2E。 / 2026-09-15 — p.2.34 extended with the **.NET target backend**: sub-items renumbered to p.2.34.1–p.2.34.6 — p.2.34.1 tiec JVM backend (tie→.class, the MC-mod main path); **p.2.34.2 tiec .NET backend (tie→CIL, cross-repo tie-main, C#-equivalent for .NET hosts — a general managed target, non-MC)**; p.2.34.3 JvmInterop; p.2.34.4 DotnetInterop (.NET API calls / interface impl / attributes / type mapping); p.2.34.5 mod-assembly skeleton; p.2.34.6 probes + E2E.
* 2026-09-15 — **p.2.33 landed（api-complete 十个契约面全落）** + **p.2.29 landed（装配面 p.2.29.1–.3，含 api.worldgen 装配契约）**：p.2.33.1–.10 各 = api 纯 JDK 契约 + engine XxxApiMirror（不 import api）+ 确定性探针（9–29 checks，合计 170），提交 c7c5969 / 2dddaa3 / 22d8e82 / 54d542b / e74a1cb / ede4b62 / 60c1dd5 / 4923468 / 5cb443f / 22dbf1e；p.2.29 引擎侧 engine.worldgen.assembly（DensityAssembly + AssemblyApiMirror + api WorldgenAssemblyApi + AssemblyProbe 6 checks）+ runtime 启动快照（SubterraDensity.setAssembly 缺省恒等 + [Subterra assembly] marker），提交 d49b307；11 新探针接入 probeAcceptance（113 任务 = 108 纯 JVM + 5 E2E，全部纯 JVM 探针前台跑绿）；诚实披露：p.2.29.4 的 MC 区块注入面（真实 chunk-gen 表面/特征）以门控快照 + 文档化接线点交付待 E2E，runtime 接线与 devkit acceptance 接线因与在途 td-plus 提交同文件而留待并入。 / 2026-09-15 — **p.2.33 landed (all ten api-complete contracts)** + **p.2.29 landed (assembly p.2.29.1–.3 incl. the api.worldgen assembly contract)**: each contract = pure-JDK api + engine XxxApiMirror (no api import) + deterministic probe (9–29 checks, 170 total); p.2.29 engine.worldgen.assembly (DensityAssembly + AssemblyApiMirror + api WorldgenAssemblyApi + AssemblyProbe 6 checks) + runtime boot snapshot; the 11 new probes wired into probeAcceptance (113 tasks = 108 pure-JVM + 5 E2E, all pure-JVM probes run green); honest disclosure: the p.2.29.4 MC chunk-injection surface is delivered as a gated snapshot + documented wiring point pending E2E, and the runtime/acceptance wiring awaits the concurrent in-flight td-plus commit touching the same files.
* 2026-09-16 — 残余项批次 1+2 收口（团队五线并行，主理人汇编）：**p.2.29.2 表面真接缝**（97e5c79）、**p.2.29.3 特征成矿真接缝**（dec272e / 30844bf / f2fd641 / 3cd19d7）、**p.2.35 Java 融合与装配缝**（6b62f38 / 7a20928 / 42e4a68 / a7a05e4）、**三缝验收矩阵 + devkit acceptance 接线并入**（e13b1d9）、**RCA 修复 rule_vein 注册守卫**（07c6c48）；`probeAcceptance` 114→119 任务全绿（含五次 E2E 开服，`ServerBootProbe` 真接缝 marker 全命中）；`tie-src/trimand` tie 侧源码入库（构建产物 gitignore）；p.2.35 状态更新为 landed（tie 侧 + Java 融合）。挂起项：gen/ DLL 与打包资源 SHA 同源性复核（p.2.35.x，tie 侧）、`ore_density`→count 倍率、表面热重载再快照。 / 2026-09-16 — residual batches 1+2 closed (five-member team, compiled by the lead): the p.2.29.2 surface real seam, the p.2.29.3 feature/ore real seam, the p.2.35 Java fusion + assembly seam, the seam acceptance matrix (+ the deferred devkit acceptance wiring), and the RCA fix for the rule_vein registration guard; probeAcceptance 114→119 all green (five E2E boots with real-seam markers); tie-src/trimand sources committed (build artifacts gitignored); p.2.35 marked landed (tie side + Java fusion). Open items: gen/ DLL vs packaged-resource SHA parity (p.2.35.x, tie side), ore_density→count multiplier, surface hot-reload re-snapshot.
* 2026-09-16 — **p.2.34 移出本计划、转入 tie 开发计划**（用户拍板：纪律主语是「非 tie 本身的开发任务」——tie 本身的开发归 tie 轨道，消费平台不修改 tie；p.2.34.1/.2 需改 tiec，故整体移交）：tie-main `ROAD.md` 以 **p.9.14「tie 模组开发一等支持（自 Subterra 计划移交）」**立项（p.9.14.1 tiec JVM 目标后端 / .2 tiec .NET 目标后端 / .3 JvmInterop / .4 DotnetInterop / .5 tie 模组装配骨架 / .6 tie 模组探针 + E2E，与原 p.2.34.1–.6 一一对应，提交 tie-main 31b20d6）；本表删除 p.2.34 行（上方 2026-09-15 两条立项记录保留为历史）；消费侧衔接（api 契约 / devkit 接线）待 tie 侧后端落地后另行排期。 / 2026-09-16 — **p.2.34 moved out of this roadmap into the tie roadmap** (user ruling: the discipline governs non-tie development tasks — tie itself is developed on the tie track and consumer platforms do not modify tie; p.2.34.1/.2 require tiec changes, so the item transfers wholesale): tie-main ROAD.md registers it as **p.9.14 "first-class tie modding (transferred from the Subterra roadmap)"** (p.9.14.1 tiec JVM backend / .2 tiec .NET backend / .3 JvmInterop / .4 DotnetInterop / .5 mod-assembly skeleton / .6 probes+E2E, one-to-one with the former p.2.34.1–.6, tie-main commit 31b20d6); the p.2.34 row is removed from this table (the two 2026-09-15 establishment entries above are kept as history); consumer-side wiring (api contracts / devkit) is re-scheduled after the tie-side backends land.
* 2026-09-16 — **p.2.31 落地（服务端工具补齐）**：p.2.31.1 StellarRTP clean-room（engine `RtpPlanner` + runtime `RtpRuntime`/`RtpCommand`/`RtpConfig` + `/subterra rtp` + td 规则 `rtp.td` + `RtpProbe` 21 checks）与 p.2.31.2 Itemban runtime 壳收口（开服 E2E marker 断言入 `ServerBootProbe` 契约）；`probeAcceptance` 119→120 任务全绿，开服契约新增 itemban(active/server) 与 rtp(shell/seed) 四项确定性 marker。 / 2026-09-16 — **p.2.31 landed (service-tool completion)**: p.2.31.1 StellarRTP clean-room (engine `RtpPlanner` + runtime shell trio + `/subterra rtp` + td rules + `RtpProbe` 21 checks) and p.2.31.2 itemban runtime-shell closure (boot E2E marker assertions joined the `ServerBootProbe` contract); probeAcceptance 119→120 all green with the four new deterministic boot markers.
* 2026-09-16 — **p.2.30 落地（LAN 打洞）**：p.2.30.1 `engine.p2p.holepunch` 确定性核心五件（端口预测 / 计划器 / 会话状态机）+ p.2.30.2 `runtime.network.holepunch` 门控接线壳（loopback 自打洞样例）；`HolepunchProbe` 25 checks 接入 probeAcceptance（120→121），开服契约新增 holepunch(shell/loop) 两项 marker。 / 2026-09-16 — **p.2.30 landed (LAN holepunch)**: the deterministic engine core quintet (port prediction / planner / session state machine) + the gated runtime wiring shell with the loopback self-punch sample; `HolepunchProbe` 25 checks wired into probeAcceptance (120→121) and the boot contract gained the holepunch(shell/loop) markers.
* 2026-09-16 — **p.2.32 落地（杂项视觉·界面自研化，Traveler 系列 clean-room 四子项）**：ui.title / weather / render.upscale / skin 各 = engine 确定性数据面 + runtime 门控壳 + `AsyncE2EProbe` marker 槽（seen 25–28）+ 纯 JVM 探针（20/14/13/18 checks）；**p.2.35.x 同源复核完成（任务 #1 收口）**：权威 tiec（`tie-repo/tiec/compiler/tiec.exe`，SHA `27982cea…` 与文档一致）独立重建——生成链逐位复现（`.gen.tie` SHA `10a1fa8f…` 与文档一致）、重建 DLL 与 `gen/` 产物六段全同且导出面均为 3；**发现捆绑资源为旧代 5 导出产物**（多出 `rfwd$cell_field`/`rfwd$coarse_field`），已替换为规范产物（`44b6b29e…`），替换后 `TrimandSeamProbe` 40 checks 与开服门全绿；四组 tie 探针复跑全绿（macrogen 22 / diffunet 9 / trainer 9 / sampler 12）。`p.2.35.x` 挂起项清零。 / 2026-09-16 — **the p.2.35.x parity check closed (task #1)**: an independent rebuild with the authoritative tiec reproduced the chain bit-exactly; the bundled resource was found to be an older 5-export build and was replaced by the canonical artifact; `TrimandSeamProbe` and the boot gate green after replacement, and all four tie probes re-run green (22/9/9/12). The p.2.35.x open item is cleared.
* 2026-09-17 — **p.1.8.34 设计变更：Subterra 自有地形模型（放弃还原原版密度配方）**。用户实测确认海 127 重锚定后仍呈波浪/条带（ASCII 地形图实为对角平行条带——原版配方气候样条+滑移结构的伪影），且拍板放弃 vanilla。落地 `SubterraTerrain`（engine.pipeline.terrain，纯 JDK 确定性）：三层自有噪声栈（大陆 ~4k 波长 / 丘陵 ~130 / 山脊 ~640 ridged，`PositionalRand.ofMaster().fromHashOf(盐)` 派生）+ 山地掩码（仅内陆隆起，平方缓入）+ 三维细节（~64 波长轻扰动），密度在 y=surface 过零；**海 127**（`SEA_LEVEL=127`）。runtime `SubterraDensity` 切换至 `SubterraTerrain.finalDensity`（原版配方仅保留于海 63 金标路径）；ASCII 地形图验证：连片高地 + 自然海岸线，无条带无波浪。`TerrainAxisProbe` 10 checks 切新模型全绿；全量门 126 任务全绿（金标 360 位零回归）。遗留：洞穴族（vanilla 配方绑定的 CaveFamilyFn）待以自有设计重做（新 p 子项）；tie 密度 DLL 需按本模型重新生成等价实现。 / 2026-09-17 — **p.1.8.34 design change: the Subterra-native terrain model (the vanilla density recipe is abandoned)**. Field testing confirmed the sea-127 re-anchoring still produced wave/banded terrain (the ASCII map showed diagonal parallel stripes — an artifact of the vanilla climate-spline + slide structure), and the user decided to drop vanilla. Landed `SubterraTerrain` (engine.pipeline.terrain, pure JDK deterministic): three own noise stacks (continent ~4k / hills ~130 / ridged mountains ~640, derived via `PositionalRand.ofMaster().fromHashOf(salt)`) + an inland-only mountain mask + a mild 3-D detail term, with the density crossing zero at y=surface; **sea level 127**. The runtime `SubterraDensity` now evaluates `SubterraTerrain.finalDensity` (the vanilla recipe remains only on the sea-63 golden path); the ASCII map shows a contiguous highland + a natural coastline — no stripes, no waves. TerrainAxisProbe 10 checks green on the new model; the full gate is green at 126 tasks (the golden's 360 bits unchanged). Deferred: the cave family (CaveFamilyFn, bound to the vanilla recipe) needs its own-design rework (a new p sub-item); the tie density DLL must be regenerated to match this model.
* 2026-09-16 — **p.2.29.x 三缝后续三小项落地**：p.2.29.3.1 `ore_density`→count 倍率（engine `withOreDensity` + 基线/有效计划分离，ServerStarting 快照期应用、不累积）/ p.2.29.2.1 表面 palette 热重载再快照（RulesRuntime 变更监听 + 活跃存储命令成功路径触发 + 原子重快照）/ p.2.29.3.2 挂载面缺省零增量（NeoForge 21.1.249 无 biome modifier 条件装载 → 空白 tag 门控）；探针增量：FeatureAssembly 9（+1 段 8 断言）/ SurfaceSeam 27（+3）/ FeatureSeam 44（+2）；全量门 125 任务复跑全绿。RCA 一例：`onRulesMutated` 误插在原 `@SubscribeEvent` 注解与方法之间 → 零参事件方法注册 FATAL，重排即绿。 / 2026-09-16 — **the three seam follow-ups landed**: p.2.29.3.1 the ore-density scalar feeding mineral counts (engine `withOreDensity` + base/effective plan split, applied at the ServerStarting snapshot without compounding) / p.2.29.2.1 the surface-palette hot-reload re-snapshot (a RulesRuntime mutation listener fired on the active-store command success path + an atomic re-snapshot) / p.2.29.3.2 the default zero-increment mount face (NeoForge 21.1.249 lacks biome-modifier conditional loading → an empty-by-default tag gate); probe deltas FeatureAssembly 9 (+1 section of 8) / SurfaceSeam 27 (+3) / FeatureSeam 44 (+2); the full gate re-ran all green at 125 tasks. One RCA: `onRulesMutated` was mis-inserted between the original `@SubscribeEvent` and its method → a zero-arg event handler FATAL, fixed by relocating the method.
* 2026-09-16 — **p.1.8.33 波浪地形 RCA + 设计变更落地（Subterra 不再还原原版海平面，海平面定为 127）**：实机回归（x 走向山峰/海洋、坡崖不对称、海浪状）取证三步——①实跑日志排除 Trimand/装配规则（skip + 恒等）；②引擎 router 双轴审计排除了引擎树单轴退化；③解析实跑世界区块高度图 + 密度采样定位真因：**td-plus 把窗口扩成 [-128, 592) + 海平面 128，但复合层 y 锚点（depth 梯度 [-64,320]、底/顶滑移带）仍 vanilla 系**——地表钉在 y≈63，被 128 的海面整体淹没 65 格，只有原高山顶部露出。修复：**复合层/路由器海平面参数化**（`DensityComposite.overworld(+seaLevel)` / `NoiseRouter.overworld(+seaLevel)`：depth 梯度带 [-64,320]、底滑移 [-64,-40]、顶滑移 [240,256] 整体上移 Δ = seaLevel−63，海相对剖面逐位保持）；设计终态：**窗口保持原版域型 [-64, 384)（sections/heightmap 与 `minecraft:overworld` 维度类型零错位），海平面抬至 127**——`Kind.OVERWORLD_FINAL` = (-64, 384, seaLevel 127) 与 JSON `min_y=-64/height=384/sea_level=127` 三者一致；诊断探针升级为 **`TerrainAxisProbe` 回归门**（3 种子地表高度场双轴 std、JSON↔窗口一致性、地表带语义，入 probeAcceptance 125→126）。金标（VanillaGolden，海 63 路径 delta=0 锚点不变）保持绿——它钉住 vanilla 形状机制，TerrainAxisProbe 钉住新设计语义，两层互补。 / 2026-09-16 — **p.1.8.33 wave-terrain RCA + design change (Subterra no longer replicates the vanilla sea level; the sea level is set to 127)**: three-step field forensics (server log rules out Trimand/assembly; an engine router two-axis audit rules out an engine-tree degeneracy; parsing the actual world's chunk heightmaps + density sampling pinned the cause: the td-plus window expansion [-128, 592) + sea 128 left the composite's y anchors vanilla-bound, so the surface stayed at y≈63 and drowned 65 blocks under the raised sea). Fix: **sea-level-parameterised composite/router** (all vanilla y anchors — the depth gradient band [-64, 320], the bottom slide [-64, -40], the top slide [240, 256] — shift up by Δ = seaLevel − 63, preserving the sea-relative profile bit-exactly); final design: the world window stays the vanilla band [-64, 384) (zero mismatch with the `minecraft:overworld` dimension type sections/heightmaps) with the sea level lifted to 127 — `Kind.OVERWORLD_FINAL` = (-64, 384, seaLevel 127) agrees with the JSON (min_y=-64/height=384/sea_level=127); the diagnostic probe became the **`TerrainAxisProbe` regression gate** (3-seed surface-height two-axis stds, JSON↔window consistency, surface-band semantics; probeAcceptance 125→126). The golden (VanillaGolden, the sea-63 path with delta=0 unchanged anchors) stays green — it pins the vanilla-shape machinery while TerrainAxisProbe pins the new design semantics.: ui.title / weather / render.upscale / skin, each an engine deterministic data plane + a gated runtime shell + an `AsyncE2EProbe` marker slot (seen 25–28) + a pure-JVM probe (20/14/13/18 checks); `probeAcceptance` 121→**125 all green** (120 pure-JVM + 5 E2E).
