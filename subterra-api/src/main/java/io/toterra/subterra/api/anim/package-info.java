/**
 * Animation contract surface (p.2.33.3): the fixed-order, pure-JDK, deterministic
 * contract facade for the {@code engine.anim} core — the fixed channel order plus
 * the pure linear / shortest-arc / clamp sampling primitives, mirrorable engine-side.
 *
 * <p>动画契约面（p.2.33.3）：{@code engine.anim} 核心的固定序、纯 JDK、确定性契约门面——
 * 固定通道序与纯线性/最短弧/clamp 采样原语，引擎侧提供实现镜像。
 */
package io.toterra.subterra.api.anim;