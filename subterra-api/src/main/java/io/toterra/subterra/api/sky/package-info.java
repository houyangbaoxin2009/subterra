/**
 * Sky contract surface (p.2.0.13): the pure-JDK, deterministic contract facade for the
 * per-dimension cloud-height override registry — the vanilla-pinned height constants plus the
 * normalize/store/clear semantics, with the runtime mixin as the render-side consumer.
 *
 * <p>天空契约面（p.2.0.13）：按维度云高覆盖注册表的纯 JDK、确定性契约门面——钉死原版云高常量与
 * 规范化/存储/清除语义，runtime mixin 为渲染侧消费端。
 */
package io.toterra.subterra.api.sky;
