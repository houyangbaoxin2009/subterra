/**
 * AI orchestration contract surface (p.2.33.4): the fixed-order, pure-JDK,
 * deterministic contract facade for the {@code engine.ai} core — the fixed
 * sense→decide→execute orchestration order and the three-cap per-tick budget
 * semantics, mirrorable engine-side.
 *
 * <p>AI 编排契约面（p.2.33.4）：{@code engine.ai} 核心的固定序、纯 JDK、确定性契约门面——
 * 固定「感知→决策→执行」编排序与三上限每 tick 预算语义，引擎侧提供实现镜像。
 */
package io.toterra.subterra.api.ai;