/**
 * Entity-scale contract surface (p.2.33.2): the fixed-order, pure-JDK, deterministic
 * contract facade for the {@code engine.scale} core — the pinned dimensioned
 * scale-type order plus the pure query/normalize semantics, mirrorable engine-side.
 *
 * <p>实体缩放契约面（p.2.33.2）：{@code engine.scale} 核心的固定序、纯 JDK、确定性契约门面——
 * 钉死按维度缩放类型序与纯查询/规范化语义，引擎侧提供实现镜像。
 */
package io.toterra.subterra.api.scale;