/**
 * p.2.16.4 事件契约面：确定性事件种类/信封 + 单调可回放事件流（固定序、禁时序）。The event contract surface: deterministic kinds/envelopes + a monotonic replayable event stream (fixed order, time-free).
 */
package io.toterra.subterra.api.event;
