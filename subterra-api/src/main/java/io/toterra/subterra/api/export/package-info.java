/**
 * 导出契约包（p.2.16.3）：api 侧 exporter 注册表契约（ExportKindSpec / ExportPortSpec /
 * ExporterRegistry）——p.2.18 export hub 的契约前哨；纯 JDK，不依赖 engine。
 *
 * <p>Export contract package (p.2.16.3): the api-side exporter registry contract
 * (ExportKindSpec / ExportPortSpec / ExporterRegistry) — the p.2.18 export hub
 * precursor; pure JDK, no engine dependency.
 */
package io.toterra.subterra.api.export;
