/**
 * UI / HUD contract surface (p.2.33.5): the fixed-order, pure-JDK, deterministic
 * contract facade for the {@code engine.ui} core — the deterministic food-value
 * computation, the fixed tooltip-row order, the fixed book-page-type order and the
 * fixed mod-metadata field order, mirrorable engine-side.
 *
 * <p>UI/HUD 契约面（p.2.33.5）：{@code engine.ui} 核心的固定序、纯 JDK、确定性契约门面——
 * 确定性食物值计算、固定 tooltip 行序、固定书籍页类型序与固定模组元数据字段序，引擎侧提供实现镜像。
 */
package io.toterra.subterra.api.ui;