/**
 * Migration translator (p.2.0 skeleton): td-described old-API -> new-API
 * mappings producing migration reports / scaffolds / rewrite patches.
 * Iron law: migrate depends only on {@code subterra-api}.
 */
package io.toterra.subterra.migrate;
